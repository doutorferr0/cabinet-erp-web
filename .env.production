# SITE REAL (branch app-real) — preview da Cloudflare Pages apontando pro
# backend de produção. cabinetonline.cc (main) continua 100% mock.
# Se as env do PAINEL da Cloudflare valerem pra preview, elas VENCEM este
# arquivo — conferir no build (o console declara a passagem).
VITE_API_MODE=http
VITE_API_URL=https://api.cabinetonline.cc
VITE_MOCK_AUTOLOGIN=0
