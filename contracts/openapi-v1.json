{
  "openapi": "3.1.1",
  "info": {
    "title": "Vitra ERP",
    "version": "v1"
  },
  "security": [
    {
      "sessaoCabinet": []
    }
  ],
  "paths": {
    "/health": {
      "get": {
        "tags": ["health"],
        "operationId": "Health",
        "security": [],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HealthStatus"
                }
              }
            }
          }
        }
      }
    },
    "/health/db": {
      "get": {
        "tags": ["health"],
        "operationId": "HealthDb",
        "security": [],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReadinessStatus"
                }
              }
            }
          },
          "503": {
            "description": "Service Unavailable. **A ÚNICA resposta 4xx/5xx do contrato que não é `ProblemDetails`, e é decisão.** Prova de vida é lida por orquestrador, não por operador: quem chama quer o documento de prontidão (migrações pendentes, detalhe da checagem) para decidir se derruba a instância. Enfiá-lo em `detail` como texto obrigaria o orquestrador a parsear frase.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReadinessStatus"
                }
              }
            }
          }
        }
      }
    },
    "/api/catalog-lookups": {
      "get": {
        "tags": ["catalogo"],
        "operationId": "ListCatalogLookups",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "kind",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortBy",
            "description": "Whitelist: `kind`, `name`, `active`. Campo fora dela é 400.\n\nEsta operação NÃO publica `filters` — o recorte por `kind` é parâmetro próprio, porque toda tela que abre uma lista de apoio já sabe qual quer.",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfCatalogLookupDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      },
      "post": {
        "tags": ["catalogo"],
        "operationId": "CreateCatalogLookup",
        "description": "Proposto. Cadastra um item de lista de apoio — o `+...` que todo combo do legado tem ao lado (§9 padrão 2). Sem ele, cadastrar produto ou cliente novo trava no primeiro combo vazio, e o operador precisa sair da tela para voltar depois. O item nasce dentro de UM `kind`, e o `kind` é do corpo porque a tabela é única e discriminada (ADR-011).",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CatalogLookupCreateRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogLookupDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict — já existe item ATIVO com este nome dentro do mesmo `kind`. Nome repetido no mesmo kind faz o combo mostrar duas linhas iguais, e a escolha entre elas passa a ser sorteio: dois registros apontariam para ids diferentes que o operador leu como a mesma coisa. O 409 não carrega membro de extensão — o cliente já tem a lista do kind em cache e acha o item existente PELO NOME que acabou de digitar.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/catalog-lookups/{id}": {
      "put": {
        "tags": ["catalogo"],
        "operationId": "UpdateCatalogLookup",
        "description": "Proposto. Renomeia ou desativa um item de lista de apoio. **`kind` não entra no corpo, e é decisão:** mover um item de lista mudaria o significado de todo registro que já aponta para ele — um `CARGO` que vira `SETOR` deixa para trás fichas de colaborador dizendo o que ninguém escreveu. Trocar de lista é desativar aqui e cadastrar lá.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CatalogLookupUpdateRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogLookupDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — o nome novo já pertence a outro item ATIVO do mesmo `kind`. Mesma razão do `POST`: renomear é o outro caminho para chegar ao par duplicado.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/partners/{partnerId}/contacts": {
      "get": {
        "tags": ["parceiros"],
        "operationId": "ListPartnerContacts",
        "description": "Proposto. Os contatos do parceiro — a grade que as telas de Fornecedor e Profissional já desenham. Sub-recurso e não `contacts[]` no `PartnerDto`: ver a descrição de `PartnerContactDto`, e a mesma forma das variantes do produto.",
        "parameters": [
          {
            "name": "partnerId",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortBy",
            "in": "query",
            "description": "Whitelist: `name`, `role`, `active`.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfPartnerContactDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found — o parceiro não existe ou não tem vínculo com a empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": ["parceiros"],
        "operationId": "CreatePartnerContact",
        "parameters": [
          {
            "name": "partnerId",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PartnerContactWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PartnerContactDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — sem empresa ativa na sessão. O pedido está bem formado e falta uma ESCOLHA da pessoa; a borda recusa toda escrita de `/api/` antes de olhar o corpo.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/partners/{partnerId}/contacts/{contactId}": {
      "put": {
        "tags": ["parceiros"],
        "operationId": "UpdatePartnerContact",
        "description": "Proposto. Altera UM contato. Não existe DELETE: contato que saiu da empresa vai a `active: false` e continua legível no documento antigo que o citou (§9 padrão 8) — apagar deixaria a linha do pedido apontando para ninguém.",
        "parameters": [
          {
            "name": "partnerId",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "contactId",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PartnerContactWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PartnerContactDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found — o contato não é deste parceiro, ou o parceiro não está ao alcance da empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — sem empresa ativa na sessão. O pedido está bem formado e falta uma ESCOLHA da pessoa; a borda recusa toda escrita de `/api/` antes de olhar o corpo.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/products": {
      "get": {
        "tags": ["catalogo"],
        "operationId": "ListProducts",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortBy",
            "description": "Whitelist: `code`, `description`, `active`. Campo fora dela é 400.\n\nÉ a MESMA de `filters` — os três campos planos do `ProductDto`. `productTypeName`/`brandName` ficam de fora dos dois: vêm de junção com a lista de apoio, e o par `id`+`name` publicado no DTO existe para a tela MOSTRAR, não para o servidor indexar.",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "filters",
            "in": "query",
            "description": "Proposto. Filtro estruturado da listagem, somado ao `q` com AND — `q` é texto livre sobre os campos que o recurso escolheu, `filters` é campo a campo. Viaja como **array JSON url-encoded** (`?filters=%%5B%%7B%%22field%%22...`), e não como parâmetro repetido: o valor é texto do operador e qualquer delimitador precisaria de escape inventado, cujo bug apareceria como resultado errado, em silêncio. Whitelist deste recurso: `code`, `description`, `active` — a mesma do `sortBy`, e cresce quando uma tela precisar. Campo fora dela é 400.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/ListFilter"
                  }
                }
              }
            }
          },
          {
            "name": "joinOperator",
            "in": "query",
            "description": "Proposto. Como as condições de `filters` se somam. Padrão `and`.",
            "schema": {
              "$ref": "#/components/schemas/ListFilterJoin"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfProductDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": ["catalogo"],
        "operationId": "CreateProduct",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ProductWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProductDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/products/{id}": {
      "get": {
        "tags": ["catalogo"],
        "operationId": "GetProduct",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProductDetailDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": ["catalogo"],
        "operationId": "UpdateProduct",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ProductWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProductDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/products/{productId}/variants": {
      "post": {
        "tags": ["catalogo"],
        "operationId": "CreateVariant",
        "parameters": [
          {
            "name": "productId",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/VariantWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProductVariantDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/products/{productId}/variants/{id}": {
      "put": {
        "tags": ["catalogo"],
        "operationId": "UpdateVariant",
        "parameters": [
          {
            "name": "productId",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/VariantWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProductVariantDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/partners": {
      "get": {
        "tags": ["parceiros"],
        "operationId": "ListPartners",
        "parameters": [
          {
            "name": "role",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortBy",
            "description": "Whitelist: `code`, `legalName`, `tradeName`, `document`, `active`, `parentId`. Campo fora dela é 400.\n\nÉ a MESMA de `filters`. `categoryName`, `parentName` e `specifierName` ficam de fora dos dois — são junções, como em produtos. `parentId` está aqui porque a hierarquia sai por `filters`, e não por um caminho aninhado.",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "filters",
            "in": "query",
            "description": "Proposto. Filtro estruturado da listagem, somado ao `q` com AND — `q` é texto livre sobre os campos que o recurso escolheu, `filters` é campo a campo. Viaja como **array JSON url-encoded** (`?filters=%%5B%%7B%%22field%%22...`), e não como parâmetro repetido: o valor é texto do operador e qualquer delimitador precisaria de escape inventado, cujo bug apareceria como resultado errado, em silêncio. Whitelist deste recurso: `code`, `legalName`, `tradeName`, `document`, `active`, `parentId` — a mesma do `sortBy`, e cresce quando uma tela precisar. Campo fora dela é 400. `parentId` entrou pela hierarquia pai/filho: é por ele que a tela do pai pede os filhos, em vez de um caminho novo só para isso.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/ListFilter"
                  }
                }
              }
            }
          },
          {
            "name": "joinOperator",
            "in": "query",
            "description": "Proposto. Como as condições de `filters` se somam. Padrão `and`.",
            "schema": {
              "$ref": "#/components/schemas/ListFilterJoin"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfPartnerDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": ["parceiros"],
        "operationId": "CreatePartner",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PartnerWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PartnerDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "501": {
            "$ref": "#/components/responses/NaoImplementado"
          }
        }
      }
    },
    "/api/partners/{id}": {
      "get": {
        "tags": ["parceiros"],
        "operationId": "GetPartner",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PartnerDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": ["parceiros"],
        "operationId": "UpdatePartner",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PartnerWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PartnerDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "501": {
            "$ref": "#/components/responses/NaoImplementado"
          }
        }
      }
    },
    "/api/partners/{id}/link": {
      "post": {
        "tags": ["parceiros"],
        "operationId": "LinkPartner",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/components/schemas/PartnerLinkRequest"
                  }
                ]
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PartnerDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/variants/{variantId}/stock-movements": {
      "post": {
        "tags": ["estoque"],
        "operationId": "CreateStockMovement",
        "description": "Proposto na parte do DEPÓSITO. O movimento passa a ter LOCAL, e `balanceAfter` passa a ser o saldo DELE — não o total do produto na empresa (api#79, decisão 2 do user em 2026-08-22).\n\n**`locationId` é opcional no corpo e obrigatório no movimento.** Omitir (ou mandar `null`) significa \"o depósito padrão da empresa ativa\", e o servidor o CRIA sob demanda se ela ainda não tem nenhum — mesmo precedente da linha de `product_tenant`, que já nasce no primeiro movimento de variante nunca precificada. **A criação implícita não é operação deste contrato: é comportamento do servidor**, escrito aqui para quem lê saber de onde saiu o depósito que ele não cadastrou. Não existe caminho para criá-la de outro jeito — o contrato não publica criação de empresa, então empresa nova não teria outro lugar onde ganhar o seu.\n\n404 quando `locationId` aponta para depósito que a empresa ativa não tem. 409 quando o depósito está inativo, e quando o movimento deixaria o saldo do DEPÓSITO negativo — a conta que recusa é a do local, não mais a do produto.",
        "parameters": [
          {
            "name": "variantId",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/StockMovementRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StockMovementDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      },
      "get": {
        "tags": ["estoque"],
        "operationId": "ListStockMovements",
        "parameters": [
          {
            "name": "variantId",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "sortBy",
            "description": "Whitelist: `occurredAt`, `delta`, `reason`. Campo fora dela é 400.\n\n`occurredAt` é o padrão, e descendente: kardex se lê do mais recente para trás.",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfStockMovementDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/auth/login": {
      "post": {
        "tags": ["auth"],
        "operationId": "AuthLogin",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LoginRequest"
              }
            }
          },
          "required": true
        },
        "security": [],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LoginOk"
                }
              }
            }
          },
          "401": {
            "description": "Unauthorized",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/auth/logout": {
      "post": {
        "tags": ["auth"],
        "operationId": "AuthLogout",
        "security": [],
        "responses": {
          "204": {
            "description": "No Content"
          }
        }
      }
    },
    "/auth/change-password": {
      "post": {
        "tags": ["auth"],
        "operationId": "AuthChangePassword",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ChangePasswordRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "No Content"
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          }
        }
      }
    },
    "/auth/me": {
      "get": {
        "tags": ["auth"],
        "operationId": "AuthMe",
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SessaoAtual"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          }
        }
      }
    },
    "/auth/tenants": {
      "get": {
        "tags": ["auth"],
        "operationId": "AuthTenants",
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/VinculoDeEmpresa"
                  }
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          }
        }
      }
    },
    "/auth/active-tenant": {
      "put": {
        "tags": ["auth"],
        "operationId": "AuthSetActiveTenant",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TrocarEmpresaRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "No Content"
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      }
    },
    "/api/dashboard/summary": {
      "get": {
        "tags": ["dashboard"],
        "operationId": "GetDashboardSummary",
        "description": "Proposto. Os quatro indicadores da faixa de KPI do Dashboard, da EMPRESA ATIVA da sessão. Números derivados do movimento — o servidor apura, o front só formata. Sessão sem empresa ativa responde tudo em zero, pela mesma regra das listagens (lista vazia, não erro).",
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DashboardSummaryDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/dashboard/agenda": {
      "get": {
        "tags": ["dashboard"],
        "operationId": "ListAgendaEvents",
        "description": "Proposto. Compromissos com data dentro do intervalo [from, to], da empresa ativa. UMA consulta serve o mini-calendário (o mês inteiro, para marcar os dias) e a Agenda de hoje (o recorte do dia) — dois caminhos devolveriam a mesma coisa em granularidades diferentes e sairiam do ar em desacordo.",
        "parameters": [
          {
            "name": "from",
            "in": "query",
            "required": true,
            "description": "Início do intervalo, inclusivo (data ISO).",
            "schema": {
              "type": "string",
              "format": "date"
            }
          },
          {
            "name": "to",
            "in": "query",
            "required": true,
            "description": "Fim do intervalo, inclusivo (data ISO).",
            "schema": {
              "type": "string",
              "format": "date"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/AgendaEventDto"
                  }
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      }
    },
    "/api/tasks": {
      "get": {
        "tags": ["dashboard"],
        "operationId": "ListTasks",
        "description": "Proposto. As tarefas do quadro da empresa ativa. NÃO é `PagedResult`, e é decisão: um quadro kanban mostra as quatro colunas de uma vez e uma página de 10 cortaria coluna no meio, dando contagem de coluna errada. Se o volume crescer, o corte certo é por período/responsável, não por página.",
        "parameters": [
          {
            "name": "status",
            "in": "query",
            "description": "Filtra uma coluna só. Ausente = o quadro inteiro.",
            "schema": {
              "type": "string",
              "enum": ["todo", "doing", "review", "done"]
            }
          },
          {
            "name": "q",
            "in": "query",
            "description": "Busca em título e descrição.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/TaskDto"
                  }
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      },
      "post": {
        "tags": ["dashboard"],
        "operationId": "CreateTask",
        "description": "Proposto. Cria tarefa no quadro da empresa ativa.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TaskWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TaskDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/tasks/{taskId}": {
      "patch": {
        "tags": ["dashboard"],
        "operationId": "PatchTask",
        "description": "Proposto. Mudança PARCIAL de uma tarefa — é o verbo do quadro: mover de coluna muda `status` e nada mais. É a única exceção ao `PUT` substitui o registro inteiro que vale no resto do contrato, e ela existe porque o cartão do quadro não carrega o registro completo; um `PUT` a partir dele apagaria o que a tela não mostra.",
        "parameters": [
          {
            "name": "taskId",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TaskPatchRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TaskDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/todos": {
      "get": {
        "tags": ["dashboard"],
        "operationId": "ListTodos",
        "description": "Proposto. A lista A fazer — lembrete curto do OPERADOR na empresa ativa, sem responsável, sem prazo e sem coluna. Recurso separado de `tasks` de propósito: fundir os dois obrigaria todo lembrete a carregar prioridade, prazo e responsável em branco, e a tela teria de adivinhar o que é lembrete e o que é tarefa de quadro.",
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/TodoDto"
                  }
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      }
    },
    "/api/todos/{todoId}": {
      "patch": {
        "tags": ["dashboard"],
        "operationId": "PatchTodo",
        "description": "Proposto. Marca ou desmarca o item da lista A fazer.",
        "parameters": [
          {
            "name": "todoId",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TodoPatchRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TodoDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/projects": {
      "get": {
        "tags": ["planner"],
        "operationId": "ListProjects",
        "description": "Proposto. Projetos da empresa ativa — o que o seletor do Planner lista.",
        "parameters": [
          {
            "name": "status",
            "in": "query",
            "description": "Ausente = todos. O toggle do Planner manda `active,proposed` ou `closed`.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/ProjectDto"
                  }
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      }
    },
    "/api/projects/{projectId}/plan": {
      "get": {
        "tags": ["planner"],
        "operationId": "GetProjectPlan",
        "description": "Proposto. O plano do projeto: fases e, dentro delas, os itens com data de início e fim — é o que o gantt do Planner desenha. Vem em UMA resposta porque a tela desenha a grade inteira de uma vez; fases e itens em caminhos separados fariam a linha aparecer depois da faixa.",
        "parameters": [
          {
            "name": "projectId",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProjectPlanDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/employees": {
      "get": {
        "tags": ["cadastros"],
        "operationId": "ListEmployees",
        "description": "Proposto. Colaboradores da empresa ativa. Somente leitura neste corte — é daqui que sai o combo de Consultor(a) do orçamento.",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortBy",
            "description": "Whitelist: `name`, `sector`, `jobTitle`, `active`. Campo fora dela é 400.\n\n`sector` e `jobTitle` são listas de apoio resolvidas por junção, e entram porque a listagem de pessoas é organizada por elas — é a exceção que o par `id`+`name` justifica quando a coluna É o critério de leitura.",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfEmployeeDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      },
      "post": {
        "tags": ["cadastros"],
        "operationId": "CreateEmployee",
        "description": "Proposto. Cria o colaborador na ORGANIZAÇÃO e o vincula à empresa ativa em um passo — cadastrar alguém que não trabalha em empresa nenhuma é estado que a tela não sabe mostrar. E-mail já usado no grupo é 409.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/EmployeeWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EmployeeDetailDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/works": {
      "get": {
        "tags": ["obras"],
        "operationId": "ListWorks",
        "description": "Proposto. As obras — coleção PRÓPRIA, e não `/api/partners/{id}/works`. A obra é escolhida no orçamento partindo do cliente, e é isso que o `filters` com `customerId` resolve; um caminho aninhado daria um segundo dialeto de listagem (paginação, ordenação e filtro próprios) para responder a mesma pergunta.",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortBy",
            "in": "query",
            "description": "Whitelist: `customerName`, `description`, `workType`, `active`. Campo fora dela é 400.\n\n**Não é a mesma lista do `filters`, e a diferença é dos dois lados.** `customerName` ordena porque o próprio `WorkDto` diz que ele existe \"para a listagem de obras mostrar de quem é\": coluna de listagem que não ordena obriga a tela a escolher entre cabeçalho morto e 400 no clique. Ordenar por coluna de junção não é precedente novo — `/api/crm/opportunities` já faz isso em `partnerName` (`LEFT JOIN partners`).\n\n`customerId` SAIU desta lista e continua em `filters`. Ordenar 9.454 obras por uuid não põe nada em ordem para quem lê a tela: ninguém procura \"a obra que vem depois de `parc-0002`\". Filtrar pelo uuid, sim — é COMO a tela pergunta \"as obras deste cliente\", com o id que o combo já lhe deu.\n\nA remoção foi escrita ANTES de existir tela de obra. Tirar campo do `sortBy` depois de a coluna ser desenhada seria quebra; agora é decisão barata.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "filters",
            "in": "query",
            "description": "Proposto. Filtro estruturado, no mesmo formato dos outros recursos (array JSON url-encoded). **Whitelist deste recurso: `customerId`, `customerName`, `description`, `workType`, `active`** — NÃO é a mesma do `sortBy`. Campo fora dela é 400.\n\nAs duas listas divergem nas DUAS pontas, de propósito: `customerId` filtra e não ordena, `customerName` faz as duas. Um recurso pode ordenar por um campo sem filtrar por ele, e o contrário — o precedente escrito é `/api/crm/opportunities`, cuja whitelist de filtro é a do `sortBy` menos `expectedValueCents`.\n\n`customerName` filtra porque procurar obra por TRECHO do nome do cliente (`iLike`) é o que a janela de busca faz, e ali ninguém tem o id na mão — quem já escolheu o cliente no combo manda `customerId` e não precisa desta. A objeção \"é a segunda forma de perguntar a mesma coisa\" vale entre `customerId` e um `GET /api/partners/{id}/works`, que perguntam com o MESMO dado; por nome não é a mesma pergunta, é a pergunta de quem não sabe o id. `partnerName` em opportunities é a mesma coluna de junção, e filtra.\n\n`customerId` está aqui porque é COMO a tela pergunta \"as obras deste cliente\": um `GET /api/partners/{id}/works` seria uma segunda forma de perguntar a mesma coisa, com paginação, ordenação e filtro próprios para manter — a decisão que o contrato já tomou quando recusou `/api/partners/{id}/children` e mandou a hierarquia sair de `filters`. Publicar este parâmetro não contradiz \"publicar o dado não é publicar a consulta\": aquela regra vale para campo NOVO em recurso existente, cujo índice no servidor não mudou; aqui a coleção nasce agora, e uma listagem de 9.454 obras sem recorte por cliente não serve à tela que a pede.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/ListFilter"
                  }
                }
              }
            }
          },
          {
            "name": "joinOperator",
            "in": "query",
            "description": "Proposto. Como as condições de `filters` se somam. Padrão `and`.",
            "schema": {
              "$ref": "#/components/schemas/ListFilterJoin"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfWorkDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      },
      "post": {
        "tags": ["obras"],
        "operationId": "CreateWork",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/WorkWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found — o cliente de `customerId` não existe, ou não tem vínculo com a empresa ativa. 404 e não 400: do ponto de vista de quem pergunta, o cliente não está lá.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — sem empresa ativa na sessão. O pedido está bem formado e falta uma ESCOLHA da pessoa; a borda recusa toda escrita de `/api/` antes de olhar o corpo.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/works/{id}": {
      "get": {
        "tags": ["obras"],
        "operationId": "GetWork",
        "description": "Proposto. Uma obra por id — o caminho do link direto e da recarga, o mesmo buraco que `GET /api/partners/{id}` fechou. Responde o MESMO `WorkDto` da listagem: não há DTO de detalhe.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": ["obras"],
        "operationId": "UpdateWork",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/WorkWriteRequest"
              }
            }
          },
          "required": true
        },
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/WorkDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — sem empresa ativa na sessão. O pedido está bem formado e falta uma ESCOLHA da pessoa; a borda recusa toda escrita de `/api/` antes de olhar o corpo. Também é 409 TROCAR O CLIENTE de uma obra que já tem orçamento: documento fechado não muda de dono.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/quotes": {
      "get": {
        "tags": ["vendas"],
        "operationId": "ListQuotes",
        "description": "Proposto. Orçamentos da empresa ativa. `sortBy` aceita `number`, `issuedAt`, `expiresAt`, `customerName`, `projectName`, `workName`; fora da lista é 400.",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortBy",
            "description": "Whitelist: `number`, `issuedAt`, `expiresAt`, `customerName`, `projectName`, `workName`. Campo fora dela é 400 — a lista não volta em outra ordem, ela não volta.\n\nÉ a de `filters` MENOS `workId` — inclusive `customerName`, `projectName` e `workName`, que vêm de junção: aqui o nome é o que a tela lê e por onde ela ordena. `series`, `totalCents` e `discountPercent` ficam de fora, pelos motivos escritos na descrição de `filters`.\n\n`workName` ordena pelo nome da OBRA (`workId` resolvido), e não pelo `projectName` digitado: são dois dados, e ordenar pelo texto livre embaralha documentos da mesma obra. `workId` fica fora daqui — ordenar por uuid não põe nada em ordem para quem lê; ele serve em `filters`, para recortar.",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "filters",
            "in": "query",
            "description": "Proposto. Filtro estruturado do orçamento, somado ao `q` com AND. Viaja como **array JSON url-encoded**, e não como parâmetro repetido: o valor é texto do operador e qualquer delimitador precisaria de escape inventado, cujo bug apareceria como resultado errado, em silêncio.\n\n**Whitelist deste recurso: `number`, `issuedAt`, `expiresAt`, `customerName`, `projectName`, `workName`, `workId`** — a do `sortBy` mais `workId`, que é COMO a tela pergunta \"os documentos desta obra\", do mesmo jeito que `customerId` responde \"as obras deste cliente\" em `/api/works`. Campo fora da whitelist é 400.\n\nFicam de fora `series` (mesmo valor em toda linha; filtro por campo de valor único não estreita nada) e tudo que trafega em unidade que o operador não digita: `totalCents` em centavos e `discountPercent` em percentual escalado por 10.000 não têm variante que converta na borda — quem digitasse `10` pediria 0,001%.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/ListFilter"
                  }
                }
              }
            }
          },
          {
            "name": "joinOperator",
            "in": "query",
            "description": "Proposto. Como as condições de `filters` se somam. Padrão `and`.",
            "schema": {
              "$ref": "#/components/schemas/ListFilterJoin"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfQuoteDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      },
      "post": {
        "tags": ["vendas"],
        "operationId": "CreateQuote",
        "description": "Proposto. Cria orçamento na empresa ativa. O número é atribuído aqui.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/QuoteWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/QuoteDetailDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "501": {
            "$ref": "#/components/responses/NaoImplementado"
          }
        }
      }
    },
    "/api/quotes/{id}": {
      "get": {
        "tags": ["vendas"],
        "operationId": "GetQuote",
        "description": "Proposto. O documento inteiro, com ambientes e itens.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/QuoteDetailDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": ["vendas"],
        "operationId": "UpdateQuote",
        "description": "Proposto. Substitui o documento INTEIRO, itens e ambientes junto. Orçamento cancelado não aceita alteração — é 409, não 400.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/QuoteWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/QuoteDetailDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "501": {
            "$ref": "#/components/responses/NaoImplementado"
          }
        }
      }
    },
    "/api/quotes/{id}/cancel": {
      "post": {
        "tags": ["vendas"],
        "operationId": "CancelQuote",
        "description": "Proposto. Cancela o orçamento (`status: cancelled`). Documento não se apaga nem se desativa: a listagem continua mostrando, com a situação. Cancelar duas vezes é 409. **Aceita motivo** (`CancelDocumentRequest`), e o corpo é OPCIONAL: quem já cancela hoje sem corpo continua valendo — exigir corpo agora quebraria o cliente que existe. O motivo fica no orçamento e volta em `cancelReasonId` / `cancelReasonName` / `cancelNote` do detalhe. Motivo é 400 quando o id não existe nesta empresa; cancelar duas vezes continua 409, com ou sem motivo.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "required": false,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CancelDocumentRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/QuoteDetailDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/quotes/{id}/revise": {
      "post": {
        "tags": ["vendas"],
        "operationId": "ReviseQuote",
        "description": "Proposto. **Cria a REVISÃO do orçamento** — documento novo, cópia de cabeçalho, ambientes e itens, com `revisionOfId` apontando o anterior e `revision` incrementado. O original fica intacto: foi ele que o cliente viu.\n\nResolve um caso REAL do legado, e não uma hipótese: dois orçamentos do mesmo cliente no mesmo dia, sem nada no dado dizendo que o segundo substitui o primeiro. Quem lia a lista contava dois negócios; quem media conversão dividia por dois.\n\n**Recusas:** orçamento cancelado é 409 (revisar o que foi retirado da mesa é ressuscitar por outro nome); orçamento que JÁ tem revisão é 409 `urn:cabinet:erro:orcamento-ja-revisado` — a segunda revisão sai da PRIMEIRA, senão a cadeia vira árvore e \"qual é a versão vigente\" deixa de ter resposta.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/QuoteDetailDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/crm/pipelines": {
      "get": {
        "tags": ["crm"],
        "operationId": "ListCrmPipelines",
        "description": "Proposto. Funis da empresa ativa. São VÁRIOS por empresa — modelos de venda distintos não cabem num funil só, e forçá-los a caber faz o operador usar o mesmo estágio com dois significados. `sortBy` aceita `name`, `sort` e `active`; fora da lista é 400.",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortBy",
            "description": "Whitelist: `name`, `sort`, `active`. Campo fora dela é 400.\n\n`sort` é a ordem manual do funil, e é por ela que o quadro desenha as colunas.",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfCrmPipelineDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      },
      "post": {
        "tags": ["crm"],
        "operationId": "CreateCrmPipeline",
        "description": "Proposto. Cria funil na empresa ativa. Funil nasce SEM estágio: quem os cria é `POST /api/crm/pipelines/{pipelineId}/stages`. Semear estágio padrão aqui inventaria o modelo de venda da empresa — e é justamente o que varia entre elas.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CrmPipelineWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CrmPipelineDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/crm/pipelines/{id}": {
      "get": {
        "tags": ["crm"],
        "operationId": "GetCrmPipeline",
        "description": "Proposto. Um funil por id. Existe pelo mesmo motivo que o de parceiro: o quadro abre por URL própria (`/crm/funil/{id}`) e recarregar a página não pode exigir voltar à listagem para ter o registro em mãos.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CrmPipelineDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": ["crm"],
        "operationId": "UpdateCrmPipeline",
        "description": "Proposto. Substitui o funil INTEIRO — corpo parcial apaga o que não veio. Os estágios NÃO viajam aqui: eles têm identidade própria e id estável, e a oportunidade aponta para eles. Mandá-los embutidos faria uma edição de nome de funil recriar estágio e órfão de cartão. Desativar é `active: false`, nunca DELETE.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CrmPipelineWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CrmPipelineDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/crm/pipelines/{pipelineId}/stages": {
      "get": {
        "tags": ["crm"],
        "operationId": "ListCrmStages",
        "description": "Proposto. Os estágios DO funil, na ordem de `sort` — as colunas do quadro. Array inteiro, sem paginação: quadro com metade das colunas não é quadro, e um funil tem dezenas de estágios no pior caso, não milhares.",
        "parameters": [
          {
            "name": "pipelineId",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/CrmStageDto"
                  }
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": ["crm"],
        "operationId": "CreateCrmStage",
        "description": "Proposto. Cria estágio NO funil do caminho. O `pipelineId` não viaja no corpo — vem da URL, e assim não há como divergirem.",
        "parameters": [
          {
            "name": "pipelineId",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CrmStageWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CrmStageDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/crm/pipelines/{pipelineId}/stages/{id}": {
      "put": {
        "tags": ["crm"],
        "operationId": "UpdateCrmStage",
        "description": "Proposto. Substitui o estágio INTEIRO. **Não há DELETE de estágio**: apagar coluna com cartão dentro obrigaria o servidor a escolher para onde os cartões vão, e essa escolha é do operador. Estágio que saiu de uso se resolve movendo os cartões e deixando a coluna vazia — o schema mergeado (#66) não tem `active` em `crm_stages`, e inventá-lo aqui criaria campo que o banco não guarda.",
        "parameters": [
          {
            "name": "pipelineId",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CrmStageWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CrmStageDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/crm/opportunities": {
      "get": {
        "tags": ["crm"],
        "operationId": "ListCrmOpportunities",
        "description": "Proposto. Oportunidades da empresa ativa — LEAD e negócio no mesmo recurso: converter é mudar de estágio, não cadastrar de novo. Os filtros são combináveis e todos opcionais. `sortBy` aceita `name`, `partnerName`, `stageName`, `expectedValueCents`, `expectedCloseDate` e `stageChangedAt`; fora da lista é 400. A ordem padrão é a do quadro (`stage.sort`, depois `order`), para que a mesma consulta sirva a listagem e o kanban — e é o que permite à tela alternar quadro ⇄ lista sem refazer a pergunta.",
        "parameters": [
          {
            "name": "pipelineId",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "Só as oportunidades deste funil."
          },
          {
            "name": "stageId",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "Só as deste estágio — uma coluna do quadro."
          },
          {
            "name": "ownerEmployeeId",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "Só as deste responsável."
          },
          {
            "name": "open",
            "in": "query",
            "schema": {
              "type": "boolean"
            },
            "description": "`true` = só as ABERTAS (estágio sem `isWon` nem `isLost`). Ausente = todas. É filtro por PROPRIEDADE do estágio, não por lista de ids: a tela não pode montar a lista de estágios ganhos de cada funil para depois pedir o resto."
          },
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortBy",
            "description": "Whitelist: `name`, `partnerName`, `stageName`, `expectedValueCents`, `expectedCloseDate`, `stageChangedAt`. Campo fora dela é 400.\n\n**NÃO é a mesma de `filters`:** `expectedValueCents` ordena e não filtra. A ordem por centavos é a mesma que por reais, mas o FILTRO não tem variante de dinheiro — `1000` traria R$ 10,00 para quem procura mil reais. É a primeira das duas subtrações do contrato; a outra é a obra, ao contrário.",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "filters",
            "in": "query",
            "description": "Proposto. Filtro estruturado da listagem, somado ao `q` com AND — `q` é texto livre sobre os campos que o recurso escolheu, `filters` é campo a campo. Viaja como **array JSON url-encoded**, e não como parâmetro repetido: o valor é texto do operador e qualquer delimitador precisaria de escape inventado, cujo bug apareceria como resultado errado, em silêncio. Whitelist deste recurso: `name`, `partnerName`, `stageName`, `expectedCloseDate`, `stageChangedAt` — é a do `sortBy` MENOS `expectedValueCents`, e a subtração é a regra do front: dinheiro trafega em centavos e o filtro não tem variante que converta na borda, então `1000` procuraria R$ 10,00 enquanto quem digitou procurava mil reais. Campo fora da whitelist é 400. **A mesma consulta serve às duas visões da tela** (quadro e lista): é o filtro que decide o conjunto, e a visão só decide como ele é desenhado.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/ListFilter"
                  }
                }
              }
            }
          },
          {
            "name": "joinOperator",
            "in": "query",
            "description": "Proposto. Como as condições de `filters` se somam. Padrão `and`.",
            "schema": {
              "$ref": "#/components/schemas/ListFilterJoin"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfCrmOpportunityDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      },
      "post": {
        "tags": ["crm"],
        "operationId": "CreateCrmOpportunity",
        "description": "Proposto. Cria a oportunidade no estágio informado, no FIM da coluna. Sem `stageId` explícito o servidor usa o primeiro estágio do funil; sem `pipelineId`, o funil `isDefault`.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CrmOpportunityWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CrmOpportunityDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/crm/opportunities/{id}": {
      "get": {
        "tags": ["crm"],
        "operationId": "GetCrmOpportunity",
        "description": "Proposto. Uma oportunidade por id. Mesmo DTO da listagem — não há DTO de detalhe, porque o cartão e o formulário mostram o mesmo registro.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CrmOpportunityDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": ["crm"],
        "operationId": "UpdateCrmOpportunity",
        "description": "Proposto. Substitui a oportunidade INTEIRA — corpo parcial apaga o que não veio. Trocar `stageId` por aqui é permitido (é o registro inteiro), mas manda o cartão para o FIM do estágio novo e reinicia `stageChangedAt`: POSICIONAR é o `PATCH /api/crm/opportunities/{id}/stage`. Estágio de OUTRO funil é 400 — o banco recusa pela FK composta, e a resposta diz isso em vez de estourar 500.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CrmOpportunityWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CrmOpportunityDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/crm/opportunities/{id}/stage": {
      "patch": {
        "tags": ["crm"],
        "operationId": "MoveCrmOpportunityStage",
        "description": "Proposto. Mover o cartão de coluna é UMA intenção, logo UMA requisição. O quadro manda o destino (`stageId`) e o VIZINHO (`precedeId`); a reordenação inteira da coluna acontece aqui, numa transação só. A alternativa — o cliente recalcular os índices e mandar um `PUT` por linha deslocada — é o desenho que a colheita de `docs/harvest/kanban-funil/` recusou: com RLS e `SET LOCAL` por transação, cada requisição é uma transação própria, e falha no meio deixa dois cartões no mesmo lugar sem ninguém saber.\n\nÉ `PATCH` e não `PUT` pela mesma razão do quadro de tarefas: o cartão não carrega o registro inteiro, e um `PUT` a partir dele apagaria o que ele não mostra.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CrmOpportunityStagePatchRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CrmOpportunityDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/crm/reports/lost-reasons": {
      "get": {
        "tags": ["crm"],
        "operationId": "GetCrmLostReasonsReport",
        "description": "Proposto. **Por que perdemos, somado no período.** É a pergunta que justifica o catálogo de motivos existir em vez de texto livre, e quem responde é o SERVIDOR: contar do lado do cliente exigiria baixar todas as oportunidades perdidas, e a listagem tem teto de 100 por página — o número sairia certo em empresa pequena e errado, sem sintoma, na primeira que passasse do teto. Ordenado por `count` decrescente: a pergunta é qual é o MAIOR motivo. Entram só as oportunidades em estágio `isLost`, pelo `closedAt` dentro do período.",
        "parameters": [
          {
            "name": "pipelineId",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "Só as perdas deste funil. Ausente = todos os funis da empresa ativa."
          },
          {
            "name": "from",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "format": "date"
            },
            "description": "Primeiro DIA do período, inclusive. Obrigatório: contagem sem recorte responde outra pergunta e cresce para sempre — um motivo aposentado há três anos continuaria liderando o quadro."
          },
          {
            "name": "to",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "format": "date"
            },
            "description": "Último DIA do período, inclusive. O recorte é por DIA e não por instante: quem pergunta por agosto quer o dia 31 inteiro."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CrmLostReasonsReportDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/crm/lost-reasons": {
      "get": {
        "tags": ["crm"],
        "operationId": "ListCrmLostReasons",
        "description": "Proposto. Motivos de perda catalogados da empresa ativa. É cadastro e não texto livre porque a pergunta que ele responde é \"por que perdemos\" somada no ano — texto livre vira trinta grafias da mesma coisa. `sortBy` aceita `name` e `active`.",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortBy",
            "description": "Whitelist: `name`, `active`. Campo fora dela é 400.",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfCrmLostReasonDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      },
      "post": {
        "tags": ["crm"],
        "operationId": "CreateCrmLostReason",
        "description": "Proposto. Cria motivo de perda na empresa ativa.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CrmLostReasonWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CrmLostReasonDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/crm/lost-reasons/{id}": {
      "put": {
        "tags": ["crm"],
        "operationId": "UpdateCrmLostReason",
        "description": "Proposto. Substitui o motivo INTEIRO; desativar é `active: false`. Não há GET por id: o registro tem dois campos e a LINHA da listagem já é o registro inteiro — caminho de detalhe aqui seria requisição para buscar o que a tela já tem.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CrmLostReasonWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CrmLostReasonDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/crm/opportunities/{id}/quote": {
      "post": {
        "tags": ["crm"],
        "operationId": "CreateQuoteFromOpportunity",
        "description": "Proposto. Gera o ORÇAMENTO da oportunidade e grava o vínculo (`quoteId`) na mesma transação.\n\n**Caminho próprio, e não `POST /api/quotes` seguido de `PUT` na oportunidade**, pela mesma razão do `PATCH …/{id}/stage`: com RLS e `SET LOCAL` por transação, cada requisição é transação própria, e falha entre as duas deixaria um orçamento órfão — criado, sem vínculo, invisível para quem pediu a conversão e visível na listagem de orçamentos. Uma intenção, uma requisição.\n\n**O orçamento nasce VAZIO de item**, com cliente e nome do projeto copiados da oportunidade. É a amarra do núcleo: a oportunidade não congela especificação nem preço — quem congela é o orçamento, e ele só passa a valer quando o operador o preenche. Copiar o `expectedValueCents` para um item inventado daria documento com preço que ninguém cotou.\n\n**Oportunidade sem `partnerId` é 400**: o orçamento exige `customerId`, e lead sem cadastro não tem. O caminho é cadastrar o parceiro antes — a tela oferece isso.\n\n**Oportunidade que já tem `quoteId` é 409**, não um segundo orçamento: dois documentos para o mesmo negócio é justamente o que o vínculo existe para impedir. Refazer é cancelar o orçamento e gerar de novo, e essa é uma decisão do operador.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/QuoteDetailDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/activities": {
      "get": {
        "tags": ["tarefas"],
        "operationId": "ListActivities",
        "description": "Proposto. Atividades agendadas da empresa ativa — a tabela POLIMÓRFICA `activities` do schema mergeado (#66). UM recurso serve oportunidade, parceiro, orçamento e pedido de compra: atividade dentro de cada módulo seria a mesma coisa escrita quatro vezes, e a primeira tela que perguntasse \"o que me espera hoje\" teria de somar quatro listagens. O recorte de um registro é `entityType` e `entityId` JUNTOS — `entityId` sozinho é 400, porque uuid sem a tabela não identifica nada. `sortBy` aceita `dueDate`, `doneAt`, `kind` e `title`; fora da lista é 400. A ordem padrão é a do painel: pendentes primeiro, por prazo crescente e sem prazo por último, depois as concluídas da mais recente para a mais antiga.",
        "parameters": [
          {
            "name": "entityType",
            "in": "query",
            "description": "Proposto. Tabela do registro dono. Vai junto com `entityId`; um sem o outro é 400.",
            "schema": {
              "type": "string",
              "enum": ["opportunity", "partner", "quote", "purchaseOrder"]
            }
          },
          {
            "name": "entityId",
            "in": "query",
            "description": "Proposto. Id do registro dono, dentro de `entityType`.",
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "open",
            "in": "query",
            "description": "Proposto. `true` traz só o que ainda espera alguém (`doneAt` nulo), `false` só o concluído. Ausente traz os dois — o painel mostra pendência e histórico na mesma consulta, e separar em duas daria duas paginações para uma lista só.",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "assigneeEmployeeId",
            "in": "query",
            "description": "Proposto. Só as atividades deste responsável. É o recorte que uma tela de \"minhas atividades\" pede, e ele não se monta no cliente: o front não tem a lista inteira em mãos.",
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortBy",
            "description": "Whitelist: `dueDate`, `doneAt`, `kind`, `title`. Campo fora dela é 400.\n\n`assigneeName` fica de fora: é junção com o colaborador. Esta operação NÃO publica `filters`, e pedir o parâmetro é 400 — a agenda recorta por `entityType`/`entityId`, que são caminho, não filtro.",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfActivityDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      },
      "post": {
        "tags": ["tarefas"],
        "operationId": "CreateActivity",
        "description": "Proposto. Agenda uma atividade sobre o registro informado, na empresa ativa. O alvo vai no CORPO, e não no caminho, porque o dono é um par (`entityType`, `entityId`) e não uma rota — caminho por módulo devolveria os quatro endpoints que a tabela polimórfica existe para evitar. Alvo inexistente é 404. **Não há DELETE**: atividade agendada por engano se corrige alterando ou concluindo, e o que foi agendado é parte do histórico que a análise lê.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ActivityWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ActivityDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/activities/{id}": {
      "put": {
        "tags": ["tarefas"],
        "operationId": "UpdateActivity",
        "description": "Proposto. Substitui a atividade INTEIRA — corpo parcial apaga o que não veio. É por aqui que se remarca prazo e se troca responsável. O alvo viaja porque o corpo é o registro completo, mas MUDÁ-LO é 400: a atividade pertence ao registro onde nasceu, e repontá-la faria a linha sumir do painel que a mostra sem aparecer em nenhum outro. Concluir não é aqui — é `POST /api/activities/{id}/done`.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ActivityWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ActivityDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/activities/{id}/done": {
      "post": {
        "tags": ["tarefas"],
        "operationId": "CompleteActivity",
        "description": "Proposto. Conclui a atividade: quem carimba `doneAt` é o servidor, com a hora dele. Verbo próprio, e não `PUT` com `doneAt` preenchido, pela mesma razão do `PATCH` do quadro de tarefas — o painel mostra a LINHA, não o registro inteiro, e um `PUT` montado a partir dela apagaria prazo, responsável e observação. Concluir a já concluída é 409: repetir o carimbo reescreveria a data do que já é histórico. **Não há como desconcluir** — atividade concluída é registro do que aconteceu; o que se refaz é uma atividade nova.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ActivityDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/employees/{id}": {
      "get": {
        "tags": ["cadastros"],
        "operationId": "GetEmployee",
        "description": "Proposto. A ficha, com os campos do vínculo resolvidos pela empresa ativa.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EmployeeDetailDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": ["cadastros"],
        "operationId": "UpdateEmployee",
        "description": "Proposto. Substitui os dados de ORGANIZAÇÃO. Não toca no vínculo: cargo, setor e papel mudam por `/link`.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/EmployeeWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EmployeeDetailDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/employees/{id}/link": {
      "post": {
        "tags": ["cadastros"],
        "operationId": "LinkEmployee",
        "description": "Proposto. Vincula à empresa ATIVA da sessão — nunca a uma empresa passada no corpo, que seria escrever no vizinho por parâmetro. Vínculo já existente é 409.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/EmployeeLinkRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EmployeeDetailDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": ["cadastros"],
        "operationId": "UpdateEmployeeLink",
        "description": "Proposto. Substitui o vínculo com a empresa ativa — é por aqui que cargo e setor mudam. Colaborador sem vínculo aqui é 404: alterar o que não existe não vira criação silenciosa.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/EmployeeLinkRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EmployeeDetailDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/permissions": {
      "get": {
        "tags": ["acesso"],
        "operationId": "ListPermissions",
        "description": "Proposto. O CATÁLOGO de permissões — tudo que um papel pode conceder, agrupado por módulo. É a lista de caixas que a tela de papéis desenha, e não é dado da empresa: vem do CÓDIGO do servidor, é igual para todas as organizações e só muda em deploy (api#84).\n\n**Viaja por HTTP justamente para o front NÃO ter a lista.** Só dev acrescenta permissão, e do lado do servidor — uma cópia aqui envelheceria em silêncio, e a tela desenharia o conjunto de ontem sem ninguém perceber. Pelo mesmo motivo o rótulo vem junto: permissão nova sem rótulo do servidor apareceria como chave crua ao lado da caixa.\n\nA granularidade é por AÇÃO, no formato `modulo:acao` — `produtos:editar`, `estoque:movimentar`, `depositos:gerenciar`, `orcamento:imprimir`. As chaves NÃO são enum deste contrato de propósito: congelá-las aqui faria cada permissão nova exigir PR de contrato mais codegen, que é exatamente o acoplamento que este caminho existe para desfazer.\n\nSem paginação: o catálogo é o conjunto inteiro, sempre. Página aqui entregaria tela de checkbox com metade das caixas, e o admin gravaria papel sem as permissões que não chegou a ver.",
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PermissionCatalogDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      }
    },
    "/api/roles": {
      "get": {
        "tags": ["acesso"],
        "operationId": "ListRoles",
        "description": "Proposto. Papéis da ORGANIZAÇÃO — os de sistema, os templates de fábrica e os que o admin criou, na mesma lista. Papel é da organização e a ATRIBUIÇÃO é por empresa (`employee_company`): a mesma pessoa pode ser \"Financeiro\" numa empresa do grupo e nada na outra.\n\nA linha traz `permissionCount`, não o conjunto de permissões: a coluna que a tela desenha é \"12 permissões\", e mandar o array inteiro em cada linha multiplicaria o corpo pelo catálogo sem ninguém ler. Quem quer as caixas marcadas abre `GET /api/roles/{id}`.\n\n`sortBy` aceita `name` e `active`.",
        "parameters": [
          {
            "name": "q",
            "description": "Busca por nome do papel.",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortBy",
            "description": "Whitelist: `name`, `active`. Campo fora dela é 400.",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfRoleDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      },
      "post": {
        "tags": ["acesso"],
        "operationId": "CreateRole",
        "description": "Proposto. Cria papel na organização, com as permissões que vierem no corpo. Nasce sempre `system: false` e `template: false` — as duas marcas são do servidor, e papel que se declara de sistema pelo corpo seria o caminho para burlar a recusa de edição.\n\n**Clonar um template é ler `GET /api/roles/{id}` e postar as `permissions` que vieram.** Não há operação de clonagem porque ela não diria nada que o corpo já não diga — e o admin quase sempre muda algo antes de gravar, que é o ponto de partir de um template.\n\nNome repetido na organização é 409 `about:blank`: a tela não tem saída própria a oferecer, quem criou corrige o nome ali mesmo. Permissão que não está no catálogo é 400 `urn:cabinet:erro:campos-invalidos`, com `fields[]` apontando `permissions`.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RoleWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RoleDetailDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/roles/{id}": {
      "get": {
        "tags": ["acesso"],
        "operationId": "GetRole",
        "description": "Proposto. O papel com o conjunto de permissões — é este corpo que marca as caixas da tela, e é por ele que se clona um template.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RoleDetailDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": ["acesso"],
        "operationId": "UpdateRole",
        "description": "Proposto. Substitui o papel INTEIRO — `permissions` que vier é o conjunto final, não um acréscimo: desmarcar caixa precisa ter efeito, e corpo que só soma nunca tira permissão.\n\n**Papel de sistema é 409 `urn:cabinet:erro:papel-de-sistema`,** em alterar e em desativar. `owner` e `admin` são a garantia de que a organização não se tranca para fora: um admin que se desmarcasse da permissão de gerenciar papéis perderia a própria tela que devolveria a permissão. Template de fábrica NÃO é de sistema — ele é da organização e o admin edita à vontade; `template: true` só conta de onde o papel veio.\n\n**Não há `DELETE`.** Vale o padrão de cadastro do produto: desativação lógica por `active: false`. Papel apagado deixaria vínculo apontando para o nada, e \"quem podia o quê em março\" ficaria sem resposta. Papel inativo some da escolha do vínculo e continua legível no que já foi atribuído.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RoleWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RoleDetailDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/orders": {
      "get": {
        "tags": ["vendas"],
        "operationId": "ListOrders",
        "description": "Proposto. Pedidos de venda da empresa ativa. `sortBy` aceita `number`, `issuedAt`, `customerName`, `projectName`, `workName`; fora da lista é 400.",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortBy",
            "description": "Whitelist: `number`, `issuedAt`, `customerName`, `projectName`, `workName`. Campo fora dela é 400 — a lista não volta em outra ordem, ela não volta.\n\nÉ a de `filters` MENOS `workId` — inclusive `customerName`, `projectName` e `workName`, que vêm de junção: aqui o nome é o que a tela lê e por onde ela ordena. `series`, `totalCents` e `discountPercent` ficam de fora, pelos motivos escritos na descrição de `filters`.\n\n`workName` ordena pelo nome da OBRA (`workId` resolvido), e não pelo `projectName` digitado: são dois dados, e ordenar pelo texto livre embaralha documentos da mesma obra. `workId` fica fora daqui — ordenar por uuid não põe nada em ordem para quem lê; ele serve em `filters`, para recortar.",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "filters",
            "in": "query",
            "description": "Proposto. Filtro estruturado do pedido, somado ao `q` com AND. Viaja como **array JSON url-encoded**, e não como parâmetro repetido: o valor é texto do operador e qualquer delimitador precisaria de escape inventado, cujo bug apareceria como resultado errado, em silêncio.\n\n**Whitelist deste recurso: `number`, `issuedAt`, `customerName`, `projectName`, `workName`, `workId`, `status`, `type`** — a do `sortBy` mais `workId`, que é COMO a tela pergunta \"os documentos desta obra\", do mesmo jeito que `customerId` responde \"as obras deste cliente\" em `/api/works`. Campo fora da whitelist é 400.\n\nFicam de fora `series` (mesmo valor em toda linha; filtro por campo de valor único não estreita nada) e tudo que trafega em unidade que o operador não digita: `totalCents` em centavos e `discountPercent` em percentual escalado por 10.000 não têm variante que converta na borda — quem digitasse `10` pediria 0,001%.\n\n**`status` e `type` entram com o ciclo de vida (G13), e é o que torna os dois estados úteis:** sem eles a tela não tem como perguntar \"o que ainda está em aberto\" nem \"quais demonstrações estão na rua\" — teria de puxar a lista inteira e contar no cliente, que é a conta que mente na página 2. Os dois são de VOCABULÁRIO FECHADO, então `eq`/`ne`/`inArray` bastam; valor fora do enum é 400, e não lista vazia: filtro que não casa nada e filtro que não existe são indistinguíveis para quem lê a tela.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/ListFilter"
                  }
                }
              }
            }
          },
          {
            "name": "joinOperator",
            "in": "query",
            "description": "Proposto. Como as condições de `filters` se somam. Padrão `and`.",
            "schema": {
              "$ref": "#/components/schemas/ListFilterJoin"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfOrderDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      },
      "post": {
        "tags": ["vendas"],
        "operationId": "CreateOrder",
        "description": "Proposto. Cria pedido direto, sem orçamento de origem. O número é atribuído aqui, na mesma sequência global do grupo.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/OrderWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OrderDetailDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "501": {
            "$ref": "#/components/responses/NaoImplementado"
          }
        }
      }
    },
    "/api/orders/{id}": {
      "get": {
        "tags": ["vendas"],
        "operationId": "GetOrder",
        "description": "Proposto. O documento inteiro, com ambientes e itens.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OrderDetailDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": ["vendas"],
        "operationId": "UpdateOrder",
        "description": "Proposto. Substitui o documento INTEIRO. Pedido cancelado não aceita alteração — é 409, não 400.\n\n**Pedido `concluded` é 409, pela mesma razão que `cancelled` é:** o documento encerrado é o que foi combinado, e reescrevê-lo muda o passado de quem já leu. Corrigir pedido concluído é reabrir — e reabertura não existe de propósito: o legado não tem o estado, então não há caso real para copiar.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/OrderWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OrderDetailDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "501": {
            "$ref": "#/components/responses/NaoImplementado"
          }
        }
      }
    },
    "/api/orders/{id}/cancel": {
      "post": {
        "tags": ["vendas"],
        "operationId": "CancelOrder",
        "description": "Proposto. Cancela o pedido (`status: cancelled`). Documento não se apaga: a listagem continua mostrando, com a situação. Cancelar duas vezes é 409. **Aceita motivo** (`CancelDocumentRequest`), e o corpo é OPCIONAL: quem já cancela hoje sem corpo continua valendo — exigir corpo agora quebraria o cliente que existe. O motivo fica no pedido e volta em `cancelReasonId` / `cancelReasonName` / `cancelNote` do detalhe. Motivo é 400 quando o id não existe nesta empresa; cancelar duas vezes continua 409, com ou sem motivo.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "required": false,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CancelDocumentRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OrderDetailDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/orders/{id}/conclude": {
      "post": {
        "tags": ["vendas"],
        "operationId": "ConcludeOrder",
        "description": "Proposto. **Conclui o pedido** — `active → concluded`. É a \"Conclusão do Pedido de Venda\" do menu do legado (`FrmFecha_projeto`): o ponto em que o documento para de ser trabalho em curso. Carimba `closedAt` com a data de hoje quando ela está nula; a que o operador digitou fica como está.\n\n**Recusas:** pedido cancelado, ou já concluído, é 409 `urn:cabinet:erro:transicao-invalida` — os dois estados são terminais. Demonstração com peça fora (`demoReturnedAt` nulo) é 409 `urn:cabinet:erro:demonstracao-em-aberto`: concluir empréstimo sem o retorno perderia o rastro do que está na rua.\n\n**O que esta operação NÃO faz, e continua sendo blocker escrito:** no legado a conclusão exige as ENTREGAS fechadas, ou força com permissão especial (`QryConclusaoPedido` do `frmControleEntrega` só carimba a data quando nenhuma linha tem entregue abaixo do vendido). **A metade que faltava chegou:** `/api/deliveries` existe, o estado físico existe (`GET /api/orders/{id}/fulfillment`), e a pergunta \"este pedido saiu inteiro?\" tem resposta — é `physicalState: delivered`. O que ainda NÃO existe é a PRÉ-CONDIÇÃO aqui: concluir segue sem olhar o físico, e um `force` sem recusa para forçar continuaria sendo botão sem efeito. Ligar as duas é decisão do trilho do ciclo do documento, não efeito colateral de publicar a entrega — e quando ela vier, o `force` nasce com o que forçar.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OrderDetailDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/orders/{id}/demo-return": {
      "post": {
        "tags": ["vendas"],
        "operationId": "ReturnDemoOrder",
        "description": "Proposto. **Registra o RETORNO da demonstração** — carimba `demoReturnedAt` e devolve a peça ao saldo. O empréstimo tem duas pontas e esta é a segunda: sem ela, \"emprestado\" e \"vendido\" tiram do estoque do mesmo jeito, e o que está na rua vira diferença de balanço.\n\n**Não mexe no estado do documento.** Demonstração que voltou pode virar venda, e concluir é decisão de quem vendeu, não consequência de a peça ter voltado.\n\n**Recusas:** pedido `sale` é 409 `urn:cabinet:erro:transicao-invalida` — não há o que devolver. Devolver duas vezes, idem. Pedido cancelado também: o cancelamento já devolveu.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OrderDetailDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/orders/{id}/professional": {
      "post": {
        "tags": ["vendas"],
        "operationId": "TransferOrderProfessional",
        "description": "Proposto. **Transfere a venda entre profissionais** — a opção \"Transferência de Venda entre Profissionais\" do menu do legado. Fecha a vigência corrente em `VendaIndicacao` e abre outra; o documento passa a apontar o novo profissional.\n\nÉ operação própria, e não campo do `PUT`, porque a troca **tem data** e a comissão pergunta por ela. O `PUT` do pedido não move `professionalId` — quem o mandasse ali estaria reescrevendo a vigência inteira sem dizer.\n\n**Recusas:** parceiro sem papel `professional` é 400 apontando o campo; transferir para o MESMO profissional é 409 (vigência de duração zero não é trilha, é ruído); pedido cancelado ou concluído é 409 — documento encerrado não troca de dono.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TransferProfessionalRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OrderDetailDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/orders/{id}/professional-history": {
      "get": {
        "tags": ["vendas"],
        "operationId": "ListOrderProfessionalHistory",
        "description": "Proposto. **A trilha de indicação do pedido**, da mais recente para a mais antiga. A linha com `endedAt` nulo é a corrente, e é ela que casa com `OrderDetailDto.professionalId` — se as duas divergirem, a trilha mente, e é por isso que ela vem do mesmo lugar e não de um espelho.\n\nPublica paginação como toda listagem, mas o conjunto é pequeno por natureza: a trilha de um documento tem tantas linhas quantas transferências houve.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "sortBy",
            "in": "query",
            "description": "Whitelist: `startedAt`. Uma só, e de propósito: a trilha é cronológica, e ordenar por nome de profissional esconderia a sequência que é o ponto dela.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfOrderProfessionalAssignmentDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/quotes/{id}/order": {
      "post": {
        "tags": ["vendas"],
        "operationId": "CreateOrderFromQuote",
        "description": "Proposto. Converte o orçamento em pedido de venda. No legado a conversão era troca de `Ven_Tipo` no mesmo registro; aqui são dois agregados, então converter é COPIAR — cabeçalho, ambientes e itens, com o preço congelado no momento da conversão. O orçamento fica intacto e rastreável por `OrderDto.quoteId`. Orçamento cancelado é 409, e converter duas vezes também: pedido em duplicata é o erro que o operador não vê até a compra sair dobrada.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OrderDetailDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/services": {
      "get": {
        "tags": ["catalogo"],
        "operationId": "ListServices",
        "description": "Proposto. O cadastro de SERVIÇOS da empresa ativa — instalação, projeto, entrega: o que a Vertz cobra à parte do material. É o item #7 dos cadastros do comparativo Softlux, e no legado ele é a tabela `Servicos` (16 colunas).\n\n**Serviço não é produto, e a diferença não é de nomenclatura.** O legado já os separa em tabelas próprias, e a razão aparece nas colunas: serviço não tem variante (acabamento × tamanho), não tem saldo, não entra no kardex, e tem duas coisas que produto nenhum tem — o percentual do eletricista, que é quanto da linha vai para quem instala, e o código do serviço na NFS-e, que é outro documento fiscal. Metê-lo em `/api/products` obrigaria toda variante, todo saldo e todo movimento a carregar um `isService` que nunca é verdadeiro.\n\n**`GrupoProduto` do legado guarda 1000 = SERVIÇOS e 1001 = FRETE como pseudo-produtos** — a gambiarra que existia para o serviço caber na tela de produto. Aqui ela não se repete: o grupo continua sendo campo do serviço (`productGroup`), e o serviço tem família própria.\n\n**Não publica `filters` de propósito.** O filtro estruturado é opt-in por recurso e cobra whitelist dos dois lados; o cadastro de serviço de uma empresa é lista curta (198 linhas em `ISSQNServicos`, ordem de grandeza parecida no cadastro), e `q` mais `sortBy` respondem o que a tela pergunta. Ele entra no dia em que uma tela precisar, e aí com whitelist escrita.",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortBy",
            "description": "Whitelist: `code`, `description`, `priceCents`, `active`. Campo fora dela é 400.\n\n`priceCents` entra porque \"do mais caro para o mais barato\" é pergunta que o operador faz de verdade num cadastro de preço — e ordenar centavos é ordenar inteiro, sem a armadilha de comparar dinheiro como texto. `electricianPercent`, `type` e `productGroup` ficam fora: os dois últimos são texto livre no legado (`varchar(12)` sem FK), e coluna cujo vocabulário não está normalizado ordena por acaso de digitação.",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfServiceDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      },
      "post": {
        "tags": ["catalogo"],
        "operationId": "CreateService",
        "description": "Proposto. Cria um serviço na empresa ativa.\n\n**Papel: `operator-full` ou superior**, a mesma linha de corte de `products` e `catalog-lookups`. O critério não é o de estoque nem o de atendimento: é o de quem mexe no CATÁLOGO que os documentos de todo mundo vão congelar. Preço de serviço errado entra em orçamento novo até alguém notar, e o percentual do eletricista errado vira pagamento errado a quem instalou.\n\n409 quando `code` já existe na empresa, e quando não há empresa ativa na sessão.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ServiceWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ServiceDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict — `code` repetido na empresa, ou sem empresa ativa na sessão.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/services/{id}": {
      "put": {
        "tags": ["catalogo"],
        "operationId": "UpdateService",
        "description": "Proposto. Substitui o serviço INTEIRO; desativar é `active: false`.\n\n**Não há DELETE** — nem aqui nem em lugar nenhum do contrato. Serviço apagado deixaria linha de orçamento e de pedido apontando para cadastro inexistente, e o documento já congelou a descrição e o preço justamente para não depender dele. Desativação lógica é o padrão 8, e aqui ela também é integridade referencial.\n\n**Não há GET por id**, pela razão que já valeu para o depósito e para o motivo de perda: o `ServiceDto` é PLANO — sem sub-recurso, sem variante, sem coleção embutida — então a LINHA da listagem já é o registro inteiro, e um caminho de detalhe seria uma requisição para buscar o que a tela tem na mão. Quando o cadastro ganhar sub-recurso (tributação por município, que o legado tem em `TributacaoServico`), o detalhe entra junto com ele.\n\n**Alterar o preço aqui NÃO reescreve documento nenhum.** `QuoteServiceItemDto` congela `description`, `unitPriceCents` e `electricianPercent` na emissão, pelo mesmo motivo que `QuoteItemDto` congela os do produto. `priceLocked` é sobre a próxima linha a ser INSERIDA, não sobre as que já existem.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ServiceWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ServiceDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found — o serviço não é da empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — `code` repetido na empresa, ou sem empresa ativa na sessão.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/stock-locations": {
      "get": {
        "tags": ["estoque"],
        "operationId": "ListStockLocations",
        "description": "Proposto. Os DEPÓSITOS da empresa ativa — a dimensão que faltava no estoque. Hoje o saldo é por variante × empresa; o legado tem `EstTp_Codigo` na chave de `Estoque_produto`, com quatro locais, e carregar sem a dimensão soma os quatro num número só. A perda é irreversível na carga, não dívida de tela.\n\n**Árvore, e ela vem PLANA.** Os cinco níveis do legado (Estoque/Prédio/Rua/Número/Apto) são profundidade, não cinco colunas: cada linha traz `parentId`, e quem monta a árvore é quem desenha. A tela pede o conjunto inteiro (`pageSize` no teto) e diz no rodapé quando o teto cortou — é o padrão 9, e é o que torna a montagem no cliente honesta.\n\n**Não publica `parentName`, e a razão vale para toda esta família.** Depósito é punhado de linhas por empresa: quem recebeu o conjunto já tem o pai na mão, e um nome derivado seria segunda cópia do que a resposta acabou de entregar. O precedente de `parentName` em parceiro não transfere — lá o pai fica FORA da página, entre milhares. Pela mesma razão nem `StockBalanceDto` nem `StockMovementDto` carregam o nome do depósito: quem lê saldo ou kardex pede esta listagem uma vez e resolve os nomes com ela.\n\n**Não publica `filters` de propósito.** O filtro estruturado é opt-in por recurso e cobra whitelist dos dois lados; sobre um punhado de linhas que já vieram inteiras ele seria parâmetro sem consumidor — e recorte por `parentId` ou por `active` é escolha de quem desenha a árvore, feita sobre o conjunto que já está em mãos.",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortBy",
            "description": "Whitelist: `code`, `name`, `active`. Campo fora dela é 400.\n\n`parentId` NÃO entra, pelo mesmo motivo que tirou `customerId` do `sortBy` da obra: ordem de uuid não põe nada em ordem para quem lê a tela. E `isDefault` também não — há UM padrão por empresa, e ordenar por uma coluna que separa 1 linha de todas as outras é agrupar, não ordenar.",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfStockLocationDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      },
      "post": {
        "tags": ["estoque"],
        "operationId": "CreateStockLocation",
        "description": "Proposto. Cria um depósito na empresa ativa.\n\n**Papel: `admin` ou superior — e é INTERINO.** Criar depósito muda a leitura de saldo de todo mundo e não é operação de atendimento, então a linha nasce junto de `/api/employees` e não de `/api/variants`. Vira a permissão nomeada `depositos:gerenciar` quando o modelo por AÇÃO (api#84) entregar — o papel aqui é o piso enquanto a matriz é por papel.\n\n`isDefault` não entra no corpo: o padrão da empresa nasce do SERVIDOR (ver `CreateStockMovement`), e trocá-lo é outra operação — teria de apagar o padrão anterior na mesma transação para não haver dois, e essa operação ainda não está publicada.\n\n404 quando `parentId` aponta para depósito que a empresa ativa não tem: do ponto de vista de quem pergunta, o pai não está lá. 409 quando `code` já existe na empresa.\n\nCiclo não é caso desta operação — nó que acaba de nascer não tem descendente para se pendurar. Ele é caso do `PUT`, e lá a escrita é que confere o ancestral: o banco pega a auto-referência de um nível com CHECK, mas A→B→A não é exprimível em constraint.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/StockLocationWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StockLocationDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found — o `parentId` não é depósito da empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — `code` repetido na empresa, pai que fecha ciclo, ou sem empresa ativa na sessão.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/stock-locations/{id}": {
      "put": {
        "tags": ["estoque"],
        "operationId": "UpdateStockLocation",
        "description": "Proposto. Substitui o depósito INTEIRO; desativar é `active: false`.\n\n**Não há DELETE** — nem aqui nem em lugar nenhum do contrato. Apagar depósito deixaria saldo órfão em `stock_balances` e movimento apontando para local inexistente; desativação lógica é o padrão 8 e aqui ela também é integridade.\n\n**Não há GET por id**, pela razão que já valeu para o motivo de perda: a LINHA da listagem é o registro inteiro (seis campos, sem sub-recurso), e um caminho de detalhe seria requisição para buscar o que a tela já tem.\n\n409 em dois casos que mantêm a coerência do padrão: desativar o depósito PADRÃO da empresa (o servidor voltaria a criá-lo no movimento seguinte, e um padrão inativo é estado que não se sustenta) e pendurar o nó em descendente próprio, fechando ciclo. Desativar depósito COM saldo é permitido e não move nada: o saldo continua na listagem de saldos, e o que o depósito inativo perde é entrar em movimento novo.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/StockLocationWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StockLocationDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found — o depósito, ou o `parentId`, não é da empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — `code` repetido, ciclo na árvore, desativação do depósito padrão, ou sem empresa ativa na sessão.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/variants/{variantId}/stock-balances": {
      "get": {
        "tags": ["estoque"],
        "operationId": "ListStockBalances",
        "description": "Proposto. O saldo da variante em CADA depósito da empresa ativa.\n\n`stock_balances` é cache DERIVADO do kardex, como manda o ADR-009 — e agora a reconciliação tem dois níveis: o último `balanceAfter` de cada (depósito, variante) bate com o `qty` daqui, e a SOMA dos depósitos bate com o `stockQty` de `ProductVariantDto`. Divergir é bug ou fraude, nunca rotina.\n\n**Depósito sem linha de saldo não aparece com zero.** A resposta traz só onde existe linha, e variante que nunca esteve em depósito nenhum responde lista VAZIA. Completar com zero por depósito cadastrado afirmaria contagem que ninguém fez — e um depósito que nunca viu a peça diz outra coisa de um que a zerou.\n\n**O inverso não está publicado** — o inventário de um depósito (todas as variantes que ele guarda) é outra pergunta, com outra tela e outra paginação, e nenhuma das duas existe ainda. Publicar consulta sem consumidor é dívida dos dois lados.",
        "parameters": [
          {
            "name": "variantId",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "sortBy",
            "description": "Whitelist: `qty`. Campo fora dela é 400.\n\nÉ lista curta e a única ordem que responde pergunta de quem lê é \"onde tem mais\". `locationId` não ordena (uuid), e o nome do depósito não está nesta resposta de propósito — ver `ListStockLocations`. Sem `sortBy` a ordem é a da chave, estável e sem significado: quem recebeu o punhado de linhas ordena por nome com a listagem de depósitos em mãos.",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfStockBalanceDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found — a variante não existe.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/payment-terms": {
      "get": {
        "tags": ["vendas"],
        "operationId": "ListPaymentTerms",
        "description": "Proposto. As CONDIÇÕES DE PAGAMENTO da empresa ativa — `Forma_Pagamento` do legado (16 linhas, `Emp_codigo` na coluna), com as parcelas embutidas.\n\n**As parcelas viajam DENTRO da condição, e não em sub-recurso.** É o argumento do `QuoteDetailDto`, um nível abaixo: `Forma_Pagamento_Parcela` tem PK `(Fpg_codigo, Fpp_parcela)` — a parcela não tem identidade fora da condição —, e gravar linha a linha faria um `Gravar` virar N requisições sem transação entre elas, deixando meio plano no banco quando a terceira falhasse.\n\n**Por isso também não há detalhe por id nesta família.** A coleção é um punhado de linhas por empresa e a listagem já entrega as parcelas inteiras; um `GET /{id}` devolveria a segunda cópia do que a listagem acabou de dar. Mesmo desenho de `/api/catalog-lookups` e `/api/stock-locations`.\n\n**E não é lista de apoio.** `catalog_lookups` guarda `kind`/`name`/`active` e nada mais; condição de pagamento carrega parcela, dias e percentual. Enfiá-la lá daria o rótulo sem os dados que decidem o vencimento — a regra do ADR-011 (`kind` novo é linha no servidor, nunca PR de contrato) vale nos dois sentidos: o que carrega dado não vira `kind`.\n\n**Não publica `filters` de propósito**, pela razão de `/api/stock-locations`: sobre um punhado de linhas que já vieram inteiras, o filtro estruturado seria parâmetro sem consumidor, cobrando whitelist dos dois lados.\n\n**Os ajustes por grupo viajam junto**, em `groupAdjustments`, pela mesma razão das parcelas: a linha não tem identidade fora da condição. Não se ordena por eles, e o argumento é o de `installments` — ordenar a coleção por um campo do filho é ordenar por qual dos N.",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "description": "Busca por `name`.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortBy",
            "in": "query",
            "description": "Whitelist: `name`, `active`, `installmentCount`. Campo fora dela é 400.\n\nNão se ordena por parcela: ela é linha de dentro da condição, e ordenar a coleção por um campo do filho é ordenar por qual dos N — pergunta que não tem resposta única. `installmentCount` entra porque é COLUNA (ver `PaymentTermDto`), e é a que responde \"quais condições parcelam mais\".",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfPaymentTermDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      },
      "post": {
        "tags": ["vendas"],
        "operationId": "CreatePaymentTerm",
        "description": "Proposto. Cria uma condição de pagamento na empresa ativa.\n\n**Papel: `admin` ou superior — e é INTERINO.** Mesma linha de corte que pôs `/api/stock-locations` acima de `/api/variants`: parcelar não é operação de atendimento, é a regra que decide o que TODO vendedor pode oferecer, e um plano errado sai em documento assinado antes de alguém notar. Vira permissão nomeada quando o modelo por AÇÃO (api#84) entregar; até lá o papel é o piso, porque a matriz é por papel.\n\n400 quando o plano não fecha — os modos estão em `PaymentTermInstallmentWriteRequest`, e nenhum deles é aparado em silêncio. Mais parcelas que o teto da empresa é 400 com `type` PRÓPRIO (`urn:cabinet:erro:parcelas-acima-do-teto`): a tela recorta o formulário pelo número que cabe, e para isso precisa distinguir esse caso dos erros de campo.\n\n409 quando a empresa já tem condição com o mesmo `name`: no legado a PK é `(Fpg_codigo, Fpg_descricao)`, e nome repetido num combo é escolha que não dá para fazer certo — as duas linhas parecem a mesma para quem escolhe.\n\n---\n\n**As recusas de `groupAdjustments`, e nenhuma apara em silêncio** (400 com `fields[]` apontando `groupAdjustments.<n>.<campo>`):\n\n1. `productGroupId` REPETIDO na mesma condição. A chave da linha é o grupo — duas linhas do mesmo grupo não dizem qual ajuste vale. Mesma recusa de `groupDiscounts` no documento.\n2. `productGroupId` que não é um `catalog-lookups` **ativo** de kind `GRUPO_PRODUTO`. Id de outro `kind` é 400 e não 404: o que está errado é o campo do corpo, não o recurso pedido.\n3. Percentual NEGATIVO em qualquer um dos dois. Desconto negativo é acréscimo escrito ao contrário, e a coluna do acréscimo existe ao lado.\n4. `discountPercent` acima de `1000000` (100%). Desconto que passa do total faz a linha do grupo valer negativo. **`surchargePercent` não tem teto de propósito** — dobrar o preço de um grupo é estranho e é legítimo, e um teto inventado aqui recusaria dado real do legado na importação.\n5. Os DOIS maiores que zero na mesma linha. Não é aparadura, é falta de leitura única: não está decidido se o acréscimo incide antes ou depois do desconto, nem sobre qual base. É a mesma recusa que este contrato já faz a percentual misturado com valor fixo na parcela — e, como lá, ela é DÍVIDA e não regra: o legado permite os dois na linha, mas os três `UPDATE` em massa que a preenchem gravam sempre um deles zerado, então dado real não é recusado. Publicar uma ordem de aplicação aqui a fixaria no servidor para sempre; recusar deixa a falta visível.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PaymentTermWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PaymentTermDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict — DUAS causas, distinguidas pelo `type` e nunca pelo texto: `urn:cabinet:erro:sem-empresa-ativa` quando a sessão não tem empresa escolhida, e o conflito de nome (já existe condição com este `name` na empresa ativa). O mesmo status com discriminador no `type` é o desenho do contrato inteiro — ver `ProblemDetails`.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/payment-terms/{id}": {
      "put": {
        "tags": ["vendas"],
        "operationId": "UpdatePaymentTerm",
        "description": "Proposto. `PUT` substitui a condição INTEIRA, **parcelas junto**: a lista que vier no corpo passa a ser a lista, e a que não veio é apagada. Corpo parcial não preserva parcela.\n\n**Alterar a condição NÃO reescreve documento já gravado.** O orçamento e o pedido CARIMBAM o plano deles (`paymentInstallments`) e a política vigente (`installmentPolicy`) no momento da gravação; o que muda aqui vale do próximo documento em diante. É o que o legado faz ao copiar `par_ParcelarVlAcima`, `Par_VlMinParcela` e `Par_QuantMaxParcela` para a linha da `Venda` em vez de ler os parâmetros na hora de imprimir. A alternativa — derivar o plano na leitura — mudaria a data de vencimento de parcela que o cliente já recebeu, e o sintoma seria um documento reimpresso diferente de si mesmo.\n\n404 quando o `id` não é condição da empresa ativa: do ponto de vista de quem pergunta, ela não está lá. 409 no `name` repetido, como no `POST`.\n\n**Não há `DELETE`** — aqui nem em lugar nenhum do contrato. Condição usada por documento antigo não pode sumir; `active: false` a tira do combo e deixa o passado legível (padrão 8).\n\n---\n\n**As recusas de `groupAdjustments`, e nenhuma apara em silêncio** (400 com `fields[]` apontando `groupAdjustments.<n>.<campo>`):\n\n1. `productGroupId` REPETIDO na mesma condição. A chave da linha é o grupo — duas linhas do mesmo grupo não dizem qual ajuste vale. Mesma recusa de `groupDiscounts` no documento.\n2. `productGroupId` que não é um `catalog-lookups` **ativo** de kind `GRUPO_PRODUTO`. Id de outro `kind` é 400 e não 404: o que está errado é o campo do corpo, não o recurso pedido.\n3. Percentual NEGATIVO em qualquer um dos dois. Desconto negativo é acréscimo escrito ao contrário, e a coluna do acréscimo existe ao lado.\n4. `discountPercent` acima de `1000000` (100%). Desconto que passa do total faz a linha do grupo valer negativo. **`surchargePercent` não tem teto de propósito** — dobrar o preço de um grupo é estranho e é legítimo, e um teto inventado aqui recusaria dado real do legado na importação.\n5. Os DOIS maiores que zero na mesma linha. Não é aparadura, é falta de leitura única: não está decidido se o acréscimo incide antes ou depois do desconto, nem sobre qual base. É a mesma recusa que este contrato já faz a percentual misturado com valor fixo na parcela — e, como lá, ela é DÍVIDA e não regra: o legado permite os dois na linha, mas os três `UPDATE` em massa que a preenchem gravam sempre um deles zerado, então dado real não é recusado. Publicar uma ordem de aplicação aqui a fixaria no servidor para sempre; recusar deixa a falta visível.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PaymentTermWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PaymentTermDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found — o `id` não é condição da empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — DUAS causas, distinguidas pelo `type` e nunca pelo texto: `urn:cabinet:erro:sem-empresa-ativa` quando a sessão não tem empresa escolhida, e o conflito de nome (já existe condição com este `name` na empresa ativa). O mesmo status com discriminador no `type` é o desenho do contrato inteiro — ver `ProblemDetails`.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/installment-policy": {
      "get": {
        "tags": ["vendas"],
        "operationId": "GetInstallmentPolicy",
        "description": "Proposto. Os três limites que governam o parcelamento na empresa ativa: `par_ParcelarVlAcima`, `Par_VlMinParcela` e `Par_QuantMaxParcela` do legado. Na tela da Vertz eles valem R$ 100, R$ 50 e 6×.\n\n**São CONFIG, e o ponto da operação é esse.** Aqueles três números são os da instalação da Vertz, não a regra do produto — escrevê-los como constante faria toda empresa que entrar depois herdar o limite de uma, e o dia em que uma delas parcelar em 10× viraria mudança de código.\n\n**Singleton por empresa: não há `POST`, não há id.** A política existe sempre. Empresa sem linha gravada lê o PADRÃO do servidor (os três do legado), e não 404: obrigar cada tela a tratar \"ainda não configurado\" como estado próprio inventaria um terceiro caso que não existe na operação.",
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InstallmentPolicyDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict — sem empresa ativa na sessão. **Aqui não vale o \"lista vazia\" das listagens:** aquilo é semântica de COLEÇÃO, onde vazio é a verdade para quem ainda não tem empresa. Isto é um singleton, e um objeto vazio não existe — a política tem três números ou não tem sentido.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": ["vendas"],
        "operationId": "UpdateInstallmentPolicy",
        "description": "Proposto. Grava os três limites da empresa ativa. `PUT` num singleton: substitui os três, e os três são obrigatórios.\n\n**Papel: `admin` ou superior**, pelo mesmo motivo de `CreatePaymentTerm` — só que um nível acima em alcance: isto não descreve UMA condição, descreve o que todas elas podem ser.\n\n400 quando `maxInstallments` é menor que 1, ou quando algum valor é negativo. Zero em `minTotalToInstallCents` é legítimo e quer dizer \"parcela qualquer valor\".\n\n**Mudar a política não reescreve documento gravado** — ver `UpdatePaymentTerm`: o carimbo em `QuoteDetailDto.installmentPolicy` é o que preserva a regra sob a qual cada documento foi feito.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/InstallmentPolicyWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InstallmentPolicyDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict — sem empresa ativa na sessão (`urn:cabinet:erro:sem-empresa-ativa`). É o 409 que o contrato reserva para \"este recurso exige empresa\", e a política é dela.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/reports/abc-curve": {
      "get": {
        "tags": ["relatorios"],
        "operationId": "GetAbcCurveReport",
        "description": "Proposto. **Curva ABC — quais produtos fazem 80% do faturamento.** A ordem canônica é faturamento decrescente, e é a única em que a curva significa alguma coisa: `cumulativePercent` é acumulado, e acumulado sobre outra ordem é outro número.\n\n**Por isso a classe e o acumulado não saem da ordem PEDIDA, e sim da canônica.** O relatório aceita `sortBy` porque a tela é exportável e quem exporta reordena — mas `abcClass` e `cumulativePercent` de cada linha continuam os mesmos em qualquer ordem, calculados sobre o período inteiro antes de paginar. Se dependessem da ordem pedida, ordenar por descrição produziria um acumulado alfabético — que não significa nada e tem exatamente a mesma aparência.\n\nEntram as linhas de PEDIDO (`orders`) ativo com `issuedAt` no período; orçamento não é venda.",
        "parameters": [
          {
            "name": "from",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "format": "date"
            },
            "description": "Primeiro DIA do período, inclusive. Obrigatório onde o relatório soma: agregação sem recorte responde outra pergunta e cresce para sempre — o campeão de vendas de 2019 lideraria o quadro até alguém notar."
          },
          {
            "name": "to",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "format": "date"
            },
            "description": "Último DIA do período, inclusive. O recorte é por DIA e não por instante: quem pergunta por agosto quer o dia 31 inteiro."
          },
          {
            "name": "supplierId",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "Só as linhas deste fornecedor. O fornecedor da LINHA do documento (congelado na emissão), não o do cadastro de hoje — trocar o fornecedor de um produto não pode reescrever o que já foi vendido."
          },
          {
            "name": "productGroup",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Só as linhas deste grupo de produto. Comparação exata com o valor congelado na linha."
          },
          {
            "name": "sortBy",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Whitelist: `revenueCents`, `quantity`, `description`. Campo fora dela é 400.\n\n**A classe e o acumulado NÃO dependem daqui.** `abcClass` e `cumulativePercent` saem sempre da ordem canônica — faturamento decrescente sobre o período inteiro — e continuam corretos linha a linha qualquer que seja a ordem pedida. Se dependessem de `sortBy`, ordenar por descrição transformaria a curva num acumulado alfabético, que não significa nada e tem exatamente a mesma aparência.\n\nPadrão: `revenueCents`."
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            },
            "description": "Inverte o sentido. O padrão de cada relatório já é o que responde à pergunta dele — o maior faturamento primeiro, o item parado há mais tempo primeiro."
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32",
              "default": 1,
              "minimum": 1
            },
            "description": "Página, 1-based, como toda listagem do contrato."
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32",
              "default": 50,
              "minimum": 1,
              "maximum": 100
            },
            "description": "Linhas por página. Teto 100, e acima dele o servidor responde 400 em vez de aparar em silêncio: quem pediu 500 e recebeu 100 conclui que só existem 100 produtos."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AbcCurveReportDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      }
    },
    "/api/reports/products-sold": {
      "get": {
        "tags": ["relatorios"],
        "operationId": "GetProductsSoldReport",
        "description": "Proposto. **Produto vendido por quantidade ou por valor, no período.** É o relatório mais pedido da operação, e os dois eixos discordam de propósito: o parafuso lidera em quantidade e não paga a conta; a cozinha inteira lidera em valor e sai uma vez por mês. Trocar o eixo por `sortBy` é a PERGUNTA mudando, não um detalhe de apresentação — e é por isso que o relatório existe separado da curva ABC, que responde a uma só.",
        "parameters": [
          {
            "name": "from",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "format": "date"
            },
            "description": "Primeiro DIA do período, inclusive. Obrigatório onde o relatório soma: agregação sem recorte responde outra pergunta e cresce para sempre — o campeão de vendas de 2019 lideraria o quadro até alguém notar."
          },
          {
            "name": "to",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "format": "date"
            },
            "description": "Último DIA do período, inclusive. O recorte é por DIA e não por instante: quem pergunta por agosto quer o dia 31 inteiro."
          },
          {
            "name": "supplierId",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "Só as linhas deste fornecedor. O fornecedor da LINHA do documento (congelado na emissão), não o do cadastro de hoje — trocar o fornecedor de um produto não pode reescrever o que já foi vendido."
          },
          {
            "name": "productGroup",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Só as linhas deste grupo de produto. Comparação exata com o valor congelado na linha."
          },
          {
            "name": "pieceType",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Só as linhas deste tipo de peça."
          },
          {
            "name": "sortBy",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Whitelist: `revenueCents`, `quantity`, `orderCount`, `description`. Campo fora dela é 400.\n\nAqui trocar o eixo É a pergunta: o parafuso lidera em quantidade e não paga a conta; a cozinha inteira lidera em valor e sai uma vez por mês.\n\nPadrão: `revenueCents`."
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            },
            "description": "Inverte o sentido. O padrão de cada relatório já é o que responde à pergunta dele — o maior faturamento primeiro, o item parado há mais tempo primeiro."
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32",
              "default": 1,
              "minimum": 1
            },
            "description": "Página, 1-based, como toda listagem do contrato."
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32",
              "default": 50,
              "minimum": 1,
              "maximum": 100
            },
            "description": "Linhas por página. Teto 100, e acima dele o servidor responde 400 em vez de aparar em silêncio: quem pediu 500 e recebeu 100 conclui que só existem 100 produtos."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProductsSoldReportDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      }
    },
    "/api/reports/sales-comparison": {
      "get": {
        "tags": ["relatorios"],
        "operationId": "GetSalesComparisonReport",
        "description": "Proposto. **Comparativo de valor de vendas entre períodos.** O servidor devolve a série JÁ COMPARADA (cada linha com o valor do período anterior ao lado) em vez de mandar a série crua e deixar a tela subtrair: com paginação, a linha do topo da página 2 não tem a anterior à mão, e a tela mostraria a primeira variação de cada página em branco. **Períodos sem venda aparecem com zero** — buraco na série é informação, e omitir a linha faz o gráfico ligar julho em setembro como se agosto não existisse.",
        "parameters": [
          {
            "name": "from",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "format": "date"
            },
            "description": "Primeiro DIA do período, inclusive. Obrigatório onde o relatório soma: agregação sem recorte responde outra pergunta e cresce para sempre — o campeão de vendas de 2019 lideraria o quadro até alguém notar."
          },
          {
            "name": "to",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "format": "date"
            },
            "description": "Último DIA do período, inclusive. O recorte é por DIA e não por instante: quem pergunta por agosto quer o dia 31 inteiro."
          },
          {
            "name": "granularity",
            "in": "query",
            "schema": {
              "type": "string",
              "enum": ["month", "quarter", "semester", "year"],
              "default": "month"
            },
            "description": "O tamanho do balde. Fora da lista é 400."
          },
          {
            "name": "sortBy",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Whitelist: `bucket`, `revenueCents`, `orderCount`. Campo fora dela é 400.\n\n**`previousRevenueCents` é sempre o do período cronologicamente anterior**, não o da linha de cima: ordenar por faturamento e comparar com o vizinho na tela produziria \"variação\" entre agosto e março.\n\nPadrão: `bucket`."
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            },
            "description": "Inverte o sentido. O padrão de cada relatório já é o que responde à pergunta dele — o maior faturamento primeiro, o item parado há mais tempo primeiro."
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32",
              "default": 1,
              "minimum": 1
            },
            "description": "Página, 1-based, como toda listagem do contrato."
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32",
              "default": 50,
              "minimum": 1,
              "maximum": 100
            },
            "description": "Linhas por página. Teto 100, e acima dele o servidor responde 400 em vez de aparar em silêncio: quem pediu 500 e recebeu 100 conclui que só existem 100 produtos."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SalesComparisonReportDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      }
    },
    "/api/reports/salesperson-performance": {
      "get": {
        "tags": ["relatorios"],
        "operationId": "GetSalespersonReport",
        "description": "Proposto. **Demonstrativo por atendente — mensal, trimestral ou semestral.** Traz orçamento e pedido na MESMA linha porque a pergunta da gerência não é \"quanto vendeu\" e sim \"quanto do que ele atendeu virou venda\": o atendente que emite 4 orçamentos e fecha 3 é outro profissional do que emite 90 e fecha 4, e os dois podem faturar igual.",
        "parameters": [
          {
            "name": "from",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "format": "date"
            },
            "description": "Primeiro DIA do período, inclusive. Obrigatório onde o relatório soma: agregação sem recorte responde outra pergunta e cresce para sempre — o campeão de vendas de 2019 lideraria o quadro até alguém notar."
          },
          {
            "name": "to",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "format": "date"
            },
            "description": "Último DIA do período, inclusive. O recorte é por DIA e não por instante: quem pergunta por agosto quer o dia 31 inteiro."
          },
          {
            "name": "granularity",
            "in": "query",
            "schema": {
              "type": "string",
              "enum": ["month", "quarter", "semester"],
              "default": "month"
            },
            "description": "O tamanho do balde."
          },
          {
            "name": "salespersonId",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "Só este atendente. Ausente = todos."
          },
          {
            "name": "sortBy",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Whitelist: `revenueCents`, `orderCount`, `quoteCount`, `conversionPercent`, `salespersonName`, `bucket`. Campo fora dela é 400.\n\nPadrão: `revenueCents`."
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            },
            "description": "Inverte o sentido. O padrão de cada relatório já é o que responde à pergunta dele — o maior faturamento primeiro, o item parado há mais tempo primeiro."
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32",
              "default": 1,
              "minimum": 1
            },
            "description": "Página, 1-based, como toda listagem do contrato."
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32",
              "default": 50,
              "minimum": 1,
              "maximum": 100
            },
            "description": "Linhas por página. Teto 100, e acima dele o servidor responde 400 em vez de aparar em silêncio: quem pediu 500 e recebeu 100 conclui que só existem 100 produtos."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SalespersonReportDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      }
    },
    "/api/reports/professional-ranking": {
      "get": {
        "tags": ["relatorios"],
        "operationId": "GetProfessionalRankingReport",
        "description": "Proposto. **Ranking de profissional — quem indica quanto.** Ordenado por faturamento decrescente. **Só entram pedidos COM profissional**: a linha \"sem indicação\" seria a maior de todas em toda loja e enterraria o ranking que o relatório existe para mostrar — e é por isso que o `summary` daqui fala de \"faturamento COM profissional\" e não bate com o total de vendas da empresa, o que é correto e precisa estar escrito.",
        "parameters": [
          {
            "name": "from",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "format": "date"
            },
            "description": "Primeiro DIA do período, inclusive. Obrigatório onde o relatório soma: agregação sem recorte responde outra pergunta e cresce para sempre — o campeão de vendas de 2019 lideraria o quadro até alguém notar."
          },
          {
            "name": "to",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "format": "date"
            },
            "description": "Último DIA do período, inclusive. O recorte é por DIA e não por instante: quem pergunta por agosto quer o dia 31 inteiro."
          },
          {
            "name": "sortBy",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Whitelist: `revenueCents`, `orderCount`, `averageTicketCents`, `professionalName`, `lastSaleAt`. Campo fora dela é 400.\n\nPadrão: `revenueCents`."
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            },
            "description": "Inverte o sentido. O padrão de cada relatório já é o que responde à pergunta dele — o maior faturamento primeiro, o item parado há mais tempo primeiro."
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32",
              "default": 1,
              "minimum": 1
            },
            "description": "Página, 1-based, como toda listagem do contrato."
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32",
              "default": 50,
              "minimum": 1,
              "maximum": 100
            },
            "description": "Linhas por página. Teto 100, e acima dele o servidor responde 400 em vez de aparar em silêncio: quem pediu 500 e recebeu 100 conclui que só existem 100 produtos."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProfessionalRankingReportDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      }
    },
    "/api/reports/supplier-movement": {
      "get": {
        "tags": ["relatorios"],
        "operationId": "GetSupplierMovementReport",
        "description": "Proposto. **Movimentação por fornecedor.** É o número que se leva para a mesa de negociação: quanto se vendeu do que ele fornece, no período. Agrega pelo fornecedor CONGELADO na linha do documento e não pelo cadastro atual do produto — trocar o fornecedor de um item hoje reescreveria a história da negociação do ano passado.\n\n**É o ÚNICO relatório que responde por fornecedor, e por falta de fonte e não por escolha:** o cadastro de produto não guarda fornecedor — quem guarda é a LINHA do documento, congelada na emissão. Por isso `supplierId` não existe nos relatórios de estoque: publicá-lo lá devolveria vazio e pareceria \"este fornecedor não tem nada em estoque\".",
        "parameters": [
          {
            "name": "from",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "format": "date"
            },
            "description": "Primeiro DIA do período, inclusive. Obrigatório onde o relatório soma: agregação sem recorte responde outra pergunta e cresce para sempre — o campeão de vendas de 2019 lideraria o quadro até alguém notar."
          },
          {
            "name": "to",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "format": "date"
            },
            "description": "Último DIA do período, inclusive. O recorte é por DIA e não por instante: quem pergunta por agosto quer o dia 31 inteiro."
          },
          {
            "name": "supplierId",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "Só as linhas deste fornecedor. O fornecedor da LINHA do documento (congelado na emissão), não o do cadastro de hoje — trocar o fornecedor de um produto não pode reescrever o que já foi vendido."
          },
          {
            "name": "productGroup",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Só as linhas deste grupo de produto. Comparação exata com o valor congelado na linha."
          },
          {
            "name": "sortBy",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Whitelist: `revenueCents`, `quantity`, `supplierName`, `orderCount`, `lineCount`, `productCount`. Campo fora dela é 400.\n\nPadrão: `revenueCents`."
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            },
            "description": "Inverte o sentido. O padrão de cada relatório já é o que responde à pergunta dele — o maior faturamento primeiro, o item parado há mais tempo primeiro."
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32",
              "default": 1,
              "minimum": 1
            },
            "description": "Página, 1-based, como toda listagem do contrato."
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32",
              "default": 50,
              "minimum": 1,
              "maximum": 100
            },
            "description": "Linhas por página. Teto 100, e acima dele o servidor responde 400 em vez de aparar em silêncio: quem pediu 500 e recebeu 100 conclui que só existem 100 produtos."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SupplierMovementReportDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      }
    },
    "/api/reports/stock-valuation": {
      "get": {
        "tags": ["relatorios"],
        "operationId": "GetStockValuationReport",
        "description": "Proposto. **Estoque atual valorizado.** Foto do agora, sem período — por isso `from`/`to` não existem aqui e `asOf` existe. Ordenado por valor decrescente. O saldo é o da EMPRESA ativa (`product_tenant`), e o preço também: o mesmo produto vale diferente em duas empresas do grupo, e é isso que o relatório tem de mostrar.",
        "parameters": [
          {
            "name": "productGroup",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Só os itens deste grupo. **No estoque o grupo é o TIPO DE PRODUTO do cadastro** (`catalog_lookups`, kind `TIPO_PRODUTO`), comparado pelo NOME — e não o `productGroup` congelado na linha do documento, que é o dos relatórios de venda. São dois campos com o mesmo papel em lados diferentes da mesma pergunta: a linha do documento guarda o que valia na venda, e o cadastro guarda o que vale hoje. O nome é o que os aproxima; o id não, porque a linha congelada nunca guardou id."
          },
          {
            "name": "includeZero",
            "in": "query",
            "schema": {
              "type": "boolean",
              "default": false
            },
            "description": "Incluir itens com saldo zero. Padrão `false`: o inventário de quem tem 4000 SKUs cadastrados e 300 em casa não é uma lista de 4000 linhas com 3700 zeros."
          },
          {
            "name": "belowMinimumOnly",
            "in": "query",
            "schema": {
              "type": "boolean",
              "default": false
            },
            "description": "Só o que está abaixo do mínimo — a lista de compras."
          },
          {
            "name": "sortBy",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Whitelist: `valueCents`, `quantity`, `minStock`, `description`. Campo fora dela é 400.\n\nItem SEM preço ordena por `valueCents` como ausente, não como zero — e vai para o fim nos dois sentidos: ele não vale zero, vale desconhecido.\n\nPadrão: `valueCents`."
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            },
            "description": "Inverte o sentido. O padrão de cada relatório já é o que responde à pergunta dele — o maior faturamento primeiro, o item parado há mais tempo primeiro."
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32",
              "default": 1,
              "minimum": 1
            },
            "description": "Página, 1-based, como toda listagem do contrato."
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32",
              "default": 50,
              "minimum": 1,
              "maximum": 100
            },
            "description": "Linhas por página. Teto 100, e acima dele o servidor responde 400 em vez de aparar em silêncio: quem pediu 500 e recebeu 100 conclui que só existem 100 produtos."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StockValuationReportDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      }
    },
    "/api/reports/stock-aging": {
      "get": {
        "tags": ["relatorios"],
        "operationId": "GetStockAgingReport",
        "description": "Proposto. **Quantidade, dias sem venda e última venda — o dinheiro parado na prateleira.** Ordenado por `daysWithoutSale` decrescente, com os que NUNCA venderam no fim e não no começo: eles não têm dias a contar, e pô-los no topo enterraria os que já venderam e pararam, que é onde mora a decisão de queima de estoque. Só itens com saldo, salvo `includeZero`.",
        "parameters": [
          {
            "name": "minDaysWithoutSale",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32",
              "default": 0,
              "minimum": 0
            },
            "description": "Só o que está parado há pelo menos tantos dias. **Itens que nunca venderam entram sempre**, qualquer que seja o valor: sem venda alguma não há dias a comparar, e filtrá-los fora por `NULL` faria o pior caso desaparecer do relatório dos piores casos."
          },
          {
            "name": "productGroup",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Só os itens deste grupo. **No estoque o grupo é o TIPO DE PRODUTO do cadastro** (`catalog_lookups`, kind `TIPO_PRODUTO`), comparado pelo NOME — e não o `productGroup` congelado na linha do documento, que é o dos relatórios de venda. São dois campos com o mesmo papel em lados diferentes da mesma pergunta: a linha do documento guarda o que valia na venda, e o cadastro guarda o que vale hoje. O nome é o que os aproxima; o id não, porque a linha congelada nunca guardou id."
          },
          {
            "name": "includeZero",
            "in": "query",
            "schema": {
              "type": "boolean",
              "default": false
            },
            "description": "Incluir itens com saldo zero."
          },
          {
            "name": "sortBy",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Whitelist: `daysWithoutSale`, `valueCents`, `quantity`, `lastSaleAt`, `description`. Campo fora dela é 400.\n\nQuem NUNCA vendeu não tem dias a contar e fica no fim em qualquer sentido — no topo, enterraria os que já venderam e pararam, que é onde mora a decisão de queima.\n\nPadrão: `daysWithoutSale`."
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            },
            "description": "Inverte o sentido. O padrão de cada relatório já é o que responde à pergunta dele — o maior faturamento primeiro, o item parado há mais tempo primeiro."
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32",
              "default": 1,
              "minimum": 1
            },
            "description": "Página, 1-based, como toda listagem do contrato."
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32",
              "default": 50,
              "minimum": 1,
              "maximum": 100
            },
            "description": "Linhas por página. Teto 100, e acima dele o servidor responde 400 em vez de aparar em silêncio: quem pediu 500 e recebeu 100 conclui que só existem 100 produtos."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StockAgingReportDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      }
    },
    "/api/reports/quote-vs-stock": {
      "get": {
        "tags": ["relatorios"],
        "operationId": "GetQuoteVsStockReport",
        "description": "Proposto. **Orçamento × estoque — o que já foi prometido contra o que existe em casa.** Só orçamentos ATIVOS (cancelado não promete nada) e só linhas com variante do catálogo: item digitado livre não tem saldo com que comparar. Ordenado por falta decrescente. **A soma dos orçamentos não desconta o que já virou pedido** — e isso é deliberado: o pedido consome estoque por conta própria, e abater aqui esconderia o compromisso duas vezes.",
        "parameters": [
          {
            "name": "from",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "format": "date"
            },
            "description": "Primeiro DIA do período, inclusive. Obrigatório onde o relatório soma: agregação sem recorte responde outra pergunta e cresce para sempre — o campeão de vendas de 2019 lideraria o quadro até alguém notar."
          },
          {
            "name": "to",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "format": "date"
            },
            "description": "Último DIA do período, inclusive. O recorte é por DIA e não por instante: quem pergunta por agosto quer o dia 31 inteiro."
          },
          {
            "name": "shortageOnly",
            "in": "query",
            "schema": {
              "type": "boolean",
              "default": false
            },
            "description": "Só o que falta. É a lista de compras que o relatório existe para gerar."
          },
          {
            "name": "sortBy",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Whitelist: `shortageQuantity`, `quotedQuantity`, `stockQuantity`, `description`. Campo fora dela é 400.\n\nPadrão: `shortageQuantity`."
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            },
            "description": "Inverte o sentido. O padrão de cada relatório já é o que responde à pergunta dele — o maior faturamento primeiro, o item parado há mais tempo primeiro."
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32",
              "default": 1,
              "minimum": 1
            },
            "description": "Página, 1-based, como toda listagem do contrato."
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32",
              "default": 50,
              "minimum": 1,
              "maximum": 100
            },
            "description": "Linhas por página. Teto 100, e acima dele o servidor responde 400 em vez de aparar em silêncio: quem pediu 500 e recebeu 100 conclui que só existem 100 produtos."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/QuoteVsStockReportDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      }
    },
    "/api/reports/birthdays": {
      "get": {
        "tags": ["relatorios"],
        "operationId": "GetBirthdaysReport",
        "description": "Proposto. **Aniversariantes do mês.** Lê `birthDate` do parceiro (pessoa física) e só entram os VINCULADOS à empresa ativa e ativos — parceiro do grupo sem vínculo com esta empresa não é cliente desta loja, e a lista de aniversário é a lista de quem se liga. Ordenado por dia do mês. Sem período: a pergunta é sempre um mês inteiro, e é por isso que o parâmetro é `month` e não `from`/`to`.",
        "parameters": [
          {
            "name": "month",
            "in": "query",
            "required": true,
            "schema": {
              "type": "integer",
              "format": "int32",
              "minimum": 1,
              "maximum": 12
            },
            "description": "Mês, 1-12. Obrigatório e sem padrão: \"mês atual\" decidido pelo servidor daria uma resposta diferente conforme o fuso de quem pergunta, e o relatório é impresso e conferido."
          },
          {
            "name": "role",
            "in": "query",
            "schema": {
              "type": "string",
              "enum": ["customer", "professional", "supplier"]
            },
            "description": "Restringe ao papel. Ausente = todos os vinculados com data de nascimento."
          },
          {
            "name": "sortBy",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Whitelist: `day`, `name`, `age`. Campo fora dela é 400.\n\nA ordem padrão é `day` crescente: a lista serve para ligar na ordem em que os dias chegam.\n\nPadrão: `day`."
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            },
            "description": "Inverte o sentido. O padrão de cada relatório já é o que responde à pergunta dele — o maior faturamento primeiro, o item parado há mais tempo primeiro."
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32",
              "default": 1,
              "minimum": 1
            },
            "description": "Página, 1-based, como toda listagem do contrato."
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32",
              "default": 50,
              "minimum": 1,
              "maximum": 100
            },
            "description": "Linhas por página. Teto 100, e acima dele o servidor responde 400 em vez de aparar em silêncio: quem pediu 500 e recebeu 100 conclui que só existem 100 produtos."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BirthdaysReportDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      }
    },
    "/api/purchase-requests": {
      "get": {
        "tags": ["compras"],
        "operationId": "ListPurchaseRequests",
        "description": "Proposto. Os PEDIDOS DE COMPRA da empresa ativa — a necessidade de comprar, antes de virar compromisso com fornecedor.\n\nÉ a primeira das duas listagens do módulo `Compras` do comparativo Softlux (vol. 03), e a tela que a consome é a de onde o comprador MONTA ordem: ela precisa das linhas em aberto, com o fornecedor de cada uma, para agrupar por fornecedor e chegar ao faturamento mínimo. Por isso `PurchaseRequestDto.items` vem na listagem, e não só no detalhe.\n\n**Sem empresa ativa devolve `{rows: [], total: 0}`** — semântica do contrato para toda leitura de lista.\n\n**Não publica `filters`.** O recorte que a tela faz é por `status` e por fornecedor, e os dois são parâmetro próprio aqui: o filtro estruturado é opt-in por recurso e cobra whitelist dos dois lados, e uma lista de trabalho com dois recortes conhecidos não precisa do vocabulário inteiro. Ele entra no dia em que uma tela precisar.",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortBy",
            "description": "Whitelist: `number`, `issuedAt`, `status`, `itemCount`. Campo fora dela é 400.\n\n`orderNumber` fica de fora e é decisão: ele é ECOADO do pedido de venda, e ordenar por coluna de outra tabela num recurso paginado é o que transforma listagem em junção obrigatória. Quem procura pelo pedido de venda usa `q`.",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "status",
            "description": "Recorta por estado do pedido. Omitido, traz todos — inclusive `cancelled`, que a tela esconde por padrão mas o relatório quer.",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "supplierId",
            "description": "Só os pedidos com ao menos uma LINHA deste fornecedor. Recorta por linha e não por cabeçalho, porque o pedido não tem fornecedor — as linhas têm, e podem ser de vários. Este é o parâmetro que a tela de montar ordem usa.",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "onlyOpenItems",
            "description": "Só os pedidos com linha ainda não levada por ordem nenhuma. É o recorte de trabalho do comprador: o pedido totalmente atendido não some, mas também não atrapalha a tela em que se monta ordem.",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfPurchaseRequestDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      },
      "post": {
        "tags": ["compras"],
        "operationId": "CreatePurchaseRequest",
        "description": "Proposto. Cria um pedido de compra na empresa ativa.\n\n**Permissão: `compras:editar`.** Não é `pedidos:editar` (que é o pedido de VENDA) nem `estoque:movimentar`: comprar é comprometer dinheiro da empresa com um terceiro, e é a primeira ação deste contrato que faz isso. O vendedor que fecha a venda não é necessariamente quem compra — no legado o setor COMPRAS tem 92 opções de RBAC próprias.\n\n**400 quando uma linha tem `destination: \"sale\"` sem `orderId` no cabeçalho**, ou com `sourceOrderItemLine` que não existe naquele pedido de venda. Encomenda sem venda que a encomendou é reposição com outro nome.\n\n**Sem empresa ativa na sessão é 409** (`urn:cabinet:erro:sem-empresa-ativa`), não 400: a empresa vem da sessão e não do cliente, então \"sem empresa\" é operador recém-criado, não requisição malformada. A LISTAGEM, por outro lado, responde `{rows: [], total: 0}` — lista vazia é a resposta certa para \"quantos pedidos tem a empresa que você não tem\".",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PurchaseRequestWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PurchaseRequestDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict — sem empresa ativa na sessão, ou `orderId` que não é pedido de venda desta empresa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/purchase-requests/{id}": {
      "get": {
        "tags": ["compras"],
        "operationId": "GetPurchaseRequest",
        "description": "Proposto. Um pedido de compra pelo id, com as linhas.\n\nExiste apesar de a listagem já trazer `items`, e a razão é a mesma de `quotes` e `orders`: o documento aberto na tela é recarregado sozinho depois de cada escrita, e buscar a página inteira para achar uma linha é o que faz a tela piscar.\n\n**Sem empresa ativa na sessão é 409** (`urn:cabinet:erro:sem-empresa-ativa`), não 400: a empresa vem da sessão e não do cliente, então \"sem empresa\" é operador recém-criado, não requisição malformada. A LISTAGEM, por outro lado, responde `{rows: [], total: 0}` — lista vazia é a resposta certa para \"quantos pedidos tem a empresa que você não tem\".",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "required": true
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PurchaseRequestDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — sem empresa ativa na sessão.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": ["compras"],
        "operationId": "UpdatePurchaseRequest",
        "description": "Proposto. Substitui o pedido INTEIRO, `items` incluído.\n\n**409 quando alguma linha já foi levada por uma ordem.** O pedido em `partially_ordered` ou `ordered` não se reescreve: a ordem já disse ao fornecedor o que comprou, e mudar a origem por baixo dela deixaria o par `sourceRequestId` + `sourceLineNumber` apontando para uma linha que virou outra coisa — com o par intacto, que é o que torna o defeito invisível. Desistir de uma linha nesse estado é cancelar o pedido e abrir outro, que é o que deixa rastro.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "required": true
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PurchaseRequestWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PurchaseRequestDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — pedido com linha já em ordem (`urn:cabinet:erro:item-ja-em-ordem`), pedido cancelado, ou sem empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/purchase-requests/{id}/cancel": {
      "post": {
        "tags": ["compras"],
        "operationId": "CancelPurchaseRequest",
        "description": "Proposto. Cancela o pedido de compra. Operação própria e não campo no PUT, pelo mesmo desenho de `quotes` e `orders`: cancelar é decisão de gente com rastro próprio, e um `status` aceito no corpo do PUT deixaria uma reedição distraída cancelar o documento.\n\n**409 quando alguma linha já está em ordem.** Cancelar o pedido cuja peça já foi comprada não desfaz a compra — ele só faria a ordem apontar para um documento cancelado. Nesse estado, o que se cancela é a ORDEM.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "required": true
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PurchaseRequestDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — pedido já cancelado, com linha em ordem (`urn:cabinet:erro:item-ja-em-ordem`), ou sem empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/purchase-orders": {
      "get": {
        "tags": ["compras"],
        "operationId": "ListPurchaseOrders",
        "description": "Proposto. As ORDENS DE COMPRA da empresa ativa — o compromisso com o fornecedor.\n\n**Sem empresa ativa devolve `{rows: [], total: 0}`.**\n\nA empresa da SESSÃO recorta a listagem; `buyingTenantId` é OUTRA coisa — é qual empresa do grupo aparece como compradora no documento, e ela é dado da ordem, não recorte de acesso. As duas coincidem no caso normal e podem divergir por decisão comercial, que é exatamente o que a multi-empresa de compra do legado faz.",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortBy",
            "description": "Whitelist: `number`, `orderedAt`, `sentAt`, `expectedAt`, `status`, `totalCents`. Campo fora dela é 400.\n\n`supplierName` fica de fora pelo motivo de sempre — é eco de outra tabela. `rescheduledAt` também, e por um motivo próprio: ele é nulo na maioria das linhas, e ordenar por coluna majoritariamente nula agrupa o irrelevante numa ponta e parece defeito. Quem quer as reagendadas usa a previsão de chegada, que já as marca.",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "status",
            "description": "Recorta por estado da ordem.",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "supplierId",
            "description": "Só as ordens deste fornecedor.",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfPurchaseOrderDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      },
      "post": {
        "tags": ["compras"],
        "operationId": "CreatePurchaseOrder",
        "description": "Proposto. Cria uma ordem de compra agrupando linhas de N pedidos de compra do MESMO fornecedor.\n\n**Permissão: `compras:editar`.**\n\n**As três recusas de negócio desta operação, e por que cada uma é 409 e não 400:**\n\n1. `urn:cabinet:erro:fornecedor-divergente` — alguma linha vem de um pedido cujo item é de outro fornecedor. A ordem é de UM fornecedor por definição, e o corpo pode estar perfeitamente bem formado: o que está errado é o estado do documento de origem, que o cliente não tinha como validar sozinho.\n2. `urn:cabinet:erro:item-ja-em-ordem` — a linha de pedido apontada já foi levada por outra ordem. É a corrida entre dois compradores montando ordem ao mesmo tempo, e é exatamente o caso que um 400 descreveria mal.\n3. `urn:cabinet:erro:faturamento-minimo-nao-atingido` — o total não alcança o mínimo do fornecedor. **É a regra que o legado carrega no cadastro e cobra na ordem**, e ela é conferida contra a soma das LINHAS com o desconto geral aplicado — e **sem o acréscimo**, que é frete e embalagem, não mercadoria: deixar o frete completar o mínimo faria uma ordem de R$ 2.900 passar por ter R$ 150 de frete, e o fornecedor recusaria o faturamento que este servidor aprovou. O desconto entra porque é percentual e uniforme, então não há rateio a arbitrar, e é contra o que o fornecedor RECEBE que o mínimo dele é medido.\n\nSão N+1 contas independentes, não uma: uma por grupo com mínimo próprio, e uma geral contra todo o resto — as linhas sem grupo e as de grupo sem mínimo próprio. **A resposta diz TODAS as contas que faltaram**, não a primeira: o comprador que corrige uma de cada vez descobre a segunda depois de refazer o trabalho. E a ordem inteiramente composta de grupos com mínimo próprio NÃO cai na conta geral contra zero. `detail` diz quanto falta e para qual grupo — a tela não tem como calcular isso, porque o mínimo é cadastro do fornecedor.\n\n**A validação do mínimo roda na criação e no PUT, não no `send`.** Deixá-la para o envio permitiria montar e guardar uma ordem que nunca poderá sair, e o comprador descobriria no fim do trabalho em vez de no começo.\n\n**Sem empresa ativa na sessão é 409** (`urn:cabinet:erro:sem-empresa-ativa`), não 400: a empresa vem da sessão e não do cliente, então \"sem empresa\" é operador recém-criado, não requisição malformada. A LISTAGEM, por outro lado, responde `{rows: [], total: 0}` — lista vazia é a resposta certa para \"quantos pedidos tem a empresa que você não tem\".",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PurchaseOrderWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PurchaseOrderDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict — fornecedor divergente entre linhas e pedidos de origem (`urn:cabinet:erro:fornecedor-divergente`), linha de pedido já levada por outra ordem (`urn:cabinet:erro:item-ja-em-ordem`), faturamento mínimo do fornecedor não atingido (`urn:cabinet:erro:faturamento-minimo-nao-atingido`), ou sem empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/purchase-orders/{id}": {
      "get": {
        "tags": ["compras"],
        "operationId": "GetPurchaseOrder",
        "description": "Proposto. Uma ordem de compra pelo id, com as linhas e a origem de cada uma.\n\n**Sem empresa ativa na sessão é 409** (`urn:cabinet:erro:sem-empresa-ativa`), não 400: a empresa vem da sessão e não do cliente, então \"sem empresa\" é operador recém-criado, não requisição malformada. A LISTAGEM, por outro lado, responde `{rows: [], total: 0}` — lista vazia é a resposta certa para \"quantos pedidos tem a empresa que você não tem\".",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "required": true
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PurchaseOrderDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — sem empresa ativa na sessão.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": ["compras"],
        "operationId": "UpdatePurchaseOrder",
        "description": "Proposto. Substitui a ordem INTEIRA.\n\n**409 na ordem já ENVIADA** (`urn:cabinet:erro:ordem-ja-enviada`). Depois do `send` o fornecedor tem o documento na mão; o que muda a partir dali é data, e por `POST /{id}/reschedule`. Uma ordem enviada que se reescreve é uma ordem que diverge, em silêncio, daquela que o fornecedor está atendendo.\n\nAs três recusas do POST valem aqui igualmente, e a do faturamento mínimo é a que mais importa no PUT: é editando que o comprador tira a linha que sustentava o mínimo.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "required": true
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PurchaseOrderWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PurchaseOrderDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — ordem já enviada (`urn:cabinet:erro:ordem-ja-enviada`) ou cancelada, fornecedor divergente (`urn:cabinet:erro:fornecedor-divergente`), linha já em outra ordem (`urn:cabinet:erro:item-ja-em-ordem`), faturamento mínimo não atingido (`urn:cabinet:erro:faturamento-minimo-nao-atingido`), ou sem empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/purchase-orders/{id}/send": {
      "post": {
        "tags": ["compras"],
        "operationId": "SendPurchaseOrder",
        "description": "Proposto. ENVIA a ordem ao fornecedor: `status` vai a `sent` e `sentAt` é gravada.\n\nÉ transição e não campo no PUT porque muda o que a ordem PERMITE — depois dela o documento para de ser editável. Um `status: \"sent\"` aceito no corpo do PUT faria essa passagem acontecer no meio de uma edição distraída, e ela não tem volta.\n\n**409 na ordem já enviada** — reenviar não é operação: se o fornecedor não recebeu, o que se reenvia é o e-mail, não o documento. Reenviar aqui reescreveria `sentAt` e apagaria quando a ordem de fato saiu, que é a data contra a qual todo atraso é medido.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "required": true
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PurchaseOrderSendRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PurchaseOrderDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — ordem já enviada (`urn:cabinet:erro:ordem-ja-enviada`), cancelada, ou sem empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/purchase-orders/{id}/reschedule": {
      "post": {
        "tags": ["compras"],
        "operationId": "ReschedulePurchaseOrder",
        "description": "Proposto. REAGENDA a entrega: a data nova vai para `rescheduledAt` e `expectedAt` fica onde está.\n\n**As duas datas ficam, e é o ponto inteiro desta operação.** Sobrescrever `expectedAt` faria a ordem sempre parecer no prazo — o fornecedor que atrasou três vezes teria, no fim, uma data cumprida. É a diferença entre as duas que mede o atraso, e o comparativo lista o reagendamento como campo do legado justamente por isso.\n\n**Só ordem ENVIADA se reagenda** (409 na `draft`): antes do envio não há promessa a quebrar — o que se muda é `expectedAt`, pelo PUT.\n\nReagendar de novo sobrescreve `rescheduledAt`, e isso é aceito: o que a ordem guarda é a promessa ORIGINAL e a VIGENTE. O histórico completo de reagendamentos é do log de auditoria, que grava cada chamada desta operação — não de mais uma coluna aqui.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "required": true
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PurchaseOrderRescheduleRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PurchaseOrderDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — ordem ainda em `draft` ou já cancelada, ou sem empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/purchase-orders/{id}/cancel": {
      "post": {
        "tags": ["compras"],
        "operationId": "CancelPurchaseOrder",
        "description": "Proposto. Cancela a ordem de compra e DEVOLVE as linhas de pedido que ela levava ao estado `open`.\n\n**A devolução é a razão de esta operação existir em vez de um `status` no PUT.** A ordem cancelada que deixasse as linhas de pedido marcadas como `ordered` criaria necessidade órfã: peça que o cliente espera, que nenhuma ordem vai trazer, e que nenhuma tela mostra como pendente. Cancelar a ordem tem de reabrir o pedido, e é escrita nos dois documentos — por isso ela é uma transação só, do lado do servidor.\n\n**Cancelar ordem ENVIADA é permitido**, e é decisão: o fornecedor cancela pedido, e um sistema que só deixasse cancelar rascunho obrigaria a operação a mentir sobre o que existe. O rastro fica no log de auditoria.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "required": true
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PurchaseOrderDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — ordem já cancelada, ou sem empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/purchases/arrival-forecast": {
      "get": {
        "tags": ["compras"],
        "operationId": "GetPurchaseArrivalForecast",
        "description": "Proposto. PREVISÃO DE CHEGADA — o que vem, quando, e para quem. É a consulta de mesmo nome no menu `Compras` do legado.\n\nLinha a linha de ITEM, não de ordem: a pergunta que a tela responde é \"quando chega a peça do cliente X\". Só entram itens de ordem **enviada e não cancelada** — ordem em `draft` é intenção do comprador, e mostrá-la como peça a caminho é o que faz o vendedor prometer data ao cliente com base numa ordem que ninguém mandou.\n\n**Sem empresa ativa devolve `{rows: [], total: 0}`.**",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortBy",
            "description": "Whitelist: `expectedAt`, `sentAt`, `purchaseOrderNumber`, `daysLate`. Campo fora dela é 400.\n\nO padrão é `expectedAt` crescente — a tela de previsão abre no que chega primeiro, e uma previsão ordenada por outra coisa obriga o operador a ordená-la toda vez.",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "supplierId",
            "description": "Só o que vem deste fornecedor.",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "customerId",
            "description": "Só o que está prometido a este cliente. Recorta os itens `sale`; linha de reposição não tem cliente e some deste recorte, que é o comportamento certo.",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "destination",
            "description": "Recorta por destino: `stock` (reposição) ou `sale` (encomenda). É a separação que o legado faz com dois botões.",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "lateOnly",
            "description": "Só as linhas cuja data válida já passou. É o recorte de cobrança do comprador.",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "from",
            "description": "Piso da data válida (inclusive).",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "date"
            }
          },
          {
            "name": "to",
            "description": "Teto da data válida (inclusive).",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "date"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfPurchaseArrivalRowDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      }
    },
    "/api/purchases/stock-replenishment": {
      "get": {
        "tags": ["compras"],
        "operationId": "GetPurchaseStockReplenishment",
        "description": "Proposto. COMPRAS PARA ESTOQUE / RESERVA — quanto há, quanto está prometido, quanto vem a caminho, e quanto falta comprar. É a `CompraEstoque` do legado.\n\n**Consome `stock_balances.quantity_allocated`** — a coluna de RESERVA que a migração `0035` criou e que, até este contrato, não saía por API nenhuma. Ela é a diferença entre esta consulta e uma listagem de saldo: sem a reserva, \"disponível\" é o saldo bruto, e o comprador repõe peça que já está prometida a um cliente que ainda não a levou.\n\n**As parcelas vêm abertas, não somadas.** O legado define a disponibilidade futura como um número só (ordem aberta − reserva − entrada por nota), e um número só não distingue \"falta comprar\" de \"sobra reserva\" — que pedem decisões opostas. A soma continua disponível: é `qtyAvailable + qtyOnOrder`.\n\n**Sem empresa ativa devolve `{rows: [], total: 0}`** — e aqui a semântica importa mais do que em qualquer outra lista: saldo é POR EMPRESA, e uma consulta de reposição que ignorasse a empresa somaria o galpão de todo mundo.",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortBy",
            "description": "Whitelist: `description`, `qtyOnHand`, `qtyAllocated`, `qtyAvailable`, `qtyOnOrder`, `qtySuggested`. Campo fora dela é 400.\n\nO padrão é `qtySuggested` decrescente: a tela abre no que mais falta, que é a única ordem em que uma lista de reposição é útil sem ninguém tocá-la.",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "supplierId",
            "description": "Só as variantes cujo fornecedor padrão é este — o recorte de quem vai montar uma ordem.",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "locationId",
            "description": "Recorta o saldo a UM depósito. Omitido, soma os depósitos da empresa ativa: reposição é decisão da empresa, e o galpão de onde a peça sai é decisão da separação.",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "belowMinimumOnly",
            "description": "Só as variantes em que `qtySuggested > 0`. É o recorte de trabalho; omitido, a consulta traz o quadro inteiro, que é o que o relatório quer.",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfPurchaseReplenishmentRowDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      }
    },
    "/api/goods-receipts": {
      "get": {
        "tags": ["compras"],
        "operationId": "ListGoodsReceipts",
        "description": "Proposto. Os RECEBIMENTOS da empresa ativa — a nota do fornecedor virando entrada no estoque.\n\n**Sem empresa ativa devolve `{rows: [], total: 0}`.**\n\nA fila de trabalho de quem responde pelo galpão é esta listagem recortada por `status`: o que ainda não foi conferido, e o que foi conferido e ainda não foi lançado. É a razão de `status` ser parâmetro próprio e não filtro estruturado — a tela abre nele, não o escolhe.",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortBy",
            "description": "Whitelist: `receivedAt`, `issuedAt`, `invoiceNumber`, `status`, `postedAt`. Campo fora dela é 400.\n\n`supplierName` fica de fora pelo motivo de sempre — é eco de outra tabela. A ordenação PADRÃO é `receivedAt` decrescente: \"o que chegou por último\" é a pergunta que abre a tela, e o índice `ix_goods_receipts_fornecedor` da tabela foi desenhado nessa ordem.",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "status",
            "description": "Recorta por estado do recebimento — `draft`, `checked` ou `posted`.",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "supplierId",
            "description": "Só os recebimentos deste fornecedor.",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfGoodsReceiptDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      },
      "post": {
        "tags": ["compras"],
        "operationId": "CreateGoodsReceipt",
        "description": "Proposto. Abre um recebimento em RASCUNHO e digita a grade da conferência.\n\n**Permissão: `compras:editar`.**\n\nNasce sempre em `draft`: criar já conferido pularia a decisão que o estado do meio existe para representar. Quem descarrega o caminhão conta; quem responde pelo estoque lança — e por isso `POST /{id}/post` pede OUTRA permissão.\n\n**`locationId` é opcional aqui e obrigatório na tabela.** Omitido, o servidor resolve o depósito padrão da empresa. Entrada sem depósito é entrada que o kardex não sabe explicar, mas exigir a escolha na tela toda faria o operador escolher sempre o mesmo — e escolher errado uma vez.\n\n**Descrição da linha NÃO vem do cliente:** é copiada do cadastro da variante na gravação, e congelada ali. Aceitá-la aqui deixaria a conferência dizer que recebeu uma peça e o catálogo dizer que ela é outra, com o `variantId` intacto entre as duas.\n\n**Sem empresa ativa na sessão é 409** (`urn:cabinet:erro:sem-empresa-ativa`), não 400: a empresa vem da sessão e não do cliente, então \"sem empresa\" é operador recém-criado, não requisição malformada. A LISTAGEM, por outro lado, responde `{rows: [], total: 0}`.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/GoodsReceiptWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GoodsReceiptDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict — sem empresa ativa na sessão.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/goods-receipts/{id}": {
      "get": {
        "tags": ["compras"],
        "operationId": "GetGoodsReceipt",
        "description": "Proposto. Um recebimento pelo id, com a grade da conferência.\n\nA grade vem ordenada por `lineNumber`, que é a ordem em que o operador digitou. Ela **não** é a ordem em que o lançamento acontece — ver `POST /{id}/post`.\n\n**Sem empresa ativa na sessão é 409** (`urn:cabinet:erro:sem-empresa-ativa`), não 400: a empresa vem da sessão e não do cliente, então \"sem empresa\" é operador recém-criado, não requisição malformada. A LISTAGEM, por outro lado, responde `{rows: [], total: 0}`.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "required": true
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GoodsReceiptDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — sem empresa ativa na sessão.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": ["compras"],
        "operationId": "UpdateGoodsReceipt",
        "description": "Proposto. Substitui o recebimento INTEIRO — cabeçalho e grade.\n\n**Permissão: `compras:editar`.**\n\n**409 fora do rascunho** (`urn:cabinet:erro:transicao-invalida`). Conferido é uma decisão já tomada sobre a grade, e lançado já virou movimento no kardex: reescrever qualquer um dos dois faria o documento divergir, em silêncio, do estoque que ele gerou. Voltar atrás numa conferência é reescrever a grade inteira, que é o que este PUT faz — não existe DELETE (ver `GoodsReceiptDto`).\n\n**Sem empresa ativa na sessão é 409** (`urn:cabinet:erro:sem-empresa-ativa`), não 400: a empresa vem da sessão e não do cliente, então \"sem empresa\" é operador recém-criado, não requisição malformada. A LISTAGEM, por outro lado, responde `{rows: [], total: 0}`.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "required": true
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/GoodsReceiptWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GoodsReceiptDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — recebimento fora do rascunho (`urn:cabinet:erro:transicao-invalida`), ou sem empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/goods-receipts/{id}/check": {
      "post": {
        "tags": ["compras"],
        "operationId": "CheckGoodsReceipt",
        "description": "Proposto. FECHA a conferência: `draft` → `checked`.\n\n**Permissão: `compras:editar`.**\n\nÉ transição e não campo do PUT porque muda o que o documento PERMITE: depois dela a grade para de ser editável e o lançamento passa a ser possível. Um `status: \"checked\"` aceito no corpo faria essa passagem acontecer no meio de uma digitação distraída.\n\n**A regra que esta transição cobra, e que o banco de propósito NÃO cobra: linha que divergiu precisa de `divergenceReason` escrito.** Durante a conferência a grade passa por todos os estados intermediários — a linha nasce com recebido zero e vai subindo enquanto alguém conta a caixa —, e nesse intervalo divergir sem motivo é o estado normal, não defeito. Por isso a regra é da transição, e não um CHECK que recusaria a digitação.\n\nA recusa é **400 com `fields[]`**, apontando `items[N].divergenceReason` linha a linha: é isso que permite à tela pôr o cursor no lugar certo. Um 409 diria só \"não dá\". Recebimento sem linha nenhuma é 409 — conferência vazia declarada conferida é a nota dizendo que o caminhão veio vazio, e isso se resolve apagando o rascunho.\n\n**Sem empresa ativa na sessão é 409** (`urn:cabinet:erro:sem-empresa-ativa`), não 400: a empresa vem da sessão e não do cliente, então \"sem empresa\" é operador recém-criado, não requisição malformada. A LISTAGEM, por outro lado, responde `{rows: [], total: 0}`.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "required": true
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GoodsReceiptDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request — linha divergente sem motivo declarado, em `fields[]`.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — recebimento fora do rascunho ou sem itens (`urn:cabinet:erro:transicao-invalida`), ou sem empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/goods-receipts/{id}/post": {
      "post": {
        "tags": ["compras"],
        "operationId": "PostGoodsReceipt",
        "description": "Proposto. LANÇA o recebimento: `checked` → `posted`. Cada linha com quantidade vira entrada no kardex (`ListStockMovements`), marcada como compra e apontando para este documento.\n\n**Permissão: `estoque:movimentar` — e ela é DIFERENTE da das outras seis de propósito.** Conferir é contar caixa; lançar é mexer no saldo de que alguém responde. O estado do meio existe justamente porque as duas decisões são de pessoas diferentes em horas diferentes, e uma permissão só faria quem conta ter de também lançar — que é como divergência vira \"depois eu ajusto\".\n\n**Divergência NÃO bloqueia o lançamento.** Chegaram 8 das 10 pedidas, com motivo declarado: lança 8. Recusar faria a mercadoria que ESTÁ no galpão não existir no sistema, que é exatamente o defeito que o kardex existe para não ter. A divergência fica na linha: ela é a pergunta do comprador ao fornecedor, não uma trava do almoxarifado.\n\n**O efeito é atômico e não repetível.** Ou o documento fica `posted` com todos os seus movimentos, ou nada acontece; e lançar de novo é 409 (`urn:cabinet:erro:transicao-invalida`), nunca repetição em silêncio — sem isso um duplo clique dobraria o estoque.\n\n**O que este lançamento NÃO baixa: `PurchaseReplenishmentRowDto.qtyAllocated`** — a reserva de VENDA, peça física já prometida a um cliente. Entrada de compra não desfaz promessa de venda: ela é o que a torna cumprível. Decrementá-la aqui liberaria, em silêncio, peça que alguém já vendeu, e a conta que o comprador lê (`qtyAvailable = qtyOnHand − qtyAllocated`) passaria a prometer duas vezes a mesma peça.\n\n**A reserva que ele de fato baixa é a de CHEGADA FUTURA: `PurchaseReplenishmentRowDto.qtyOnOrder`**, \"o que vem em ordem enviada e ainda não recebida\". Ela mora na ordem de compra, não no saldo, e é por ela que a sugestão de reposição para de pedir de novo o que já desceu do caminhão. Sem esse elo, a peça recebida conta duas vezes e a sugestão sai MENOR que a real — o comprador compra de menos.\n\n**Sem empresa ativa na sessão é 409** (`urn:cabinet:erro:sem-empresa-ativa`), não 400: a empresa vem da sessão e não do cliente, então \"sem empresa\" é operador recém-criado, não requisição malformada. A LISTAGEM, por outro lado, responde `{rows: [], total: 0}`.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "required": true
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GoodsReceiptDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — recebimento ainda não conferido ou já lançado (`urn:cabinet:erro:transicao-invalida`), ou sem empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/cost-profiles": {
      "get": {
        "tags": ["precos"],
        "operationId": "ListCostProfiles",
        "description": "Proposto. Os perfis de custo da empresa ativa — a metade de COMPRA da formação de preço (`Custo` do legado).\n\nEmpresa ativa vazia devolve `{rows: [], total: 0}`, como toda listagem deste contrato: aqui a empresa vem da sessão, e \"sem empresa\" é o operador recém-criado, não erro do cliente.",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "description": "Busca por `name`.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "supplierId",
            "in": "query",
            "description": "Filtra pelos perfis de UM fornecedor. É a pergunta que a tela do fornecedor faz, e ela não cabe em `q` — `q` casa texto, e aqui a chave é identidade.",
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "sortBy",
            "in": "query",
            "description": "Whitelist: `name`, `supplierName`, `active`, `createdAt`. Campo fora dela é 400 `ordenacao-invalida`.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfCostProfileDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      },
      "post": {
        "tags": ["precos"],
        "operationId": "CreateCostProfile",
        "description": "Proposto. Cria um perfil de custo para um fornecedor da empresa ativa.\n\n**Permissão: `custo:gerenciar`.** Não é `parceiros:editar` nem `produtos:editar`, e a separação tem consequência: a cascata de descontos é o que decide a margem de TODA venda daquele fornecedor, e errar um dígito aqui não aparece em nenhuma tela de venda — aparece no fim do mês. Mesma linha de corte que separou `pagamento:gerenciar` de `orcamento:editar`.\n\n409 `codigo-ja-cadastrado` quando o fornecedor já tem perfil com este nome na empresa.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CostProfileWriteRequest"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CostProfileDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/cost-profiles/{id}": {
      "get": {
        "tags": ["precos"],
        "operationId": "GetCostProfile",
        "description": "Proposto. Um perfil de custo. Sem empresa ativa é **409** `sem-empresa-ativa` — detalhe por id exige empresa, ao contrário da listagem.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CostProfileDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": ["precos"],
        "operationId": "UpdateCostProfile",
        "description": "Proposto. Substitui o perfil INTEIRO. Campo omitido volta ao padrão — não conserva o que estava lá.\n\n**É também o caminho da desativação** (`active: false`): não há `DELETE` neste contrato, e perfil de custo é o caso em que a regra mais se paga — ver `CostProfileDto.active`.\n\n**O fornecedor pode ser trocado**, e isso é escrita normal: o perfil é a condição comercial, e ela migra quando o cadastro do fornecedor é consolidado. O que não muda é a empresa.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CostProfileWriteRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CostProfileDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/cost-profiles/{id}/simulate": {
      "post": {
        "tags": ["precos"],
        "operationId": "SimulateCostProfile",
        "description": "Proposto. Roda a cascata de custo de UM item sob este perfil e devolve a decomposição — e o lucro, quando a venda líquida vem junto.\n\n**É `POST` e não `GET` com query.** São dezoito parcelas de entrada e saída, e a decomposição não é cacheável nem endereçável: o mesmo perfil com outro preço de tabela é outra resposta. `POST` também deixa a operação fora da whitelist de ordenação e paginação, que não significam nada aqui.\n\n**Não grava nada.** É cálculo puro sobre o perfil — nenhuma linha de `audit_log` nasce daqui, porque não há mutação a rastrear. A permissão é a de LEITURA da família: quem tem vínculo na empresa simula.\n\nO custo sai **sem ICMS** (`excludesIcms: true`); ver `CostSimulationDto`.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CostSimulationRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CostSimulationDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/orders/{id}/participants": {
      "get": {
        "tags": ["comissoes"],
        "operationId": "ListOrderParticipants",
        "description": "Proposto. **Quem participa deste pedido** — atendentes e profissionais, com o percentual e as faixas CONGELADOS nele.\n\nÉ sub-recurso e não campo do documento porque a participação tem vida própria depois da emissão: ela é transferida (`POST .../professional`), congela cadastro que muda depois, e é a origem do que a apuração paga. Ler tudo junto no `GET` do pedido faria a tela de venda carregar dinheiro de comissão em toda abertura.\n\n**A grade da aba Participação chega em uma requisição**, com as faixas embutidas em cada linha — ver `OrderParticipantDto.tiers`.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "sortBy",
            "in": "query",
            "description": "Whitelist: `personName`, `role`, `percent`, `isPrincipal`. Campo fora dela é 400.\n\nNão se ordena por faixa: ela é linha de dentro da participação, e ordenar o conjunto por um campo do filho é ordenar por qual dos N.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfOrderParticipantDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found — o `id` não é pedido da empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": ["comissoes"],
        "operationId": "ReplaceOrderParticipants",
        "description": "Proposto. Substitui a grade de participação INTEIRA. Linha que não vier no corpo é linha removida.\n\n**Papel: `admin` ou superior, e é INTERINO** — mesma linha de corte de `/api/payment-terms`. Quem edita participação decide quanto sai do caixa da empresa para quem indicou a venda, e o erro só aparece no fechamento, quando o título já nasceu. Vira permissão nomeada quando o modelo por AÇÃO (api#84) entregar; até lá o papel é o piso, porque a matriz é por papel.\n\n**O que o servidor faz com cada linha, e é a regra do congelamento:**\n\n- linha COM `id` conhecido — mesma participação: as faixas já congeladas nela FICAM, e muda só o que este corpo traz;\n- linha SEM `id` — nova: o servidor copia para dentro dela as faixas do PERFIL da pessoa (`/api/employees/{id}/commission-tiers` ou `/api/partners/{id}/commission-tiers`) como elas estão HOJE, e daí em diante elas são deste documento;\n- linha ausente — removida, com as faixas dela.\n\nA cópia é atômica com a criação da linha: perfil que mude no meio da gravação não deixa o documento com metade de cada versão.\n\n---\n\n**As recusas, e nenhuma apara em silêncio** (400 com `fields[]` apontando `participants.<n>.<campo>`):\n\n1. `role` sem a pessoa dele, ou com as duas (`employeeId` **e** `partnerId`). Participação que a apuração não sabe pagar.\n2. `partnerId` de parceiro **sem o papel `professional`** — mesma recusa de `TransferProfessionalRequest`.\n3. Duas linhas do MESMO papel com `isPrincipal: true`. Dois principais não significam nada; a recusa é mais restrita que o legado nos dois papéis, de propósito, e o risco que ela cria é de ETL — documento antigo com dois `Principal = 1` bate aqui.\n4. A mesma pessoa duas vezes no mesmo papel.\n5. `percent` fora de `0`–`1000000`.\n6. `id` que não é participação DESTE pedido.\n\n**Não há recusa por SOMA dos percentuais, e a ausência é medida e não esquecimento:** o legado não tem essa restrição em `VendaAtendente` nem em `VendaIndicacao` — `Porcentagem` é coluna por linha, sem `CHECK` de conjunto —, e três atendentes com 100% cada é dado que existe lá. Fixar um teto aqui recusaria dado real na importação. Fica declarado como PONTO ABERTO: o caso real conferido do trilho responde se a soma tem significado, e se tiver, a regra entra por PR neste contrato.\n\n**409 quando a grade tenta trocar o profissional PRINCIPAL** (`urn:cabinet:erro:profissional-exige-transferencia`): essa troca tem data e justificativa, e quem a faz é `POST /api/orders/{id}/professional`. Acrescentar, remover ou mudar o percentual de profissional NÃO principal passa por aqui normalmente.\n\n**409 quando a linha já virou dinheiro** (`urn:cabinet:erro:participante-ja-apurado`): participação com linha de fechamento não se remove nem muda de percentual pelo documento. O que já foi pago se corrige com fechamento novo.\n\n**409 no pedido cancelado** (`urn:cabinet:erro:transicao-invalida`): documento encerrado não muda de participação.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/OrderParticipantsWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfOrderParticipantDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found — o `id` não é pedido da empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — QUATRO causas, distinguidas pelo `type` e nunca pelo texto: `urn:cabinet:erro:sem-empresa-ativa`, `urn:cabinet:erro:profissional-exige-transferencia`, `urn:cabinet:erro:participante-ja-apurado` e `urn:cabinet:erro:transicao-invalida`.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/employees/{id}/commission-tiers": {
      "get": {
        "tags": ["comissoes"],
        "operationId": "ListEmployeeCommissionTiers",
        "description": "Proposto. O PERFIL de faixas de um COLABORADOR — a comissão.\n\nÉ o cadastro que diz quanto a pessoa COSTUMA ganhar. O que ela ganhou num documento está congelado lá, e não aqui — ver `OrderParticipantTierDto`.\n\n**A faixa GERAL é a linha de `productGroupId` nulo**, e é ela que vira o percentual do participante quando a pessoa entra num documento. Perfil sem nenhuma faixa é lista vazia, e significa \"ainda não configurado\": a pessoa participa e a apuração não acha percentual.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "sortBy",
            "in": "query",
            "description": "Whitelist: `productGroupName`, `discountPercent`, `percent`, `active`. Campo fora dela é 400.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfCommissionTierDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found — o `id` não é colaborador da empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": ["comissoes"],
        "operationId": "ReplaceEmployeeCommissionTiers",
        "description": "Proposto. Substitui o perfil de faixas de um colaborador INTEIRO. Faixa que não vier é faixa removida.\n\n**Papel: `admin` ou superior, e é INTERINO** — pelo motivo de `ReplaceOrderParticipants`: é o número que decide quanto sai do caixa.\n\n**Mudar o perfil NÃO reescreve documento já gravado.** Cada participação carrega a cópia das faixas que valiam quando ela nasceu; o que muda aqui vale da PRÓXIMA participação em diante. É o que o legado faz ao copiar `IndicacaoGrupProd` para `VendaIndicacaoGrupProd` — e a razão de a segunda ter 30× as linhas da primeira. A alternativa (ler o perfil na hora de apurar) faria o relatório do mês passado mudar de valor sozinho.\n\n**O desconto que reduz o ganho do colaborador é o mesmo mecanismo, e o legado o escreve por extenso:** `Venda.Ven_DescGanhoVenda` é preenchida a partir de `Ven_DescontoPorc` (`update venda set Ven_DescGanhoVenda = Ven_DescontoPorc …`). É o análogo interno da faixa do profissional — quanto mais desconto, menor o ganho.\n\n---\n\n**As recusas** (400 com `fields[]` apontando `tiers.<n>.<campo>`):\n\n1. Duas faixas com a MESMA condição — mesmo `productGroupId` (nulo inclusive), mesmo `operator` e mesmo `discountPercent`. Condição repetida com percentuais diferentes faz o cálculo depender da ordem de leitura, que é o defeito que ninguém reproduz.\n2. `productGroupId` que não é um `catalog-lookups` **ativo** de kind `GRUPO_PRODUTO`. Id de outro `kind` é 400 e não 404: o que está errado é o campo do corpo.\n3. `percent` ou `discountPercent` fora de `0`–`1000000`.\n4. `id` que não é faixa DESTA pessoa.\n\n`operator` fora do vocabulário é barrado pelo schema, antes do handler — é o que o vocabulário fechado existe para fazer.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CommissionTiersWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfCommissionTierDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found — o `id` não é colaborador da empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — `urn:cabinet:erro:sem-empresa-ativa` quando a sessão não tem empresa escolhida. Toda escrita de `/api/` responde isso antes de olhar o corpo.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/partners/{id}/commission-tiers": {
      "get": {
        "tags": ["comissoes"],
        "operationId": "ListPartnerCommissionTiers",
        "description": "Proposto. O PERFIL de faixas de um PROFISSIONAL EXTERNO — a participação/Reserva Técnica.\n\nÉ o cadastro que diz quanto a pessoa COSTUMA ganhar. O que ela ganhou num documento está congelado lá, e não aqui — ver `OrderParticipantTierDto`.\n\n**A faixa GERAL é a linha de `productGroupId` nulo**, e é ela que vira o percentual do participante quando a pessoa entra num documento. Perfil sem nenhuma faixa é lista vazia, e significa \"ainda não configurado\": a pessoa participa e a apuração não acha percentual.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "sortBy",
            "in": "query",
            "description": "Whitelist: `productGroupName`, `discountPercent`, `percent`, `active`. Campo fora dela é 400.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfCommissionTierDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found — o `id` não é parceiro da empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": ["comissoes"],
        "operationId": "ReplacePartnerCommissionTiers",
        "description": "Proposto. Substitui o perfil de faixas de um profissional externo INTEIRO. Faixa que não vier é faixa removida.\n\n**Papel: `admin` ou superior, e é INTERINO** — pelo motivo de `ReplaceOrderParticipants`: é o número que decide quanto sai do caixa.\n\n**Mudar o perfil NÃO reescreve documento já gravado.** Cada participação carrega a cópia das faixas que valiam quando ela nasceu; o que muda aqui vale da PRÓXIMA participação em diante. É o que o legado faz ao copiar `IndicacaoGrupProd` para `VendaIndicacaoGrupProd` — e a razão de a segunda ter 30× as linhas da primeira. A alternativa (ler o perfil na hora de apurar) faria o relatório do mês passado mudar de valor sozinho.\n\n**Parceiro sem o papel `professional` é 400**: perfil de participação só existe para quem indica venda.\n\n---\n\n**As recusas** (400 com `fields[]` apontando `tiers.<n>.<campo>`):\n\n1. Duas faixas com a MESMA condição — mesmo `productGroupId` (nulo inclusive), mesmo `operator` e mesmo `discountPercent`. Condição repetida com percentuais diferentes faz o cálculo depender da ordem de leitura, que é o defeito que ninguém reproduz.\n2. `productGroupId` que não é um `catalog-lookups` **ativo** de kind `GRUPO_PRODUTO`. Id de outro `kind` é 400 e não 404: o que está errado é o campo do corpo.\n3. `percent` ou `discountPercent` fora de `0`–`1000000`.\n4. `id` que não é faixa DESTA pessoa.\n\n`operator` fora do vocabulário é barrado pelo schema, antes do handler — é o que o vocabulário fechado existe para fazer.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CommissionTiersWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfCommissionTierDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found — o `id` não é parceiro da empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — `urn:cabinet:erro:sem-empresa-ativa` quando a sessão não tem empresa escolhida. Toda escrita de `/api/` responde isso antes de olhar o corpo.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/technical-reserves": {
      "get": {
        "tags": ["comissoes"],
        "operationId": "ListTechnicalReserves",
        "description": "Proposto. Os lançamentos de Reserva Técnica da empresa ativa — `Reserva_tecnica` do legado.\n\n**Não publica `filters` de propósito**, pela razão de `/api/stock-locations`: os recortes que a tela precisa (pedido, profissional, situação) já são parâmetros nomeados, e o filtro estruturado aqui seria parâmetro sem consumidor, cobrando whitelist dos dois lados.",
        "parameters": [
          {
            "name": "orderId",
            "in": "query",
            "description": "Só as reservas deste pedido.",
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "partnerId",
            "in": "query",
            "description": "Só as deste profissional.",
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "status",
            "in": "query",
            "description": "`active` ou `cancelled`. Ausente = as duas — cancelada continua legível, como todo documento encerrado deste contrato.",
            "schema": {
              "type": "string",
              "enum": ["active", "cancelled"]
            }
          },
          {
            "name": "from",
            "in": "query",
            "description": "Primeiro dia de `issuedAt`, inclusive.",
            "schema": {
              "type": "string",
              "format": "date"
            }
          },
          {
            "name": "to",
            "in": "query",
            "description": "Último dia de `issuedAt`, inclusive.",
            "schema": {
              "type": "string",
              "format": "date"
            }
          },
          {
            "name": "sortBy",
            "in": "query",
            "description": "Whitelist: `issuedAt`, `partnerName`, `orderNumber`, `totalCents`, `status`. Campo fora dela é 400.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfTechnicalReserveDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      },
      "post": {
        "tags": ["comissoes"],
        "operationId": "CreateTechnicalReserve",
        "description": "Proposto. Lança a Reserva Técnica de um profissional sobre um pedido — e o VALOR é do servidor.\n\n**Papel: `admin` ou superior, e é INTERINO** — pelo motivo de `ReplaceOrderParticipants`.\n\nO servidor apura sobre a participação CONGELADA no documento: a faixa cujo `operator` casa com o desconto do pedido decide o percentual por grupo, o percentual geral vale para o resto e para serviço, e a devolução é abatida da base antes da conta (`FrmRT.Qrydevolucao` desconta `DevolucaoProduto` por grupo — fechamento que a ignorasse pagaria a mais). O resultado sai quebrado em `productCents` e `serviceCents`, como o legado quebra em `Ret_tec_luminaria`/`_materiais`/`_servico`.\n\n**Recusas:**\n\n- pedido cancelado é 409 (`urn:cabinet:erro:transicao-invalida`) — venda desfeita não paga indicação;\n- pedido SEM participação deste profissional é 409 (`urn:cabinet:erro:transicao-invalida`): não há sobre o que apurar, e inventar uma participação aqui seria pagar quem o documento não diz que indicou;\n- parceiro sem o papel `professional` é 400 apontando o campo;\n- o par (pedido, profissional) já com reserva ATIVA é 409 (`urn:cabinet:erro:reserva-ja-lancada`) — lançar de novo duplicaria o que o fechamento vai pagar.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TechnicalReserveWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TechnicalReserveDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found — `orderId` ou `partnerId` não é da empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — TRÊS causas, distinguidas pelo `type`: `urn:cabinet:erro:sem-empresa-ativa`, `urn:cabinet:erro:transicao-invalida` e `urn:cabinet:erro:reserva-ja-lancada`.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/technical-reserves/{id}/cancel": {
      "post": {
        "tags": ["comissoes"],
        "operationId": "CancelTechnicalReserve",
        "description": "Proposto. Cancela um lançamento de Reserva Técnica.\n\n**Cancela, não apaga** — `Ret_situacao` do legado, e a mesma regra do documento de venda: o que existiu continua legível.\n\n**409 quando a reserva já virou linha de fechamento** (`urn:cabinet:erro:participante-ja-apurado`): cancelar o que já foi pago deixaria um título no contas a pagar sem origem. A correção é fechamento novo.\n\nReserva já cancelada é 409 (`urn:cabinet:erro:transicao-invalida`) — cancelar duas vezes não é idempotência, é sinal de que quem pediu está vendo outra tela.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TechnicalReserveDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found — o `id` não é reserva da empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — TRÊS causas, distinguidas pelo `type`: `urn:cabinet:erro:sem-empresa-ativa`, `urn:cabinet:erro:participante-ja-apurado` e `urn:cabinet:erro:transicao-invalida`.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/commissions/earnings": {
      "get": {
        "tags": ["comissoes"],
        "operationId": "GetCommissionEarnings",
        "description": "Proposto. **A APURAÇÃO** — o que cada pessoa ganhou no período, calculado no servidor sobre documento fechado. É consulta: nada aqui grava.\n\n**O eixo do período é a CONCLUSÃO do projeto, e não a data de fechamento da venda.** A função do legado que responde esta pergunta (`VendaAnoAtendente`, `rotinas.sql:2253`) recorta por `Ven_DataConclusao`; `Ven_DataFechaVenda` é outra coisa. Trocar os dois mudaria de mês o ganho de todo projeto longo.\n\n**A devolução entra pela competência de CAIXA**, também medido no legado: ela abate a base no mês em que o dinheiro voltou, não no da venda.\n\n**A fórmula do legado NÃO foi portada, e a decisão está documentada:** `Emp_Codigo = 1` está FIXO no código de lá (defeito, não regra — aqui a empresa é a da sessão), o eixo `@Desconto='N'` nunca chegou a ser implementado, e o guard `IF @VLProjeto = NULL` está quebrado em SEIS funções (`= NULL` nunca é verdadeiro com `ANSI_NULLS ON`), devolvendo `NULL` onde deveria devolver zero. O que foi portado é o EIXO e a base, não a aritmética.\n\n**Publica paginação porque a lista é por PARTICIPAÇÃO** — um mês com 300 pedidos e dois participantes cada dá 600 linhas —, e `summary` traz os totais do período INTEIRO, não os da página: somar a página daria o total de 20 registros com cara de total do mês.",
        "parameters": [
          {
            "name": "from",
            "in": "query",
            "required": true,
            "description": "Primeiro DIA do período, inclusive. Obrigatório: apuração sem recorte responde outra pergunta e cresce para sempre.",
            "schema": {
              "type": "string",
              "format": "date"
            }
          },
          {
            "name": "to",
            "in": "query",
            "required": true,
            "description": "Último DIA do período, inclusive. O recorte é por DIA e não por instante: quem pergunta por agosto quer o dia 31 inteiro.",
            "schema": {
              "type": "string",
              "format": "date"
            }
          },
          {
            "name": "role",
            "in": "query",
            "description": "`attendant` ou `professional`. Ausente = os dois.",
            "schema": {
              "type": "string",
              "enum": ["attendant", "professional"]
            }
          },
          {
            "name": "employeeId",
            "in": "query",
            "description": "Só o ganho deste colaborador.",
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "partnerId",
            "in": "query",
            "description": "Só o ganho deste profissional externo.",
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "sortBy",
            "in": "query",
            "description": "Whitelist: `amountCents`, `personName`, `orderNumber`, `baseCents`, `percent`. Campo fora dela é 400.\n\nPadrão: `amountCents`, decrescente — a pergunta que a tela faz primeiro é quem ganhou mais.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CommissionEarningsReportDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      }
    },
    "/api/commissions/closings": {
      "get": {
        "tags": ["comissoes"],
        "operationId": "ListCommissionClosings",
        "description": "Proposto. Os fechamentos de comissão da empresa ativa — `FechamentoComissao` do legado.\n\n**Um fechamento por período**, e a listagem é o que a tela consulta antes de fechar outro: período que já tem fechamento recusa em 409, e ver a lista evita o gesto.",
        "parameters": [
          {
            "name": "sortBy",
            "in": "query",
            "description": "Whitelist: `periodStart`, `periodEnd`, `status`, `closedAt`, `totalCents`. Campo fora dela é 400.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfCommissionClosingDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      },
      "post": {
        "tags": ["comissoes"],
        "operationId": "CreateCommissionClosing",
        "description": "Proposto. **FECHA o período** — o passo em que participação apurada vira dinheiro.\n\n**Papel: `admin` ou superior, e é INTERINO** — este é o gesto que gera título no contas a pagar. Vira permissão nomeada quando o modelo por AÇÃO (api#84) entregar.\n\n**A idempotência é do BANCO, e a chave é a do legado.** Cada linha gravada carrega o par (`sourceType`, `sourceId`) — o mesmo papel do par (tipo de documento RESERVADO, id do lançamento) que o Softlux usa em `contas_apagar` com `Tpd_codigo = 1004` e `Ctp_cod_documento = Reserva_tecnica.Ret_codigo`. A unicidade **não inclui o fechamento**, de propósito: fosse por fechamento, refechar o mesmo mês com datas um dia diferentes pagaria tudo de novo e cada duplicata seria legítima. Conferir antes de inserir não resolveria — entre a consulta e a gravação cabe outro pedido; quem responde é a chave.\n\n**Dois desfechos bem-sucedidos, e nenhum é erro** — ver `CommissionClosingResultDto`. Período sem ganho responde `nothing-to-close` e **não grava**: fechamento vazio travaria o período para quando o lançamento atrasado entrasse.\n\n**Recusas:**\n\n- período que já tem fechamento é 409 (`urn:cabinet:erro:periodo-ja-fechado`), com o `detail` dizendo quando fechou;\n- alguma origem do período JÁ paga em outro fechamento é 409 (`urn:cabinet:erro:origem-ja-paga`), e **nada é gravado** — a transação inteira volta. `detail` diz qual origem, porque a saída é ajustar o período, não insistir;\n- `periodEnd` anterior a `periodStart` é 400 apontando o campo.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CommissionClosingWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CommissionClosingResultDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict — TRÊS causas, distinguidas pelo `type` e nunca pelo texto: `urn:cabinet:erro:sem-empresa-ativa`, `urn:cabinet:erro:periodo-ja-fechado` e `urn:cabinet:erro:origem-ja-paga`.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/commissions/closings/{id}/entries": {
      "get": {
        "tags": ["comissoes"],
        "operationId": "ListCommissionClosingEntries",
        "description": "Proposto. As LINHAS de um fechamento — quem recebeu, por qual fato, sobre qual base.\n\n**Append-only e imutáveis**: não há escrita nesta coleção, aqui nem em lugar nenhum. Linha de fechamento é o registro de que alguém foi pago, e correção se faz com fechamento novo — mesmo regime do `audit_log`.\n\nÉ sub-recurso e não campo do fechamento porque a coleção é grande: um mês de operação real tem centenas de linhas, e a listagem de fechamentos carregaria todas elas a cada abertura da tela.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "sortBy",
            "in": "query",
            "description": "Whitelist: `personName`, `amountCents`, `baseCents`, `sourceNumber`. Campo fora dela é 400.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfCommissionClosingEntryDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found — o `id` não é fechamento da empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/orders/{id}/fulfillment": {
      "get": {
        "tags": ["vendas"],
        "operationId": "GetOrderFulfillment",
        "description": "Proposto. A SITUAÇÃO FÍSICA do pedido, item a item — a consulta nº 17 do volume 02 do legado.\n\nLeitura, e por isso NÃO é filtrada por papel: quem tem vínculo com a empresa lê tudo o que ela tem, e é para isso que `viewer` existe. **Sem empresa ativa é 409**, como todo detalhe por id.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OrderFulfillmentDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — sem empresa ativa na sessão.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/orders/{id}/items/{lineNumber}/release": {
      "post": {
        "tags": ["vendas"],
        "operationId": "ReleaseOrderItem",
        "description": "Proposto. LIBERA a linha para separação e RESERVA o saldo no depósito.\n\n**É a única operação da escada que exige a permissão `venda:liberar-entrega`** — separar e entregar são execução do que já foi autorizado, e no legado o gesto é o mesmo `FrmLiberarProdutos` (\"Liberar separação e entrega de produtos\"). Quem não tem a permissão recebe 403 `urn:cabinet:erro:papel-insuficiente`.\n\nLiberar não baixa estoque: a peça continua no galpão, só deixa de estar disponível para a próxima venda. Acima do vendido é 409 `urn:cabinet:erro:liberacao-acima-do-vendido`.\n\nO depósito se decide AQUI e a separação o herda. Passar um depósito diferente na separação é a exceção, não a regra.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "lineNumber",
            "in": "path",
            "required": true,
            "description": "Coluna `Item` da linha do pedido.",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ReleaseOrderItemRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FulfillmentFactDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found — pedido, linha ou depósito inexistente nesta empresa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — sem empresa ativa, pedido cancelado, ou quantidade acima do vendido (`urn:cabinet:erro:liberacao-acima-do-vendido`).",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/orders/{id}/items/{lineNumber}/pick": {
      "post": {
        "tags": ["vendas"],
        "operationId": "PickOrderItem",
        "description": "Proposto. SEPARA a linha: a peça sai da prateleira e o estoque BAIXA, na mesma transação do fato. O movimento do kardex nasce com origem tipada (`sale` + o pedido), então o estorno não precisa casar texto livre para achar a linha.\n\nA reserva morre junto com a baixa — separar e continuar reservado contaria a mesma peça duas vezes para a próxima venda.\n\n**Acima do liberado é 409 `urn:cabinet:erro:separacao-sem-liberacao`.** O gate é o CHECK monótono do banco e não depende de papel nenhum: quem pula a liberação não separa.\n\nLinha de SERVIÇO separa sem mover peça — ela não tem variante, e resolver depósito para ela apontaria a instalação de uma cozinha para uma prateleira.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "lineNumber",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PickOrderItemRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FulfillmentFactDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — sem empresa ativa, pedido cancelado, ou quantidade acima do liberado (`urn:cabinet:erro:separacao-sem-liberacao`).",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/picking-queue": {
      "get": {
        "tags": ["vendas"],
        "operationId": "ListPickingQueue",
        "description": "Proposto. A FILA DE SEPARAÇÃO: as linhas liberadas que ainda não saíram da prateleira, de todos os pedidos ativos da empresa.\n\n**Sem empresa ativa devolve `{rows: [], total: 0}`** — o operador recém-criado, ainda sem vínculo, não tem fila; não é erro dele.\n\nA ordem padrão é a data prometida (`NULLS LAST`) e o número do pedido como desempate: o que atrasa aparece em cima, e item sem data combinada não fura a fila de quem tem uma.",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "description": "Busca por número do pedido, cliente ou descrição do item.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortBy",
            "in": "query",
            "description": "Whitelist: `scheduledDeliveryAt`, `orderNumber`, `customerName`, `pendingPick`. Campo fora dela é 400 `urn:cabinet:erro:ordenacao-invalida`.\n\n`description` fica de fora: ordenar a fila do galpão por nome de peça embaralha as linhas do mesmo pedido, que é justamente o agrupamento que quem separa usa para não fazer duas viagens.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "orderId",
            "in": "query",
            "description": "Só a fila deste pedido.",
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfPickingQueueItemDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      }
    },
    "/api/deliveries": {
      "get": {
        "tags": ["vendas"],
        "operationId": "ListDeliveries",
        "description": "Proposto. Os ROMANEIOS da empresa ativa — o \"Controle de Entrega\" do legado.\n\n**Sem empresa ativa devolve `{rows: [], total: 0}`.**",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "description": "Busca por número do romaneio, pedido, cliente ou transportadora.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortBy",
            "in": "query",
            "description": "Whitelist: `number`, `scheduledFor`, `deliveredAt`, `status`. Campo fora dela é 400.\n\n`customerName` e `carrierName` ficam de fora pelo motivo de sempre — são eco de outra tabela, congelado num caso e por junção no outro.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "status",
            "in": "query",
            "description": "Recorta por situação do romaneio (`open`, `closed`, `cancelled`).",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "orderId",
            "in": "query",
            "description": "Só os romaneios deste pedido.",
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfDeliveryDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      },
      "post": {
        "tags": ["vendas"],
        "operationId": "CreateDelivery",
        "description": "Proposto. Abre um romaneio para o pedido. Nasce `open` e vazio; os itens entram por `POST /api/deliveries/{id}/items`.",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateDeliveryRequest"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DeliveryDetailDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found — pedido inexistente nesta empresa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — sem empresa ativa, ou pedido cancelado.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/deliveries/{id}": {
      "get": {
        "tags": ["vendas"],
        "operationId": "GetDelivery",
        "description": "Proposto. O romaneio com os itens lançados nele.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DeliveryDetailDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — sem empresa ativa na sessão.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/deliveries/{id}/items": {
      "post": {
        "tags": ["vendas"],
        "operationId": "AddDeliveryItem",
        "description": "Proposto. Lança um item ENTREGUE dentro do romaneio. Não mexe em estoque — a peça já saiu do galpão na separação.\n\n**Só entra em romaneio ABERTO** (409 `urn:cabinet:erro:entrega-fechada`): fechado é documento assinado, e acrescentar item a ele reescreveria o que o cliente conferiu; cancelado, idem, pelo motivo oposto.\n\nO romaneio tem de ser do MESMO pedido da linha (409 `urn:cabinet:erro:entrega-de-outro-pedido`), e a quantidade não passa do separado (409 `urn:cabinet:erro:entrega-sem-separacao`).",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AddDeliveryItemRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FulfillmentFactDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found — romaneio ou linha inexistente nesta empresa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — sem empresa ativa, romaneio não aberto (`urn:cabinet:erro:entrega-fechada`), romaneio de outro pedido (`urn:cabinet:erro:entrega-de-outro-pedido`) ou quantidade acima do separado (`urn:cabinet:erro:entrega-sem-separacao`).",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/deliveries/{id}/close": {
      "post": {
        "tags": ["vendas"],
        "operationId": "CloseDelivery",
        "description": "Proposto. FECHA o romaneio, exigindo quando saiu e quem recebeu — seis meses depois, quando o cliente diz que não recebeu, é essa linha que responde.\n\n**Romaneio vazio não fecha** (409 `urn:cabinet:erro:entrega-vazia`). Fechar de novo, ou fechar o cancelado, é 409 `urn:cabinet:erro:entrega-fechada`.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CloseDeliveryRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DeliveryDetailDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — sem empresa ativa, romaneio já fechado ou cancelado (`urn:cabinet:erro:entrega-fechada`), ou romaneio sem itens (`urn:cabinet:erro:entrega-vazia`).",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/deliveries/{id}/cancel": {
      "post": {
        "tags": ["vendas"],
        "operationId": "CancelDelivery",
        "description": "Proposto. Cancela o romaneio. **NÃO desfaz os fatos já lançados nele** — o log de entregas é append-only por GRANT, e corrigir o passado é estornar, não editar. O ato de estorno (o `cep_quantidade_entregueRET` do legado) é a fase seguinte e ainda não tem caminho.\n\nCancelar duas vezes, ou cancelar o fechado, é 409 `urn:cabinet:erro:entrega-fechada`.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DeliveryDetailDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — sem empresa ativa, ou romaneio já fechado/cancelado (`urn:cabinet:erro:entrega-fechada`).",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/price-indexes": {
      "get": {
        "tags": ["produtos"],
        "operationId": "ListPriceIndexes",
        "description": "Proposto. Os índices de venda da empresa ativa, um por fornecedor.\n\n**Não há detalhe por id nesta família**, pelo mesmo desenho de `/api/payment-terms` e `/api/stock-locations`: é um punhado de linhas por empresa e a listagem já entrega cada uma inteira — um `GET /{id}` devolveria a segunda cópia do que a listagem acabou de dar.\n\n**Não publica `filters`** pela razão de sempre: sobre um punhado de linhas que já vieram inteiras, o filtro estruturado seria parâmetro sem consumidor, cobrando whitelist dos dois lados.",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "description": "Busca por nome do fornecedor.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortBy",
            "in": "query",
            "description": "Whitelist: `supplierName`, `indexValue`, `active`. Campo fora dela é 400.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfPriceIndexDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      },
      "post": {
        "tags": ["produtos"],
        "operationId": "CreatePriceIndex",
        "description": "Proposto. Cadastra o índice de um fornecedor na empresa ativa.\n\n**Papel: `admin` ou superior**, a mesma linha de corte de `/api/cost-profiles` e pela mesma razão: índice e perfil de custo são os dois fatores do mesmo preço, e proteger um deixando o outro aberto não protege nada. Isto não é operação de atendimento — o índice decide o preço de TODA peça daquele fornecedor, em todo orçamento que vier depois.\n\nFornecedor que já tem índice é **409** — ver `PriceIndexDto.supplierId`.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PriceIndexWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PriceIndexDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/price-indexes/{id}": {
      "put": {
        "tags": ["produtos"],
        "operationId": "UpdatePriceIndex",
        "description": "Proposto. Substitui o índice INTEIRO.\n\n**Alterar o índice NÃO reescreve documento já gravado.** O orçamento CARIMBA `unitPriceCents` na emissão, e é esse valor congelado que vale — repreçar documento existente é ação própria e explícita (\"Atualizar Valores\" do legado), não efeito colateral de mexer no cadastro. Sem isso, corrigir um índice mudaria em silêncio o total de todo orçamento em aberto, e ninguém saberia dizer por quê.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PriceIndexWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PriceIndexDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/table-prices/{variantId}": {
      "get": {
        "tags": ["produtos"],
        "operationId": "ListVariantTablePrices",
        "description": "Proposto. As tabelas de preço desta variante, uma por fornecedor.\n\nLista curta e sem paginação — são os fornecedores da peça, não um cadastro. Mesmo desenho de `/api/variants/{variantId}/stock-balances`, que também devolve o conjunto inteiro por variante.\n\n**O caminho é `/api/table-prices/{variantId}` e não `/api/variants/{variantId}/table-prices`, e a razão é do SERVIDOR.** A matriz de permissão do `cabinet-erp-api` casa por PREFIXO de caminho, e o trecho que distinguiria a tabela de preço das outras duas coisas que pendem da variante (`stock-movements`, `stock-balances`) vem DEPOIS do parâmetro — onde prefixo nenhum alcança. Sob o caminho aninhado, a tabela herdaria a exigência do kardex: quem movimenta estoque passaria a editar PREÇO, em silêncio e sem ninguém ter decidido isso. Pôr o recurso na frente custa uma URL menos bonita e não deixa a armadilha.",
        "parameters": [
          {
            "name": "variantId",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/VariantTablePriceDto"
                  }
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": ["produtos"],
        "operationId": "ReplaceVariantTablePrices",
        "description": "Proposto. Substitui a lista inteira de tabelas desta variante.\n\n**Papel: `admin` ou superior**, o mesmo do índice e do perfil de custo — tabela, índice e deduções são os três fatores do preço, e proteger dois deixando o terceiro aberto não protege nada.\n\nFornecedor repetido no corpo é **400**: duas tabelas para o mesmo par deixariam o preço depender da ordem do array.\n\n**O caminho é `/api/table-prices/{variantId}` e não `/api/variants/{variantId}/table-prices`, e a razão é do SERVIDOR.** A matriz de permissão do `cabinet-erp-api` casa por PREFIXO de caminho, e o trecho que distinguiria a tabela de preço das outras duas coisas que pendem da variante (`stock-movements`, `stock-balances`) vem DEPOIS do parâmetro — onde prefixo nenhum alcança. Sob o caminho aninhado, a tabela herdaria a exigência do kardex: quem movimenta estoque passaria a editar PREÇO, em silêncio e sem ninguém ter decidido isso. Pôr o recurso na frente custa uma URL menos bonita e não deixa a armadilha.",
        "parameters": [
          {
            "name": "variantId",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/VariantTablePricesWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/VariantTablePriceDto"
                  }
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/financial-titles": {
      "get": {
        "tags": ["financeiro"],
        "operationId": "ListFinancialTitles",
        "description": "Proposto. Os TÍTULOS financeiros da empresa ativa — contas a pagar e a receber, discriminadas por `direction`.\n\n**Sem empresa ativa devolve `{rows: [], total: 0}`** — a empresa vem da SESSÃO, e lista vazia é a resposta certa para \"quantos títulos tem a empresa que você não tem\".\n\n**`direction` é filtro e não caminho.** As duas telas do legado (`Contas a Pagar → Lançamento` e `Contas a Receber → Lançamentos`) chamam a mesma operação com o valor trocado — ver `FinancialTitleDto` para por que um recurso só.\n\n**Ausente, `direction` traz os dois lados**, e isso é de propósito: é a leitura que a busca por número de documento precisa, quando o operador tem a nota na mão e não sabe se ela é de compra ou de venda.",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Busca por número do título, número do documento e nome da parte."
          },
          {
            "name": "sortBy",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Whitelist: `number`, `issuedAt`, `totalCents`, `openCents`, `status`, `partnerName`. Campo fora dela é 400.\n\n`dueDate` NÃO está aqui, e a ausência é a decisão que mais importa nesta listagem: vencimento é da PARCELA, e um título de cinco parcelas tem cinco. Ordenar título por vencimento exigiria escolher uma delas em silêncio — a menor, a maior, a próxima —, e cada escolha produz uma lista diferente que parece a mesma. Quem quer a agenda de vencimentos usa `GET /api/financial-installments`, que é a listagem cuja linha É um vencimento.\n\n`partnerName` entra, ao contrário do resto do contrato, porque aqui ele é coluna CONGELADA no título e não eco de outra tabela."
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "direction",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "`payable` ou `receivable`. Ausente traz os dois."
          },
          {
            "name": "status",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "`open`, `settled` ou `cancelled`."
          },
          {
            "name": "partnerId",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "Só os títulos desta parte."
          },
          {
            "name": "issuedFrom",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "date"
            },
            "description": "Emitidos a partir desta data, inclusive.\n\n**Nome qualificado, e não o `from`/`to` dos relatórios**, de propósito: lá o par é OBRIGATÓRIO e define o período que o relatório soma — agregação sem recorte responde outra pergunta. Aqui é filtro opcional, e o título tem TRÊS datas (emissão, competência e o vencimento das parcelas): um `from` sem qualificação obrigaria quem lê a adivinhar qual delas recorta."
          },
          {
            "name": "issuedTo",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "date"
            },
            "description": "Emitidos até esta data, inclusive."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfFinancialTitleDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      },
      "post": {
        "tags": ["financeiro"],
        "operationId": "CreateFinancialTitle",
        "description": "Proposto. Lança um título com as suas parcelas.\n\n**Permissão: `financeiro:editar`.**\n\nAs recusas de negócio:\n\n1. **400** — `sequence` das parcelas fora de 1..N, parcela de valor zero ou negativo, `competenceMonth` com dia diferente de 1, ou a parte sem o PAPEL da direção (fornecedor em `payable`, cliente em `receivable`). Todas são conferíveis pelo cliente, e por isso vêm com `fields[]`.\n2. **409** — sem empresa ativa.\n\n**Sem empresa ativa é 409** (`urn:cabinet:erro:sem-empresa-ativa`), não 400: falta uma ação da pessoa (escolher empresa), o pedido não está malformado.\n\n**O título nasce `open` e sem baixa**, sempre: criar já quitado exigiria a baixa no mesmo corpo, e a baixa tem destino, meio e data próprios — o que geraria dois fatos numa requisição que a auditoria registraria como um.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/FinancialTitleWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FinancialTitleDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict — sem empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/financial-titles/{id}": {
      "get": {
        "tags": ["financeiro"],
        "operationId": "GetFinancialTitle",
        "description": "Proposto. Um título pelo id, com as parcelas e as BAIXAS de cada uma — é o extrato do título.\n\n**Sem empresa ativa é 409** (`urn:cabinet:erro:sem-empresa-ativa`), não 400: falta uma ação da pessoa (escolher empresa), o pedido não está malformado.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FinancialTitleDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — sem empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": ["financeiro"],
        "operationId": "UpdateFinancialTitle",
        "description": "Proposto. SUBSTITUI o título inteiro, parcelas incluídas.\n\n**Permissão: `financeiro:editar`.**\n\n**Recusa (409, `urn:cabinet:erro:titulo-com-baixa`) o título que já tem qualquer baixa.** Reescrever parcela já quitada moveria dinheiro conciliado, e a tela não teria como mostrar o que aconteceu: a baixa aponta a parcela, e a parcela teria mudado de valor por baixo dela.\n\n**Recusa (409, `urn:cabinet:erro:transicao-invalida`) o título cancelado.** Cancelado é estado final; o caminho de voltar atrás é lançar outro título.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/FinancialTitleWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FinancialTitleDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — título com baixa (`urn:cabinet:erro:titulo-com-baixa`), título cancelado (`urn:cabinet:erro:transicao-invalida`) ou sem empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/financial-titles/{id}/cancel": {
      "post": {
        "tags": ["financeiro"],
        "operationId": "CancelFinancialTitle",
        "description": "Proposto. CANCELA o título. É o lugar do DELETE que este contrato não tem: o título fica, com o estado dizendo que a dívida não vale mais.\n\n**Permissão: `financeiro:editar`.**\n\n**Recusa (409) o título que já tem baixa** — dinheiro que andou não se cancela, se estorna, e estorno não existe neste contrato (ver `FinancialSettlementDto`). Recusa também (409, `urn:cabinet:erro:transicao-invalida`) o que já está cancelado: repetir o gesto não é erro do operador, mas responder 200 faria a tela concluir que houve um segundo cancelamento.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FinancialTitleDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — título com baixa (`urn:cabinet:erro:titulo-com-baixa`), já cancelado (`urn:cabinet:erro:transicao-invalida`) ou sem empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/financial-installments": {
      "get": {
        "tags": ["financeiro"],
        "operationId": "ListFinancialInstallments",
        "description": "Proposto. As PARCELAS da empresa ativa — a agenda de vencimentos, e a lista que a QUITAÇÃO EM LOTE consome.\n\n**Sem empresa ativa devolve `{rows: [], total: 0}`** — a empresa vem da SESSÃO, e lista vazia é a resposta certa para \"quantos títulos tem a empresa que você não tem\".\n\n**Existe além da listagem de títulos porque a linha é outra.** Lá a linha é uma dívida; aqui é um vencimento. A pergunta do financeiro todo dia — \"o que vence hoje\" — não tem resposta na lista de títulos sem escolher uma parcela por título em silêncio (ver o `sortBy` de `ListFinancialTitles`). E é esta lista que alimenta a seleção do lote: o lote paga PARCELAS, de títulos diferentes.\n\n**`overdue` vem do servidor**, não do cliente: \"hoje\" é o dia do servidor, e o relógio errado de uma estação marcaria vencido o que não está — bem na coluna que decide o que será pago.",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Busca por número do título, número do documento da parcela e nome da parte."
          },
          {
            "name": "sortBy",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Whitelist: `dueDate`, `amountCents`, `openCents`, `partnerName`, `titleNumber`, `sequence`. Campo fora dela é 400. O padrão é `dueDate` crescente — a agenda começa pelo que vence primeiro."
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "direction",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "`payable` ou `receivable`. Ausente traz os dois."
          },
          {
            "name": "status",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "`open` ou `settled`. O padrão da tela de quitação é `open`, mas o filtro não tem valor implícito: quem pede a lista sem `status` recebe as duas, e uma lista que esconde o que já foi pago sem dizer é o que faz o operador pagar de novo."
          },
          {
            "name": "partnerId",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "Só as parcelas de títulos desta parte."
          },
          {
            "name": "dueFrom",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "date"
            },
            "description": "Vencimento a partir desta data, inclusive."
          },
          {
            "name": "dueTo",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "date"
            },
            "description": "Vencimento até esta data, inclusive. Com `dueFrom`, é o recorte da tela de quitação em lote."
          },
          {
            "name": "overdue",
            "in": "query",
            "schema": {
              "type": "boolean"
            },
            "description": "`true` traz só as vencidas e em aberto."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfFinancialInstallmentDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      }
    },
    "/api/financial-installments/{id}/settlements": {
      "post": {
        "tags": ["financeiro"],
        "operationId": "SettleInstallment",
        "description": "Proposto. QUITA uma parcela — a baixa avulsa.\n\n**Permissão: `financeiro:quitar`** — separada de `financeiro:editar` porque lançar a conta e pagá-la são funções diferentes em qualquer financeiro que tenha mais de uma pessoa. Quitar A MENOS que o saldo pede ainda a ação fina `financeiro:quitacao-a-menor` (403 sem ela), que é a permissão especial nº 45 do legado.\n\n**A baixa gera o MOVIMENTO de caixa na mesma transação** — é o que faz o dinheiro aparecer no extrato da conta escolhida, com `sourceType = 'settlement'`. Por isso o destino é obrigatório: ver `SettlementWriteRequest`.\n\nAs recusas:\n\n1. **400** — destino ausente ou duplicado, valor zero ou negativo, modo de pagamento que não serve para quitação (`usableInSettlement = false`).\n2. **403** — quitação a menor sem a ação fina.\n3. **409** — parcela já quitada (`urn:cabinet:erro:parcela-ja-quitada`), valor acima do saldo (`urn:cabinet:erro:valor-acima-do-saldo`), título cancelado (`urn:cabinet:erro:transicao-invalida`), data em período fechado (`urn:cabinet:erro:periodo-fechado`) ou sem empresa ativa.\n\n**Data dentro de período já fechado é 409** (`urn:cabinet:erro:periodo-fechado`). O fechamento de contas existe para que o saldo conferido de ontem não mude hoje; sem a recusa, ele seria um carimbo decorativo.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "A PARCELA que está sendo quitada."
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SettlementWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FinancialSettlementDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — parcela já quitada, valor acima do saldo, título cancelado, período fechado ou sem empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/financial-settlements/batch": {
      "post": {
        "tags": ["financeiro"],
        "operationId": "SettleBatch",
        "description": "Proposto. QUITAÇÃO EM LOTE — paga N parcelas num ato só, com um `batchId` que as amarra.\n\n**Permissão: `financeiro:quitar`**, e a ação fina `financeiro:quitacao-a-menor` quando QUALQUER item do lote abate menos que o saldo da sua parcela.\n\n**Tudo ou nada.** Uma parcela recusada derruba o lote inteiro e nenhuma baixa fica gravada — ver `SettlementBatchRequest` para por que o parcial seria pior. A resposta de erro diz em `detail` qual parcela recusou e por quê.\n\nAs recusas são as mesmas da baixa avulsa, aplicadas a qualquer item do lote, mais o **400** de parcela repetida na mesma requisição.\n\n**Data dentro de período já fechado é 409** (`urn:cabinet:erro:periodo-fechado`). O fechamento de contas existe para que o saldo conferido de ontem não mude hoje; sem a recusa, ele seria um carimbo decorativo.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SettlementBatchRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SettlementBatchResultDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — alguma parcela já quitada, valor acima do saldo, título cancelado, período fechado ou sem empresa ativa. Nenhuma baixa foi gravada.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/cash-movements": {
      "get": {
        "tags": ["financeiro"],
        "operationId": "ListCashMovements",
        "description": "Proposto. O EXTRATO — os movimentos de caixa e de conta bancária da empresa ativa. Serve as duas telas do legado (`Caixa → Lançamento` e `Movimentos Bancários → Lançamentos`), que se distinguem pelo filtro de conta.\n\n**Sem empresa ativa devolve `{rows: [], total: 0}`** — a empresa vem da SESSÃO, e lista vazia é a resposta certa para \"quantos títulos tem a empresa que você não tem\".\n\n**`bankAccountId` e `cashRegisterId` são filtros exclusivos entre si** — mandar os dois é 400. Extrato é de uma conta: somar duas contas numa lista ordenada por data produz um saldo corrente que não existe em nenhuma delas.",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Busca no histórico do lançamento."
          },
          {
            "name": "sortBy",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Whitelist: `occurredOn`, `amountCents`, `direction`, `createdAt`. Campo fora dela é 400. O padrão é `occurredOn` — extrato se lê em ordem de dia."
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "bankAccountId",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "Extrato desta conta bancária."
          },
          {
            "name": "cashRegisterId",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "Extrato deste caixa."
          },
          {
            "name": "direction",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "`in` ou `out`."
          },
          {
            "name": "occurredFrom",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "date"
            },
            "description": "A partir desta data, inclusive."
          },
          {
            "name": "occurredTo",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "date"
            },
            "description": "Até esta data, inclusive."
          },
          {
            "name": "reconciled",
            "in": "query",
            "schema": {
              "type": "boolean"
            },
            "description": "`false` traz só o que ainda NÃO foi conciliado — é a lista que a tela de conciliação bancária abre."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfCashMovementDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      },
      "post": {
        "tags": ["financeiro"],
        "operationId": "CreateCashMovement",
        "description": "Proposto. Lança um movimento MANUAL — entrada ou saída avulsa de caixa ou banco.\n\n**Permissão: `financeiro:editar`.**\n\n**Só `manual` entra por aqui.** Movimento de transferência nasce em `POST /api/cash-transfers` e movimento de baixa nasce na quitação; aceitar os dois tipos neste caminho deixaria existir transferência de uma perna só e baixa sem parcela.\n\n**Não há PUT nem DELETE de movimento** — a correção é o lançamento contrário. Extrato que se reescreve não concilia com o do banco.\n\n**Data dentro de período já fechado é 409** (`urn:cabinet:erro:periodo-fechado`). O fechamento de contas existe para que o saldo conferido de ontem não mude hoje; sem a recusa, ele seria um carimbo decorativo.\n\n**Sem empresa ativa é 409** (`urn:cabinet:erro:sem-empresa-ativa`), não 400: falta uma ação da pessoa (escolher empresa), o pedido não está malformado.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CashMovementWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CashMovementDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict — período fechado (`urn:cabinet:erro:periodo-fechado`) ou sem empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/cash-movements/{id}/reconcile": {
      "post": {
        "tags": ["financeiro"],
        "operationId": "ReconcileCashMovement",
        "description": "Proposto. Marca o movimento como CONCILIADO contra o extrato do banco. Menu `Financeiro → Conciliação Bancária` do legado.\n\n**Permissão: `financeiro:editar`.**\n\n**Conciliar é declarar que a linha foi CONFERIDA**, e por isso o servidor grava quem conferiu e quando — conciliação anônima não responde à única pergunta que se faz dela depois, que é quem viu.\n\n**Repetir é 409** (`urn:cabinet:erro:movimento-ja-conciliado`) e não 200: a segunda chamada quase sempre é a tela desatualizada de outra pessoa, e responder OK esconderia que dois operadores estavam conferindo o mesmo extrato.\n\n**Desconciliar não existe aqui**, pelo mesmo motivo do estorno: é decisão de alçada que ninguém tomou.\n\n**O que esta operação ainda NÃO faz, e é dívida nomeada:** o legado guarda em `Cpp_IDConcBancaria` o ID DA LINHA DO EXTRATO do banco — é o único ponto do módulo que fala com dado de fora (`docs/harvest/financeiro.md` §3.5). Aqui a conciliação registra QUEM conferiu e QUANDO, e não contra qual linha do extrato. Casar com o extrato exige importá-lo, que é trilho próprio; publicar o campo antes da importação seria pedir à tela um id que ela não tem de onde tirar.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CashMovementDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — movimento já conciliado (`urn:cabinet:erro:movimento-ja-conciliado`) ou sem empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/cash-transfers": {
      "post": {
        "tags": ["financeiro"],
        "operationId": "CreateCashTransfer",
        "description": "Proposto. TRANSFERE dinheiro entre duas contas da empresa — as quatro direções do legado (caixa→conta, caixa→caixa, conta→caixa, conta→conta) num caminho só.\n\n**Permissão: `financeiro:editar`.**\n\n**Grava DOIS movimentos ou nenhum**, os dois com o mesmo `transferId`. Ver `CashTransferRequest`.\n\nAs recusas:\n\n1. **400** — origem ausente, destino ausente, os dois ids do mesmo lado preenchidos, ou origem igual ao destino.\n2. **409** — data em período fechado de QUALQUER uma das duas contas, ou sem empresa ativa. As duas pontas contam: metade da transferência dentro de um período fechado é o buraco que o fechamento existe para não ter.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CashTransferRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CashTransferDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict — período fechado em alguma das contas (`urn:cabinet:erro:periodo-fechado`) ou sem empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/bank-accounts": {
      "get": {
        "tags": ["financeiro"],
        "operationId": "ListBankAccounts",
        "description": "Proposto. As CONTAS BANCÁRIAS da empresa ativa — o combo de destino do lançamento, da transferência e da baixa.\n\n**Sem empresa ativa devolve `{rows: [], total: 0}`** — a empresa vem da SESSÃO, e lista vazia é a resposta certa para \"quantos títulos tem a empresa que você não tem\".\n\n**Só leitura**: o cadastro é do trilho `Tabelas → Financeiro`, que traz junto banco e agência. Ver `BankAccountDto`.",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Busca por nome, número da conta e nome do banco."
          },
          {
            "name": "sortBy",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Whitelist: `name`, `number`, `bankCode`, `active`. Campo fora dela é 400."
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "active",
            "in": "query",
            "schema": {
              "type": "boolean"
            },
            "description": "`true` traz só as ativas — o que todo combo quer."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfBankAccountDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      }
    },
    "/api/cash-registers": {
      "get": {
        "tags": ["financeiro"],
        "operationId": "ListCashRegisters",
        "description": "Proposto. Os CAIXAS da empresa ativa. Mesmo papel e mesmo recorte de `GET /api/bank-accounts`.\n\n**Sem empresa ativa devolve `{rows: [], total: 0}`** — a empresa vem da SESSÃO, e lista vazia é a resposta certa para \"quantos títulos tem a empresa que você não tem\".",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Busca por código e nome."
          },
          {
            "name": "sortBy",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Whitelist: `code`, `name`, `active`. Campo fora dela é 400."
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "active",
            "in": "query",
            "schema": {
              "type": "boolean"
            },
            "description": "`true` traz só os ativos."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfCashRegisterDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      }
    },
    "/api/payment-modes": {
      "get": {
        "tags": ["financeiro"],
        "operationId": "ListPaymentModes",
        "description": "Proposto. Os MODOS DE PAGAMENTO da empresa ativa (`Modo`, 26 linhas no legado).\n\n**Sem empresa ativa devolve `{rows: [], total: 0}`** — a empresa vem da SESSÃO, e lista vazia é a resposta certa para \"quantos títulos tem a empresa que você não tem\".\n\n**Publicado porque a baixa não existe sem ele** — `paymentModeId` é obrigatório em toda quitação. Não confundir com `GET /api/payment-terms`, que é a CONDIÇÃO (em quantas vezes e quando); o modo é o MEIO (dinheiro, transferência, cartão).",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Busca por código e nome."
          },
          {
            "name": "sortBy",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Whitelist: `code`, `name`, `active`. Campo fora dela é 400."
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "active",
            "in": "query",
            "schema": {
              "type": "boolean"
            },
            "description": "`true` traz só os ativos."
          },
          {
            "name": "usableInSettlement",
            "in": "query",
            "schema": {
              "type": "boolean"
            },
            "description": "`true` traz só os que servem para QUITAR — é o recorte que o combo da baixa usa."
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfPaymentModeDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      }
    },
    "/api/employees/{id}/reset-password": {
      "post": {
        "tags": ["cadastros"],
        "operationId": "ResetEmployeePassword",
        "description": "Proposto. O único caminho que dá credencial a um colaborador: `CreateEmployee` grava senha inutilizável de propósito, então sem esta operação a conta nunca entra. O SERVIDOR gera a senha provisória — o admin não escolhe — e ela aparece UMA única vez, nesta resposta; não há leitura que a devolva depois, nem para quem a gerou. `mustChangePassword` liga junto: o primeiro login exige `/auth/change-password` antes de qualquer outra coisa. Repetir a chamada é o reset de quem esqueceu — invalida a credencial atual e gera outra provisória.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TemporaryPasswordDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — colaborador sem e-mail não tem credencial para resetar.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/quotes/{id}/print": {
      "get": {
        "tags": ["impressao"],
        "operationId": "PrintQuote",
        "description": "Proposto. O orçamento como **PDF pronto**, renderizado no servidor.\n\n**Por que o servidor renderiza, quando as 11 rotas de `/api/reports/*` fazem o contrário.** Aquelas devolvem `application/json` porque relatório é dado, e a tela decide como mostrar. O orçamento impresso não é dado: é o ENTREGÁVEL que o cliente assina, e três coisas o tiram do padrão delas. O layout do legado repete o cabeçalho em toda página e numera `Página :N` — conteúdo em margem de página, que o CSS de impressão do browser não sabe pôr. O documento precisa existir como ARQUIVO, para anexar e arquivar, e `window.print()` não produz arquivo sem alguém salvar um. E `Imprimir` é ação de primeira classe no RBAC do legado (5 ações, e esta é uma delas): permissão só existe atrás de um caminho que a exija — sem esta operação, `orcamento:imprimir` seria um checkbox que não protege nada.\n\n**O que o servidor junta aqui e o cliente não teria.** O timbre da empresa (razão social, IE, endereço, logo) mora em `tenants` e não trafega em `VinculoDeEmpresa`; o bloco do cliente (endereço, bairro, cidade, UF, CEP, CNPJ\\CPF, Insc.Est.\\RG) não está em `QuoteDetailDto`, que carrega só `customerId` e `customerName`. Renderizar aqui evita publicar as duas coisas no contrato só para o browser remontá-las.\n\n**Sem storage: o PDF é gerado a cada chamada e não fica guardado.** Não existe 'via emitida' arquivada — o documento sai sempre como o orçamento está AGORA. Guardar a via é decisão própria, e traz junto onde mora o blob.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK — `application/pdf`. Uma página por bloco quando couber; 3 páginas é o típico do legado.",
            "content": {
              "application/pdf": {
                "schema": {
                  "type": "string",
                  "format": "binary"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found — o orçamento não existe na empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — sem empresa ativa na sessão (`urn:cabinet:erro:sem-empresa-ativa`). Documento impresso é papel timbrado de UMA empresa: sem ela não há cabeçalho, e um PDF sem timbre é pior que erro nenhum.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/print-settings": {
      "get": {
        "tags": ["impressao"],
        "operationId": "GetPrintSettings",
        "description": "Proposto. Os textos configuráveis que entram no impresso da empresa ativa: o título do orçamento e as cláusulas comerciais da página final.\n\n**São DADO, e o legado prova.** `Par_TituloImpOrcamento` é coluna de `Paramentros` (`EQUIPAMENTOS PARA A PROPOSTA DE PROJETO Nº` na instalação da Vertz), e o `QRMemo` das cláusulas está VAZIO no formulário do QuickReport — o texto vem do banco em tempo de impressão. Escrevê-los no template faria cada empresa que entrar herdar as condições comerciais de outra, e mudar 'prazo de troca de 10 dias' viraria deploy.\n\n**Singleton por empresa, como `GetInstallmentPolicy`:** não há `POST`, não há id. Empresa sem linha lê o padrão do servidor — as cláusulas nascem como marcadores explícitos de pendência, nunca como texto plausível: cláusula comercial inventada por um servidor é a única espécie de dado faltando que sai assinada pelo cliente.",
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PrintSettingsDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict — sem empresa ativa na sessão (`urn:cabinet:erro:sem-empresa-ativa`). Documento impresso é papel timbrado de UMA empresa: sem ela não há cabeçalho, e um PDF sem timbre é pior que erro nenhum.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": ["impressao"],
        "operationId": "UpdatePrintSettings",
        "description": "Proposto. Substitui o conjunto INTEIRO — título e a lista de cláusulas. `PUT` de singleton, e cláusula ausente do corpo deixa de existir.\n\nA ordem sai de `order`, não da posição no array: é o que numera as cláusulas na página final sem depender de o cliente mandar a lista ordenada.\n\n400 quando `order` repete, quando `text` é vazio, ou quando `quoteTitle` passa de 200 caracteres — o título é uma linha do cabeçalho, e o que não cabe nela não é aparado em silêncio.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PrintSettingsWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PrintSettingsDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict — sem empresa ativa na sessão (`urn:cabinet:erro:sem-empresa-ativa`). Documento impresso é papel timbrado de UMA empresa: sem ela não há cabeçalho, e um PDF sem timbre é pior que erro nenhum.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/label-layouts": {
      "get": {
        "tags": ["impressao"],
        "operationId": "ListLabelLayouts",
        "description": "Proposto. Os layouts de etiqueta da empresa ativa.\n\n**Por que o layout é dado e não código.** O legado tem uma tela só para isto — `FrmEtiqueta`, *Configuração de Etiquetas* — e 21 layouts gravados em `EtiquetaPronta`. Etiqueta muda com o rolo de papel que a loja comprou, não com a versão do sistema: medida em código faria toda troca de insumo virar release.",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sortBy",
            "in": "query",
            "schema": {
              "type": "string"
            },
            "description": "Whitelist: `name`, `active`, `isDefault`. Campo fora dela é 400."
          },
          {
            "name": "sortDesc",
            "in": "query",
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "page",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PagedResultOfLabelLayoutDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request — `sortBy` fora da whitelist ou `pageSize` acima de 100.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          }
        }
      },
      "post": {
        "tags": ["impressao"],
        "operationId": "CreateLabelLayout",
        "description": "Proposto. Cria um layout. As medidas são em MILÍMETROS porque etiqueta é objeto físico: o `PaperSize = Custom` e o `Page.Columns = 4` do QuickReport são milímetros de papel, e pixel não sobrevive à troca de impressora.\n\n400 quando a grade não cabe na folha — `marginLeftMm + marginRightMm + (columns − 1) × columnGapMm` maior ou igual a `pageWidthMm`, ou `marginTopMm + marginBottomMm + labelHeightMm` maior que `pageHeightMm`. Etiqueta que não cabe imprime cortada, e isso só se descobre no papel, depois do rolo gasto.",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LabelLayoutWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LabelLayoutDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "409": {
            "description": "Conflict — sem empresa ativa na sessão (`urn:cabinet:erro:sem-empresa-ativa`). Documento impresso é papel timbrado de UMA empresa: sem ela não há cabeçalho, e um PDF sem timbre é pior que erro nenhum.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/label-layouts/{id}": {
      "get": {
        "tags": ["impressao"],
        "operationId": "GetLabelLayout",
        "description": "Proposto. Um layout com seus elementos.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LabelLayoutDto"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — sem empresa ativa na sessão (`urn:cabinet:erro:sem-empresa-ativa`). Documento impresso é papel timbrado de UMA empresa: sem ela não há cabeçalho, e um PDF sem timbre é pior que erro nenhum.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      },
      "put": {
        "tags": ["impressao"],
        "operationId": "UpdateLabelLayout",
        "description": "Proposto. Substitui o layout INTEIRO.\n\n**Desativar é `active: false`, não apagar.** Layout usado numa impressão já feita é o que explica a etiqueta que está colada na peça.",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LabelLayoutWriteRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LabelLayoutDto"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — sem empresa ativa na sessão (`urn:cabinet:erro:sem-empresa-ativa`). Documento impresso é papel timbrado de UMA empresa: sem ela não há cabeçalho, e um PDF sem timbre é pior que erro nenhum.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    },
    "/api/labels/products/print": {
      "get": {
        "tags": ["impressao"],
        "operationId": "PrintProductLabels",
        "description": "Proposto. Folha de etiquetas dos produtos do catálogo, como PDF — o `FrmSelEtiqFornecedor` / *Etiquetas de Produtos Cadastrados* do legado.\n\nAs etiquetas preenchem a grade do layout na ordem em que os produtos saem, coluna a coluna, e `copies` repete cada uma. Sem filtro nenhum, imprime o catálogo inteiro da empresa ativa — o que é um rolo de papel, e por isso `pageSize` do layout é o teto real.\n\n`productIds` e `supplierId` são combináveis com `q`: são um AND, não alternativas.",
        "parameters": [
          {
            "name": "layoutId",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "format": "uuid"
            },
            "description": "O layout a usar. Obrigatório: não há layout implícito, porque imprimir com o desenho errado gasta o rolo e só se descobre no papel."
          },
          {
            "name": "supplierId",
            "in": "query",
            "schema": {
              "type": "string",
              "format": "uuid"
            }
          },
          {
            "name": "productIds",
            "in": "query",
            "schema": {
              "type": "array",
              "items": {
                "type": "string",
                "format": "uuid"
              }
            },
            "description": "Seleção explícita. Vazio quer dizer 'todos os que os outros filtros deixarem'."
          },
          {
            "name": "q",
            "in": "query",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "copies",
            "in": "query",
            "schema": {
              "type": "integer",
              "format": "int32"
            },
            "description": "Cópias por produto. Padrão 1; acima de 100 é 400, porque um zero a mais aqui é o rolo inteiro."
          }
        ],
        "responses": {
          "200": {
            "description": "OK — `application/pdf`.",
            "content": {
              "application/pdf": {
                "schema": {
                  "type": "string",
                  "format": "binary"
                }
              }
            }
          },
          "400": {
            "description": "Bad Request — `copies` fora de 1..100, ou `layoutId` que não é layout da empresa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "401": {
            "$ref": "#/components/responses/NaoAutenticado"
          },
          "403": {
            "$ref": "#/components/responses/SemPermissao"
          },
          "404": {
            "description": "Not Found — o layout não existe na empresa ativa.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          },
          "409": {
            "description": "Conflict — sem empresa ativa na sessão (`urn:cabinet:erro:sem-empresa-ativa`). Documento impresso é papel timbrado de UMA empresa: sem ela não há cabeçalho, e um PDF sem timbre é pior que erro nenhum.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/ProblemDetails"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "securitySchemes": {
      "sessaoCabinet": {
        "description": "Sessão server-side por COOKIE OPACO (ADR-010, sem JWT). O cookie carrega um identificador e nada mais — papel, empresa ativa e validade moram no servidor, onde podem ser revogados de um lado só. `POST /auth/login` o emite; `POST /auth/logout` o encerra.\n\n`apiKey`+`cookie` é como o OpenAPI expressa isto: não existe tipo `cookieSession`, e descrever a sessão como `http`/`bearer` seria descrever outro mecanismo. O cliente NÃO lê nem monta o cookie — o navegador o envia sozinho, e o front pede com `credentials: 'include'`.\n\n**Vale por HERANÇA:** o `security` do topo do documento aplica esta exigência a TODA operação, e as quatro que não a têm (`Health`, `HealthDb`, `AuthLogin`, `AuthLogout`) a desligam explicitamente com `security: []`. O padrão é exigir; a exceção é que se declara.",
        "type": "apiKey",
        "in": "cookie",
        "name": "cabinet_sessao"
      }
    },
    "responses": {
      "NaoAutenticado": {
        "description": "Sem sessão: ausente, expirada ou encerrada. **É o único significado deste código nas operações de domínio** — \"autenticado mas não pode\" é 403, e confundir os dois põe o cliente num laço de relogin que não resolve nada.\n\nResposta reutilizável, e não repetida operação a operação: o cliente trata 401 num lugar só (redirecionar para o login preservando a rota de origem), e a repetição faria 50 cópias da mesma frase divergirem uma a uma.\n\nExceção declarada: o 401 de `AuthLogin` NÃO é este — lá ele diz \"credencial inválida\", com mensagem genérica de propósito, e continua descrito na própria operação.",
        "content": {
          "application/problem+json": {
            "schema": {
              "$ref": "#/components/schemas/ProblemDetails"
            }
          }
        }
      },
      "SemPermissao": {
        "description": "Autenticado e barrado. Três casos, e os três são 403 por decisão:\n\n1. **`mustChangePassword`** — a credencial vale, falta um passo. 401 aqui derrubaria a sessão recém-criada e o cliente tentaria logar de novo, em laço.\n2. **Empresa ativa sem vínculo** — 403 e não 404, porque o `tenantId` não é segredo (ele viaja em `GET /auth/tenants`). O que é controlado é o acesso, não a existência.\n3. **Papel sem a permissão da operação** (RBAC por empresa).\n\nQuem distingue os três é `type` (URN estável), não o texto de `detail`: código é para código, frase é para gente. O cliente mostra a tela própria de \"sem permissão\", nunca o erro genérico.\n\n**Não cobre \"não encontrado nesta empresa\"**, que é 404: dizer \"existe, mas não é sua\" confirmaria um registro da empresa vizinha.",
        "content": {
          "application/problem+json": {
            "schema": {
              "$ref": "#/components/schemas/ProblemDetails"
            }
          }
        }
      },
      "NaoImplementado": {
        "description": "O caminho está NESTE contrato e o servidor ainda não serve ESTA parte dele — `urn:cabinet:erro:nao-implementado`.\n\nÉ a marca de fase que o `Proposto` já anuncia, agora visível em tempo de execução: 501 e nunca 404, para que \"não existe\" continue significando \"não existe\". O cliente não deve reenviar o mesmo corpo — o pedido está bem formado, falta implementação do outro lado.\n\n**Declarada só onde o servidor pode mesmo respondê-la.** Estas operações aceitam campos que o backend ainda não grava — `discountMode: \"group\"` e `groupDiscounts`, `serviceItems` nos documentos, e a configuração de compras do fornecedor (`deliveryDays`, `minimumBillingCents`, `buyingCompanies`, `groupMinimums`) em `/api/partners`. Corpo que os CARREGA recebe 501 em vez de gravar metade; corpo que não os carrega segue o caminho normal. **O critério é o VALOR e não a presença da chave:** `PUT` manda o registro inteiro, então `null` e `[]` viajam sempre e não podem, sozinhos, recusar a gravação do resto.\n\nOperação sem handler NENHUM também responde 501, e é por isso que esta resposta não está em todas: quem inventaria o que ainda não existe é o `Proposto`, não uma resposta declarada em cada operação do documento.",
        "content": {
          "application/problem+json": {
            "schema": {
              "$ref": "#/components/schemas/ProblemDetails"
            }
          }
        }
      }
    },
    "schemas": {
      "ListFilter": {
        "required": ["field", "operator"],
        "type": "object",
        "description": "Proposto. UMA condição do filtro estruturado da listagem. Só o que o servidor precisa para consultar: o front carrega também a variante do campo (qual controle desenhar) e uma chave de linha, e as duas são UI — mandá-las no fio faria o servidor receber decisão de tela.",
        "properties": {
          "field": {
            "type": "string",
            "description": "Campo do DTO, na whitelist do recurso. Fora dela é 400, como no `sortBy` — filtro ignorado faria a tela mostrar resultado errado sem sintoma."
          },
          "operator": {
            "$ref": "#/components/schemas/ListFilterOperator"
          },
          "value": {
            "description": "Ausente em `isEmpty`/`isNotEmpty`. Array em `inArray`/`notInArray` (as opções aceitas) e em `isBetween` (`[de, ate]`, ponta vazia = extremo aberto). String no resto — inclusive em número e booleano, que o servidor converte pelo tipo do campo.",
            "oneOf": [
              {
                "type": "string"
              },
              {
                "type": "array",
                "items": {
                  "type": "string"
                }
              }
            ]
          }
        }
      },
      "ListFilterOperator": {
        "type": "string",
        "description": "Proposto. Operadores do filtro estruturado. `iLike`/`notILike` comparam trecho ignorando caixa e acento; `eq`/`ne` comparam o valor inteiro.",
        "enum": [
          "iLike",
          "notILike",
          "eq",
          "ne",
          "lt",
          "lte",
          "gt",
          "gte",
          "isBetween",
          "inArray",
          "notInArray",
          "isEmpty",
          "isNotEmpty"
        ]
      },
      "ListFilterJoin": {
        "type": "string",
        "description": "Proposto. Como as condições se somam. UMA para a lista inteira: junção por condição permitiria `A e B ou C`, cuja precedência ninguém lê corretamente numa lista sem parênteses.",
        "default": "and",
        "enum": ["and", "or"]
      },
      "CatalogLookupDto": {
        "required": ["id", "kind", "name", "active"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "kind": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "active": {
            "type": "boolean"
          }
        }
      },
      "ChangePasswordRequest": {
        "required": ["currentPassword", "newPassword"],
        "type": "object",
        "properties": {
          "currentPassword": {
            "type": "string"
          },
          "newPassword": {
            "type": "string"
          }
        }
      },
      "HealthStatus": {
        "required": ["status"],
        "type": "object",
        "properties": {
          "status": {
            "type": "string"
          }
        }
      },
      "LoginOk": {
        "required": ["mustChangePassword"],
        "type": "object",
        "properties": {
          "mustChangePassword": {
            "type": "boolean"
          }
        }
      },
      "LoginRequest": {
        "required": ["email", "password"],
        "type": "object",
        "properties": {
          "email": {
            "type": "string"
          },
          "password": {
            "type": "string"
          }
        }
      },
      "CatalogLookupCreateRequest": {
        "required": ["kind", "name", "active"],
        "type": "object",
        "description": "Proposto. Corpo do `+...`. `kind` viaja aqui e não no caminho porque a tabela é UMA, discriminada por ele (ADR-011) — e porque o mesmo diálogo de cadastro rápido serve os 22 kinds, parametrizado. O VOCABULÁRIO de kinds não é enumerado no contrato de propósito: ele é dado da instalação, não forma do documento, e enumerá-lo faria cadastrar uma lista nova virar PR de contrato.",
        "properties": {
          "kind": {
            "type": "string",
            "description": "A lista em que o item nasce, em MAIÚSCULA_COM_UNDERSCORE (`CATEGORIA_CLIENTE`). Kind desconhecido é 400: aceitar qualquer string criaria lista fantasma que combo nenhum lê."
          },
          "name": {
            "type": "string",
            "description": "O que o operador lê no combo. Único entre os ATIVOS do mesmo kind — repetido é 409."
          },
          "active": {
            "type": "boolean",
            "description": "Nasce `true` na prática (o operador está cadastrando para usar agora), mas viaja explícito: o `+...` de um diálogo de manutenção pode cadastrar já desativado, e um padrão implícito no servidor esconderia isso da tela."
          }
        }
      },
      "CatalogLookupUpdateRequest": {
        "required": ["name", "active"],
        "type": "object",
        "description": "Proposto. Renomear e desativar — as duas únicas edições que um item de apoio aceita. **Sem `kind`**: ver a descrição da operação. `PUT` substitui o registro inteiro, como no resto do contrato.",
        "properties": {
          "name": {
            "type": "string",
            "description": "Nome novo. Único entre os ATIVOS do kind — repetido é 409."
          },
          "active": {
            "type": "boolean",
            "description": "`false` é a desativação LÓGICA do §9 padrão 8: o item some do combo e continua legível na ficha de quem já apontava para ele. Não existe DELETE aqui, e é por isso."
          }
        }
      },
      "PagedResultOfCatalogLookupDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CatalogLookupDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "PagedResultOfPartnerDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PartnerDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "PagedResultOfProductDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ProductDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "PagedResultOfStockMovementDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/StockMovementDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "PartnerPayoutBankInfo": {
        "required": ["bankNumber", "bankName", "branchNumber", "accountNumber"],
        "type": "object",
        "description": "Proposto. Dados bancários do parceiro, para pagamento de comissão ao Profissional Externo (§3, bloco `Dados Bancários`). Um objeto e não quatro campos soltos no `PartnerDto`: os quatro só fazem sentido juntos, e parceiro sem conta cadastrada manda o objeto inteiro em `null` em vez de quatro nulos que a tela teria de recombinar. O endereço do banco, que a tela também mostra, fica FORA — endereço é bloco compartilhado e o contrato ainda não tem endereço de parceiro nenhum; entrar só o do banco daria a única exceção sem a regra.",
        "properties": {
          "bankNumber": {
            "type": ["null", "string"],
            "description": "Nº do banco (código FEBRABAN). Somente leitura na tela: vem da busca de banco."
          },
          "bankName": {
            "type": ["null", "string"],
            "description": "Nome do banco. Somente leitura na tela, pelo mesmo motivo."
          },
          "branchNumber": {
            "type": ["null", "string"],
            "description": "Nº da agência, com dígito se houver. String e não número: agência tem zero à esquerda."
          },
          "accountNumber": {
            "type": ["null", "string"],
            "description": "Nº da conta, com dígito. String pelo mesmo motivo da agência."
          }
        }
      },
      "WorkDto": {
        "required": [
          "id",
          "customerId",
          "customerName",
          "description",
          "workType",
          "address",
          "active"
        ],
        "type": "object",
        "description": "Proposto. A OBRA do cliente — `Obras` no legado (9.454 linhas), entidade própria e não um campo do orçamento: um cliente tem várias, elas duram mais que um documento, e a `Venda` aponta para ela por `Obr_codigo`. É pré-requisito do orçamento, cujo corpo é agrupado por AMBIENTE da obra (sala, cozinha…), o conceito central de projeto luminotécnico.\n\n**CAMADA: EMPRESA (RLS), não organização — e isto não foi deduzido, foi medido.** A tabela `Obras` do legado carrega `Emp_codigo`, e a `Venda` que a referencia é da empresa. O cadastro do CLIENTE é da organização porque é identidade (o mesmo CNPJ é o mesmo cliente em qualquer empresa do grupo); a obra é COMÉRCIO — ela existe porque alguém está vendendo para ela. Publicá-la como dado da organização faria a matriz enxergar o endereço e os ambientes do projeto que a filial está atendendo, sem que ninguém tenha pedido. A consequência para o backend é uma só e vale escrever: `works` entra nas políticas de RLS por `tenant_id`, como `quotes`, e NÃO no conjunto mestre de `partners`.",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "customerId": {
            "type": "string",
            "format": "uuid",
            "description": "O cliente de quem é a obra (`Obras.Cli_codigo`). Obrigatório: obra sem cliente não é obra, é endereço solto — e o orçamento chega nela partindo do cliente."
          },
          "customerName": {
            "type": ["null", "string"],
            "description": "Razão social do cliente, para a listagem de obras mostrar de quem é sem uma segunda consulta. Par `id`+`name` como `parentId`/`parentName` — o id é para escrever, o nome é o que a tela lê."
          },
          "description": {
            "type": "string",
            "description": "Como o operador chama a obra (`Obr_Descricao`): \"APARTAMENTO IBIRAPUERA\", \"CASA ALPHAVILLE\". É por ela que a obra é escolhida no orçamento, e é a única coluna que a listagem de orçamentos mostra além do cliente."
          },
          "workType": {
            "type": ["null", "string"],
            "description": "Tipo da obra (`Obr_tipoObra`): residencial, comercial, corporativa. Texto e não item de lista de apoio POR ORA — o legado guarda string livre de 11 caracteres, e promovê-la a `kind` exigiria saber o vocabulário fechado, que a extração não fixou. Vira lookup no dia em que a lista for conhecida; o campo não muda de nome quando isso acontecer."
          },
          "address": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/PartnerAddress"
              }
            ],
            "description": "Onde a obra fica. **Reusa `PartnerAddress`** — é o mesmo formato de endereço do parceiro, e um `WorkAddress` idêntico seria a segunda definição da mesma coisa, divergindo na primeira mudança. `null` quando ainda não se sabe o endereço: no legado a obra costuma nascer com a descrição e ganhar o endereço depois."
          },
          "active": {
            "type": "boolean",
            "description": "Desativação lógica (§9 padrão 8). Obra terminada não some — os orçamentos dela continuam apontando para cá, e uma obra apagada deixaria documento órfão."
          }
        }
      },
      "WorkWriteRequest": {
        "required": ["customerId", "description", "active"],
        "type": "object",
        "description": "Proposto. Corpo de escrita da obra. `PUT` substitui o registro inteiro, como no resto do contrato: o que não vier é apagado.",
        "properties": {
          "customerId": {
            "type": "string",
            "format": "uuid",
            "description": "O cliente. **Trocar o cliente de uma obra existente é 409**, não 200: os orçamentos já emitidos apontam para esta obra, e mudar o dono faria documento fechado passar a pertencer a outra pessoa. Obra do cliente errado se resolve desativando e cadastrando de novo."
          },
          "description": {
            "type": "string",
            "description": "Descrição da obra. Vazia é 400 — é o nome pelo qual ela é escolhida no orçamento."
          },
          "workType": {
            "type": ["null", "string"],
            "description": "Tipo da obra. Omitir apaga."
          },
          "address": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/PartnerAddress"
              }
            ],
            "description": "Endereço da obra, INTEIRO: mandar `null` apaga o endereço cadastrado, e mandar o objeto com um campo a menos apaga aquele campo. Não há atualização parcial de endereço."
          },
          "active": {
            "type": "boolean",
            "description": "Desativa a obra sem apagá-la."
          }
        }
      },
      "PagedResultOfWorkDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/WorkDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "PartnerContactDto": {
        "required": ["id", "name", "role", "phone", "mobilePhone", "fax", "email", "active"],
        "type": "object",
        "description": "Proposto. UM contato do parceiro — a pessoa que atende, com o vínculo dela (`Contatos` no legado: `Con_nome`, `Con_vinculo`, `Con_fone`, `Con_fax`, `Con_celular`, `Con_email`). É lista, não campo: fornecedor tem representante, gerente e financeiro, e o cadastro já traz uma GRADE para isso nas telas de Fornecedor e Profissional.\n\n**CAMADA: ORGANIZAÇÃO, e também medido.** A tabela `Contatos` do legado NÃO tem `Emp_codigo` — ela é discriminada por `Con_tpcadastro` (cliente, fornecedor, transportadora) e vive junto do cadastro. Faz sentido: quem atende no fornecedor é a mesma pessoa para as duas empresas do grupo, e duplicá-la por empresa criaria dois registros da mesma pessoa que divergem no dia em que o telefone dela mudar. É o mesmo raciocínio que põe `legalName` na organização e `paymentTerms` no vínculo.\n\n**Por que sub-recurso e não `contacts[]` dentro do `PartnerDto`:** o `PUT /api/partners/{id}` é INTEGRAL, e uma coleção dentro dele obrigaria toda tela a devolver as N linhas que não mostra — a regra \"ausente ≠ nulo\" resolve escalar, não coleção: a primeira leitura velha venceria e apagaria o contato que outra tela acabou de incluir. É a mesma forma que o contrato já usa para as variantes do produto (`/api/products/{productId}/variants`), e pela mesma razão.",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "name": {
            "type": "string",
            "description": "Nome de quem atende (`Con_nome`)."
          },
          "role": {
            "type": ["null", "string"],
            "description": "O VÍNCULO dele com o cadastro (`Con_vinculo`): representante, gerente, comprador, cônjuge. Texto livre, como no legado — não é cargo formal, é \"o que essa pessoa é para nós\"."
          },
          "phone": {
            "type": ["null", "string"],
            "description": "Telefone (`Con_fone`)."
          },
          "mobilePhone": {
            "type": ["null", "string"],
            "description": "Celular (`Con_celular`)."
          },
          "fax": {
            "type": ["null", "string"],
            "description": "Fax (`Con_fax`). Continua no cadastro do legado."
          },
          "email": {
            "type": ["null", "string"],
            "description": "E-mail (`Con_email`)."
          },
          "active": {
            "type": "boolean",
            "description": "Desativação lógica: contato que saiu da empresa some da grade e continua legível no documento antigo que o citou."
          }
        }
      },
      "PartnerContactWriteRequest": {
        "required": ["name", "active"],
        "type": "object",
        "description": "Proposto. Corpo de escrita de um contato. `PUT` substitui o contato inteiro.",
        "properties": {
          "name": {
            "type": "string",
            "description": "Nome. Vazio é 400 — contato sem nome não identifica ninguém."
          },
          "role": {
            "type": ["null", "string"],
            "description": "Vínculo com o cadastro. Omitir apaga."
          },
          "phone": {
            "type": ["null", "string"],
            "description": "Telefone. Omitir apaga."
          },
          "mobilePhone": {
            "type": ["null", "string"],
            "description": "Celular. Omitir apaga."
          },
          "fax": {
            "type": ["null", "string"],
            "description": "Fax. Omitir apaga."
          },
          "email": {
            "type": ["null", "string"],
            "description": "E-mail. Omitir apaga."
          },
          "active": {
            "type": "boolean",
            "description": "Desativa o contato sem apagá-lo."
          }
        }
      },
      "PagedResultOfPartnerContactDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PartnerContactDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "PartnerAddress": {
        "required": ["zipCode", "street", "number", "complement", "district", "city", "state"],
        "type": "object",
        "description": "Proposto. O endereço do parceiro — um só, na ordem em que a tela o desenha. Objeto e não sete campos soltos no `PartnerDto`, pela mesma razão do `PartnerPayoutBankInfo`: parceiro sem endereço manda o objeto inteiro em `null` em vez de sete nulos que a tela teria de recombinar. **Todo campo é anulável, e endereço PARCIAL é o caso normal, não a exceção** — do legado vem muita ficha com cidade e UF e nada mais, e exigir CEP para gravar o resto obrigaria a inventar um. É o PRIMEIRO endereço do contrato: colaborador e oportunidade seguem sem ele, e se algum dia precisarem, este schema é o que se reaproveita.",
        "properties": {
          "zipCode": {
            "type": ["null", "string"],
            "description": "CEP, sem máscara. String e não número: CEP tem zero à esquerda."
          },
          "street": {
            "type": ["null", "string"],
            "description": "Logradouro."
          },
          "number": {
            "type": ["null", "string"],
            "description": "Número. String porque `S/N`, `12-A` e `KM 4` são o que o operador digita."
          },
          "complement": {
            "type": ["null", "string"],
            "description": "Complemento — apartamento, bloco, sala."
          },
          "district": {
            "type": ["null", "string"],
            "description": "Bairro."
          },
          "city": {
            "type": ["null", "string"],
            "description": "Nome da cidade, como texto. **Não é o id de uma tabela de cidades** — o contrato não publica nenhuma, e a busca de CEP da tela devolve nome. O dia em que houver cadastro de cidade, o id entra ao lado deste campo, não no lugar dele: a ficha antiga continua valendo pelo nome que gravou."
          },
          "state": {
            "type": ["null", "string"],
            "description": "UF, duas letras."
          }
        }
      },
      "PartnerDto": {
        "required": [
          "id",
          "code",
          "legalName",
          "tradeName",
          "document",
          "email",
          "isCustomer",
          "isSupplier",
          "isProfessional",
          "paymentTerms",
          "active",
          "registrationActive"
        ],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "code": {
            "type": ["null", "string"]
          },
          "legalName": {
            "type": "string"
          },
          "tradeName": {
            "type": ["null", "string"]
          },
          "document": {
            "type": ["null", "string"]
          },
          "email": {
            "type": ["null", "string"]
          },
          "isCustomer": {
            "type": "boolean"
          },
          "isSupplier": {
            "type": "boolean"
          },
          "isProfessional": {
            "type": "boolean"
          },
          "paymentTerms": {
            "type": ["null", "string"]
          },
          "active": {
            "type": "boolean"
          },
          "registrationActive": {
            "type": "boolean"
          },
          "registration": {
            "type": ["null", "string"],
            "description": "Proposto. Registro Profissional — CREA, CAU ou CFT (§3). Vive no cadastro do parceiro (ORG), não no vínculo com a empresa: o conselho é do profissional, não da empresa que o contrata. Vazio em cliente e fornecedor."
          },
          "payoutBankInfo": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/PartnerPayoutBankInfo"
              }
            ],
            "description": "Proposto. Conta para pagamento de comissão. `null` quando o parceiro não tem conta cadastrada."
          },
          "parentId": {
            "type": ["null", "string"],
            "format": "uuid",
            "description": "Proposto. O parceiro a que este está VINCULADO — escritório de arquitetura ↔ profissionais dele (`partners.parent_id`, que já existe no schema). `null` quando o parceiro não pende de ninguém, que é o caso da maioria. Um nível por vez: o filho aponta o pai, e a lista de filhos sai de `filters` com `parentId`. CICLO É 400 — A pai de B pai de A não é hierarquia, é laço, e o servidor recusa alto em vez de guardar uma árvore que ninguém consegue percorrer."
          },
          "parentName": {
            "type": ["null", "string"],
            "description": "Proposto. Nome do parceiro-pai, para a tela mostrar o vínculo sem uma segunda consulta. Mesmo par `id`+`name` que `productTypeId`/`productTypeName` já usa: o id é para escrever, o nome é o que a tela lê. Acompanha `parentId` — os dois são `null` juntos."
          },
          "mobilePhone": {
            "type": ["null", "string"],
            "description": "Proposto. Celular. É o telefone que a tela de Cliente pede como OBRIGATÓRIO, e o contrato o publica anulável assim mesmo: a obrigatoriedade é regra de tela, e parceiro importado do legado sem telefone precisa entrar."
          },
          "businessPhone": {
            "type": ["null", "string"],
            "description": "Proposto. Telefone comercial."
          },
          "homePhone": {
            "type": ["null", "string"],
            "description": "Proposto. Telefone residencial."
          },
          "fax": {
            "type": ["null", "string"],
            "description": "Proposto. Fax. Continua na ficha do legado e ainda é o único canal de alguns fornecedores."
          },
          "address": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/PartnerAddress"
              }
            ],
            "description": "Proposto. Endereço do parceiro; `null` quando não há nenhum campo preenchido. Fora da whitelist de `sortBy`/`filters` de propósito: publicar o dado não é publicar a consulta — filtrar por cidade pede coluna indexada no servidor, e isso é decisão própria."
          },
          "stateRegistration": {
            "type": ["null", "string"],
            "description": "Proposto. Inscrição Estadual (`Cli_IE_rg` do Cliente, `For_IE` do Fornecedor no legado). **Não se confunde com `registration`**, que é o conselho profissional (CREA/CAU/CFT) de quem é Profissional Externo: um é do fisco e vale para a empresa, o outro é da pessoa e vale para o ofício. Estavam sendo lidos como o mesmo campo, e não são."
          },
          "ruralProducerRegistration": {
            "type": ["null", "string"],
            "description": "Proposto. Inscrição Estadual de Produtor Rural (`Cli_IEProdRural`). Campo próprio e não uma segunda IE: o produtor rural pessoa física tem inscrição de produtor SEM ter IE de empresa, e guardar os dois no mesmo lugar apagaria um deles na primeira gravação."
          },
          "categoryId": {
            "type": ["null", "string"],
            "format": "uuid",
            "description": "Proposto. Categoria do cliente (ARQUITETO, CONSUMIDOR FINAL, REVENDA…), item da lista de apoio `CATEGORIA_CLIENTE`. É o que decide tabela de preço e desconto na conversa comercial, e hoje a tela oferece o combo sem ter onde gravar a escolha."
          },
          "categoryName": {
            "type": ["null", "string"],
            "description": "Proposto. Nome da categoria, para a tela mostrar sem uma segunda consulta. Mesmo par `id`+`name` de `productTypeId`/`productTypeName`: o id é para escrever, o nome é o que a tela lê. Acompanha `categoryId` — os dois são `null` juntos."
          },
          "specifierId": {
            "type": ["null", "string"],
            "format": "uuid",
            "description": "Proposto. O ESPECIFICADOR — o profissional que indicou este cliente (`Ind_codigo` no legado). **É um `partners.id`, e não item de lista de apoio.** A descrição anterior dizia que era item da lista `PROFISSIONAL` e estava errada: o profissional externo JÁ É um parceiro (`isProfessional`, um dos três papéis do mesmo cadastro), então uma lista paralela com os mesmos nomes seria o par duplicado que o 409 de `catalog-lookups` existe para impedir — e ela não teria onde guardar o conselho (`registration`) nem a participação, que são justamente o que faz alguém ser especificador. O servidor confere: uuid que não exista em `partners` é 400. **NÃO é `parentId`, e os dois coexistem no mesmo cadastro:** `parentId` liga um profissional ao ESCRITÓRIO de que ele faz parte; `specifierId` liga um CLIENTE a quem o trouxe. Colapsar os dois faria a comissão de indicação ser paga a um escritório que nunca especificou nada. Mesmo shape, vínculos diferentes — e é por serem o mesmo shape que a tela busca os dois em `GET /api/partners`, o especificador com `role=professional`."
          },
          "specifierName": {
            "type": ["null", "string"],
            "description": "Proposto. Nome do especificador — a razão social do PARCEIRO apontado por `specifierId`, não o rótulo de um item de lista. Acompanha `specifierId`: os dois são `null` juntos."
          },
          "notes": {
            "type": ["null", "string"],
            "description": "Proposto. Observação interna (`Cli_OBS`/`for_obs`), texto livre e longo. Só a equipe lê — não sai em documento —, e é por isso que ela não vira coluna de listagem: texto livre numa grade de 25 linhas empurra as colunas úteis para fora da tela."
          },
          "facebook": {
            "type": ["null", "string"],
            "description": "Proposto. Perfil no Facebook (`Cli_Facebook`). Guardado como o operador digitou: URL completa e `@usuario` convivem no legado, e normalizar aqui inventaria endereço que ninguém conferiu."
          },
          "instagram": {
            "type": ["null", "string"],
            "description": "Proposto. Perfil no Instagram (`Cli_Instagram`). Mesma regra do Facebook."
          },
          "billingAddress": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/PartnerAddress"
              }
            ],
            "description": "Proposto. Endereço de COBRANÇA (`Cli_*_cob` no legado) — para onde vai o boleto, que não é onde o cliente mora. `null` quando cobrança e cadastro são o mesmo endereço, que é o caso comum: repetir o endereço principal aqui obrigaria a tela a manter os dois em sincronia e a errar na primeira mudança de um só."
          },
          "businessAddress": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/PartnerAddress"
              }
            ],
            "description": "Proposto. Endereço COMERCIAL (`Cli_*_cor`) — onde a pessoa trabalha, que no cliente pessoa física é o endereço da empresa dela. Junto dele vêm `businessName`, `businessRole`, `businessDocument` e `foundedOn`, que descrevem esse mesmo vínculo de trabalho."
          },
          "businessName": {
            "type": ["null", "string"],
            "description": "Proposto. Empresa onde a pessoa trabalha (`Cli_empresa_cor`). É informação do CLIENTE, não um segundo cadastro: a Vertz vende para a pessoa, e a empresa dela é contexto de crédito e de contato."
          },
          "businessRole": {
            "type": ["null", "string"],
            "description": "Proposto. Cargo dela nessa empresa (`Cli_cargo_cor`)."
          },
          "businessDocument": {
            "type": ["null", "string"],
            "description": "Proposto. CNPJ comercial (`cli_CNPJComercial`) — o da empresa onde trabalha, não o do cadastro. Sem máscara no dado, como `document`. Campo próprio e não uma segunda gravação em `document`: sobrescrever ali trocaria a identidade do cliente pela da empregadora dele."
          },
          "foundedOn": {
            "type": ["null", "string"],
            "format": "date",
            "description": "Proposto. Data de fundação da empresa comercial (`Cli_DataFundacao_cor`). ISO no dado, exibição pt-BR na borda."
          },
          "personType": {
            "type": ["null", "string"],
            "enum": ["individual", "company", null],
            "description": "Proposto. Pessoa FÍSICA ou JURÍDICA — o radio da aba `Principal` do cadastro de cliente. **Campo próprio e não inferência de `document`:** dá para adivinhar pelo tamanho (11 dígitos CPF, 14 CNPJ), mas desde 31/07/2026 o CNPJ é ALFANUMÉRICO, e regra que lê formato de documento envelhece junto com a Receita. O legado também não infere: o radio é preenchido antes do documento, porque o cadastro nasce com o nome e ganha o CPF depois. `null` é o cadastro que nunca respondeu, não um terceiro tipo."
          },
          "identityDocument": {
            "type": ["null", "string"],
            "description": "Proposto. RG da pessoa física (aba `Principal`). Sem máscara no dado, como `document`. A coluna do legado não foi capturada na extração — este campo vem do print da tela, não do schema."
          },
          "identityIssuer": {
            "type": ["null", "string"],
            "description": "Proposto. Órgão expedidor do RG (`SSP`, `DETRAN`, `Ministério da Defesa`). Texto livre e NÃO lista de apoio por ora, pela mesma razão de `workType`: o legado guarda string e a extração não fixou o vocabulário. Vira lookup no dia em que a lista for conhecida, e o campo não muda de nome quando isso acontecer."
          },
          "identityIssuerState": {
            "type": ["null", "string"],
            "description": "Proposto. UF do órgão expedidor — combo PRÓPRIO na tela do legado, separado da UF do endereço. São coisas diferentes: quem tirou o RG em Minas e mora em São Paulo tem as duas divergindo, e reusar `address.state` apagaria uma delas."
          },
          "gender": {
            "type": ["null", "string"],
            "description": "Proposto. Sexo, combo na aba `Principal`. Texto livre e não `enum`: a captura mostra o combo mas NÃO os valores dele, e fechar o vocabulário aqui seria inventar o legado em vez de copiá-lo. **Dado pessoal (LGPD)** — o contrato publica porque o legado já coleta; quem decide exibição e permissão é a tela."
          },
          "birthDate": {
            "type": ["null", "string"],
            "format": "date",
            "description": "Proposto. Data de nascimento da pessoa física (`Dt. de Nasc.`, aba `Principal`). **Não confundir com `foundedOn`**, que é a fundação da EMPRESA onde a pessoa trabalha e entrou no bloco 2: uma descreve a pessoa, a outra o vínculo de trabalho, e o mesmo cadastro pode ter as duas preenchidas. ISO no dado, exibição pt-BR na borda. **Dado pessoal (LGPD).**"
          },
          "deliveryDays": {
            "description": "Proposto. PRAZO DE ENTREGA do fornecedor, em dias corridos — item #2 dos cadastros do comparativo. É o que a ordem de compra usa para SUGERIR `expectedAt` a partir de `orderedAt`; a ordem guarda a data que foi acordada, não este número, porque prazo de cadastro muda e data prometida não.\n\nDias CORRIDOS e não úteis: é assim que o fornecedor promete (\"entrega em 30 dias\"), e converter para úteis exigiria calendário de feriado por UF, que este contrato não tem. `null` no fornecedor sem prazo acordado — e então a ordem não sugere data nenhuma, em vez de sugerir hoje.\n\nSó faz sentido com `isSupplier: true`. O contrato não o recusa no cliente, pela mesma razão que já aceita `paymentTerms` em qualquer parceiro: o cadastro é UM e os papéis são flags, então campo de papel é campo opcional do cadastro inteiro.",
            "type": ["integer", "null"],
            "format": "int32"
          },
          "minimumBillingCents": {
            "description": "Proposto. FATURAMENTO MÍNIMO GERAL do fornecedor, em centavos — quanto a ordem precisa somar para ele aceitar faturar.\n\nÉ regra de NEGÓCIO do fornecedor, e por isso mora no cadastro dele e não na ordem: a ordem só ECOA o valor que valia na emissão (`PurchaseOrderDto.minimumBillingCents`), para que a recusa de ontem continue explicável depois de o fornecedor mudar o mínimo.\n\n`null` = sem mínimo, e é diferente de `0`: zero é mínimo declarado e atendido por qualquer ordem; nulo é \"o fornecedor não impõe mínimo\", e a validação nem roda. Ver `groupMinimums` para a exceção por grupo de produto.",
            "type": ["integer", "null"],
            "format": "int64"
          },
          "buyingCompanies": {
            "description": "Proposto. O histórico de EMPRESA COMPRADORA deste fornecedor, mais recente primeiro. Lista vazia no parceiro que não é fornecedor, ou no fornecedor comprado por qualquer empresa — ausência aqui não bloqueia ordem nenhuma.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SupplierBuyingCompanyDto"
            }
          },
          "groupMinimums": {
            "description": "Proposto. Os faturamentos mínimos POR GRUPO DE PRODUTO, que substituem o geral para os itens do grupo. Lista vazia = só vale `minimumBillingCents`.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SupplierGroupMinimumDto"
            }
          }
        }
      },
      "PartnerLinkRequest": {
        "required": ["code", "paymentTerms", "active"],
        "type": "object",
        "properties": {
          "code": {
            "type": ["null", "string"]
          },
          "paymentTerms": {
            "type": ["null", "string"]
          },
          "active": {
            "type": ["null", "boolean"]
          }
        }
      },
      "PartnerWriteRequest": {
        "required": [
          "document",
          "legalName",
          "tradeName",
          "email",
          "isCustomer",
          "isSupplier",
          "isProfessional",
          "code",
          "paymentTerms",
          "active"
        ],
        "type": "object",
        "properties": {
          "document": {
            "type": ["null", "string"]
          },
          "legalName": {
            "type": ["null", "string"]
          },
          "tradeName": {
            "type": ["null", "string"]
          },
          "email": {
            "type": ["null", "string"]
          },
          "isCustomer": {
            "type": ["null", "boolean"]
          },
          "isSupplier": {
            "type": ["null", "boolean"]
          },
          "isProfessional": {
            "type": ["null", "boolean"]
          },
          "code": {
            "type": ["null", "string"]
          },
          "paymentTerms": {
            "type": ["null", "string"]
          },
          "active": {
            "type": ["null", "boolean"]
          },
          "registration": {
            "type": ["null", "string"],
            "description": "Proposto. Registro Profissional (CREA, CAU, CFT). `PUT` substitui o registro inteiro: omitir apaga."
          },
          "payoutBankInfo": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/PartnerPayoutBankInfo"
              }
            ],
            "description": "Proposto. Conta de comissão. Vale a mesma regra do `PUT`: mandar `null` apaga a conta cadastrada."
          },
          "parentId": {
            "type": ["null", "string"],
            "format": "uuid",
            "description": "Proposto. Vínculo pai/filho. Só o ID viaja na escrita — `parentName` é derivado, e aceitá-lo de volta abriria a porta para o nome divergir do id. `PUT` substitui o registro inteiro: omitir DESVINCULA. Apontar para si mesmo, ou para um descendente, é 400."
          },
          "mobilePhone": {
            "type": ["null", "string"],
            "description": "Proposto. Celular. É o telefone que a tela de Cliente pede como OBRIGATÓRIO, e o contrato o publica anulável assim mesmo: a obrigatoriedade é regra de tela, e parceiro importado do legado sem telefone precisa entrar."
          },
          "businessPhone": {
            "type": ["null", "string"],
            "description": "Proposto. Telefone comercial."
          },
          "homePhone": {
            "type": ["null", "string"],
            "description": "Proposto. Telefone residencial."
          },
          "fax": {
            "type": ["null", "string"],
            "description": "Proposto. Fax. Continua na ficha do legado e ainda é o único canal de alguns fornecedores."
          },
          "address": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/PartnerAddress"
              }
            ],
            "description": "Proposto. Endereço. Vale a regra do `PUT` INTEGRAL, e aqui ela morde mais que nos outros campos: mandar `null` APAGA o endereço inteiro, e omitir na gravação feita por uma tela que não desenha o bloco apagaria o que outra tela gravou. Quem não edita endereço devolve o objeto como veio."
          },
          "stateRegistration": {
            "type": ["null", "string"],
            "description": "Proposto. Inscrição Estadual. `PUT` substitui o registro inteiro: omitir APAGA."
          },
          "ruralProducerRegistration": {
            "type": ["null", "string"],
            "description": "Proposto. IE de Produtor Rural. Mesma regra do `PUT`: omitir apaga."
          },
          "categoryId": {
            "type": ["null", "string"],
            "format": "uuid",
            "description": "Proposto. Categoria do cliente. **Só o ID viaja na escrita** — `categoryName` é derivado, e aceitá-lo de volta abriria a porta para o nome divergir do id, exatamente como o contrato já decidiu em `parentName` e em `productTypeId`."
          },
          "specifierId": {
            "type": ["null", "string"],
            "format": "uuid",
            "description": "Proposto. Especificador — o `partners.id` do profissional que indicou o cliente. Só o ID, pela mesma razão de `categoryId`. **Apontar para si mesmo é 400**, como em `parentId`: cliente que se indica sozinho não é indicação. `null` DESVINCULA — vincular e desvincular são o mesmo ato com valores diferentes."
          },
          "notes": {
            "type": ["null", "string"],
            "description": "Proposto. Observação interna. Mesma regra do `PUT`: omitir apaga."
          },
          "facebook": {
            "type": ["null", "string"],
            "description": "Proposto. Facebook. Mesma regra do `PUT`: omitir apaga."
          },
          "instagram": {
            "type": ["null", "string"],
            "description": "Proposto. Instagram. Mesma regra do `PUT`: omitir apaga."
          },
          "billingAddress": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/PartnerAddress"
              }
            ],
            "description": "Proposto. Endereço de cobrança. O objeto viaja INTEIRO: `null` apaga o endereço cadastrado, e um campo a menos apaga aquele campo."
          },
          "businessAddress": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/PartnerAddress"
              }
            ],
            "description": "Proposto. Endereço comercial. Mesma regra do endereço de cobrança."
          },
          "businessName": {
            "type": ["null", "string"],
            "description": "Proposto. Empresa onde a pessoa trabalha (`Cli_empresa_cor`). É informação do CLIENTE, não um segundo cadastro: a Vertz vende para a pessoa, e a empresa dela é contexto de crédito e de contato. `PUT` substitui o registro inteiro: omitir apaga."
          },
          "businessRole": {
            "type": ["null", "string"],
            "description": "Proposto. Cargo dela nessa empresa (`Cli_cargo_cor`). `PUT` substitui o registro inteiro: omitir apaga."
          },
          "businessDocument": {
            "type": ["null", "string"],
            "description": "Proposto. CNPJ comercial (`cli_CNPJComercial`) — o da empresa onde trabalha, não o do cadastro. Sem máscara no dado, como `document`. Campo próprio e não uma segunda gravação em `document`: sobrescrever ali trocaria a identidade do cliente pela da empregadora dele. `PUT` substitui o registro inteiro: omitir apaga."
          },
          "foundedOn": {
            "type": ["null", "string"],
            "format": "date",
            "description": "Proposto. Data de fundação da empresa comercial (`Cli_DataFundacao_cor`). ISO no dado, exibição pt-BR na borda. `PUT` substitui o registro inteiro: omitir apaga."
          },
          "personType": {
            "type": ["null", "string"],
            "enum": ["individual", "company", null],
            "description": "Proposto. Pessoa FÍSICA ou JURÍDICA — o radio da aba `Principal` do cadastro de cliente. **Campo próprio e não inferência de `document`:** dá para adivinhar pelo tamanho (11 dígitos CPF, 14 CNPJ), mas desde 31/07/2026 o CNPJ é ALFANUMÉRICO, e regra que lê formato de documento envelhece junto com a Receita. O legado também não infere: o radio é preenchido antes do documento, porque o cadastro nasce com o nome e ganha o CPF depois. `null` é o cadastro que nunca respondeu, não um terceiro tipo. `PUT` substitui o registro inteiro: omitir apaga."
          },
          "identityDocument": {
            "type": ["null", "string"],
            "description": "Proposto. RG da pessoa física (aba `Principal`). Sem máscara no dado, como `document`. A coluna do legado não foi capturada na extração — este campo vem do print da tela, não do schema. `PUT` substitui o registro inteiro: omitir apaga."
          },
          "identityIssuer": {
            "type": ["null", "string"],
            "description": "Proposto. Órgão expedidor do RG (`SSP`, `DETRAN`, `Ministério da Defesa`). Texto livre e NÃO lista de apoio por ora, pela mesma razão de `workType`: o legado guarda string e a extração não fixou o vocabulário. Vira lookup no dia em que a lista for conhecida, e o campo não muda de nome quando isso acontecer. `PUT` substitui o registro inteiro: omitir apaga."
          },
          "identityIssuerState": {
            "type": ["null", "string"],
            "description": "Proposto. UF do órgão expedidor — combo PRÓPRIO na tela do legado, separado da UF do endereço. São coisas diferentes: quem tirou o RG em Minas e mora em São Paulo tem as duas divergindo, e reusar `address.state` apagaria uma delas. `PUT` substitui o registro inteiro: omitir apaga."
          },
          "gender": {
            "type": ["null", "string"],
            "description": "Proposto. Sexo, combo na aba `Principal`. Texto livre e não `enum`: a captura mostra o combo mas NÃO os valores dele, e fechar o vocabulário aqui seria inventar o legado em vez de copiá-lo. **Dado pessoal (LGPD)** — o contrato publica porque o legado já coleta; quem decide exibição e permissão é a tela. `PUT` substitui o registro inteiro: omitir apaga."
          },
          "birthDate": {
            "type": ["null", "string"],
            "format": "date",
            "description": "Proposto. Data de nascimento da pessoa física (`Dt. de Nasc.`, aba `Principal`). **Não confundir com `foundedOn`**, que é a fundação da EMPRESA onde a pessoa trabalha e entrou no bloco 2: uma descreve a pessoa, a outra o vínculo de trabalho, e o mesmo cadastro pode ter as duas preenchidas. ISO no dado, exibição pt-BR na borda. **Dado pessoal (LGPD).** `PUT` substitui o registro inteiro: omitir apaga."
          },
          "deliveryDays": {
            "type": ["integer", "null"],
            "format": "int32"
          },
          "minimumBillingCents": {
            "type": ["integer", "null"],
            "format": "int64"
          },
          "buyingCompanies": {
            "description": "Proposto. SUBSTITUI o histórico inteiro — o PUT deste contrato troca o registro completo, e coleção embutida segue a mesma regra. Omitir o campo mantém o que está gravado; mandar `[]` apaga o histórico.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SupplierBuyingCompanyWriteRequest"
            }
          },
          "groupMinimums": {
            "description": "Proposto. SUBSTITUI as linhas de mínimo por grupo, mesma regra de `buyingCompanies`.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SupplierGroupMinimumWriteRequest"
            }
          }
        }
      },
      "ProblemDetails": {
        "description": "O formato ÚNICO de erro do contrato — RFC 9457 Problem Details, servido como `application/problem+json`. Toda resposta 4xx/5xx aponta para cá: sem formato único, cada caminho inventaria o seu e a tela trataria caso a caso. **`title` e `status` são obrigatórios** — `title` é o rótulo estável do TIPO de erro (não muda com a instância) e `status` repete o da resposta, porque o corpo circula fora dela (log, fila, tela). `detail` é a frase daquele caso, e é a única informação acionável que o servidor escolheu dar: a tela mostra o que veio, nunca \\\"algo deu errado\\\".\\n\\nA RFC permite MEMBROS DE EXTENSÃO, e o contrato usa dois: `fields` (validação por campo) e `existingPartnerId` (no 409 de documento repetido, que é o que habilita a oferta de vincular). Extensão nova entra aqui, documentada — não solta na resposta de um caminho só.\n\n**Quem diz QUAL erro é o `type`**, do vocabulário fechado de `ProblemType` — `status` agrupa e `detail` é frase para gente. Ler o tipo pelo texto foi o que este contrato passou a impedir.",
        "required": ["type", "title", "status"],
        "type": "object",
        "properties": {
          "type": {
            "description": "O DISCRIMINADOR de máquina do erro — ver `ProblemType` para o vocabulário inteiro e o `title` canônico de cada um. Obrigatório: a RFC deixa `about:blank` como padrão do membro ausente, mas erro sem `type` obriga a tela a adivinhar pelo texto de `detail`, e texto muda na primeira revisão de frase.",
            "$ref": "#/components/schemas/ProblemType"
          },
          "title": {
            "description": "Rótulo curto e ESTÁVEL do tipo — não varia de uma ocorrência para outra. É o que a tela mostra como cabeçalho do erro, e por isso vem em PT-BR: a aplicação imprime o `title` como veio, sem camada de tradução. **O texto de cada `type` é o da tabela do `ProblemType`** — título escolhido caso a caso faria o mesmo erro aparecer com dois cabeçalhos.",
            "type": "string"
          },
          "status": {
            "description": "O mesmo status da resposta HTTP, repetido no corpo: o problem+json circula fora da resposta (log, fila, tela) e sem ele perde o que aconteceu.",
            "type": "integer",
            "format": "int32"
          },
          "detail": {
            "description": "A frase DESTA ocorrência, para o operador. É a única parte acionável — trocá-la por texto genérico na tela joga fora o que o servidor escolheu dizer.",
            "type": ["null", "string"]
          },
          "instance": {
            "description": "URI da ocorrência específica, quando existir.",
            "type": ["null", "string"]
          },
          "fields": {
            "description": "Validação POR CAMPO — o membro de extensão que deixa o erro chegar ao controle certo do formulário, em vez de virar uma frase solta no topo. Vazio ou ausente = o erro não é de campo. **Viaja no 400 que o contrato já usa para validação; não existe 422 aqui** — dois códigos para a mesma coisa obrigariam cada caminho a escolher um, e a tela a tratar os dois.",
            "type": ["array", "null"],
            "items": {
              "$ref": "#/components/schemas/ProblemFieldError"
            }
          },
          "existingPartnerId": {
            "description": "Só no 409 de parceiro com documento já cadastrado: o id do cadastro que já existe. É ele que habilita \\\"vincular esta empresa ao cadastro existente\\\" (`POST /api/partners/{id}/link`) — sem o campo, o 409 vira beco sem saída.",
            "type": ["null", "string"],
            "format": "uuid"
          }
        }
      },
      "ProblemFieldError": {
        "description": "Um campo que a validação recusou.",
        "required": ["path", "message"],
        "type": "object",
        "properties": {
          "path": {
            "description": "Caminho do campo no CORPO da requisição, como o cliente o mandou (`code`, `variants.0.priceCents`). É a chave que a tela usa para achar o controle — nome de coluna do banco não serve, porque a tela não conhece o banco.",
            "type": "string"
          },
          "message": {
            "description": "O que há de errado com ESTE campo, na frase que o operador lê ao lado do controle.",
            "type": "string"
          }
        }
      },
      "ProblemType": {
        "description": "O VOCABULÁRIO de `type` — a lista FECHADA de tipos de problema, e o discriminador de MÁQUINA de toda 4xx/5xx.\n\nExiste porque `status` é grosso e `detail` é frase: 409 é o conflito de sete coisas diferentes, e a tela que tem uma saída a oferecer (vincular ao cadastro que já existe, abrir o pedido que já saiu, mandar escolher empresa) precisa saber QUAL delas aconteceu. Distinguir por texto quebra na primeira revisão de frase; distinguir só por status manda a tela adivinhar.\n\n**URN nova é PR NESTE repositório**, como todo o resto do contrato. Servidor que inventa `type` fora desta lista responde o que a tela não tem como tratar, e front que lê URN não declarada aqui a escreve à mão — que é justamente o que o codegen existe para impedir.\n\n`title` é o rótulo do TIPO (RFC 9457 §3.1.2) e não varia de uma ocorrência para outra: cada `type` tem UM título canônico, o da tabela abaixo, e o servidor manda aquele texto. Em PT-BR porque é o cabeçalho que o operador lê — não há camada de tradução, e a tela imprime o `title` como veio. Título derivado do status devolveria a informação que o status já deu: `Conflict` em cima de sete conflitos diferentes não distingue nenhum deles.\n\n| `type` | status | `title` | quando |\n| --- | --- | --- | --- |\n| `about:blank` | o do próprio caso | a frase do status: `Requisição inválida` (400) · `Não autenticado` (401) · `Sem permissão` (403) · `Não encontrado` (404) · `Conflito` (409) · `Erro interno` (500) | o tipo É o status, e não há nada a distinguir — recusa de negócio sem saída própria na tela |\n| `urn:cabinet:erro:sem-sessao` | 401 | `Sem sessão` | sessão ausente, expirada ou encerrada — o único 401 das operações de domínio. **O 401 do `POST /auth/login` NÃO é este:** lá a recusa é de credencial, o tipo é `about:blank`, e mandar `sem-sessao` na resposta do próprio login diz ao cliente para reautenticar quem acabou de tentar |\n| `urn:cabinet:erro:senha-precisa-trocar` | 403 | `Senha precisa ser trocada` | credencial vale, falta o passo. 403 e não 401: derrubar a sessão recém-criada põe o cliente em laço de relogin |\n| `urn:cabinet:erro:sem-vinculo-com-empresa` | 403 | `Sem vínculo com a empresa` | a empresa ativa pedida não está entre os vínculos. 403 e não 404 — `tenantId` não é segredo, ele viaja em `GET /auth/tenants` |\n| `urn:cabinet:erro:papel-insuficiente` | 403 | `Papel insuficiente` | o papel não alcança a escrita. É o único dos três 403 em que a pessoa não resolve sozinha, e é por isso que tem URN própria: a tela esconde o controle em vez de mandar tentar de novo |\n| `urn:cabinet:erro:campos-invalidos` | 400 | `Campos inválidos` | validação por campo — vem com `fields[]`, e é ele que leva o erro ao controle certo |\n| `urn:cabinet:erro:ordenacao-invalida` | 400 | `Ordenação inválida` | `sortBy` fora da whitelist da listagem |\n| `urn:cabinet:erro:paginacao-invalida` | 400 | `Paginação inválida` | `page`/`pageSize` fora do que a listagem aceita (teto de 100) |\n| `urn:cabinet:erro:filtro-invalido` | 400 | `Filtro inválido` | `filters`/`joinOperator` malformado, campo fora da whitelist ou operador que o tipo do campo não aceita |\n| `urn:cabinet:erro:papel-invalido` | 400 | `Papel inválido` | o `roleId` pedido no vínculo não é papel desta organização, ou é papel inativo. **Não cobre a ausência**: vínculo sem `roleId` é `campos-invalidos`, porque falta um campo — aqui o campo veio e o papel é que não serve |\n| `urn:cabinet:erro:hierarquia-em-laco` | 400 | `Hierarquia em laço` | o pai escolhido é descendente do próprio registro |\n| `urn:cabinet:erro:senha-atual-invalida` | 400 | `Senha atual não confere` | troca de senha com a atual errada |\n| `urn:cabinet:erro:senha-fraca` | 400 | `Senha fraca` | a senha nova não passa na política do servidor |\n| `urn:cabinet:erro:liberacao-acima-do-vendido` | 409 | `Liberação acima do vendido` | liberar mais peça do que a linha vendeu. Sem ação na tela: o operador digitou o número errado, e o campo é que se corrige |\n| `urn:cabinet:erro:separacao-sem-liberacao` | 409 | `Separação sem liberação` | separar acima do que foi liberado. A tela oferece `liberar` — e o gate é o CHECK monótono do banco, que não depende de papel: quem pula a liberação não separa |\n| `urn:cabinet:erro:entrega-sem-separacao` | 409 | `Entrega sem separação` | entregar acima do que saiu da prateleira. A tela oferece `separar` |\n| `urn:cabinet:erro:entrega-fechada` | 409 | `Entrega fechada` | o romaneio não está `open` — fechado ou cancelado. **Uma URN para os dois estados**, porque a tela faz a mesma coisa: abrir outro romaneio. Qual dos dois é informação de leitura, e viaja em `status` do próprio recurso |\n| `urn:cabinet:erro:entrega-vazia` | 409 | `Entrega vazia` | fechar romaneio sem um item lançado. A saída é a OPOSTA da tentada — quem não saiu se cancela, não se fecha —, e sem o discriminador o \"conflito\" no botão Fechar sugere tentar de novo |\n| `urn:cabinet:erro:entrega-de-outro-pedido` | 409 | `Entrega de outro pedido` | lançar item de um pedido no romaneio de outro. Acontece de verdade: a expedição tem vários romaneios abertos ao mesmo tempo. A tela troca o romaneio; um 409 sem nome a faria duvidar da quantidade |\n| `urn:cabinet:erro:nao-encontrado` | 404 | `Não encontrado` | id que não existe, ou que existe fora do recorte da sessão |\n| `urn:cabinet:erro:sem-empresa-ativa` | 409 | `Sem empresa ativa` | o recurso EXIGE empresa e a sessão não tem uma. 409 e não 400: falta uma AÇÃO da pessoa (escolher empresa), o pedido não está malformado. **Listagem não usa isto** — ela devolve `{rows:[],total:0}` |\n| `urn:cabinet:erro:documento-ja-cadastrado` | 409 | `Documento já cadastrado` | CNPJ/CPF repetido no grupo. Vem com `existingPartnerId`, que é o que habilita vincular em vez de duplicar |\n| `urn:cabinet:erro:email-ja-cadastrado` | 409 | `E-mail já cadastrado` | e-mail de colaborador repetido. Separado do documento porque é CREDENCIAL: não existe \"vincular ao existente\", duas pessoas não compartilham login |\n| `urn:cabinet:erro:codigo-ja-cadastrado` | 409 | `Código já cadastrado` | código único do recurso já em uso (produto no grupo, variante no produto) |\n| `urn:cabinet:erro:vinculo-ja-existe` | 409 | `Vínculo já existe` | `POST` de vínculo que já existe — o caminho de mudar é o `PUT` |\n| `urn:cabinet:erro:papel-de-sistema` | 409 | `Papel de sistema` | tentativa de alterar ou desativar `owner`/`admin`, que toda organização tem e ninguém edita. 409 e não 403: quem pede TEM a permissão de gerenciar papéis — o que recusa é o RECURSO, não o pedinte, e um 403 aqui mandaria o admin procurar uma permissão que não existe. URN própria porque a tela tem o que fazer com ela: esconder `Alterar` na linha do papel de sistema |\n| `urn:cabinet:erro:pedido-ja-convertido` | 409 | `Pedido já gerado` | o orçamento já virou pedido. URN própria porque a tela tem uma ação para ela — abrir o pedido que existe. Sem discriminador, o operador tenta de novo e a compra sai dobrada |\n| `urn:cabinet:erro:valor-nao-parcelavel` | 400 | `Valor não parcelável` | o total do documento não alcança `minTotalToInstallCents` e a condição escolhida tem mais de uma parcela. URN própria porque a tela tem UMA saída clara: oferecer só as condições de parcela única — sem o discriminador ela mostraria a mesma lista e o operador tentaria a segunda condição, que falha igual |\n| `urn:cabinet:erro:parcelas-acima-do-teto` | 400 | `Parcelas acima do teto` | mais parcelas que `maxInstallments` da empresa. Vale nos DOIS lados: cadastrar a condição e escolhê-la no documento. A saída da tela é diferente da de cima — aqui o recorte é por número de parcelas, não por valor |\n| `urn:cabinet:erro:parcela-abaixo-do-minimo` | 400 | `Parcela abaixo do mínimo` | alguma parcela do plano ficaria abaixo de `minInstallmentCents`. É o limite que corta na prática, e o único dos três que depende do TOTAL e do número de parcelas ao mesmo tempo: a mesma condição passa num documento e falha no outro, então a tela não consegue prevê-lo filtrando o combo — ela precisa da recusa nomeada para explicar por que aquela condição não serve PARA ESTE documento |\n| `urn:cabinet:erro:transicao-invalida` | 409 | `Transição inválida` | o documento não pode ir para o estado pedido: concluir cancelado, cancelar concluído, repetir qualquer uma das duas, devolver demonstração que já voltou ou devolver pedido que não é demonstração. **Uma URN para toda a máquina de estados, e não uma por transição:** a saída da tela é a mesma nos cinco casos — reler o documento, porque o estado que ela mostra é passado —, e `detail` diz qual foi. URN por transição multiplicaria o vocabulário sem dar à tela uma decisão nova |\n| `urn:cabinet:erro:demonstracao-em-aberto` | 409 | `Demonstração em aberto` | concluir pedido de demonstração cuja peça ainda não voltou (`demoReturnedAt` nulo). **Não cabe na URN de cima** porque a saída é OUTRA e é acionável: registrar o retorno (`POST .../demo-return`) e concluir de novo. Sem o discriminador, a tela ofereceria \"recarregue\" para um caso que se resolve com dois cliques |\n| `urn:cabinet:erro:orcamento-ja-revisado` | 409 | `Orçamento já revisado` | pedir revisão de um orçamento que já tem uma. A saída é abrir a revisão existente e revisar A PARTIR dela — mesmo papel de `pedido-ja-convertido`, que é o precedente: sem a URN, o operador repete o gesto e a cadeia de versões vira árvore |\n| `urn:cabinet:erro:faturamento-minimo-nao-atingido` | 409 | `Faturamento mínimo não atingido` | a ordem de compra não alcança o mínimo que o fornecedor exige — geral ou o do grupo de produto. `detail` diz quanto falta e para qual grupo, porque o mínimo é cadastro do fornecedor e a tela não tem como calculá-lo |\n| `urn:cabinet:erro:item-ja-em-ordem` | 409 | `Item já está em uma ordem` | a linha de pedido de compra apontada já foi levada por outra ordem, ou o pedido que se tenta reescrever tem linha nesse estado — é a corrida entre dois compradores montando ordem ao mesmo tempo |\n| `urn:cabinet:erro:ordem-ja-enviada` | 409 | `Ordem já enviada` | a ordem de compra já saiu para o fornecedor e não se reescreve nem se reenvia; o que muda a partir dali é data, por reagendamento |\n| `urn:cabinet:erro:fornecedor-divergente` | 409 | `Fornecedor divergente` | alguma linha da ordem vem de um pedido cujo item é de outro fornecedor — a ordem é de UM fornecedor por definição |\n| `urn:cabinet:erro:periodo-ja-fechado` | 409 | `Período já fechado` | pedir o fechamento de um período que já tem um. `detail` diz quando fechou. Sem a URN o operador repete o gesto achando que o primeiro não pegou — mesmo papel de `pedido-ja-convertido`, que é o precedente |\n| `urn:cabinet:erro:origem-ja-paga` | 409 | `Origem já paga` | alguma origem do período JÁ foi paga em OUTRO fechamento, e nada é gravado. Acontece quando dois períodos se sobrepõem — refechar com datas um dia diferentes pagaria tudo de novo, e cada pagamento duplicado seria legítimo para quem só olhasse o fechamento. `detail` diz qual origem, porque a saída é ajustar o período, não insistir |\n| `urn:cabinet:erro:participante-ja-apurado` | 409 | `Participação já apurada` | remover ou mexer no percentual de uma participação que já virou linha de fechamento. O que já foi pago não se reescreve pelo documento — a saída é fechamento novo |\n| `urn:cabinet:erro:profissional-exige-transferencia` | 409 | `Troca de profissional exige transferência` | trocar o profissional PRINCIPAL pela grade de participação. A troca tem DATA e justificativa, e a comissão pergunta por elas: quem a faz é `POST /api/orders/{id}/professional`. Pela grade, a vigência seria reescrita sem ninguém dizer — mesma razão de o `PUT` do pedido não mover `professionalId` |\n| `urn:cabinet:erro:reserva-ja-lancada` | 409 | `Reserva técnica já lançada` | o par (pedido, profissional) já tem reserva ativa. Lançar de novo duplicaria o que o fechamento vai pagar; a saída é cancelar a existente e lançar outra |\n| `urn:cabinet:erro:periodo-fechado` | 409 | `Período fechado` | lançamento, baixa ou transferência com data dentro de período já fechado para aquela conta ou caixa. URN própria porque a saída da tela é específica e não é \"tente de novo\": mudar a data, ou pedir a reabertura a quem pode — que é permissão que este contrato ainda não tem |\n| `urn:cabinet:erro:titulo-com-baixa` | 409 | `Título com baixa` | reescrever (PUT) ou cancelar um título financeiro que já tem pagamento lançado. Distinta de `transicao-invalida` porque a saída é outra: não é reler o documento, é lançar um título novo — o passado não se reescreve depois que o dinheiro andou |\n| `urn:cabinet:erro:parcela-ja-quitada` | 409 | `Parcela já quitada` | baixa sobre parcela cujo saldo já é zero. É a corrida entre dois operadores no mesmo vencimento, e é o caso que produz pagamento em dobro quando a recusa não é nomeada: sem a URN, a tela mostra erro genérico e o operador tenta de novo |\n| `urn:cabinet:erro:valor-acima-do-saldo` | 409 | `Valor acima do saldo` | a baixa abate mais do que a parcela deve. Não tem permissão que libere, ao contrário da quitação a MENOS: pagar mais do que se deve não é alçada, é engano — e o troco não teria onde ser lançado |\n| `urn:cabinet:erro:movimento-ja-conciliado` | 409 | `Movimento já conciliado` | conciliar um movimento que outra pessoa já conferiu. 409 e não 200 porque o segundo pedido quase sempre vem de uma tela desatualizada, e responder OK esconderia que dois operadores estavam conferindo o mesmo extrato |\n| `urn:cabinet:erro:nao-implementado` | 501 | `Não implementado` | a operação está no contrato e ESTE servidor ainda não a serve. É a marca da fase, não erro do pedido: 404 aqui faria a tela concluir que o caminho não existe |\n| `urn:cabinet:erro:resposta-nao-json` | 0 | `Resposta não é da API` | **nenhum servidor emite este.** O CLIENTE o sintetiza quando a resposta não é do contrato — tipicamente o `index.html` do fallback da SPA chegando com 200 porque o proxy do dev não está no ar. Está declarado aqui porque um `type` que a tela lê e o contrato não conhece é a mesma dívida pelo outro lado |\n\nO 409 de nome repetido em `/api/catalog-lookups` fica no `about:blank` de propósito: a tela não tem saída própria a oferecer — quem inclui pelo `+...` corrige o nome ali mesmo.\n\n**Os três de parcelamento são 400 e não 409, e a escolha tem precedente:** o par mais próximo é `hierarquia-em-laco` — id bem formado cuja RELAÇÃO com o estado existente é inválida. O 409 do contrato é para conflito com um recurso que JÁ EXISTE (documento repetido, pedido já gerado, empresa ausente); aqui não há segundo recurso: há um `paymentTermId` que não serve para este documento. A api#103 propôs 409 — fica registrada a divergência, e o motivo dela.",
        "type": "string",
        "enum": [
          "about:blank",
          "urn:cabinet:erro:sem-sessao",
          "urn:cabinet:erro:senha-precisa-trocar",
          "urn:cabinet:erro:sem-vinculo-com-empresa",
          "urn:cabinet:erro:papel-insuficiente",
          "urn:cabinet:erro:campos-invalidos",
          "urn:cabinet:erro:ordenacao-invalida",
          "urn:cabinet:erro:paginacao-invalida",
          "urn:cabinet:erro:filtro-invalido",
          "urn:cabinet:erro:papel-invalido",
          "urn:cabinet:erro:hierarquia-em-laco",
          "urn:cabinet:erro:senha-atual-invalida",
          "urn:cabinet:erro:senha-fraca",
          "urn:cabinet:erro:liberacao-acima-do-vendido",
          "urn:cabinet:erro:separacao-sem-liberacao",
          "urn:cabinet:erro:entrega-sem-separacao",
          "urn:cabinet:erro:entrega-fechada",
          "urn:cabinet:erro:entrega-vazia",
          "urn:cabinet:erro:entrega-de-outro-pedido",
          "urn:cabinet:erro:nao-encontrado",
          "urn:cabinet:erro:sem-empresa-ativa",
          "urn:cabinet:erro:documento-ja-cadastrado",
          "urn:cabinet:erro:email-ja-cadastrado",
          "urn:cabinet:erro:codigo-ja-cadastrado",
          "urn:cabinet:erro:vinculo-ja-existe",
          "urn:cabinet:erro:papel-de-sistema",
          "urn:cabinet:erro:pedido-ja-convertido",
          "urn:cabinet:erro:valor-nao-parcelavel",
          "urn:cabinet:erro:parcelas-acima-do-teto",
          "urn:cabinet:erro:parcela-abaixo-do-minimo",
          "urn:cabinet:erro:transicao-invalida",
          "urn:cabinet:erro:demonstracao-em-aberto",
          "urn:cabinet:erro:orcamento-ja-revisado",
          "urn:cabinet:erro:faturamento-minimo-nao-atingido",
          "urn:cabinet:erro:item-ja-em-ordem",
          "urn:cabinet:erro:ordem-ja-enviada",
          "urn:cabinet:erro:fornecedor-divergente",
          "urn:cabinet:erro:periodo-ja-fechado",
          "urn:cabinet:erro:origem-ja-paga",
          "urn:cabinet:erro:participante-ja-apurado",
          "urn:cabinet:erro:profissional-exige-transferencia",
          "urn:cabinet:erro:reserva-ja-lancada",
          "urn:cabinet:erro:periodo-fechado",
          "urn:cabinet:erro:titulo-com-baixa",
          "urn:cabinet:erro:parcela-ja-quitada",
          "urn:cabinet:erro:valor-acima-do-saldo",
          "urn:cabinet:erro:movimento-ja-conciliado",
          "urn:cabinet:erro:nao-implementado",
          "urn:cabinet:erro:resposta-nao-json"
        ]
      },
      "ProductDetailDto": {
        "required": ["id", "code", "description", "active", "variants"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "code": {
            "type": "string"
          },
          "description": {
            "type": "string"
          },
          "active": {
            "type": "boolean"
          },
          "specialCode": {
            "type": ["null", "string"],
            "description": "Proposto. `Código Especial` (§6) — ver `ProductDto`."
          },
          "shortCode": {
            "type": ["null", "string"],
            "description": "Proposto. `Código Reduzido` (§6) — ver `ProductDto`."
          },
          "unitIn": {
            "type": ["null", "string"],
            "description": "Proposto. `Unid. Entrada` (§6) — ver `ProductDto`."
          },
          "unitInQty": {
            "type": ["null", "string"],
            "description": "Proposto. `Qtd. Entrada` (§6), decimal em string."
          },
          "unitOut": {
            "type": ["null", "string"],
            "description": "Proposto. `Unid. Saída` (§6) — ver `ProductDto`."
          },
          "unitOutQty": {
            "type": ["null", "string"],
            "description": "Proposto. `Qtd. Saída` (§6), decimal em string."
          },
          "productTypeId": {
            "type": ["null", "string"],
            "format": "uuid",
            "description": "Proposto. `Tipo de Produto` (§6) — id do `catalog-lookups` de kind `TIPO_PRODUTO`. Id E nome viajam juntos, o mesmo par de `customerId`/`customerName` do orçamento: o id é a referência que a escrita usa, o nome é o que a LISTAGEM mostra sem precisar carregar a lista de apoio inteira para resolver três colunas."
          },
          "productTypeName": {
            "type": ["null", "string"],
            "description": "Proposto. Nome do tipo, para exibição. Não é escrita — o `PUT` manda o id."
          },
          "brandId": {
            "type": ["null", "string"],
            "format": "uuid",
            "description": "Proposto. `Marca` (§6) — kind `MARCA`."
          },
          "brandName": {
            "type": ["null", "string"],
            "description": "Proposto. Nome da marca, para exibição."
          },
          "factoryId": {
            "type": ["null", "string"],
            "format": "uuid",
            "description": "Proposto. `Fábrica` (§6) — kind `FABRICA`."
          },
          "factoryName": {
            "type": ["null", "string"],
            "description": "Proposto. Nome da fábrica, para exibição."
          },
          "specs": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ProductSpecs"
              }
            ],
            "description": "Proposto. Ficha técnica (§6.2). `null` quando o produto não tem nenhuma medida cadastrada."
          },
          "variants": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ProductVariantDto"
            }
          },
          "suppliers": {
            "description": "Proposto. A grade multi-fornecedor da aba `Dados Principais` (§6.1) — de QUEM se compra esta peça, com o código e a descrição que o fornecedor usa. Ver `ProductSupplierDto`.\n\nVem no detalhe e TAMBÉM em `ProductDto`, que é a resposta de `POST`/`PUT` — sem ela a tela grava e não recebe de volta o que guardou. O que continua valendo é o recorte por USO, não por schema: a LISTAGEM não emite nenhuma das duas, porque grade de FORMULÁRIO em cada linha de uma página de 50 produtos custaria dois JOINs por linha para uma coluna que a listagem não mostra.\n\n**FORA de `required`, e isso é regra desta fila e não descuido:** campo obrigatório em resposta que servidor nenhum emite ainda troca \"não implementado\" por \"contrato quebrado\" — 500 em rota que ninguém tocou, o preço já pago duas vezes (`paymentInstallments`, `serviceItems`). Ausente = o servidor não serve a grade; `[]` = o produto não tem fornecedor cadastrado. São coisas diferentes, e a tela precisa distinguir as duas.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ProductSupplierDto"
            }
          },
          "relatedProducts": {
            "description": "Proposto. Os produtos relacionados (§6.4) — kit e sugestão na MESMA coleção, porque `quantity` é o que os separa: preenchida = KIT (o item entra no documento junto, nesta quantidade por unidade do pai), nula = SUGESTÃO (a tela oferece, o vendedor decide). Ver `ProductRelatedDto`.\n\nNão há campo de tipo ao lado: enum de dois valores duplicaria o que a quantidade já diz, e duas verdades sobre a mesma coisa divergem na primeira gravação que atualize uma e esqueça a outra.\n\nFora de `required` pelo mesmo motivo de `suppliers`.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ProductRelatedDto"
            }
          }
        }
      },
      "ProductSupplierDto": {
        "description": "Proposto. Uma linha da grade de fornecedores do produto (§6.1). O par código/descrição é o do FORNECEDOR — o que ele imprime no catálogo dele —, e é o que a linha do documento de compra carimba.\n\nO carimbo na linha do documento continua sendo VALOR (`supplierCode`/`supplierDescription` do item): esta grade é a ORIGEM no momento da digitação, nunca a verdade retroativa. Trocar o código aqui não pode reescrever o pedido do ano passado.\n\n**A ESCRITA não está publicada, e a ausência é deliberada.** O `PUT` do produto substitui o registro inteiro; publicar `suppliers` em `ProductWriteRequest` antes de a aba existir faria a primeira gravação da tela de produto APAGAR uma grade que o operador nunca viu. Ela entra junto com a aba.",
        "required": ["id", "supplierId", "supplierName", "isDefault", "active"],
        "type": "object",
        "properties": {
          "id": {
            "description": "Id da LINHA da grade — não do fornecedor. É ele que distingue duas linhas do mesmo fornecedor enquanto a tela edita.",
            "type": "string",
            "format": "uuid"
          },
          "supplierId": {
            "description": "O PARCEIRO de papel fornecedor (`PartnerDto.id`, o mesmo id que `GET /api/partners?role=supplier` devolve) — não um id de `catalog-lookups`. Uuid não diz o alvo sozinho, e apontar para a lista errada só apareceria na tela, em branco.",
            "type": "string",
            "format": "uuid"
          },
          "supplierName": {
            "description": "Razão social do fornecedor, para exibição — o mesmo par id×nome de `productTypeId`/`productTypeName`. Não é escrita: a grade grava o id.",
            "type": "string"
          },
          "supplierCode": {
            "description": "`Cód. Prod. Fornecedor` (§6.1) — o código desta peça no catálogo de quem vende. `null` quando o fornecedor não tem código próprio.",
            "type": ["null", "string"]
          },
          "supplierDescription": {
            "description": "`Descrição Fornecedor` (§6.1) — como a peça se chama no catálogo dele, que raramente é como ela se chama aqui.",
            "type": ["null", "string"]
          },
          "isDefault": {
            "description": "`Padrão` (§6.1): o fornecedor que o documento carimba sem perguntar. No máximo UM por produto, e só o ATIVO conta — padrão desativado carimbaria quem não se compra mais, e desativar é justamente o gesto de parar de comprar dele.",
            "type": "boolean"
          },
          "active": {
            "description": "`Ativo` — desativação lógica da linha (§9). Fornecedor que saiu não some da grade: perde o `Ativo`, e os documentos antigos continuam explicáveis.",
            "type": "boolean"
          }
        }
      },
      "ProductRelatedDto": {
        "description": "Proposto. Uma linha da grade de produtos relacionados (§6.4) — o kit que sai junto ou a sugestão que a tela oferece.",
        "required": [
          "id",
          "relatedProductId",
          "relatedProductCode",
          "relatedProductDescription",
          "sortOrder"
        ],
        "type": "object",
        "properties": {
          "id": {
            "description": "Id da LINHA da grade.",
            "type": "string",
            "format": "uuid"
          },
          "relatedProductId": {
            "description": "O OUTRO produto (`ProductDto.id`), nunca o próprio: kit de si mesmo é recursão infinita, e a escrita recusa com 400 dizendo qual linha.",
            "type": "string",
            "format": "uuid"
          },
          "relatedProductCode": {
            "description": "Código do produto relacionado, para exibição — a grade mostra código e descrição, e sem eles a tela teria de buscar cada item para desenhar uma linha.",
            "type": "string"
          },
          "relatedProductDescription": {
            "description": "Descrição do produto relacionado, para exibição.",
            "type": "string"
          },
          "quantity": {
            "description": "Decimal em string, até 3 casas — a mesma regra de `unitInQty`, pelo mesmo motivo (float perde centésimo).\n\n**É o DISCRIMINADOR:** preenchida = kit, `null` = sugestão. Zero e negativo o servidor recusa, e o `0` é o que engana — um kit \"de zero unidades\" passa na tela e leva nada para o documento.",
            "type": ["null", "string"]
          },
          "sortOrder": {
            "description": "A ordem em que a grade mostra. É do DADO e não da tela: o operador arruma a lista uma vez e ela abre igual para o próximo.",
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "ProductDimensions": {
        "type": "object",
        "description": "Proposto. Medidas de uma caixa: as quatro do bloco §6.2, em string decimal pelo mesmo motivo da quantidade — 3 casas, e float perde centésimo. Sem unidade no valor: a unidade é do CAMPO (cm), não do dado; guardar \"10 cm\" tornaria o número inutilizável para conta e para filtro.",
        "properties": {
          "height": {
            "type": ["null", "string"],
            "description": "`Altura`."
          },
          "width": {
            "type": ["null", "string"],
            "description": "`Largura`."
          },
          "length": {
            "type": ["null", "string"],
            "description": "`Comprimento`."
          },
          "radius": {
            "type": ["null", "string"],
            "description": "`Raio`."
          }
        }
      },
      "ProductSpecs": {
        "type": "object",
        "description": "Proposto. A ficha TÉCNICA do produto — a aba `Outros Dados` (§6.2) inteira. É um objeto e não 17 campos soltos no `ProductDto` porque isto é a VERTICAL: iluminação tem lúmen, ângulo e temperatura de cor; outro ramo do mesmo ERP teria outra lista, e um dia esta ficha muda sem que o cadastro de produto mude. A modelagem do banco guarda isto em `products.specs jsonb` pela mesma razão.\n\nO que ficou FORA, e por que: `Marca`, `Fábrica` e `Tipo de Produto` são CLASSIFICAÇÃO e viraram campo de primeiro nível — viram coluna de listagem e um dia entram na whitelist de ordenação, e nada disso funciona dentro de um jsonb. `Descrição livre` e `Publicar no site` também ficaram fora: são conteúdo e canal, não especificação.\n\nTodo valor é STRING: os campos da §6.2 são livres na tela do legado, e converter para número aqui inventaria precisão que o dado não tem.",
        "properties": {
          "lampsPerBallast": {
            "type": ["null", "string"],
            "description": "`Qtd. lâmp. por reator`."
          },
          "watts": {
            "type": ["null", "string"],
            "description": "`Consumo (Watts)`."
          },
          "volts": {
            "type": ["null", "string"],
            "description": "`Tensão (Volts)`."
          },
          "biVolts": {
            "type": ["null", "string"],
            "description": "`Tensão (BiVolts)`."
          },
          "colorTemperature": {
            "type": ["null", "string"],
            "description": "`Temperatura de Cor`."
          },
          "beamAngle": {
            "type": ["null", "string"],
            "description": "`Ângulo`."
          },
          "lumen": {
            "type": ["null", "string"],
            "description": "`Lúmen`."
          },
          "clearSpan": {
            "type": ["null", "string"],
            "description": "`Vão livre`."
          },
          "nicheCut": {
            "type": ["null", "string"],
            "description": "`Corte do nicho`."
          },
          "netWeight": {
            "type": ["null", "string"],
            "description": "`Peso líquido`."
          },
          "grossWeight": {
            "type": ["null", "string"],
            "description": "`Peso bruto`."
          },
          "installationMinutes": {
            "type": ["null", "string"],
            "description": "`Tempo de instalação`, em minutos."
          },
          "warrantyMonths": {
            "type": ["null", "string"],
            "description": "`Garantia`, em meses."
          },
          "productDimensions": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ProductDimensions"
              }
            ],
            "description": "Medidas do PRODUTO."
          },
          "packageDimensions": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ProductDimensions"
              }
            ],
            "description": "Medidas da EMBALAGEM — são outras, e é por isso que são dois blocos."
          }
        }
      },
      "ProductDto": {
        "required": ["id", "code", "description", "active"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "code": {
            "type": "string"
          },
          "description": {
            "type": "string"
          },
          "active": {
            "type": "boolean"
          },
          "specialCode": {
            "type": ["null", "string"],
            "description": "Proposto. `Código Especial` (§6). Um dos TRÊS códigos que o legado guarda por produto, e os três continuam porque a operação usa os três: o nosso, o especial e o reduzido. Unificar em um só quebraria busca de operador que decorou o outro."
          },
          "shortCode": {
            "type": ["null", "string"],
            "description": "Proposto. `Código Reduzido` (§6) — o código curto que o vendedor digita no balcão."
          },
          "unitIn": {
            "type": ["null", "string"],
            "description": "Proposto. `Unid. Entrada` (§6): a unidade em que o produto é COMPRADO. Nome da unidade, não id — é o mesmo texto do lookup `UNIDADE`, e o documento congela texto, não referência."
          },
          "unitInQty": {
            "type": ["null", "string"],
            "description": "Proposto. `Qtd. Entrada` (§6). String decimal e não número: quantidade tem 3 casas e float perde centésimo — a mesma regra que o dinheiro segue em centavos."
          },
          "unitOut": {
            "type": ["null", "string"],
            "description": "Proposto. `Unid. Saída` (§6): a unidade em que o produto é VENDIDO. Pode diferir da entrada — comprar em caixa e vender em peça é rotina do ramo."
          },
          "unitOutQty": {
            "type": ["null", "string"],
            "description": "Proposto. `Qtd. Saída` (§6). O par entrada×saída é que dá o fator de conversão; o front manda os quatro campos que a tela tem, e quem deriva o fator é o servidor — a modelagem do banco guarda `unit_factor`, a tela não."
          },
          "productTypeId": {
            "type": ["null", "string"],
            "format": "uuid",
            "description": "Proposto. `Tipo de Produto` (§6) — id do `catalog-lookups` de kind `TIPO_PRODUTO`. Id E nome viajam juntos, o mesmo par de `customerId`/`customerName` do orçamento: o id é a referência que a escrita usa, o nome é o que a LISTAGEM mostra sem precisar carregar a lista de apoio inteira para resolver três colunas."
          },
          "productTypeName": {
            "type": ["null", "string"],
            "description": "Proposto. Nome do tipo, para exibição. Não é escrita — o `PUT` manda o id."
          },
          "brandId": {
            "type": ["null", "string"],
            "format": "uuid",
            "description": "Proposto. `Marca` (§6) — kind `MARCA`."
          },
          "brandName": {
            "type": ["null", "string"],
            "description": "Proposto. Nome da marca, para exibição."
          },
          "factoryId": {
            "type": ["null", "string"],
            "format": "uuid",
            "description": "Proposto. `Fábrica` (§6) — kind `FABRICA`."
          },
          "factoryName": {
            "type": ["null", "string"],
            "description": "Proposto. Nome da fábrica, para exibição."
          },
          "specs": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ProductSpecs"
              }
            ],
            "description": "Proposto. Ficha técnica (§6.2). `null` quando o produto não tem nenhuma medida cadastrada."
          },
          "suppliers": {
            "description": "Proposto. A grade multi-fornecedor da aba `Dados Principais` (§6.1), a MESMA de `ProductDetailDto.suppliers` e com o mesmo item (`ProductSupplierDto`).\n\n**Ela existe aqui pelo OUTRO uso de `ProductDto`, não pela listagem.** `ProductDto` é a resposta de `POST /api/products` e de `PUT /api/products/{id}` além de ser a linha de `PagedResultOfProductDto`, e é a primeira que faltava: a tela que grava o produto recebia de volta um corpo SEM a grade e ficava sem saber o que o servidor guardou — precisaria de um `GET` do detalhe logo depois para reconciliar, e entre os dois a tela mostra um produto sem fornecedor que tem fornecedor.\n\n**A LISTAGEM continua NÃO emitindo, e a razão da #326 continua de pé:** é grade de FORMULÁRIO, e servi-la em cada linha de uma página de 50 produtos custaria dois JOINs por linha para uma coluna que a listagem não mostra. Ser opcional é o que deixa os dois usos conviverem no mesmo schema — a listagem omite, a resposta da escrita emite.\n\n**FORA de `required`, e isso é regra desta fila e não descuido:** campo obrigatório em resposta que servidor nenhum emite ainda troca \"não implementado\" por \"contrato quebrado\" — 500 em rota que ninguém tocou, o preço já pago duas vezes (`paymentInstallments`, `serviceItems`). Ausente = o servidor não serve a grade AQUI; `[]` = o produto não tem fornecedor cadastrado. São coisas diferentes, e a tela precisa distinguir as duas.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ProductSupplierDto"
            }
          },
          "relatedProducts": {
            "description": "Proposto. Os produtos relacionados (§6.4) — kit e sugestão na MESMA coleção, porque `quantity` é o que os separa: preenchida = KIT (o item entra no documento junto, nesta quantidade por unidade do pai), nula = SUGESTÃO (a tela oferece, o vendedor decide). Ver `ProductRelatedDto`.\n\nAqui pelo mesmo motivo de `suppliers`: é a resposta de `POST`/`PUT` que precisava dela, não a linha da listagem — que segue sem emitir.\n\nNão há campo de tipo ao lado: enum de dois valores duplicaria o que a quantidade já diz, e duas verdades sobre a mesma coisa divergem na primeira gravação que atualize uma e esqueça a outra.\n\nFora de `required` pelo mesmo motivo de `suppliers`.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ProductRelatedDto"
            }
          }
        }
      },
      "ProductVariantDto": {
        "required": ["id", "finish", "size", "active", "priceCents", "stockQty", "minStock"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "finish": {
            "type": "string"
          },
          "size": {
            "type": "string"
          },
          "active": {
            "type": "boolean"
          },
          "priceCents": {
            "type": ["null", "integer"],
            "format": "int64"
          },
          "stockQty": {
            "type": ["null", "number"],
            "format": "double"
          },
          "minStock": {
            "type": ["null", "number"],
            "format": "double"
          }
        }
      },
      "ProductWriteRequest": {
        "required": ["code", "description", "active"],
        "type": "object",
        "properties": {
          "code": {
            "type": ["null", "string"]
          },
          "description": {
            "type": ["null", "string"]
          },
          "active": {
            "type": ["null", "boolean"]
          },
          "specialCode": {
            "type": ["null", "string"],
            "description": "Proposto. `PUT` substitui o registro inteiro: omitir apaga."
          },
          "shortCode": {
            "type": ["null", "string"],
            "description": "Proposto. Mesma regra do `PUT`."
          },
          "unitIn": {
            "type": ["null", "string"],
            "description": "Proposto. Mesma regra do `PUT`."
          },
          "unitInQty": {
            "type": ["null", "string"],
            "description": "Proposto. Decimal em string, 3 casas."
          },
          "unitOut": {
            "type": ["null", "string"],
            "description": "Proposto. Mesma regra do `PUT`."
          },
          "unitOutQty": {
            "type": ["null", "string"],
            "description": "Proposto. Decimal em string, 3 casas."
          },
          "productTypeId": {
            "type": ["null", "string"],
            "format": "uuid",
            "description": "Proposto. Só o ID viaja na escrita: o nome é do cadastro de apoio, e aceitá-lo aqui deixaria a tela renomear a lista de apoio por engano."
          },
          "brandId": {
            "type": ["null", "string"],
            "format": "uuid",
            "description": "Proposto. Só o id — ver `productTypeId`."
          },
          "factoryId": {
            "type": ["null", "string"],
            "format": "uuid",
            "description": "Proposto. Só o id — ver `productTypeId`."
          },
          "specs": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ProductSpecs"
              }
            ],
            "description": "Proposto. Ficha técnica (§6.2). `null` quando o produto não tem nenhuma medida cadastrada."
          }
        }
      },
      "ReadinessStatus": {
        "required": ["status", "detail", "pendingMigrations"],
        "type": "object",
        "properties": {
          "status": {
            "type": "string"
          },
          "detail": {
            "type": ["null", "string"]
          },
          "pendingMigrations": {
            "type": ["null", "integer"],
            "format": "int32"
          }
        }
      },
      "SessaoAtual": {
        "required": [
          "organizationId",
          "employeeId",
          "activeTenantId",
          "expiresAt",
          "mustChangePassword"
        ],
        "type": "object",
        "properties": {
          "organizationId": {
            "type": "string",
            "format": "uuid"
          },
          "employeeId": {
            "type": "string",
            "format": "uuid"
          },
          "activeTenantId": {
            "type": ["null", "string"],
            "format": "uuid"
          },
          "expiresAt": {
            "type": "string",
            "format": "date-time"
          },
          "mustChangePassword": {
            "type": "boolean"
          },
          "displayName": {
            "description": "Proposto. Como o operador se chama na tela — a saudação do Dashboard. `null` quando o servidor não sabe: a tela então cumprimenta sem nome, em vez de exibir e-mail ou id, que é identificador de sistema e não nome de gente.",
            "type": ["string", "null"]
          }
        }
      },
      "StockMovementDto": {
        "required": [
          "id",
          "variantId",
          "locationId",
          "delta",
          "balanceAfter",
          "reason",
          "occurredAt",
          "employeeId"
        ],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "variantId": {
            "type": "string",
            "format": "uuid"
          },
          "locationId": {
            "description": "O depósito onde o movimento aconteceu. Sempre presente: a coluna é `NOT NULL` e o movimento sem local é o que o backfill da migração eliminou, pendurando tudo que existia no padrão da empresa.",
            "type": "string",
            "format": "uuid"
          },
          "delta": {
            "type": "number",
            "format": "double"
          },
          "balanceAfter": {
            "description": "O saldo do DEPÓSITO depois deste movimento — não o total do produto na empresa. Mudou de sentido junto com `locationId` (api#79, decisão 2): é este número que reconcilia contra `GET /api/variants/{variantId}/stock-balances`. Enquanto houver um depósito só, local e total coincidem; a partir do segundo, não.",
            "type": "number",
            "format": "double"
          },
          "reason": {
            "type": "string"
          },
          "occurredAt": {
            "type": "string",
            "format": "date-time"
          },
          "employeeId": {
            "type": ["null", "string"],
            "format": "uuid"
          }
        }
      },
      "StockMovementRequest": {
        "required": ["delta", "reason"],
        "type": "object",
        "properties": {
          "locationId": {
            "description": "Ausente ou `null` = o depósito PADRÃO da empresa ativa, criado sob demanda se ela ainda não tem nenhum (ver a descrição da operação). Fica FORA de `required` de propósito, ao contrário de `delta` e `reason`: aqui omitir é uma escolha com significado, e o tipo gerado precisa saber exprimi-la.",
            "type": ["null", "string"],
            "format": "uuid"
          },
          "delta": {
            "type": ["null", "number"],
            "format": "double"
          },
          "reason": {
            "type": ["null", "string"]
          }
        }
      },
      "TrocarEmpresaRequest": {
        "required": ["tenantId"],
        "type": "object",
        "properties": {
          "tenantId": {
            "type": "string",
            "format": "uuid"
          }
        }
      },
      "VariantWriteRequest": {
        "required": ["finish", "size", "active", "priceCents", "minStock"],
        "type": "object",
        "properties": {
          "finish": {
            "type": ["null", "string"]
          },
          "size": {
            "type": ["null", "string"]
          },
          "active": {
            "type": ["null", "boolean"]
          },
          "priceCents": {
            "type": ["null", "integer"],
            "format": "int64"
          },
          "minStock": {
            "type": ["null", "number"],
            "format": "double"
          }
        }
      },
      "VinculoDeEmpresa": {
        "required": ["tenantId", "name", "role", "features"],
        "type": "object",
        "properties": {
          "tenantId": {
            "type": "string",
            "format": "uuid"
          },
          "name": {
            "type": "string"
          },
          "role": {
            "type": "string"
          },
          "features": {
            "description": "Proposto. Recursos CONTRATADOS por esta empresa — o que ela opera, não o que o usuário pode fazer nela (isso é `role`). Conjunto fechado; valor fora dele é ignorado pelo cliente. Sempre presente: empresa sem recurso opcional devolve `[]`, nunca omite o campo — ausência viraria 'não sei' e o front teria de adivinhar entre esconder tudo e mostrar tudo.",
            "type": "array",
            "items": {
              "type": "string",
              "enum": ["suppliers", "professionals", "employees"]
            }
          }
        }
      },
      "DashboardSummaryDto": {
        "description": "Proposto. Os quatro números da faixa de KPI. Dinheiro em CENTAVOS, inteiro — a regra do repo vale no contrato.",
        "required": [
          "openQuotes",
          "openQuotesDueThisWeek",
          "incomingOrders",
          "incomingOrdersToday",
          "criticalStockItems",
          "monthSalesCents",
          "previousMonthSalesCents"
        ],
        "type": "object",
        "properties": {
          "openQuotes": {
            "description": "Orçamentos abertos.",
            "type": "integer",
            "format": "int32"
          },
          "openQuotesDueThisWeek": {
            "description": "Destes, quantos vencem nos próximos 7 dias.",
            "type": "integer",
            "format": "int32"
          },
          "incomingOrders": {
            "description": "Pedidos de compra a receber.",
            "type": "integer",
            "format": "int32"
          },
          "incomingOrdersToday": {
            "description": "Destes, quantos com previsão para hoje.",
            "type": "integer",
            "format": "int32"
          },
          "criticalStockItems": {
            "description": "Variantes com saldo abaixo do mínimo.",
            "type": "integer",
            "format": "int32"
          },
          "monthSalesCents": {
            "description": "Vendas do mês corrente, em centavos.",
            "type": "integer",
            "format": "int64"
          },
          "previousMonthSalesCents": {
            "description": "Vendas do mês anterior fechado, em centavos. O front deriva a variação dos dois — percentual pronto no DTO seria número que ninguém consegue conferir na tela.",
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "AgendaEventDto": {
        "description": "Proposto. Um compromisso da agenda.",
        "required": ["id", "startsAt", "title", "kind"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "startsAt": {
            "description": "Início, com hora (ISO 8601).",
            "type": "string",
            "format": "date-time"
          },
          "title": {
            "type": "string"
          },
          "context": {
            "description": "A quem o compromisso se refere (cliente, obra, fornecedor).",
            "type": ["string", "null"]
          },
          "kind": {
            "description": "Conjunto fechado — é ele que dá a cor da barra e a legenda do calendário.",
            "type": "string",
            "enum": ["delivery", "quote", "meeting", "payment"]
          }
        }
      },
      "TaskAssigneeDto": {
        "description": "Proposto. Quem responde pela tarefa.",
        "required": ["id", "name", "initials"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "name": {
            "type": "string"
          },
          "initials": {
            "description": "As iniciais que o avatar mostra. Vêm do servidor porque quem sabe partir 'Maria da Silva' em 'MS' é quem guarda o nome — o front partindo string faria 'DS' em nome com preposição.",
            "type": "string"
          }
        }
      },
      "TaskDto": {
        "description": "Proposto. Um cartão do quadro.",
        "required": [
          "id",
          "title",
          "status",
          "priority",
          "commentCount",
          "attachmentCount",
          "assignees"
        ],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "title": {
            "type": "string"
          },
          "description": {
            "type": ["string", "null"]
          },
          "status": {
            "description": "A coluna do quadro.",
            "type": "string",
            "enum": ["todo", "doing", "review", "done"]
          },
          "priority": {
            "type": "string",
            "enum": ["high", "medium", "low"]
          },
          "dueOn": {
            "description": "Prazo (data, sem hora).",
            "type": ["string", "null"],
            "format": "date"
          },
          "commentCount": {
            "type": "integer",
            "format": "int32"
          },
          "attachmentCount": {
            "type": "integer",
            "format": "int32"
          },
          "assignees": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/TaskAssigneeDto"
            }
          }
        }
      },
      "TaskWriteRequest": {
        "description": "Proposto. Corpo de criação de tarefa.",
        "required": ["title", "status", "priority"],
        "type": "object",
        "properties": {
          "title": {
            "type": "string"
          },
          "description": {
            "type": ["string", "null"]
          },
          "status": {
            "description": "A coluna do quadro.",
            "type": "string",
            "enum": ["todo", "doing", "review", "done"]
          },
          "priority": {
            "type": "string",
            "enum": ["high", "medium", "low"]
          },
          "dueOn": {
            "description": "Prazo (data, sem hora).",
            "type": ["string", "null"],
            "format": "date"
          }
        }
      },
      "TaskPatchRequest": {
        "description": "Proposto. Corpo do PATCH: só o que muda. Campo ausente fica como está — ausente NÃO é o mesmo que `null`, que apaga.",
        "type": "object",
        "properties": {
          "title": {
            "type": "string"
          },
          "description": {
            "type": ["string", "null"]
          },
          "status": {
            "description": "A coluna do quadro.",
            "type": "string",
            "enum": ["todo", "doing", "review", "done"]
          },
          "priority": {
            "type": "string",
            "enum": ["high", "medium", "low"]
          },
          "dueOn": {
            "description": "Prazo (data, sem hora).",
            "type": ["string", "null"],
            "format": "date"
          }
        }
      },
      "TodoDto": {
        "description": "Proposto. Um item da lista A fazer.",
        "required": ["id", "title", "done"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "title": {
            "type": "string"
          },
          "done": {
            "type": "boolean"
          }
        }
      },
      "TodoPatchRequest": {
        "description": "Proposto. Marca ou desmarca.",
        "required": ["done"],
        "type": "object",
        "properties": {
          "done": {
            "type": "boolean"
          }
        }
      },
      "ProjectDto": {
        "description": "Proposto. Um projeto do Planner.",
        "required": ["id", "name", "status"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "name": {
            "type": "string"
          },
          "status": {
            "type": "string",
            "enum": ["active", "proposed", "closed"]
          }
        }
      },
      "PlanItemDto": {
        "description": "Proposto. Uma barra do gantt.",
        "required": ["id", "label", "kind", "startsOn", "endsOn", "progressPercent"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "label": {
            "type": "string"
          },
          "kind": {
            "description": "O que a barra é — dá o rótulo da pill.",
            "type": "string",
            "enum": ["task", "order", "delivery"]
          },
          "startsOn": {
            "type": "string",
            "format": "date"
          },
          "endsOn": {
            "description": "Previsão de término, inclusiva.",
            "type": "string",
            "format": "date"
          },
          "progressPercent": {
            "description": "0 a 100. Progresso NÃO é dinheiro — a tela não o pinta de verde.",
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "PlanPhaseDto": {
        "description": "Proposto. Uma fase do projeto, com as barras que moram nela.",
        "required": ["id", "name", "startsOn", "endsOn", "items"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "name": {
            "type": "string"
          },
          "startsOn": {
            "type": "string",
            "format": "date"
          },
          "endsOn": {
            "type": "string",
            "format": "date"
          },
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PlanItemDto"
            }
          }
        }
      },
      "ProjectPlanDto": {
        "description": "Proposto. O plano inteiro de um projeto.",
        "required": ["projectId", "phases"],
        "type": "object",
        "properties": {
          "projectId": {
            "type": "string",
            "format": "uuid"
          },
          "phases": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PlanPhaseDto"
            }
          }
        }
      },
      "EmployeeDto": {
        "description": "Proposto. Colaborador da empresa ativa. Somente leitura neste corte: existe para resolver o `salespersonId` do orçamento e a listagem de Colaboradores. O formulário de RH (transcrição §2, ~30 campos) entra num corte próprio, com POST e PUT. Não há `code`: o legado identifica funcionário por CPF e não guarda código humano, então a coluna `Código` da listagem sai da tela em vez de exibir um uuid.",
        "required": ["id", "name", "active"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "name": {
            "type": "string"
          },
          "sector": {
            "description": "Setor — kind `SETOR`.",
            "type": ["string", "null"]
          },
          "jobTitle": {
            "description": "Cargo — kind `CARGO`.",
            "type": ["string", "null"]
          },
          "active": {
            "type": "boolean"
          }
        }
      },
      "PagedResultOfEmployeeDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/EmployeeDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "QuoteEnvironmentDto": {
        "description": "Proposto. Ambiente da obra DENTRO do orçamento — o `Ambiente F5` da tela. São três camadas, e esta é a do meio: o catálogo por empresa vive em `GET /api/catalog-lookups?kind=AMBIENTE`, esta é a instância no documento, e o item aponta para ela por `environmentCode`. O `name` é CONGELADO na emissão, como o legado já faz em `VendaAmbiente.VenAmb_Descricao`: renomear o ambiente no catálogo não pode reescrever orçamento fechado, e reescrever item a item seria pior ainda. Ambiente sem item nenhum é estado legítimo — por isso é coleção própria, não um campo derivado dos itens.",
        "required": ["code", "name", "order"],
        "type": "object",
        "properties": {
          "code": {
            "description": "Id do ambiente no catálogo (`CatalogLookupDto.id`, kind `AMBIENTE`).",
            "type": "string",
            "format": "uuid"
          },
          "name": {
            "description": "Nome congelado na emissão.",
            "type": "string"
          },
          "order": {
            "description": "Ordem de exibição. O PDF do orçamento agrupa por ambiente e vai para o cliente, então a ordem é dado, não apresentação. NÃO vem do legado — `VendaAmbiente` não tem coluna de ordem.",
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "QuoteItemDto": {
        "description": "Proposto. Linha da grade do orçamento. Os campos de descrição, acabamento, tamanho e preço são SNAPSHOT da emissão, não leitura do catálogo: documento comercial congela spec e preço, senão corrigir o cadastro reescreveria orçamento do ano passado.",
        "required": [
          "lineNumber",
          "description",
          "quantity",
          "unitPriceCents",
          "discountPercent",
          "totalCents"
        ],
        "type": "object",
        "properties": {
          "lineNumber": {
            "description": "Coluna `Item`.",
            "type": "integer",
            "format": "int32"
          },
          "environmentCode": {
            "description": "Ambiente a que o item pertence. `null` = item fora de ambiente, que o legado permite (`VendaProduto.CodAmbiente` é nulável).",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "variantId": {
            "description": "Variante do catálogo (produto × acabamento × tamanho). `null` quando a linha não veio do catálogo.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "description": {
            "description": "Descrição congelada na emissão.",
            "type": "string"
          },
          "finish": {
            "description": "Acabamento congelado.",
            "type": ["string", "null"]
          },
          "size": {
            "description": "Tamanho congelado.",
            "type": ["string", "null"]
          },
          "quantity": {
            "description": "Até 3 casas.",
            "type": "number",
            "format": "double"
          },
          "unit": {
            "type": ["string", "null"]
          },
          "unitPriceCents": {
            "description": "Valor unitário congelado, em centavos.",
            "type": "integer",
            "format": "int64"
          },
          "discountPercent": {
            "description": "Desconto do item. Int com 4 casas implícitas: `10000` = 1%.",
            "type": "integer",
            "format": "int32"
          },
          "supplierId": {
            "description": "Fornecedor da linha (`PartnerDto.id`).",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "supplierName": {
            "type": ["string", "null"]
          },
          "supplierCode": {
            "description": "Código do produto NO fornecedor — a língua em que o item é pedido.",
            "type": ["string", "null"]
          },
          "supplierDescription": {
            "type": ["string", "null"]
          },
          "productGroup": {
            "description": "Nome do grupo, CONGELADO na emissão como o resto da linha. O id é `productGroupId` — este campo é o que se lê, não o que se casa.",
            "type": ["string", "null"]
          },
          "productGroupId": {
            "description": "Grupo de produto da linha — `CatalogLookupDto.id`, kind `GRUPO_PRODUTO`. É por ele que a linha entra numa das linhas de `groupDiscounts`. A atribuição não pode sair de `productGroup`, que é NOME congelado: nome nunca foi chave, e dois grupos homônimos (ou um renomeado depois da emissão) mandariam o desconto para o grupo errado sem ninguém acusar. `null` na linha sem grupo — que então não recebe desconto de grupo nenhum.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "pieceType": {
            "description": "Tipo da peça — kind `TIPO_PECA`.",
            "type": ["string", "null"]
          },
          "totalCents": {
            "description": "Valor do item. Calculado pelo servidor — a escrita não manda.",
            "type": "integer",
            "format": "int64"
          },
          "calculatedUnitPriceCents": {
            "type": ["integer", "null"],
            "format": "int64",
            "description": "Proposto. O preço que a FÓRMULA produziu para esta variante, em centavos — `round(líquido de compra × indexValue)`, com o líquido saindo de `VariantTablePriceDto.tablePriceCents` menos a cascata e os créditos do `CostProfileDto` daquele fornecedor.\n\n**É eco, não escrita.** O `PUT` ignora o que vier aqui. Quem manda no total da linha é `unitPriceCents`.\n\n**CONGELADO na emissão, como o resto da linha.** Recalcular na leitura faria todo orçamento antigo parecer descontado no dia seguinte a um ajuste de índice — o operador veria \"praticado R$ 100, calculado R$ 130\" numa venda que saiu no preço cheio de ontem. A linha já congela descrição, acabamento, tamanho e fornecedor pela mesma razão.\n\n**Por que os dois viajam juntos.** Sem o calculado ao lado do praticado, a tela não tem como mostrar que aquela linha saiu abaixo do preço — o operador vê um número e não sabe se ele é o da fórmula ou o que alguém digitou. Com os dois, a diferença é visível sem que o item precise carregar custo ou margem, que são de `CostSimulationDto` e de outro papel.\n\n**`null` quando a fórmula não tem o que multiplicar:** item sem `variantId` (linha livre), variante sem tabela do fornecedor, fornecedor sem índice, ou índice inativo. `null` é a resposta honesta — devolver zero faria a tela desenhar \"preço sugerido: R$ 0,00\" e sugerir que a peça é de graça."
          }
        }
      },
      "QuoteItemWriteRequest": {
        "description": "Proposto. Linha da grade na escrita. Sem `totalCents`: quem soma é o servidor.",
        "required": ["lineNumber", "description", "quantity", "discountPercent"],
        "type": "object",
        "properties": {
          "lineNumber": {
            "description": "Coluna `Item`.",
            "type": "integer",
            "format": "int32"
          },
          "environmentCode": {
            "description": "Ambiente a que o item pertence. `null` = item fora de ambiente, que o legado permite (`VendaProduto.CodAmbiente` é nulável).",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "variantId": {
            "description": "Variante do catálogo (produto × acabamento × tamanho). `null` quando a linha não veio do catálogo.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "description": {
            "description": "Descrição congelada na emissão.",
            "type": "string"
          },
          "finish": {
            "description": "Acabamento congelado.",
            "type": ["string", "null"]
          },
          "size": {
            "description": "Tamanho congelado.",
            "type": ["string", "null"]
          },
          "quantity": {
            "description": "Até 3 casas.",
            "type": "number",
            "format": "double"
          },
          "unit": {
            "type": ["string", "null"]
          },
          "unitPriceCents": {
            "type": ["integer", "null"],
            "format": "int64",
            "description": "Valor unitário da linha, em centavos.\n\n**Passou a ser OPCIONAL para que o item possa NASCER PRECIFICADO.** Ausente ou `null` com `variantId` preenchido, o servidor calcula pela fórmula e grava o resultado — é o gesto de \"incluir a peça e ela já vem com preço\", que no legado é o comportamento normal e aqui exigia que a tela soubesse o preço antes de pedir.\n\n**Valor presente continua mandando**, e é assim que o preço negociado sobrevive: quem digita um valor está dizendo que aquela linha sai por ele, e o servidor não o substitui. O que a fórmula produziu segue visível em `QuoteItemDto.calculatedUnitPriceCents`, ao lado.\n\n**Ausente e sem `variantId` é zero**, não erro: a linha livre não tem de onde tirar preço, e recusá-la impediria o item de texto que o orçamento usa para descrever serviço ou observação."
          },
          "discountPercent": {
            "description": "Desconto do item. Int com 4 casas implícitas: `10000` = 1%.",
            "type": "integer",
            "format": "int32"
          },
          "supplierId": {
            "description": "Fornecedor da linha (`PartnerDto.id`).",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "supplierName": {
            "type": ["string", "null"]
          },
          "supplierCode": {
            "description": "Código do produto NO fornecedor — a língua em que o item é pedido.",
            "type": ["string", "null"]
          },
          "supplierDescription": {
            "type": ["string", "null"]
          },
          "productGroup": {
            "description": "Nome do grupo, CONGELADO na emissão como o resto da linha. O id é `productGroupId` — este campo é o que se lê, não o que se casa.",
            "type": ["string", "null"]
          },
          "productGroupId": {
            "description": "Grupo de produto da linha — `CatalogLookupDto.id`, kind `GRUPO_PRODUTO`. É por ele que a linha entra numa das linhas de `groupDiscounts`. A atribuição não pode sair de `productGroup`, que é NOME congelado: nome nunca foi chave, e dois grupos homônimos (ou um renomeado depois da emissão) mandariam o desconto para o grupo errado sem ninguém acusar. `null` na linha sem grupo — que então não recebe desconto de grupo nenhum.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "pieceType": {
            "description": "Tipo da peça — kind `TIPO_PECA`.",
            "type": ["string", "null"]
          }
        }
      },
      "QuoteServiceItemDto": {
        "description": "Proposto. Linha da ABA SERVIÇOS do documento — a seção que o comparativo (02-vendas) apontou como faltando, e que no legado é `VendaServico` (4.450 linhas; `Orcamento_servico_det` e `pedido_servico_det` existem no schema e estão ZERADAS, então quem descreve a operação real é `VendaServico`).\n\nColeção SEPARADA de `items`, e não um item com flag: as duas linhas não têm as mesmas colunas. Serviço não tem acabamento, tamanho, unidade, fornecedor nem código no fornecedor, e tem percentual de eletricista, que produto nenhum tem. Uma coleção só obrigaria metade dos campos a serem `null` conforme a outra metade — e o total do documento é a soma das DUAS, calculada pelo servidor.\n\nComo em `QuoteItemDto`, descrição e preço são SNAPSHOT da emissão: corrigir o cadastro não pode reescrever documento fechado.",
        "required": [
          "lineNumber",
          "description",
          "quantity",
          "unitPriceCents",
          "discountPercent",
          "electricianPercent",
          "electricianAmountCents",
          "totalCents"
        ],
        "type": "object",
        "properties": {
          "lineNumber": {
            "description": "Coluna `Item`.",
            "type": "integer",
            "format": "int32"
          },
          "environmentCode": {
            "description": "Ambiente a que o serviço pertence. `null` = fora de ambiente, que o legado permite (`VendaServico.CodAmbiente` é nulável, como o do produto).",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "serviceId": {
            "description": "Serviço do cadastro (`ServiceDto.id`). `null` quando a linha não veio do cadastro — o legado permite descrição avulsa (`Orcamento_servico_det.ose_descricao` existe ao lado do `Sev_cod`).",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "description": {
            "description": "Descrição congelada na emissão.",
            "type": "string"
          },
          "quantity": {
            "description": "Até 3 casas.",
            "type": "number",
            "format": "double"
          },
          "unitPriceCents": {
            "description": "Valor unitário congelado, em centavos.",
            "type": "integer",
            "format": "int64"
          },
          "discountPercent": {
            "description": "Desconto da linha. Int com 4 casas implícitas: `10000` = 1%.\n\n**O legado guarda DOIS descontos por linha de serviço** (`VenSer_DescVl` em valor e `VenSer_DescPorc` em percentual) e nada garante que concordem. Aqui só o percentual sobe, como já vale em `QuoteItemDto`: dois campos para uma decisão é a fonte dupla que faz o total do documento divergir do total das linhas.",
            "type": "integer",
            "format": "int32"
          },
          "electricianPercent": {
            "description": "Percentual do eletricista congelado na emissão — o do cadastro, ou o override que a escrita mandou. Nunca `null` na leitura: o servidor já resolveu a herança, e devolver `null` obrigaria quem lê a refazer a conta contra um cadastro que pode ter mudado desde então.",
            "type": "integer",
            "format": "int32"
          },
          "electricianAmountCents": {
            "description": "Quanto desta linha é do instalador, em centavos — `VenSer_VlEletricista` do legado. Calculado pelo servidor: a escrita não manda.\n\nEle é campo, e não conta que quem lê refaz, porque é o número que vira PAGAMENTO (`acerto_eletrecistas_servicos`). Recalcular no cliente daria um arredondamento por cliente sobre uma linha que alguém recebe.",
            "type": "integer",
            "format": "int64"
          },
          "totalCents": {
            "description": "Valor da linha. Calculado pelo servidor — a escrita não manda.",
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "QuoteServiceItemWriteRequest": {
        "description": "Proposto. Linha da aba Serviços na escrita. Sem `totalCents` nem `electricianAmountCents`: quem calcula é o servidor.",
        "required": [
          "lineNumber",
          "description",
          "quantity",
          "unitPriceCents",
          "discountPercent",
          "electricianPercent"
        ],
        "type": "object",
        "properties": {
          "lineNumber": {
            "description": "Coluna `Item`.",
            "type": "integer",
            "format": "int32"
          },
          "environmentCode": {
            "description": "Ambiente a que o serviço pertence. `null` = fora de ambiente, que o legado permite (`VendaServico.CodAmbiente` é nulável, como o do produto).",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "serviceId": {
            "description": "Serviço do cadastro (`ServiceDto.id`). `null` quando a linha não veio do cadastro — o legado permite descrição avulsa (`Orcamento_servico_det.ose_descricao` existe ao lado do `Sev_cod`).",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "description": {
            "description": "Descrição congelada na emissão.",
            "type": "string"
          },
          "quantity": {
            "description": "Até 3 casas.",
            "type": "number",
            "format": "double"
          },
          "unitPriceCents": {
            "description": "Valor unitário congelado, em centavos.",
            "type": "integer",
            "format": "int64"
          },
          "discountPercent": {
            "description": "Desconto da linha. Int com 4 casas implícitas: `10000` = 1%.\n\n**O legado guarda DOIS descontos por linha de serviço** (`VenSer_DescVl` em valor e `VenSer_DescPorc` em percentual) e nada garante que concordem. Aqui só o percentual sobe, como já vale em `QuoteItemDto`: dois campos para uma decisão é a fonte dupla que faz o total do documento divergir do total das linhas.",
            "type": "integer",
            "format": "int32"
          },
          "electricianPercent": {
            "description": "Percentual do eletricista NESTA linha. **`null` = herda do cadastro** (`ServiceDto.electricianPercent`) no momento da gravação; qualquer valor é OVERRIDE explícito, inclusive `0`.\n\nA distinção entre `null` e `0` é a que o campo existe para carregar: zero é \"esta linha não paga instalador\", e omitir é \"use o que o cadastro diz\". Um campo não-nulável forçaria o cliente a copiar o percentual do cadastro para dentro do corpo — e aí quem gravasse com o cadastro desatualizado na tela congelaria o número velho sem ninguém pedir.",
            "type": ["integer", "null"],
            "format": "int32"
          }
        }
      },
      "QuoteDto": {
        "description": "Proposto. Orçamento na listagem — só o que a grade mostra.",
        "required": ["id", "number", "customerId", "customerName", "status", "totalCents"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "number": {
            "description": "Número do documento. Sequência GLOBAL do grupo, atribuída pelo servidor — por isso não existe na escrita. No legado `Ven_CodigoPre` já é única entre as empresas, e cliente que escolhe número colide entre elas.",
            "type": "string"
          },
          "series": {
            "description": "Série do documento.",
            "type": ["string", "null"]
          },
          "issuedAt": {
            "description": "Data de emissão.",
            "type": ["string", "null"],
            "format": "date"
          },
          "expiresAt": {
            "description": "Data de validade — curta, ~5 dias na operação atual.",
            "type": ["string", "null"],
            "format": "date"
          },
          "customerId": {
            "type": "string",
            "format": "uuid"
          },
          "customerName": {
            "description": "Nome do cliente na emissão.",
            "type": "string"
          },
          "projectName": {
            "description": "**Substituído por `workId`/`workName`.** Era a `Descrição da Obra` digitada NO documento — texto livre, sem elo com o cadastro de obra, que não existia quando o campo nasceu.\n\nContinua publicado, e não removido, porque a listagem ordena e filtra por ele e as telas o leem: tirar agora quebraria as duas pontas no mesmo dia. Enquanto convivem, os dois NÃO são o mesmo dado — `projectName` é o que foi digitado, `workName` é como a obra se chama hoje. Divergir é legítimo em documento antigo, e sobrescrever um com o outro apagaria o que o operador escreveu.",
            "type": ["string", "null"],
            "deprecated": true
          },
          "workId": {
            "description": "A OBRA do documento — `WorkDto.id`; `Venda.Obr_codigo` no legado. **É o elo que faltava.** A obra já é entidade própria (`/api/works`, `Obras` com 9.454 linhas) e o documento só a nomeava por TEXTO LIVRE em `projectName`: dois orçamentos da mesma obra não eram a mesma obra para ninguém — nem para a tela, nem para uma consulta, nem para o histórico do cliente.\n\n`null` porque o legado permite (`Venda.Obr_codigo` é nulável, e há venda de balcão sem obra). O que NÃO é opcional é a coerência: **obra que não é do cliente do documento é 400**, com `fields[]` apontando `workId`. A obra pertence ao cliente (`Obras.Cli_codigo`) e o documento não reescreve esse vínculo — aceitar calado deixaria o endereço de entrega de um cliente pendurado na venda de outro.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "workName": {
            "description": "Descrição da obra (`Obras.Obr_Descricao`), resolvida pelo servidor — o par `id`+`name` de sempre: o id é para escrever, o nome é o que a tela lê, sem uma segunda consulta. `null` quando `workId` é `null`.",
            "type": ["string", "null"]
          },
          "status": {
            "description": "Documento CANCELA, não desativa: `active` de cadastro não serve aqui. Espelha `Ven_Situacao` (A/C) do legado.",
            "type": "string",
            "enum": ["active", "cancelled"]
          },
          "revision": {
            "description": "Número da revisão, 1-based. `1` é o orçamento original; `2` é a primeira revisão. Resolve um caso REAL do legado: dois orçamentos do mesmo cliente no mesmo dia, sem nada no dado dizendo que o segundo substitui o primeiro — quem lia a lista contava dois negócios onde havia um.",
            "type": "integer",
            "minimum": 1
          },
          "revisionOfId": {
            "description": "O orçamento que esta revisão substitui, ou `null` no original. A revisão é documento NOVO (`POST .../revise` copia cabeçalho, ambientes e itens) porque o anterior é o que foi mostrado ao cliente: sobrescrevê-lo apagaria a proposta que já saiu pela porta.",
            "type": ["null", "string"],
            "format": "uuid"
          },
          "totalCents": {
            "description": "Total do orçamento, em centavos. Calculado pelo servidor.",
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "QuoteDetailDto": {
        "description": "Proposto. O orçamento inteiro: cabeçalho, ambientes e itens numa resposta só. Itens e ambientes viajam embutidos porque não têm identidade fora do documento, e porque sub-recurso por linha faria um `Gravar` virar N requisições sem transação entre elas — falha no meio deixaria metade da grade gravada.",
        "required": [
          "id",
          "number",
          "customerId",
          "customerName",
          "status",
          "totalCents",
          "discountMode",
          "discountPercent",
          "environments",
          "items",
          "paymentInstallments",
          "serviceItems"
        ],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "number": {
            "description": "Número do documento. Sequência GLOBAL do grupo, atribuída pelo servidor — por isso não existe na escrita. No legado `Ven_CodigoPre` já é única entre as empresas, e cliente que escolhe número colide entre elas.",
            "type": "string"
          },
          "series": {
            "description": "Série do documento.",
            "type": ["string", "null"]
          },
          "issuedAt": {
            "description": "Data de emissão.",
            "type": ["string", "null"],
            "format": "date"
          },
          "expiresAt": {
            "description": "Data de validade — curta, ~5 dias na operação atual.",
            "type": ["string", "null"],
            "format": "date"
          },
          "customerId": {
            "type": "string",
            "format": "uuid"
          },
          "customerName": {
            "description": "Nome do cliente na emissão.",
            "type": "string"
          },
          "projectName": {
            "description": "**Substituído por `workId`/`workName`.** Era a `Descrição da Obra` digitada NO documento — texto livre, sem elo com o cadastro de obra, que não existia quando o campo nasceu.\n\nContinua publicado, e não removido, porque a listagem ordena e filtra por ele e as telas o leem: tirar agora quebraria as duas pontas no mesmo dia. Enquanto convivem, os dois NÃO são o mesmo dado — `projectName` é o que foi digitado, `workName` é como a obra se chama hoje. Divergir é legítimo em documento antigo, e sobrescrever um com o outro apagaria o que o operador escreveu.",
            "type": ["string", "null"],
            "deprecated": true
          },
          "workId": {
            "description": "A OBRA do documento — `WorkDto.id`; `Venda.Obr_codigo` no legado. **É o elo que faltava.** A obra já é entidade própria (`/api/works`, `Obras` com 9.454 linhas) e o documento só a nomeava por TEXTO LIVRE em `projectName`: dois orçamentos da mesma obra não eram a mesma obra para ninguém — nem para a tela, nem para uma consulta, nem para o histórico do cliente.\n\n`null` porque o legado permite (`Venda.Obr_codigo` é nulável, e há venda de balcão sem obra). O que NÃO é opcional é a coerência: **obra que não é do cliente do documento é 400**, com `fields[]` apontando `workId`. A obra pertence ao cliente (`Obras.Cli_codigo`) e o documento não reescreve esse vínculo — aceitar calado deixaria o endereço de entrega de um cliente pendurado na venda de outro.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "workName": {
            "description": "Descrição da obra (`Obras.Obr_Descricao`), resolvida pelo servidor — o par `id`+`name` de sempre: o id é para escrever, o nome é o que a tela lê, sem uma segunda consulta. `null` quando `workId` é `null`.",
            "type": ["string", "null"]
          },
          "status": {
            "description": "Documento CANCELA, não desativa: `active` de cadastro não serve aqui. Espelha `Ven_Situacao` (A/C) do legado.",
            "type": "string",
            "enum": ["active", "cancelled"]
          },
          "revision": {
            "description": "Número da revisão, 1-based. `1` é o orçamento original; `2` é a primeira revisão. Resolve um caso REAL do legado: dois orçamentos do mesmo cliente no mesmo dia, sem nada no dado dizendo que o segundo substitui o primeiro — quem lia a lista contava dois negócios onde havia um.",
            "type": "integer",
            "minimum": 1
          },
          "revisionOfId": {
            "description": "O orçamento que esta revisão substitui, ou `null` no original. A revisão é documento NOVO (`POST .../revise` copia cabeçalho, ambientes e itens) porque o anterior é o que foi mostrado ao cliente: sobrescrevê-lo apagaria a proposta que já saiu pela porta.",
            "type": ["null", "string"],
            "format": "uuid"
          },
          "revisionOfNumber": {
            "type": ["null", "string"],
            "description": "Número do orçamento revisado, resolvido na leitura — o que a tela mostra sem uma segunda requisição."
          },
          "totalCents": {
            "description": "Total do orçamento, em centavos. Calculado pelo servidor.",
            "type": "integer",
            "format": "int64"
          },
          "folderNumber": {
            "description": "`Nº Pasta`.",
            "type": ["string", "null"]
          },
          "closedAt": {
            "description": "Data de fechamento.",
            "type": ["string", "null"],
            "format": "date"
          },
          "cancelledAt": {
            "type": ["null", "string"],
            "description": "Quando o documento foi cancelado. `null` enquanto não for.",
            "format": "date-time"
          },
          "cancelReasonId": {
            "description": "Motivo do cancelamento — id da lista de apoio. É o `Mod_codigo` que o legado gravava junto de `ven_situacao='C'`. O `kind` da lista NÃO passa pelo contrato (ADR-011): entra por linha no catálogo do servidor.",
            "type": ["null", "string"],
            "format": "uuid"
          },
          "cancelReasonName": {
            "type": ["null", "string"],
            "description": "Nome do motivo, resolvido na leitura. `null` quando cancelaram sem motivo."
          },
          "cancelNote": {
            "type": ["null", "string"],
            "description": "Observação livre de quem cancelou. O motivo diz a CLASSE; a nota diz o caso.",
            "maxLength": 200
          },
          "salespersonId": {
            "description": "Consultor(a) que atende — `EmployeeDto.id`.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "salespersonName": {
            "type": ["string", "null"]
          },
          "professionalId": {
            "description": "Profissional Externo (arquiteto/especificador) — é `PartnerDto.id` com papel `professional`, não cadastro à parte.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "professionalName": {
            "type": ["string", "null"]
          },
          "discountMode": {
            "description": "Como o desconto é aplicado — `Ven_TipoDesc` do legado.\n\n- `product` — cada linha tem o seu (`discountPercent` do item); o do documento é zero.\n- `general` — um percentual só, no documento; o das linhas é zero.\n- `group` — **por GRUPO DE PRODUTO**, em `groupDiscounts`; documento e linhas ficam em zero.\n\n`group` é o que faltava, e não é caso de canto: no legado `VendaDesconto` tem **300.337 linhas** para **37.707** vendas, ~8 grupos por documento. Era o modo mais usado da operação e o único dos três que o contrato não publicava — quem descontasse por grupo tinha de escolher entre espalhar o percentual linha a linha (e perder de qual grupo veio) ou achatar tudo num desconto geral (e mudar o valor).",
            "type": "string",
            "enum": ["product", "general", "group"]
          },
          "discountPercent": {
            "description": "Desconto geral do documento, em percentual com **4 casas decimais**, transportado como inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Zero quando o modo não é `general`.\n\nInteiro e não `number` **de propósito**: a fidelidade que o legado pede é de CASAS (`Ven_DescontoPorc` é `float`), não de ponto flutuante, e desconto multiplica dinheiro — `0.1 + 0.2` já não fecha, e a diferença aparece como centavo que não bate no total. As 4 casas são o dado; a escala é só como ele viaja.\n\nO **teto** por cliente (`Par_LimitDescCli`, 10% na operação de hoje) é regra de negócio do servidor, não deste schema: acima do teto é 400 com `fields[]` apontando o campo. Publicá-lo aqui como `maximum` trocaria esse erro nomeado por uma recusa genérica do validador, e congelaria no contrato um número que é parâmetro da empresa.",
            "type": "integer",
            "format": "int32"
          },
          "groupDiscounts": {
            "description": "Desconto por grupo de produto — uma linha por grupo, como `VendaDesconto` no legado. Lista **vazia** quando `discountMode` não é `group`, e nunca ausente por preguiça: ausente e vazia leem igual na tela e diferente na conta.\n\nFora de `required` de propósito: o servidor ainda não a emite, e campo obrigatório numa resposta que ninguém produz troca \"não implementado\" por \"contrato quebrado\" — o cliente gerado passaria a exigir o que o backend não manda. Entra em `required` no mesmo PR em que o backend passar a servi-la.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/QuoteGroupDiscountDto"
            }
          },
          "environments": {
            "description": "Ambientes do documento, na ordem de exibição.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/QuoteEnvironmentDto"
            }
          },
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/QuoteItemDto"
            }
          },
          "paymentTermId": {
            "type": ["null", "string"],
            "format": "uuid",
            "description": "A condição escolhida — `PaymentTermDto.id`, `Ven_formaPag` do legado. `null` enquanto o documento não tem plano de pagamento, que é o estado do orçamento recém-aberto."
          },
          "paymentTermName": {
            "type": ["null", "string"],
            "description": "Nome da condição na emissão, resolvido pelo servidor — mesmo par de `customerName`/`salespersonName`. Sem ele a grade do documento precisaria da listagem inteira de condições só para escrever uma linha."
          },
          "paymentInstallments": {
            "type": "array",
            "description": "O plano CARIMBADO na gravação: as parcelas da condição resolvidas contra `totalCents` e `issuedAt`.\n\n**Ecoadas, nunca escritas** — não existem em `QuoteWriteRequest`/`OrderWriteRequest`. Quem manda o `paymentTermId` recebe o plano de volta; deixar o cliente propor as parcelas abriria a porta para um documento cujo plano não soma o total dele.\n\n**Carimbado e não derivado na leitura:** alterar a condição depois não pode mudar o vencimento de parcela que o cliente já recebeu (ver `UpdatePaymentTerm`). É o que o legado faz ao copiar os parâmetros para a linha da `Venda`.\n\n**`[]` quando não há condição escolhida** — array vazio, nunca ausente: campo que às vezes não vem é o que faz a tela quebrar num `.map` só no documento sem plano.",
            "items": {
              "$ref": "#/components/schemas/DocumentInstallmentDto"
            }
          },
          "installmentPolicy": {
            "$ref": "#/components/schemas/InstallmentPolicyDto",
            "description": "Os três limites que valiam quando este documento foi gravado — `par_ParcelarVlAcima`, `Par_VlMinParcela` e `Par_QuantMaxParcela`, que no legado são COLUNAS da própria `Venda`.\n\n**Ausente (e não `null`) no documento anterior à publicação deste bloco.** Carimbo que nunca foi feito não se inventa com o vigente de hoje: seria reescrever a regra sob a qual o documento foi assinado, e o valor todo do carimbo é justamente não fazer isso."
          },
          "serviceItems": {
            "description": "As linhas da aba Serviços, na ordem de exibição. Coleção própria — ver `QuoteServiceItemDto`. Vem sempre, vazia quando o documento não tem serviço nenhum: ausência e lista vazia significariam a mesma coisa, e a de leitura opcional só criaria um ramo `?? []` em cada consumidor.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/QuoteServiceItemDto"
            }
          }
        }
      },
      "QuoteWriteRequest": {
        "description": "Proposto. Corpo de criação e de alteração. **`PUT` substitui o documento INTEIRO**, itens e ambientes junto: corpo parcial apaga o que não veio. Sem `number` (o servidor atribui), sem `status` (muda por `/cancel`), sem `totalCents` nem `customerName` (o servidor calcula e resolve).",
        "required": ["customerId", "discountMode", "discountPercent", "environments", "items"],
        "type": "object",
        "properties": {
          "series": {
            "description": "Série do documento.",
            "type": ["string", "null"]
          },
          "issuedAt": {
            "description": "Data de emissão.",
            "type": ["string", "null"],
            "format": "date"
          },
          "expiresAt": {
            "description": "Data de validade — curta, ~5 dias na operação atual.",
            "type": ["string", "null"],
            "format": "date"
          },
          "customerId": {
            "type": "string",
            "format": "uuid"
          },
          "projectName": {
            "description": "**Substituído por `workId`/`workName`.** Era a `Descrição da Obra` digitada NO documento — texto livre, sem elo com o cadastro de obra, que não existia quando o campo nasceu.\n\nContinua publicado, e não removido, porque a listagem ordena e filtra por ele e as telas o leem: tirar agora quebraria as duas pontas no mesmo dia. Enquanto convivem, os dois NÃO são o mesmo dado — `projectName` é o que foi digitado, `workName` é como a obra se chama hoje. Divergir é legítimo em documento antigo, e sobrescrever um com o outro apagaria o que o operador escreveu.",
            "type": ["string", "null"],
            "deprecated": true
          },
          "workId": {
            "description": "A OBRA do documento — `WorkDto.id`; `Venda.Obr_codigo` no legado. **É o elo que faltava.** A obra já é entidade própria (`/api/works`, `Obras` com 9.454 linhas) e o documento só a nomeava por TEXTO LIVRE em `projectName`: dois orçamentos da mesma obra não eram a mesma obra para ninguém — nem para a tela, nem para uma consulta, nem para o histórico do cliente.\n\n`null` porque o legado permite (`Venda.Obr_codigo` é nulável, e há venda de balcão sem obra). O que NÃO é opcional é a coerência: **obra que não é do cliente do documento é 400**, com `fields[]` apontando `workId`. A obra pertence ao cliente (`Obras.Cli_codigo`) e o documento não reescreve esse vínculo — aceitar calado deixaria o endereço de entrega de um cliente pendurado na venda de outro.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "folderNumber": {
            "description": "`Nº Pasta`.",
            "type": ["string", "null"]
          },
          "closedAt": {
            "description": "Data de fechamento.",
            "type": ["string", "null"],
            "format": "date"
          },
          "salespersonId": {
            "description": "Consultor(a) que atende — `EmployeeDto.id`.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "professionalId": {
            "description": "Profissional Externo (arquiteto/especificador) — é `PartnerDto.id` com papel `professional`, não cadastro à parte.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "discountMode": {
            "description": "Como o desconto é aplicado — `Ven_TipoDesc` do legado.\n\n- `product` — cada linha tem o seu (`discountPercent` do item); o do documento é zero.\n- `general` — um percentual só, no documento; o das linhas é zero.\n- `group` — **por GRUPO DE PRODUTO**, em `groupDiscounts`; documento e linhas ficam em zero.\n\n`group` é o que faltava, e não é caso de canto: no legado `VendaDesconto` tem **300.337 linhas** para **37.707** vendas, ~8 grupos por documento. Era o modo mais usado da operação e o único dos três que o contrato não publicava — quem descontasse por grupo tinha de escolher entre espalhar o percentual linha a linha (e perder de qual grupo veio) ou achatar tudo num desconto geral (e mudar o valor).",
            "type": "string",
            "enum": ["product", "general", "group"]
          },
          "discountPercent": {
            "description": "Desconto geral do documento, em percentual com **4 casas decimais**, transportado como inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Zero quando o modo não é `general`.\n\nInteiro e não `number` **de propósito**: a fidelidade que o legado pede é de CASAS (`Ven_DescontoPorc` é `float`), não de ponto flutuante, e desconto multiplica dinheiro — `0.1 + 0.2` já não fecha, e a diferença aparece como centavo que não bate no total. As 4 casas são o dado; a escala é só como ele viaja.\n\nO **teto** por cliente (`Par_LimitDescCli`, 10% na operação de hoje) é regra de negócio do servidor, não deste schema: acima do teto é 400 com `fields[]` apontando o campo. Publicá-lo aqui como `maximum` trocaria esse erro nomeado por uma recusa genérica do validador, e congelaria no contrato um número que é parâmetro da empresa.",
            "type": "integer",
            "format": "int32"
          },
          "groupDiscounts": {
            "description": "Desconto por grupo de produto — uma linha por grupo, como `VendaDesconto` no legado. Lista **vazia** quando `discountMode` não é `group`, e nunca ausente por preguiça: ausente e vazia leem igual na tela e diferente na conta.\n\nFora de `required` de propósito: o servidor ainda não a emite, e campo obrigatório numa resposta que ninguém produz troca \"não implementado\" por \"contrato quebrado\" — o cliente gerado passaria a exigir o que o backend não manda. Entra em `required` no mesmo PR em que o backend passar a servi-la.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/QuoteGroupDiscountWriteRequest"
            }
          },
          "environments": {
            "description": "Ambientes do documento, na ordem de exibição.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/QuoteEnvironmentDto"
            }
          },
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/QuoteItemWriteRequest"
            }
          },
          "paymentTermId": {
            "type": ["null", "string"],
            "format": "uuid",
            "description": "A condição de pagamento. O servidor resolve o resto do bloco — nome, parcelas e o carimbo da política — e o devolve no detalhe. 400 quando o id não é condição ATIVA da empresa ativa, e 400 quando o plano dela não cabe na política vigente, e aí o `type` diz QUAL das três: `urn:cabinet:erro:valor-nao-parcelavel` (total abaixo de `minTotalToInstallCents` com condição parcelada) · `urn:cabinet:erro:parcelas-acima-do-teto` (mais parcelas que `maxInstallments`) · `urn:cabinet:erro:parcela-abaixo-do-minimo` (alguma parcela abaixo de `minInstallmentCents`). São três correções DIFERENTES para quem está na tela, e conflatá-las em `campos-invalidos` obrigaria a ler a frase — que é o que este vocabulário existe para impedir."
          },
          "serviceItems": {
            "description": "As linhas da aba Serviços. **OPCIONAL, e ausente é VAZIO — nunca \"preserva o que estava\".** O `PUT` é integral, e essa semântica vale para os serviços exatamente como vale para `items` e `environments`: gravar sem o campo APAGA os serviços do documento.\n\nÉ opcional, e não obrigatório como `items`, por uma razão de transição e só: a seção nasce no contrato antes da tela, e um campo obrigatório faria toda escrita existente de documento virar 400 no dia do merge — inclusive a conversão do CRM. Vira obrigatório quando a aba existir dos dois lados.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/QuoteServiceItemWriteRequest"
            }
          }
        }
      },
      "PagedResultOfQuoteDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/QuoteDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "CrmPipelineDto": {
        "description": "Proposto. Funil de venda. Vários por empresa — ver `crm_pipelines` no schema mergeado (#66).",
        "required": ["id", "name", "sort", "isDefault", "active"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "name": {
            "description": "Nome do funil, como o operador o chama.",
            "type": "string"
          },
          "sort": {
            "description": "Ordem de exibição entre os funis.",
            "type": "integer",
            "format": "int32"
          },
          "isDefault": {
            "description": "Funil da oportunidade criada sem escolha explícita. Um só por empresa: marcar outro desmarca o anterior, e quem resolve isso é o servidor, na mesma transação.",
            "type": "boolean"
          },
          "active": {
            "description": "Desativação lógica (padrão 8 dos cadastros). Funil inativo some da escolha e não recebe oportunidade nova; as que já estão nele continuam abrindo — senão desativar viraria apagar, com outro nome.",
            "type": "boolean"
          }
        }
      },
      "CrmPipelineWriteRequest": {
        "description": "Proposto. Corpo de criação e de alteração do funil. `PUT` substitui o registro inteiro: omitir apaga.",
        "required": ["name", "sort", "isDefault", "active"],
        "type": "object",
        "properties": {
          "name": {
            "description": "Nome do funil, como o operador o chama.",
            "type": "string"
          },
          "sort": {
            "description": "Ordem de exibição entre os funis.",
            "type": "integer",
            "format": "int32"
          },
          "isDefault": {
            "description": "Funil da oportunidade criada sem escolha explícita. Um só por empresa: marcar outro desmarca o anterior, e quem resolve isso é o servidor, na mesma transação.",
            "type": "boolean"
          },
          "active": {
            "description": "Desativação lógica (padrão 8 dos cadastros). Funil inativo some da escolha e não recebe oportunidade nova; as que já estão nele continuam abrindo — senão desativar viraria apagar, com outro nome.",
            "type": "boolean"
          }
        }
      },
      "PagedResultOfCrmPipelineDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CrmPipelineDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "CrmStageDto": {
        "description": "Proposto. Estágio DE UM funil — a coluna do quadro. Não tem `active`: o schema mergeado (#66) não guarda desativação por estágio, e coluna que some com cartão dentro esconderia oportunidade sem apagá-la.",
        "required": ["id", "pipelineId", "name", "sort", "probability", "isWon", "isLost"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "pipelineId": {
            "description": "Funil a que o estágio pertence.",
            "type": "string",
            "format": "uuid"
          },
          "name": {
            "description": "Nome do estágio — o cabeçalho da coluna no quadro.",
            "type": "string"
          },
          "sort": {
            "description": "Ordem da coluna no quadro.",
            "type": "integer",
            "format": "int32"
          },
          "probability": {
            "description": "Probabilidade de fechamento. Int com 4 casas implícitas, a mesma convenção de `discountPercent`: `10000` = 1%, `1000000` = 100%. Só compara DENTRO do mesmo funil — dois funis com modelos de venda diferentes não têm escala comum.",
            "type": "integer",
            "format": "int32"
          },
          "isWon": {
            "description": "Estágio de GANHO. É propriedade do estágio e não do funil porque cada funil fecha no seu; e é dado, não rótulo, senão \"Fechado\" e \"Ganho\" viram a mesma coluna com dois nomes.",
            "type": "boolean"
          },
          "isLost": {
            "description": "Estágio de PERDA. Oportunidade que entra num estágio assim exige `lostReasonId` — sem motivo, a perda não vira análise nenhuma.",
            "type": "boolean"
          },
          "rotDays": {
            "description": "Apodrecimento: cartão parado mais dias que isto NO ESTÁGIO é sinalizado no quadro. `null` = o estágio não apodrece. Conta a partir de `stageChangedAt`, não da criação — apodrecer é \"parado desde\", não \"velho\".",
            "type": ["integer", "null"],
            "format": "int32"
          }
        }
      },
      "CrmStageWriteRequest": {
        "description": "Proposto. Corpo de criação e de alteração do estágio. Sem `pipelineId`: ele vem do caminho, e dois lugares para o mesmo fato divergem.",
        "required": ["name", "sort", "probability", "isWon", "isLost"],
        "type": "object",
        "properties": {
          "name": {
            "description": "Nome do estágio — o cabeçalho da coluna no quadro.",
            "type": "string"
          },
          "sort": {
            "description": "Ordem da coluna no quadro.",
            "type": "integer",
            "format": "int32"
          },
          "probability": {
            "description": "Probabilidade de fechamento. Int com 4 casas implícitas, a mesma convenção de `discountPercent`: `10000` = 1%, `1000000` = 100%. Só compara DENTRO do mesmo funil — dois funis com modelos de venda diferentes não têm escala comum.",
            "type": "integer",
            "format": "int32"
          },
          "isWon": {
            "description": "Estágio de GANHO. É propriedade do estágio e não do funil porque cada funil fecha no seu; e é dado, não rótulo, senão \"Fechado\" e \"Ganho\" viram a mesma coluna com dois nomes.",
            "type": "boolean"
          },
          "isLost": {
            "description": "Estágio de PERDA. Oportunidade que entra num estágio assim exige `lostReasonId` — sem motivo, a perda não vira análise nenhuma.",
            "type": "boolean"
          },
          "rotDays": {
            "description": "Apodrecimento: cartão parado mais dias que isto NO ESTÁGIO é sinalizado no quadro. `null` = o estágio não apodrece. Conta a partir de `stageChangedAt`, não da criação — apodrecer é \"parado desde\", não \"velho\".",
            "type": ["integer", "null"],
            "format": "int32"
          }
        }
      },
      "CrmOpportunityDto": {
        "description": "Proposto. Lead e negócio no MESMO registro — converter é mudar de estágio, não cadastrar de novo. Os campos `*Name` vêm resolvidos pelo servidor: o cartão do quadro mostra três palavras e não pode carregar lista de apoio para traduzir id.",
        "required": [
          "id",
          "name",
          "pipelineId",
          "pipelineName",
          "stageId",
          "stageName",
          "order",
          "stageChangedAt"
        ],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "name": {
            "description": "Título do negócio. Existe separado do parceiro porque o cartão precisa escrever algo quando `partnerId` é nulo — e porque a mesma empresa dá origem a vários negócios.",
            "type": "string"
          },
          "pipelineId": {
            "description": "Funil em que a oportunidade está.",
            "type": "string",
            "format": "uuid"
          },
          "pipelineName": {
            "description": "Nome do funil, resolvido pelo servidor.",
            "type": "string"
          },
          "stageId": {
            "description": "Estágio atual — sempre do MESMO funil (FK composta no banco).",
            "type": "string",
            "format": "uuid"
          },
          "stageName": {
            "description": "Nome do estágio, resolvido pelo servidor.",
            "type": "string"
          },
          "order": {
            "description": "Posição DENTRO do estágio, crescente. Quem atribui é o servidor — ver `PATCH /api/crm/opportunities/{id}/stage`. O cliente nunca calcula posição: índice é posição numa lista que pode estar filtrada.",
            "type": "integer",
            "format": "int32"
          },
          "partnerId": {
            "description": "Cliente/prospect já cadastrado (`PartnerDto.id`). `null` = lead ainda sem cadastro, e aí o contato vive nos três campos `contact*`.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "partnerName": {
            "description": "Nome do parceiro, resolvido pelo servidor.",
            "type": ["string", "null"]
          },
          "contactName": {
            "description": "Nome do contato do lead sem cadastro.",
            "type": ["string", "null"]
          },
          "contactEmail": {
            "description": "E-mail do contato do lead sem cadastro.",
            "type": ["string", "null"]
          },
          "contactPhone": {
            "description": "Telefone do contato do lead sem cadastro.",
            "type": ["string", "null"]
          },
          "ownerEmployeeId": {
            "description": "Responsável pela oportunidade (`EmployeeDto.id`). `null` = lead que entrou por formulário e ainda não tem dono.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "ownerName": {
            "description": "Nome do responsável, resolvido pelo servidor.",
            "type": ["string", "null"]
          },
          "expectedValueCents": {
            "description": "Valor previsto do negócio, em centavos. `null` = ainda não estimado, que é diferente de zero.",
            "type": ["integer", "null"],
            "format": "int64"
          },
          "expectedCloseDate": {
            "description": "Data prevista de fechamento.",
            "type": ["string", "null"],
            "format": "date"
          },
          "source": {
            "description": "Origem do lead (indicação, feira, site).",
            "type": ["string", "null"]
          },
          "stageChangedAt": {
            "description": "Instante da última mudança de estágio. É a base do `rotDays` do estágio: sem ele o apodrecimento não calcula nada.",
            "type": "string",
            "format": "date-time"
          },
          "lostReasonId": {
            "description": "Motivo da perda (`CrmLostReasonDto.id`). Só faz sentido em estágio `isLost`.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "lostReasonName": {
            "description": "Nome do motivo de perda, resolvido pelo servidor.",
            "type": ["string", "null"]
          },
          "quoteId": {
            "description": "Orçamento gerado a partir da oportunidade (`QuoteDto.id`). A oportunidade NÃO congela preço: quem congela é o orçamento.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "closedAt": {
            "description": "Instante do fechamento (ganho ou perda). Escrito pelo SERVIDOR quando a oportunidade entra num estágio `isWon`/`isLost`, e limpo quando ela volta a um estágio aberto.",
            "type": ["string", "null"],
            "format": "date-time"
          }
        }
      },
      "CrmOpportunityWriteRequest": {
        "description": "Proposto. Corpo de criação e de alteração. `PUT` substitui o registro INTEIRO: campo ausente é campo apagado. Sem os `*Name` (o servidor resolve), sem `order`, sem `stageChangedAt` e sem `closedAt` — os três são consequência do movimento, e deixar o cliente escrevê-los permitiria um cartão dizer que está parado há um mês desde ontem.",
        "required": ["name"],
        "type": "object",
        "properties": {
          "name": {
            "description": "Título do negócio. Existe separado do parceiro porque o cartão precisa escrever algo quando `partnerId` é nulo — e porque a mesma empresa dá origem a vários negócios.",
            "type": "string"
          },
          "pipelineId": {
            "description": "Funil. Ausente ou `null` na criação = o funil `isDefault` da empresa.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "stageId": {
            "description": "Estágio. Ausente ou `null` na criação = o primeiro estágio do funil. Trocar o estágio por aqui manda o cartão para o FIM da coluna nova e reinicia `stageChangedAt` — posicionar é o `PATCH .../stage`.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "partnerId": {
            "description": "Cliente/prospect já cadastrado (`PartnerDto.id`). `null` = lead ainda sem cadastro, e aí o contato vive nos três campos `contact*`.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "contactName": {
            "description": "Nome do contato do lead sem cadastro.",
            "type": ["string", "null"]
          },
          "contactEmail": {
            "description": "E-mail do contato do lead sem cadastro.",
            "type": ["string", "null"]
          },
          "contactPhone": {
            "description": "Telefone do contato do lead sem cadastro.",
            "type": ["string", "null"]
          },
          "ownerEmployeeId": {
            "description": "Responsável pela oportunidade (`EmployeeDto.id`). `null` = lead que entrou por formulário e ainda não tem dono.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "expectedValueCents": {
            "description": "Valor previsto do negócio, em centavos. `null` = ainda não estimado, que é diferente de zero.",
            "type": ["integer", "null"],
            "format": "int64"
          },
          "expectedCloseDate": {
            "description": "Data prevista de fechamento.",
            "type": ["string", "null"],
            "format": "date"
          },
          "source": {
            "description": "Origem do lead (indicação, feira, site).",
            "type": ["string", "null"]
          },
          "lostReasonId": {
            "description": "Motivo da perda (`CrmLostReasonDto.id`). Só faz sentido em estágio `isLost`.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "quoteId": {
            "description": "Orçamento gerado a partir da oportunidade (`QuoteDto.id`). A oportunidade NÃO congela preço: quem congela é o orçamento.",
            "type": ["string", "null"],
            "format": "uuid"
          }
        }
      },
      "CrmOpportunityStagePatchRequest": {
        "description": "Proposto. O destino de UM movimento do quadro. Só o que muda viaja — ausente NÃO é `null`: `precedeId` ausente é \"sem preferência de posição\" (fim da coluna), e é por isso que ele é nulável em vez de obrigatório.",
        "required": ["stageId"],
        "type": "object",
        "properties": {
          "stageId": {
            "description": "Estágio de destino. De outro funil é 400 — mover de FUNIL é `PUT`, e reseta a posição.",
            "type": "string",
            "format": "uuid"
          },
          "precedeId": {
            "description": "Id do cartão na frente do qual a oportunidade fica; `null` = fim da coluna. É referência a VIZINHO, não índice: índice é posição numa lista que pode estar filtrada, vizinho é um fato sobre dois registros que o servidor confere. Cartão que não está no estágio de destino é 400.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "lostReasonId": {
            "description": "Motivo da perda. **Obrigatório quando o estágio de destino tem `isLost`** — sem ele a resposta é 400, porque perda sem motivo não vira análise. Ignorado nos demais estágios.",
            "type": ["string", "null"],
            "format": "uuid"
          }
        }
      },
      "PagedResultOfCrmOpportunityDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CrmOpportunityDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "CrmLostReasonCountDto": {
        "description": "Proposto. Uma linha do relatório de perdas: o motivo e quantas vezes ele apareceu no período.",
        "required": ["lostReasonId", "lostReasonName", "count"],
        "type": "object",
        "properties": {
          "lostReasonId": {
            "description": "`null` só em registro MIGRADO: o `PATCH …/stage` exige o motivo, então perda sem motivo não nasce aqui. O legado tem, e omitir a linha faria a soma do relatório não bater com o total — erro que ninguém consegue explicar olhando a tela.",
            "type": "string",
            "format": "uuid",
            "nullable": true
          },
          "lostReasonName": {
            "description": "Nome resolvido pelo servidor. Na linha sem motivo, o texto que o servidor escolheu para ela.",
            "type": "string"
          },
          "count": {
            "description": "Oportunidades perdidas por este motivo no período.",
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "CrmLostReasonsReportDto": {
        "description": "Proposto. O relatório inteiro. `total` vem do servidor e NÃO é a soma das linhas feita pelo cliente: as duas têm de bater, e é justamente a divergência entre elas que denuncia perda sem motivo escapando do agrupamento.",
        "required": ["from", "to", "total", "rows"],
        "type": "object",
        "properties": {
          "from": {
            "description": "O período apurado, ecoado. O cliente pediu, o servidor confirma o que usou.",
            "type": "string",
            "format": "date"
          },
          "to": {
            "type": "string",
            "format": "date"
          },
          "total": {
            "description": "Oportunidades perdidas no período, com motivo ou sem.",
            "type": "integer",
            "format": "int32"
          },
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CrmLostReasonCountDto"
            }
          }
        }
      },
      "CrmLostReasonDto": {
        "description": "Proposto. Motivo de perda catalogado — vira análise, não texto livre.",
        "required": ["id", "name", "active"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "name": {
            "description": "Motivo da perda, como entra no relatório.",
            "type": "string"
          },
          "active": {
            "description": "Desativação lógica: motivo aposentado some da escolha, mas continua legível nas oportunidades antigas.",
            "type": "boolean"
          }
        }
      },
      "CrmLostReasonWriteRequest": {
        "description": "Proposto. Corpo de criação e de alteração do motivo de perda.",
        "required": ["name", "active"],
        "type": "object",
        "properties": {
          "name": {
            "description": "Motivo da perda, como entra no relatório.",
            "type": "string"
          },
          "active": {
            "description": "Desativação lógica: motivo aposentado some da escolha, mas continua legível nas oportunidades antigas.",
            "type": "boolean"
          }
        }
      },
      "PagedResultOfCrmLostReasonDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CrmLostReasonDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "ActivityDto": {
        "description": "Proposto. Uma atividade agendada sobre qualquer registro — a linha do painel Atividades.",
        "required": ["id", "entityType", "entityId", "kind", "title"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "entityType": {
            "description": "Tabela do registro dono. Conjunto fechado — é o CHECK de `entity_type` no banco, e a razão de a tabela não ter FK para o alvo.",
            "type": "string",
            "enum": ["opportunity", "partner", "quote", "purchaseOrder"]
          },
          "entityId": {
            "description": "Id do registro dono, dentro de `entityType`.",
            "type": "string",
            "format": "uuid"
          },
          "kind": {
            "description": "O que a atividade É — e o que dá o ícone e o rótulo no painel. Conjunto fechado. A lista é PROPOSTA do front: a transcrição do SoftLux não cobre atividade e o schema (#66) guarda `kind varchar` sem fixar valores — cresce por PR aqui, junto com o CHECK do banco.",
            "type": "string",
            "enum": ["call", "meeting", "email", "task"]
          },
          "title": {
            "description": "O que precisa ser feito, em uma linha — é o que a linha do painel mostra.",
            "type": "string"
          },
          "dueDate": {
            "description": "Prazo (data, sem hora). Nulo = sem prazo: a atividade existe e não cobra dia.",
            "type": ["string", "null"],
            "format": "date"
          },
          "assigneeEmployeeId": {
            "description": "Colaborador responsável (`GET /api/employees`). Nulo = ninguém assumiu ainda.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "assigneeName": {
            "description": "Nome do responsável, resolvido pelo servidor — o painel mostra gente, não uuid, e uma consulta a mais por linha para traduzir id seria a listagem inteira de colaboradores por painel aberto.",
            "type": ["string", "null"]
          },
          "doneAt": {
            "description": "Quando foi concluída; nulo = ainda pendente. Escrito pelo SERVIDOR em `POST /api/activities/{id}/done` — o relógio do cliente não carimba histórico.",
            "type": ["string", "null"],
            "format": "date-time"
          },
          "notes": {
            "description": "Observação livre. NÃO é histórico do registro — histórico é Auditoria e Notificações.",
            "type": ["string", "null"]
          }
        }
      },
      "ActivityWriteRequest": {
        "description": "Proposto. Corpo de criação e de alteração da atividade. `doneAt` não entra: quem conclui é `POST /api/activities/{id}/done`, e aceitar a data aqui deixaria o cliente carimbar histórico.",
        "required": ["entityType", "entityId", "kind", "title"],
        "type": "object",
        "properties": {
          "entityType": {
            "description": "Tabela do registro dono. Conjunto fechado — é o CHECK de `entity_type` no banco, e a razão de a tabela não ter FK para o alvo.",
            "type": "string",
            "enum": ["opportunity", "partner", "quote", "purchaseOrder"]
          },
          "entityId": {
            "description": "Id do registro dono. Na alteração precisa ser o MESMO do registro — mudar de alvo é 400.",
            "type": "string",
            "format": "uuid"
          },
          "kind": {
            "description": "O que a atividade É — e o que dá o ícone e o rótulo no painel. Conjunto fechado. A lista é PROPOSTA do front: a transcrição do SoftLux não cobre atividade e o schema (#66) guarda `kind varchar` sem fixar valores — cresce por PR aqui, junto com o CHECK do banco.",
            "type": "string",
            "enum": ["call", "meeting", "email", "task"]
          },
          "title": {
            "description": "O que precisa ser feito, em uma linha — é o que a linha do painel mostra.",
            "type": "string"
          },
          "dueDate": {
            "description": "Prazo (data, sem hora). Nulo = sem prazo: a atividade existe e não cobra dia.",
            "type": ["string", "null"],
            "format": "date"
          },
          "assigneeEmployeeId": {
            "description": "Colaborador responsável (`GET /api/employees`). Nulo = ninguém assumiu ainda.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "notes": {
            "description": "Observação livre. NÃO é histórico do registro — histórico é Auditoria e Notificações.",
            "type": ["string", "null"]
          }
        }
      },
      "PagedResultOfActivityDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ActivityDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "EmployeeDetailDto": {
        "description": "Proposto. A ficha do colaborador — o cadastro que o legado tem em `Funcionario` e que a listagem só arranhava. Os campos vêm de DOIS níveis e o corpo os junta de propósito: nome, documento e contato são da ORGANIZAÇÃO (a mesma pessoa nas duas empresas do grupo), enquanto papel, cargo, setor e as datas de vínculo são da EMPRESA ATIVA — o mesmo colaborador pode ser vendedor numa e gerente na outra (decisão do user, 2026-08-18). Colaborador sem vínculo na empresa ativa devolve os campos do vínculo em `null`, e não 404: ele existe, só não trabalha aqui.",
        "required": ["id", "name", "active"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "name": {
            "type": "string"
          },
          "document": {
            "description": "CPF. O legado identifica funcionário por ele, e é por isso que não existe código humano nesta ficha.",
            "type": ["string", "null"]
          },
          "email": {
            "description": "Também é a credencial de acesso: `employees` guarda o hash da senha e o e-mail é único, sem diferença de caixa. Trocar aqui troca o login.",
            "type": ["string", "null"]
          },
          "phone": {
            "type": ["string", "null"]
          },
          "photoUrl": {
            "type": ["string", "null"]
          },
          "active": {
            "description": "Ativo na ORGANIZAÇÃO. Desligar aqui tira a pessoa de todas as empresas do grupo.",
            "type": "boolean"
          },
          "roleId": {
            "description": "`RoleDto.id` do papel atribuído na empresa ativa — `null` SÓ quando não há vínculo. Era acompanhado de `role`, o identificador antigo da escala fechada, que saiu na fase 3 do api#84: a coluna `employee_company.role` não existe desde a migração `0054` do servidor, e todo vínculo aponta um papel de `GET /api/roles`.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "roleName": {
            "description": "Nome do papel, resolvido pelo servidor — o mesmo serviço que `sector` e `jobTitle` prestam. A tela imprime isto; sem ele imprimiria uuid ou faria uma busca só para achar o nome de uma linha.",
            "type": ["string", "null"]
          },
          "sectorId": {
            "description": "Setor na empresa ativa — `CatalogLookupDto.id`, kind `SETOR`.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "sector": {
            "description": "Nome do setor, resolvido pelo servidor.",
            "type": ["string", "null"]
          },
          "jobTitleId": {
            "description": "Cargo na empresa ativa — `CatalogLookupDto.id`, kind `CARGO`.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "jobTitle": {
            "description": "Nome do cargo, resolvido pelo servidor.",
            "type": ["string", "null"]
          },
          "hiredAt": {
            "description": "Admissão NESTA empresa.",
            "type": ["string", "null"],
            "format": "date"
          },
          "dismissedAt": {
            "description": "Demissão NESTA empresa. Preenchida = vínculo encerrado aqui, sem apagar a pessoa da organização.",
            "type": ["string", "null"],
            "format": "date"
          },
          "customerFacing": {
            "description": "`Atendimento ao cliente` do legado — é quem aparece no combo de Consultor(a) do orçamento. `null` quando não há vínculo.",
            "type": ["boolean", "null"]
          },
          "linkActive": {
            "description": "Vínculo ativo NESTA empresa. Separado de `active` porque as duas perguntas são diferentes: sair de uma empresa não é sair do grupo.",
            "type": ["boolean", "null"]
          }
        }
      },
      "EmployeeWriteRequest": {
        "description": "Proposto. Só o nível ORGANIZAÇÃO. Cargo, setor e papel não entram aqui — moram no vínculo e mudam por `/link`, senão gravar a ficha numa empresa reescreveria em silêncio o cargo que a pessoa tem na outra. **Salário e o resto do bloco de RH ficam fora deste corte**: a pergunta de LGPD sobre dado sensível de funcionário segue sem resposta, e campo sem regra de acesso é pior que campo ausente. `PUT` substitui o registro inteiro: omitir apaga.",
        "required": ["name", "document", "email", "phone", "active"],
        "type": "object",
        "properties": {
          "name": {
            "type": "string"
          },
          "document": {
            "type": ["string", "null"]
          },
          "email": {
            "description": "Único no produto inteiro, sem diferença de caixa — é a credencial. E-mail repetido é 409, não 400.",
            "type": ["string", "null"]
          },
          "phone": {
            "type": ["string", "null"]
          },
          "photoUrl": {
            "type": ["string", "null"]
          },
          "active": {
            "type": ["null", "boolean"]
          }
        }
      },
      "EmployeeLinkRequest": {
        "description": "Proposto. O vínculo com a EMPRESA ATIVA — o que é dela e de mais ninguém. `POST` cria o vínculo (repetir é 409), `PUT` substitui o que existe (sem vínculo é 404).\n\n**O papel entra por `roleId`, e só por ele.** Até a fase 3 do api#84 havia um segundo caminho — `role`, o identificador antigo da escala fechada (`owner`, `admin`, `operator-full`, `operator-sales`, `viewer`) —, e ele saiu junto com a escala: os cinco viraram linhas de `org_roles` como qualquer papel que o admin monta, e a coluna que guardava o texto não existe mais no servidor. **`roleId` e não o `slug` porque o nome do papel é editável pelo CRUD** — `PUT /api/roles/{id}` renomeia `Vendedor` para `Consultor` sem mover vínculo nenhum, e uma chave que a tela pode reescrever não serve de referência.",
        "required": ["active"],
        "type": "object",
        "properties": {
          "roleId": {
            "description": "`RoleDto.id` — papel desta organização, e o ÚNICO caminho de atribuição. **Obrigatório na prática:** omitir ou mandar `null` é 400 `urn:cabinet:erro:campos-invalidos` com `fields[].path` = `roleId`, porque o vínculo não existe sem papel. Papel inexistente, de outra organização ou inativo é 400 `urn:cabinet:erro:papel-invalido` — a distinção importa para a tela: o primeiro é campo em branco, o segundo é combo desatualizado.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "sectorId": {
            "description": "`CatalogLookupDto.id`, kind `SETOR`.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "jobTitleId": {
            "description": "`CatalogLookupDto.id`, kind `CARGO`.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "hiredAt": {
            "type": ["string", "null"],
            "format": "date"
          },
          "dismissedAt": {
            "type": ["string", "null"],
            "format": "date"
          },
          "customerFacing": {
            "type": ["null", "boolean"]
          },
          "active": {
            "type": ["null", "boolean"]
          }
        }
      },
      "OrderEnvironmentDto": {
        "description": "Proposto. Ambiente da obra dentro do PEDIDO. Mesma forma do ambiente do orçamento e schema próprio de propósito: os dois documentos são agregados distintos (decisão de 2026-08-10), e compartilhar o tipo faria uma mudança no orçamento chegar ao pedido sem ninguém pedir. O nome continua CONGELADO na emissão.",
        "required": ["code", "name", "order"],
        "type": "object",
        "properties": {
          "code": {
            "description": "Id do ambiente no catálogo (`CatalogLookupDto.id`, kind `AMBIENTE`).",
            "type": "string",
            "format": "uuid"
          },
          "name": {
            "description": "Nome congelado na emissão.",
            "type": "string"
          },
          "order": {
            "description": "Ordem de exibição. O PDF do orçamento agrupa por ambiente e vai para o cliente, então a ordem é dado, não apresentação. NÃO vem do legado — `VendaAmbiente` não tem coluna de ordem.",
            "type": "integer",
            "format": "int32"
          },
          "scheduledDeliveryAt": {
            "description": "Data prometida do AMBIENTE inteiro. É a data que os itens sem data própria herdam na leitura — a cozinha combina um dia, e não cada peça dela.",
            "type": ["string", "null"],
            "format": "date"
          }
        }
      },
      "OrderItemDto": {
        "description": "Proposto. Linha do pedido. Snapshot da CONVERSÃO, não do orçamento: o pedido nasce com cópia de descrição, acabamento, tamanho e preço, e repreçar o orçamento depois não move o pedido.",
        "required": [
          "lineNumber",
          "description",
          "quantity",
          "unitPriceCents",
          "discountPercent",
          "totalCents"
        ],
        "type": "object",
        "properties": {
          "lineNumber": {
            "description": "Coluna `Item`.",
            "type": "integer",
            "format": "int32"
          },
          "environmentCode": {
            "description": "Ambiente a que o item pertence. `null` = item fora de ambiente, que o legado permite (`VendaProduto.CodAmbiente` é nulável).",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "variantId": {
            "description": "Variante do catálogo (produto × acabamento × tamanho). `null` quando a linha não veio do catálogo.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "description": {
            "description": "Descrição congelada na emissão.",
            "type": "string"
          },
          "finish": {
            "description": "Acabamento congelado.",
            "type": ["string", "null"]
          },
          "size": {
            "description": "Tamanho congelado.",
            "type": ["string", "null"]
          },
          "quantity": {
            "description": "Até 3 casas.",
            "type": "number",
            "format": "double"
          },
          "unit": {
            "type": ["string", "null"]
          },
          "unitPriceCents": {
            "description": "Valor unitário congelado, em centavos.",
            "type": "integer",
            "format": "int64"
          },
          "discountPercent": {
            "description": "Desconto do item. Int com 4 casas implícitas: `10000` = 1%.",
            "type": "integer",
            "format": "int32"
          },
          "supplierId": {
            "description": "Fornecedor da linha (`PartnerDto.id`).",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "supplierName": {
            "type": ["string", "null"]
          },
          "supplierCode": {
            "description": "Código do produto NO fornecedor — a língua em que o item é pedido.",
            "type": ["string", "null"]
          },
          "supplierDescription": {
            "type": ["string", "null"]
          },
          "productGroup": {
            "description": "Nome do grupo, CONGELADO na emissão como o resto da linha. O id é `productGroupId` — este campo é o que se lê, não o que se casa.",
            "type": ["string", "null"]
          },
          "productGroupId": {
            "description": "Grupo de produto da linha — `CatalogLookupDto.id`, kind `GRUPO_PRODUTO`. É por ele que a linha entra numa das linhas de `groupDiscounts`. A atribuição não pode sair de `productGroup`, que é NOME congelado: nome nunca foi chave, e dois grupos homônimos (ou um renomeado depois da emissão) mandariam o desconto para o grupo errado sem ninguém acusar. `null` na linha sem grupo — que então não recebe desconto de grupo nenhum.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "pieceType": {
            "description": "Tipo da peça — kind `TIPO_PECA`.",
            "type": ["string", "null"]
          },
          "totalCents": {
            "description": "Valor do item. Calculado pelo servidor — a escrita não manda.",
            "type": "integer",
            "format": "int64"
          },
          "quantityReleased": {
            "description": "Quanto da linha já foi LIBERADO para separação. Sai de `order_fulfillments`, não de coluna que alguém digita. `0` na linha que ninguém liberou ainda.",
            "type": "number",
            "format": "double"
          },
          "quantityPicked": {
            "description": "Quanto já saiu da prateleira. É o ato que baixa estoque.",
            "type": "number",
            "format": "double"
          },
          "quantityDelivered": {
            "description": "Quanto já saiu com romaneio.",
            "type": "number",
            "format": "double"
          },
          "scheduledDeliveryAt": {
            "description": "Data prometida da linha, JÁ com a herança do ambiente aplicada. O detalhe completo — com `scheduledDateInherited` dizendo de onde ela veio — está em `GET /api/orders/{id}/fulfillment`.",
            "type": ["string", "null"],
            "format": "date"
          }
        }
      },
      "OrderItemWriteRequest": {
        "description": "Proposto. Linha do pedido na escrita. Sem `totalCents`: quem soma é o servidor.",
        "required": ["lineNumber", "description", "quantity", "unitPriceCents", "discountPercent"],
        "type": "object",
        "properties": {
          "lineNumber": {
            "description": "Coluna `Item`.",
            "type": "integer",
            "format": "int32"
          },
          "environmentCode": {
            "description": "Ambiente a que o item pertence. `null` = item fora de ambiente, que o legado permite (`VendaProduto.CodAmbiente` é nulável).",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "variantId": {
            "description": "Variante do catálogo (produto × acabamento × tamanho). `null` quando a linha não veio do catálogo.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "description": {
            "description": "Descrição congelada na emissão.",
            "type": "string"
          },
          "finish": {
            "description": "Acabamento congelado.",
            "type": ["string", "null"]
          },
          "size": {
            "description": "Tamanho congelado.",
            "type": ["string", "null"]
          },
          "quantity": {
            "description": "Até 3 casas.",
            "type": "number",
            "format": "double"
          },
          "unit": {
            "type": ["string", "null"]
          },
          "unitPriceCents": {
            "description": "Valor unitário congelado, em centavos.",
            "type": "integer",
            "format": "int64"
          },
          "discountPercent": {
            "description": "Desconto do item. Int com 4 casas implícitas: `10000` = 1%.",
            "type": "integer",
            "format": "int32"
          },
          "supplierId": {
            "description": "Fornecedor da linha (`PartnerDto.id`).",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "supplierName": {
            "type": ["string", "null"]
          },
          "supplierCode": {
            "description": "Código do produto NO fornecedor — a língua em que o item é pedido.",
            "type": ["string", "null"]
          },
          "supplierDescription": {
            "type": ["string", "null"]
          },
          "productGroup": {
            "description": "Nome do grupo, CONGELADO na emissão como o resto da linha. O id é `productGroupId` — este campo é o que se lê, não o que se casa.",
            "type": ["string", "null"]
          },
          "productGroupId": {
            "description": "Grupo de produto da linha — `CatalogLookupDto.id`, kind `GRUPO_PRODUTO`. É por ele que a linha entra numa das linhas de `groupDiscounts`. A atribuição não pode sair de `productGroup`, que é NOME congelado: nome nunca foi chave, e dois grupos homônimos (ou um renomeado depois da emissão) mandariam o desconto para o grupo errado sem ninguém acusar. `null` na linha sem grupo — que então não recebe desconto de grupo nenhum.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "pieceType": {
            "description": "Tipo da peça — kind `TIPO_PECA`.",
            "type": ["string", "null"]
          }
        }
      },
      "OrderServiceItemDto": {
        "description": "Proposto. Linha da ABA SERVIÇOS do documento — a seção que o comparativo (02-vendas) apontou como faltando, e que no legado é `VendaServico` (4.450 linhas; `Orcamento_servico_det` e `pedido_servico_det` existem no schema e estão ZERADAS, então quem descreve a operação real é `VendaServico`).\n\nColeção SEPARADA de `items`, e não um item com flag: as duas linhas não têm as mesmas colunas. Serviço não tem acabamento, tamanho, unidade, fornecedor nem código no fornecedor, e tem percentual de eletricista, que produto nenhum tem. Uma coleção só obrigaria metade dos campos a serem `null` conforme a outra metade — e o total do documento é a soma das DUAS, calculada pelo servidor.\n\nComo em `QuoteItemDto`, descrição e preço são SNAPSHOT da emissão: corrigir o cadastro não pode reescrever documento fechado.",
        "required": [
          "lineNumber",
          "description",
          "quantity",
          "unitPriceCents",
          "discountPercent",
          "electricianPercent",
          "electricianAmountCents",
          "totalCents"
        ],
        "type": "object",
        "properties": {
          "lineNumber": {
            "description": "Coluna `Item`.",
            "type": "integer",
            "format": "int32"
          },
          "environmentCode": {
            "description": "Ambiente a que o serviço pertence. `null` = fora de ambiente, que o legado permite (`VendaServico.CodAmbiente` é nulável, como o do produto).",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "serviceId": {
            "description": "Serviço do cadastro (`ServiceDto.id`). `null` quando a linha não veio do cadastro — o legado permite descrição avulsa (`Orcamento_servico_det.ose_descricao` existe ao lado do `Sev_cod`).",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "description": {
            "description": "Descrição congelada na emissão.",
            "type": "string"
          },
          "quantity": {
            "description": "Até 3 casas.",
            "type": "number",
            "format": "double"
          },
          "unitPriceCents": {
            "description": "Valor unitário congelado, em centavos.",
            "type": "integer",
            "format": "int64"
          },
          "discountPercent": {
            "description": "Desconto da linha. Int com 4 casas implícitas: `10000` = 1%.\n\n**O legado guarda DOIS descontos por linha de serviço** (`VenSer_DescVl` em valor e `VenSer_DescPorc` em percentual) e nada garante que concordem. Aqui só o percentual sobe, como já vale em `QuoteItemDto`: dois campos para uma decisão é a fonte dupla que faz o total do documento divergir do total das linhas.",
            "type": "integer",
            "format": "int32"
          },
          "electricianPercent": {
            "description": "Percentual do eletricista congelado na emissão — o do cadastro, ou o override que a escrita mandou. Nunca `null` na leitura: o servidor já resolveu a herança, e devolver `null` obrigaria quem lê a refazer a conta contra um cadastro que pode ter mudado desde então.",
            "type": "integer",
            "format": "int32"
          },
          "electricianAmountCents": {
            "description": "Quanto desta linha é do instalador, em centavos — `VenSer_VlEletricista` do legado. Calculado pelo servidor: a escrita não manda.\n\nEle é campo, e não conta que quem lê refaz, porque é o número que vira PAGAMENTO (`acerto_eletrecistas_servicos`). Recalcular no cliente daria um arredondamento por cliente sobre uma linha que alguém recebe.",
            "type": "integer",
            "format": "int64"
          },
          "totalCents": {
            "description": "Valor da linha. Calculado pelo servidor — a escrita não manda.",
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "OrderServiceItemWriteRequest": {
        "description": "Proposto. Linha da aba Serviços na escrita. Sem `totalCents` nem `electricianAmountCents`: quem calcula é o servidor.",
        "required": [
          "lineNumber",
          "description",
          "quantity",
          "unitPriceCents",
          "discountPercent",
          "electricianPercent"
        ],
        "type": "object",
        "properties": {
          "lineNumber": {
            "description": "Coluna `Item`.",
            "type": "integer",
            "format": "int32"
          },
          "environmentCode": {
            "description": "Ambiente a que o serviço pertence. `null` = fora de ambiente, que o legado permite (`VendaServico.CodAmbiente` é nulável, como o do produto).",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "serviceId": {
            "description": "Serviço do cadastro (`ServiceDto.id`). `null` quando a linha não veio do cadastro — o legado permite descrição avulsa (`Orcamento_servico_det.ose_descricao` existe ao lado do `Sev_cod`).",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "description": {
            "description": "Descrição congelada na emissão.",
            "type": "string"
          },
          "quantity": {
            "description": "Até 3 casas.",
            "type": "number",
            "format": "double"
          },
          "unitPriceCents": {
            "description": "Valor unitário congelado, em centavos.",
            "type": "integer",
            "format": "int64"
          },
          "discountPercent": {
            "description": "Desconto da linha. Int com 4 casas implícitas: `10000` = 1%.\n\n**O legado guarda DOIS descontos por linha de serviço** (`VenSer_DescVl` em valor e `VenSer_DescPorc` em percentual) e nada garante que concordem. Aqui só o percentual sobe, como já vale em `QuoteItemDto`: dois campos para uma decisão é a fonte dupla que faz o total do documento divergir do total das linhas.",
            "type": "integer",
            "format": "int32"
          },
          "electricianPercent": {
            "description": "Percentual do eletricista NESTA linha. **`null` = herda do cadastro** (`ServiceDto.electricianPercent`) no momento da gravação; qualquer valor é OVERRIDE explícito, inclusive `0`.\n\nA distinção entre `null` e `0` é a que o campo existe para carregar: zero é \"esta linha não paga instalador\", e omitir é \"use o que o cadastro diz\". Um campo não-nulável forçaria o cliente a copiar o percentual do cadastro para dentro do corpo — e aí quem gravasse com o cadastro desatualizado na tela congelaria o número velho sem ninguém pedir.",
            "type": ["integer", "null"],
            "format": "int32"
          }
        }
      },
      "OrderDto": {
        "description": "Proposto. Pedido de venda na listagem. É a metade que faltava da cadeia do legado (Pedido de Venda → Pedido de Compra → Ordem de Compra): sem pedido, Compras não tem de onde puxar.",
        "required": ["id", "number", "customerId", "customerName", "status", "totalCents"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "number": {
            "description": "Número do documento. Sequência GLOBAL do grupo, atribuída pelo servidor — por isso não existe na escrita. No legado `Ven_CodigoPre` já é única entre as empresas, e cliente que escolhe número colide entre elas.",
            "type": "string"
          },
          "series": {
            "description": "Série do documento.",
            "type": ["string", "null"]
          },
          "issuedAt": {
            "description": "Data de emissão.",
            "type": ["string", "null"],
            "format": "date"
          },
          "customerId": {
            "type": "string",
            "format": "uuid"
          },
          "customerName": {
            "description": "Nome do cliente na emissão.",
            "type": "string"
          },
          "projectName": {
            "description": "**Substituído por `workId`/`workName`.** Era a `Descrição da Obra` digitada NO documento — texto livre, sem elo com o cadastro de obra, que não existia quando o campo nasceu.\n\nContinua publicado, e não removido, porque a listagem ordena e filtra por ele e as telas o leem: tirar agora quebraria as duas pontas no mesmo dia. Enquanto convivem, os dois NÃO são o mesmo dado — `projectName` é o que foi digitado, `workName` é como a obra se chama hoje. Divergir é legítimo em documento antigo, e sobrescrever um com o outro apagaria o que o operador escreveu.",
            "type": ["string", "null"],
            "deprecated": true
          },
          "workId": {
            "description": "A OBRA do documento — `WorkDto.id`; `Venda.Obr_codigo` no legado. **É o elo que faltava.** A obra já é entidade própria (`/api/works`, `Obras` com 9.454 linhas) e o documento só a nomeava por TEXTO LIVRE em `projectName`: dois orçamentos da mesma obra não eram a mesma obra para ninguém — nem para a tela, nem para uma consulta, nem para o histórico do cliente.\n\n`null` porque o legado permite (`Venda.Obr_codigo` é nulável, e há venda de balcão sem obra). O que NÃO é opcional é a coerência: **obra que não é do cliente do documento é 400**, com `fields[]` apontando `workId`. A obra pertence ao cliente (`Obras.Cli_codigo`) e o documento não reescreve esse vínculo — aceitar calado deixaria o endereço de entrega de um cliente pendurado na venda de outro.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "workName": {
            "description": "Descrição da obra (`Obras.Obr_Descricao`), resolvida pelo servidor — o par `id`+`name` de sempre: o id é para escrever, o nome é o que a tela lê, sem uma segunda consulta. `null` quando `workId` é `null`.",
            "type": ["string", "null"]
          },
          "status": {
            "description": "Documento CANCELA, não desativa: `active` de cadastro não serve aqui. **Três estados, e o do meio é NOVO em relação ao legado.** Lá `Ven_Situacao` só tem A/C e o fechamento é DATA (`Ven_DataFechaVenda`, gravada pelo `FrmFecha_projeto`) — quem lê o registro não distingue \"pedido em andamento\" de \"pedido encerrado\" sem interpretar uma data. Aqui `concluded` é ESTADO porque é ele que fecha o documento para escrita: `PUT` em pedido concluído é 409, como já é em cancelado. `closedAt` continua sendo a data e não muda de papel. Transições: `active → concluded` (`POST .../conclude`) · `active → cancelled` (`POST .../cancel`). **`concluded` e `cancelled` são terminais** — concluir cancelado, cancelar concluído e repetir qualquer uma das duas é 409.",
            "type": "string",
            "enum": ["active", "concluded", "cancelled"]
          },
          "type": {
            "description": "O QUE o documento é. Espelha `Ven_Tipo` do legado, onde orçamento, pedido, pré-venda e demonstração são o MESMO registro físico discriminado por uma letra — aqui orçamento e pedido já são agregados separados, e o que sobra de `Ven_Tipo` é a distinção que muda o COMPORTAMENTO do pedido: demonstração sai do estoque como empréstimo e tem de voltar. `sale` é o padrão e o que todo pedido de hoje é.\n\n**`presale` NÃO existe aqui, e é decisão medida, não esquecimento.** O menu do legado tem \"Pré-venda\", mas o domínio de `Ven_Tipo` no dado real é `O`=23.033 e `P`=11.103 — soma 34.136, o total de `Ven_Situacao`. Zero pré-vendas em 34 mil documentos: a tela existia, o uso não. Publicar o tipo obrigaria os dois lados a carregar um ramo que nenhum operador percorreu.",
            "type": "string",
            "enum": ["sale", "demo"]
          },
          "demoDueDate": {
            "type": ["null", "string"],
            "description": "Prazo de retorno da demonstração — a data até quando a peça pode ficar fora. Só faz sentido com `type: demo`; em `sale` é `null` e a escrita que mandar valor é 400. É o que torna a demonstração cobrável: sem prazo, \"emprestado\" e \"perdido\" são o mesmo registro.",
            "format": "date"
          },
          "demoReturnedAt": {
            "type": ["null", "string"],
            "description": "Quando a peça voltou (`POST .../demo-return`). `null` = ainda fora. Demonstração com `demoDueDate` vencida e `demoReturnedAt` nulo é o que a consulta de pendências procura.",
            "format": "date-time"
          },
          "totalCents": {
            "description": "Total do orçamento, em centavos. Calculado pelo servidor.",
            "type": "integer",
            "format": "int64"
          },
          "quoteId": {
            "description": "Orçamento de origem. `null` quando o pedido foi lançado direto, que o legado permite.",
            "type": ["string", "null"],
            "format": "uuid"
          }
        }
      },
      "OrderDetailDto": {
        "description": "Proposto. O pedido inteiro: cabeçalho, ambientes e itens numa resposta só, pela mesma razão do orçamento — item não tem identidade fora do documento e gravar em N requisições deixaria metade da grade salva.",
        "required": [
          "id",
          "number",
          "customerId",
          "customerName",
          "status",
          "totalCents",
          "discountMode",
          "discountPercent",
          "environments",
          "items",
          "paymentInstallments",
          "serviceItems"
        ],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "number": {
            "description": "Número do documento. Sequência GLOBAL do grupo, atribuída pelo servidor — por isso não existe na escrita. No legado `Ven_CodigoPre` já é única entre as empresas, e cliente que escolhe número colide entre elas.",
            "type": "string"
          },
          "series": {
            "description": "Série do documento.",
            "type": ["string", "null"]
          },
          "issuedAt": {
            "description": "Data de emissão.",
            "type": ["string", "null"],
            "format": "date"
          },
          "customerId": {
            "type": "string",
            "format": "uuid"
          },
          "customerName": {
            "description": "Nome do cliente na emissão.",
            "type": "string"
          },
          "projectName": {
            "description": "**Substituído por `workId`/`workName`.** Era a `Descrição da Obra` digitada NO documento — texto livre, sem elo com o cadastro de obra, que não existia quando o campo nasceu.\n\nContinua publicado, e não removido, porque a listagem ordena e filtra por ele e as telas o leem: tirar agora quebraria as duas pontas no mesmo dia. Enquanto convivem, os dois NÃO são o mesmo dado — `projectName` é o que foi digitado, `workName` é como a obra se chama hoje. Divergir é legítimo em documento antigo, e sobrescrever um com o outro apagaria o que o operador escreveu.",
            "type": ["string", "null"],
            "deprecated": true
          },
          "workId": {
            "description": "A OBRA do documento — `WorkDto.id`; `Venda.Obr_codigo` no legado. **É o elo que faltava.** A obra já é entidade própria (`/api/works`, `Obras` com 9.454 linhas) e o documento só a nomeava por TEXTO LIVRE em `projectName`: dois orçamentos da mesma obra não eram a mesma obra para ninguém — nem para a tela, nem para uma consulta, nem para o histórico do cliente.\n\n`null` porque o legado permite (`Venda.Obr_codigo` é nulável, e há venda de balcão sem obra). O que NÃO é opcional é a coerência: **obra que não é do cliente do documento é 400**, com `fields[]` apontando `workId`. A obra pertence ao cliente (`Obras.Cli_codigo`) e o documento não reescreve esse vínculo — aceitar calado deixaria o endereço de entrega de um cliente pendurado na venda de outro.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "workName": {
            "description": "Descrição da obra (`Obras.Obr_Descricao`), resolvida pelo servidor — o par `id`+`name` de sempre: o id é para escrever, o nome é o que a tela lê, sem uma segunda consulta. `null` quando `workId` é `null`.",
            "type": ["string", "null"]
          },
          "status": {
            "description": "Documento CANCELA, não desativa: `active` de cadastro não serve aqui. **Três estados, e o do meio é NOVO em relação ao legado.** Lá `Ven_Situacao` só tem A/C e o fechamento é DATA (`Ven_DataFechaVenda`, gravada pelo `FrmFecha_projeto`) — quem lê o registro não distingue \"pedido em andamento\" de \"pedido encerrado\" sem interpretar uma data. Aqui `concluded` é ESTADO porque é ele que fecha o documento para escrita: `PUT` em pedido concluído é 409, como já é em cancelado. `closedAt` continua sendo a data e não muda de papel. Transições: `active → concluded` (`POST .../conclude`) · `active → cancelled` (`POST .../cancel`). **`concluded` e `cancelled` são terminais** — concluir cancelado, cancelar concluído e repetir qualquer uma das duas é 409.",
            "type": "string",
            "enum": ["active", "concluded", "cancelled"]
          },
          "type": {
            "description": "O QUE o documento é. Espelha `Ven_Tipo` do legado, onde orçamento, pedido, pré-venda e demonstração são o MESMO registro físico discriminado por uma letra — aqui orçamento e pedido já são agregados separados, e o que sobra de `Ven_Tipo` é a distinção que muda o COMPORTAMENTO do pedido: demonstração sai do estoque como empréstimo e tem de voltar. `sale` é o padrão e o que todo pedido de hoje é.\n\n**`presale` NÃO existe aqui, e é decisão medida, não esquecimento.** O menu do legado tem \"Pré-venda\", mas o domínio de `Ven_Tipo` no dado real é `O`=23.033 e `P`=11.103 — soma 34.136, o total de `Ven_Situacao`. Zero pré-vendas em 34 mil documentos: a tela existia, o uso não. Publicar o tipo obrigaria os dois lados a carregar um ramo que nenhum operador percorreu.",
            "type": "string",
            "enum": ["sale", "demo"]
          },
          "demoDueDate": {
            "type": ["null", "string"],
            "description": "Prazo de retorno da demonstração — a data até quando a peça pode ficar fora. Só faz sentido com `type: demo`; em `sale` é `null` e a escrita que mandar valor é 400. É o que torna a demonstração cobrável: sem prazo, \"emprestado\" e \"perdido\" são o mesmo registro.",
            "format": "date"
          },
          "demoReturnedAt": {
            "type": ["null", "string"],
            "description": "Quando a peça voltou (`POST .../demo-return`). `null` = ainda fora. Demonstração com `demoDueDate` vencida e `demoReturnedAt` nulo é o que a consulta de pendências procura.",
            "format": "date-time"
          },
          "totalCents": {
            "description": "Total do orçamento, em centavos. Calculado pelo servidor.",
            "type": "integer",
            "format": "int64"
          },
          "folderNumber": {
            "description": "`Nº Pasta`.",
            "type": ["string", "null"]
          },
          "closedAt": {
            "description": "Data de fechamento.",
            "type": ["string", "null"],
            "format": "date"
          },
          "cancelledAt": {
            "type": ["null", "string"],
            "description": "Quando o documento foi cancelado. `null` enquanto não for.",
            "format": "date-time"
          },
          "cancelReasonId": {
            "description": "Motivo do cancelamento — id da lista de apoio. É o `Mod_codigo` que o legado gravava junto de `ven_situacao='C'`. O `kind` da lista NÃO passa pelo contrato (ADR-011): entra por linha no catálogo do servidor.",
            "type": ["null", "string"],
            "format": "uuid"
          },
          "cancelReasonName": {
            "type": ["null", "string"],
            "description": "Nome do motivo, resolvido na leitura. `null` quando cancelaram sem motivo."
          },
          "cancelNote": {
            "type": ["null", "string"],
            "description": "Observação livre de quem cancelou. O motivo diz a CLASSE; a nota diz o caso.",
            "maxLength": 200
          },
          "salespersonId": {
            "description": "Consultor(a) que atende — `EmployeeDto.id`.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "salespersonName": {
            "type": ["string", "null"]
          },
          "professionalId": {
            "description": "Profissional Externo (arquiteto/especificador) — é `PartnerDto.id` com papel `professional`, não cadastro à parte.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "professionalName": {
            "type": ["string", "null"]
          },
          "discountMode": {
            "description": "Como o desconto é aplicado — `Ven_TipoDesc` do legado.\n\n- `product` — cada linha tem o seu (`discountPercent` do item); o do documento é zero.\n- `general` — um percentual só, no documento; o das linhas é zero.\n- `group` — **por GRUPO DE PRODUTO**, em `groupDiscounts`; documento e linhas ficam em zero.\n\n`group` é o que faltava, e não é caso de canto: no legado `VendaDesconto` tem **300.337 linhas** para **37.707** vendas, ~8 grupos por documento. Era o modo mais usado da operação e o único dos três que o contrato não publicava — quem descontasse por grupo tinha de escolher entre espalhar o percentual linha a linha (e perder de qual grupo veio) ou achatar tudo num desconto geral (e mudar o valor).",
            "type": "string",
            "enum": ["product", "general", "group"]
          },
          "discountPercent": {
            "description": "Desconto geral do documento, em percentual com **4 casas decimais**, transportado como inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Zero quando o modo não é `general`.\n\nInteiro e não `number` **de propósito**: a fidelidade que o legado pede é de CASAS (`Ven_DescontoPorc` é `float`), não de ponto flutuante, e desconto multiplica dinheiro — `0.1 + 0.2` já não fecha, e a diferença aparece como centavo que não bate no total. As 4 casas são o dado; a escala é só como ele viaja.\n\nO **teto** por cliente (`Par_LimitDescCli`, 10% na operação de hoje) é regra de negócio do servidor, não deste schema: acima do teto é 400 com `fields[]` apontando o campo. Publicá-lo aqui como `maximum` trocaria esse erro nomeado por uma recusa genérica do validador, e congelaria no contrato um número que é parâmetro da empresa.",
            "type": "integer",
            "format": "int32"
          },
          "groupDiscounts": {
            "description": "Desconto por grupo de produto — uma linha por grupo, como `VendaDesconto` no legado. Lista **vazia** quando `discountMode` não é `group`, e nunca ausente por preguiça: ausente e vazia leem igual na tela e diferente na conta.\n\nFora de `required` de propósito: o servidor ainda não a emite, e campo obrigatório numa resposta que ninguém produz troca \"não implementado\" por \"contrato quebrado\" — o cliente gerado passaria a exigir o que o backend não manda. Entra em `required` no mesmo PR em que o backend passar a servi-la.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/OrderGroupDiscountDto"
            }
          },
          "environments": {
            "description": "Ambientes do documento, na ordem de exibição.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/OrderEnvironmentDto"
            }
          },
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/OrderItemDto"
            }
          },
          "quoteId": {
            "description": "Orçamento de origem, quando houve conversão.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "quoteNumber": {
            "description": "Número do orçamento de origem, resolvido pelo servidor — a tela mostra 'veio do orçamento 1234' sem uma segunda consulta.",
            "type": ["string", "null"]
          },
          "paymentTermId": {
            "type": ["null", "string"],
            "format": "uuid",
            "description": "A condição escolhida — `PaymentTermDto.id`, `Ven_formaPag` do legado. `null` enquanto o documento não tem plano de pagamento, que é o estado do orçamento recém-aberto."
          },
          "paymentTermName": {
            "type": ["null", "string"],
            "description": "Nome da condição na emissão, resolvido pelo servidor — mesmo par de `customerName`/`salespersonName`. Sem ele a grade do documento precisaria da listagem inteira de condições só para escrever uma linha."
          },
          "paymentInstallments": {
            "type": "array",
            "description": "O plano CARIMBADO na gravação: as parcelas da condição resolvidas contra `totalCents` e `issuedAt`.\n\n**Ecoadas, nunca escritas** — não existem em `QuoteWriteRequest`/`OrderWriteRequest`. Quem manda o `paymentTermId` recebe o plano de volta; deixar o cliente propor as parcelas abriria a porta para um documento cujo plano não soma o total dele.\n\n**Carimbado e não derivado na leitura:** alterar a condição depois não pode mudar o vencimento de parcela que o cliente já recebeu (ver `UpdatePaymentTerm`). É o que o legado faz ao copiar os parâmetros para a linha da `Venda`.\n\n**`[]` quando não há condição escolhida** — array vazio, nunca ausente: campo que às vezes não vem é o que faz a tela quebrar num `.map` só no documento sem plano.",
            "items": {
              "$ref": "#/components/schemas/DocumentInstallmentDto"
            }
          },
          "installmentPolicy": {
            "$ref": "#/components/schemas/InstallmentPolicyDto",
            "description": "Os três limites que valiam quando este documento foi gravado — `par_ParcelarVlAcima`, `Par_VlMinParcela` e `Par_QuantMaxParcela`, que no legado são COLUNAS da própria `Venda`.\n\n**Ausente (e não `null`) no documento anterior à publicação deste bloco.** Carimbo que nunca foi feito não se inventa com o vigente de hoje: seria reescrever a regra sob a qual o documento foi assinado, e o valor todo do carimbo é justamente não fazer isso."
          },
          "serviceItems": {
            "description": "As linhas da aba Serviços, na ordem de exibição. Coleção própria — ver `OrderServiceItemDto`. Vem sempre, vazia quando o documento não tem serviço nenhum: ausência e lista vazia significariam a mesma coisa, e a de leitura opcional só criaria um ramo `?? []` em cada consumidor.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/OrderServiceItemDto"
            }
          }
        }
      },
      "OrderWriteRequest": {
        "description": "Proposto. Corpo de criação e de alteração do pedido. `PUT` substitui o documento INTEIRO, itens e ambientes junto. Sem `number` (o servidor atribui), sem `status` (muda por `/cancel`) e sem `quoteId`: a origem se estabelece na conversão e não se reescreve, senão um `PUT` mudaria de qual orçamento o pedido nasceu.",
        "required": ["customerId", "discountMode", "discountPercent", "environments", "items"],
        "type": "object",
        "properties": {
          "series": {
            "description": "Série do documento.",
            "type": ["string", "null"]
          },
          "issuedAt": {
            "description": "Data de emissão.",
            "type": ["string", "null"],
            "format": "date"
          },
          "customerId": {
            "type": "string",
            "format": "uuid"
          },
          "projectName": {
            "description": "**Substituído por `workId`/`workName`.** Era a `Descrição da Obra` digitada NO documento — texto livre, sem elo com o cadastro de obra, que não existia quando o campo nasceu.\n\nContinua publicado, e não removido, porque a listagem ordena e filtra por ele e as telas o leem: tirar agora quebraria as duas pontas no mesmo dia. Enquanto convivem, os dois NÃO são o mesmo dado — `projectName` é o que foi digitado, `workName` é como a obra se chama hoje. Divergir é legítimo em documento antigo, e sobrescrever um com o outro apagaria o que o operador escreveu.",
            "type": ["string", "null"],
            "deprecated": true
          },
          "workId": {
            "description": "A OBRA do documento — `WorkDto.id`; `Venda.Obr_codigo` no legado. **É o elo que faltava.** A obra já é entidade própria (`/api/works`, `Obras` com 9.454 linhas) e o documento só a nomeava por TEXTO LIVRE em `projectName`: dois orçamentos da mesma obra não eram a mesma obra para ninguém — nem para a tela, nem para uma consulta, nem para o histórico do cliente.\n\n`null` porque o legado permite (`Venda.Obr_codigo` é nulável, e há venda de balcão sem obra). O que NÃO é opcional é a coerência: **obra que não é do cliente do documento é 400**, com `fields[]` apontando `workId`. A obra pertence ao cliente (`Obras.Cli_codigo`) e o documento não reescreve esse vínculo — aceitar calado deixaria o endereço de entrega de um cliente pendurado na venda de outro.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "folderNumber": {
            "description": "`Nº Pasta`.",
            "type": ["string", "null"]
          },
          "closedAt": {
            "description": "Data de fechamento digitada pelo operador, como no `FrmFecha_projeto`. Continua escrevível enquanto o pedido está `active`; `POST .../conclude` carimba a data de hoje quando ela está nula. O ESTADO não vem por aqui — `status` só se move pelas operações de ciclo de vida.",
            "type": ["string", "null"],
            "format": "date"
          },
          "type": {
            "description": "Só na CRIAÇÃO. `PUT` que troca o tipo é 409: demonstração e venda movimentam estoque de formas diferentes, e trocar a letra depois do movimento deixaria saldo sem documento que o explique. Ausente = `sale`.",
            "type": ["null", "string"],
            "enum": ["sale", "demo", null]
          },
          "demoDueDate": {
            "type": ["null", "string"],
            "description": "Prazo de retorno, obrigatório quando `type: demo` e proibido quando `sale` — as duas recusas são 400 apontando o campo.",
            "format": "date"
          },
          "salespersonId": {
            "description": "Consultor(a) que atende — `EmployeeDto.id`.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "professionalId": {
            "description": "Profissional Externo (arquiteto/especificador) — é `PartnerDto.id` com papel `professional`, não cadastro à parte.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "discountMode": {
            "description": "Como o desconto é aplicado — `Ven_TipoDesc` do legado.\n\n- `product` — cada linha tem o seu (`discountPercent` do item); o do documento é zero.\n- `general` — um percentual só, no documento; o das linhas é zero.\n- `group` — **por GRUPO DE PRODUTO**, em `groupDiscounts`; documento e linhas ficam em zero.\n\n`group` é o que faltava, e não é caso de canto: no legado `VendaDesconto` tem **300.337 linhas** para **37.707** vendas, ~8 grupos por documento. Era o modo mais usado da operação e o único dos três que o contrato não publicava — quem descontasse por grupo tinha de escolher entre espalhar o percentual linha a linha (e perder de qual grupo veio) ou achatar tudo num desconto geral (e mudar o valor).",
            "type": "string",
            "enum": ["product", "general", "group"]
          },
          "discountPercent": {
            "description": "Desconto geral do documento, em percentual com **4 casas decimais**, transportado como inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Zero quando o modo não é `general`.\n\nInteiro e não `number` **de propósito**: a fidelidade que o legado pede é de CASAS (`Ven_DescontoPorc` é `float`), não de ponto flutuante, e desconto multiplica dinheiro — `0.1 + 0.2` já não fecha, e a diferença aparece como centavo que não bate no total. As 4 casas são o dado; a escala é só como ele viaja.\n\nO **teto** por cliente (`Par_LimitDescCli`, 10% na operação de hoje) é regra de negócio do servidor, não deste schema: acima do teto é 400 com `fields[]` apontando o campo. Publicá-lo aqui como `maximum` trocaria esse erro nomeado por uma recusa genérica do validador, e congelaria no contrato um número que é parâmetro da empresa.",
            "type": "integer",
            "format": "int32"
          },
          "groupDiscounts": {
            "description": "Desconto por grupo de produto — uma linha por grupo, como `VendaDesconto` no legado. Lista **vazia** quando `discountMode` não é `group`, e nunca ausente por preguiça: ausente e vazia leem igual na tela e diferente na conta.\n\nFora de `required` de propósito: o servidor ainda não a emite, e campo obrigatório numa resposta que ninguém produz troca \"não implementado\" por \"contrato quebrado\" — o cliente gerado passaria a exigir o que o backend não manda. Entra em `required` no mesmo PR em que o backend passar a servi-la.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/OrderGroupDiscountWriteRequest"
            }
          },
          "environments": {
            "description": "Ambientes do documento, na ordem de exibição.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/OrderEnvironmentDto"
            }
          },
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/OrderItemWriteRequest"
            }
          },
          "paymentTermId": {
            "type": ["null", "string"],
            "format": "uuid",
            "description": "A condição de pagamento. O servidor resolve o resto do bloco — nome, parcelas e o carimbo da política — e o devolve no detalhe. 400 quando o id não é condição ATIVA da empresa ativa, e 400 quando o plano dela não cabe na política vigente, e aí o `type` diz QUAL das três: `urn:cabinet:erro:valor-nao-parcelavel` (total abaixo de `minTotalToInstallCents` com condição parcelada) · `urn:cabinet:erro:parcelas-acima-do-teto` (mais parcelas que `maxInstallments`) · `urn:cabinet:erro:parcela-abaixo-do-minimo` (alguma parcela abaixo de `minInstallmentCents`). São três correções DIFERENTES para quem está na tela, e conflatá-las em `campos-invalidos` obrigaria a ler a frase — que é o que este vocabulário existe para impedir."
          },
          "serviceItems": {
            "description": "As linhas da aba Serviços. **OPCIONAL, e ausente é VAZIO — nunca \"preserva o que estava\".** O `PUT` é integral, e essa semântica vale para os serviços exatamente como vale para `items` e `environments`: gravar sem o campo APAGA os serviços do documento.\n\nÉ opcional, e não obrigatório como `items`, por uma razão de transição e só: a seção nasce no contrato antes da tela, e um campo obrigatório faria toda escrita existente de documento virar 400 no dia do merge — inclusive a conversão do CRM. Vira obrigatório quando a aba existir dos dois lados.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/OrderServiceItemWriteRequest"
            }
          }
        }
      },
      "PagedResultOfOrderDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/OrderDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "CancelDocumentRequest": {
        "description": "Motivo do cancelamento. O corpo é OPCIONAL — cancelar sem dizer por quê continua valendo, e é o que os 3.354 cancelamentos do legado são: `Mod_codigo` existe lá, e a variante que o grava é uma de duas. Exigi-lo agora reprovaria o gesto que o operador já faz, para ganhar um campo que ele preencheria com o primeiro item da lista.",
        "type": "object",
        "properties": {
          "reasonId": {
            "description": "Motivo, da lista de apoio. O `kind` NÃO passa pelo contrato (ADR-011) — é linha no catálogo do servidor. Id que não existe **ou que é de outra LISTA** é 400 apontando o campo — quem separa motivo de marca é o `kind`, e não a empresa: `catalog_lookups` é cadastro da organização, digitado uma vez para o grupo inteiro.",
            "type": ["null", "string"],
            "format": "uuid"
          },
          "note": {
            "description": "Observação livre. O motivo diz a CLASSE do cancelamento; a nota diz o caso — e é ela que sobrevive à pergunta \"por que este aqui?\".",
            "type": ["null", "string"],
            "maxLength": 200
          }
        }
      },
      "TransferProfessionalRequest": {
        "description": "Troca o Profissional Externo do pedido. **Transferir não é editar o campo**: no legado a indicação vive em `VendaIndicacao`, com vigência, porque é ela que decide comissão — e comissão paga com base em quem estava no documento HOJE, sem saber quem estava ontem, não se audita. Por isso a troca é operação própria, com trilha, e o `PUT` do documento NÃO move `professionalId`.",
        "type": "object",
        "required": ["professionalId"],
        "properties": {
          "professionalId": {
            "description": "O novo profissional — `PartnerDto.id` com papel `professional`. Parceiro sem esse papel é 400 apontando o campo. `null` REMOVE a indicação, e isso é caso legítimo: venda de balcão que entrou com especificador por engano.",
            "type": ["null", "string"],
            "format": "uuid"
          },
          "note": {
            "description": "Por que transferiu. Fica na linha da trilha, não no documento.",
            "type": ["null", "string"],
            "maxLength": 200
          }
        }
      },
      "OrderProfessionalAssignmentDto": {
        "description": "Uma linha da trilha de indicação — quem foi o profissional do pedido, e DE QUANDO ATÉ QUANDO. Modelada em `VendaIndicacao` (vigência), sem o percentual por grupo de produto: participação é assunto do Financeiro e não entra por esta porta.",
        "required": ["id", "startedAt"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "professionalId": {
            "description": "O profissional desta vigência. `null` = período sem indicação.",
            "type": ["null", "string"],
            "format": "uuid"
          },
          "professionalName": {
            "description": "Nome resolvido na leitura.",
            "type": ["null", "string"]
          },
          "startedAt": {
            "description": "Início da vigência.",
            "type": "string",
            "format": "date-time"
          },
          "endedAt": {
            "description": "Fim da vigência. `null` na linha CORRENTE — é ela que casa com `OrderDetailDto.professionalId`.",
            "type": ["null", "string"],
            "format": "date-time"
          },
          "changedByEmployeeId": {
            "description": "Quem fez a troca que ABRIU esta vigência. `null` na linha que nasceu com o documento.",
            "type": ["null", "string"],
            "format": "uuid"
          },
          "note": {
            "description": "A justificativa dada na troca.",
            "type": ["null", "string"]
          }
        }
      },
      "PagedResultOfOrderProfessionalAssignmentDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/OrderProfessionalAssignmentDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "ServiceDto": {
        "description": "Proposto. Um SERVIÇO do cadastro da empresa ativa. Fonte: `Servicos` do legado (16 colunas). As quatro de auditoria (`usr_cod_criacao`/`usr_dt_hr_*`) e `Emp_codigo` não sobem: as primeiras são trilho próprio (auditoria viva), e a empresa é decidida pela sessão, nunca declarada pelo cliente.",
        "required": [
          "id",
          "code",
          "description",
          "priceCents",
          "electricianPercent",
          "priceLocked",
          "delivery",
          "active"
        ],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "code": {
            "type": "string",
            "description": "Código curto do serviço, único na empresa. É onde o `sev_cod` do legado aterrissa na carga — mesmo papel que `code` faz no depósito. `sev_cod` é `int` lá e `string` aqui porque um dia ele deixa de ser sequência e vira código que o operador escolhe; a chave de verdade é o `id`."
          },
          "description": {
            "type": "string",
            "description": "`Serv_Desc` — o nome do serviço, e o texto que a linha do documento CONGELA na emissão. Chama-se `description` e não `name` porque é isso que ele é do outro lado: `QuoteServiceItemDto.description` sai daqui."
          },
          "priceCents": {
            "type": "integer",
            "format": "int64",
            "description": "`Serv_Preco` em CENTAVOS. `money` do SQL Server tem 4 casas; centavos são 2, e a carga precisa conferir se alguma linha usa as duas casas extras — arredondar em silêncio na migração é perder dinheiro que ninguém procura depois."
          },
          "electricianPercent": {
            "type": "integer",
            "format": "int32",
            "description": "`Serv_Pc_Eletricista` — quanto da linha vai para quem instala. Int com 4 casas implícitas: `10000` = 1%, a MESMA convenção de `discountPercent`. É o padrão do cadastro, e a linha do documento pode sobrepô-lo (ver `QuoteServiceItemWriteRequest.electricianPercent`)."
          },
          "type": {
            "type": ["null", "string"],
            "description": "`Serv_tipo`. **Texto livre, e é assim de propósito.** O legado o guarda em `varchar(12)` sem FK e sem tabela de domínio, e o dump de schema não traz linhas: publicar `enum` ou kind de `catalog-lookups` aqui seria inventar o vocabulário em vez de lê-lo. Vira lista de apoio quando houver dado na mão para escrever a lista — e essa é mudança de contrato, não de servidor."
          },
          "installationMinutes": {
            "type": ["null", "integer"],
            "format": "int32",
            "description": "`Serv_tempoInstalacao` — quanto tempo a instalação leva, insumo da agenda. **A UNIDADE é decisão desta publicação, não leitura do legado:** lá a coluna é `int` e não diz se conta minuto ou hora. MINUTOS porque é a unidade que não perde informação nas duas leituras possíveis. A carga precisa da conferência com quem opera antes de multiplicar por 60."
          },
          "nfseCode": {
            "type": ["null", "string"],
            "description": "`Serv_NFSECodigo` — o código do serviço na NFS-e, que é lista MUNICIPAL. Viaja como texto porque tem ponto (`7.02`) e porque cada município publica a sua; validá-lo aqui exigiria a tabela do município da empresa, que este contrato ainda não tem."
          },
          "productGroup": {
            "type": ["null", "string"],
            "description": "`GrupoProduto_codigo` — o grupo, para relatório e para a NFS-e. Texto, e não par `id`+`name`, pela mesma razão que `QuoteItemDto.productGroup` já é texto: não existe kind `GRUPO_PRODUTO` em `catalog-lookups`. É dívida declarada, e ela aparece nos dois lugares ao mesmo tempo quando for paga."
          },
          "priceLocked": {
            "type": "boolean",
            "description": "`Serv_NaoAtualizarValor` — a linha nova do documento NÃO puxa o preço do cadastro; quem digita o valor é o operador. Bit nulável no legado, booleano aqui: `null` e `false` significam a mesma coisa, e três estados num campo de dois seria estado que ninguém sabe ler."
          },
          "delivery": {
            "type": "boolean",
            "description": "`Serv_Entrega` — este serviço É a entrega (frete), não instalação. É o que separa o 1001 = FRETE do legado do resto, e o que a impressão do documento usa para pôr a linha no lugar certo."
          },
          "active": {
            "type": "boolean",
            "description": "`Serv_situacao` (`char(1)`) virado booleano — padrão 8, desativação lógica."
          }
        }
      },
      "ServiceWriteRequest": {
        "description": "Proposto. Corpo de criação e de alteração. **`PUT` substitui o registro INTEIRO**: campo ausente APAGA, não preserva. Sem `id`.",
        "required": [
          "code",
          "description",
          "priceCents",
          "electricianPercent",
          "priceLocked",
          "delivery",
          "active"
        ],
        "type": "object",
        "properties": {
          "code": {
            "type": ["null", "string"]
          },
          "description": {
            "type": ["null", "string"]
          },
          "priceCents": {
            "type": ["null", "integer"],
            "format": "int64",
            "description": "Em centavos."
          },
          "electricianPercent": {
            "type": ["null", "integer"],
            "format": "int32",
            "description": "Int com 4 casas implícitas: `10000` = 1%."
          },
          "type": {
            "type": ["null", "string"]
          },
          "installationMinutes": {
            "type": ["null", "integer"],
            "format": "int32",
            "description": "Em minutos — ver `ServiceDto`."
          },
          "nfseCode": {
            "type": ["null", "string"]
          },
          "productGroup": {
            "type": ["null", "string"]
          },
          "priceLocked": {
            "type": ["null", "boolean"]
          },
          "delivery": {
            "type": ["null", "boolean"]
          },
          "active": {
            "type": ["null", "boolean"]
          }
        }
      },
      "PagedResultOfServiceDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ServiceDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "StockLocationDto": {
        "required": ["id", "parentId", "code", "name", "isDefault", "active"],
        "type": "object",
        "description": "Proposto. Um DEPÓSITO da empresa ativa — nó da árvore de locais de estoque. Ver `ListStockLocations` para por que ele vem plano e sem o nome do pai.",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "parentId": {
            "type": ["null", "string"],
            "format": "uuid",
            "description": "O nó acima, ou `null` na raiz. É o que faz a árvore: profundidade no lugar das cinco colunas do legado."
          },
          "code": {
            "type": "string",
            "description": "Código curto do depósito, único na empresa. É por ele que o ETL do legado casa `EstTp_Codigo`."
          },
          "name": {
            "type": "string"
          },
          "isDefault": {
            "type": "boolean",
            "description": "O depósito para onde vai movimento sem `locationId`. UM por empresa, garantido por índice parcial. Não se escreve pelo corpo — nasce do servidor."
          },
          "active": {
            "type": "boolean"
          }
        }
      },
      "StockLocationWriteRequest": {
        "required": ["parentId", "code", "name", "active"],
        "type": "object",
        "description": "Proposto. `PUT` substitui o registro INTEIRO: campo ausente APAGA, não preserva. `isDefault` e `id` não entram — ver `CreateStockLocation`.",
        "properties": {
          "parentId": {
            "type": ["null", "string"],
            "format": "uuid"
          },
          "code": {
            "type": ["null", "string"]
          },
          "name": {
            "type": ["null", "string"]
          },
          "active": {
            "type": ["null", "boolean"]
          }
        }
      },
      "PagedResultOfStockLocationDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/StockLocationDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "StockBalanceDto": {
        "required": ["locationId", "variantId", "qty", "updatedAt"],
        "type": "object",
        "description": "Proposto. O saldo de UMA variante em UM depósito. Cache derivado do kardex (ADR-009) — anda por delta, na mesma transação do movimento.",
        "properties": {
          "locationId": {
            "type": "string",
            "format": "uuid"
          },
          "variantId": {
            "type": "string",
            "format": "uuid"
          },
          "qty": {
            "type": "number",
            "format": "double",
            "description": "Quantidade, até 3 casas. **O tipo admite negativo** — o ADR-009 ficou sem o CHECK de propósito (api#79, decisão 5), porque venda antes de a nota entrar acontece e carga do legado chega como está. Recusar que o saldo FIQUE negativo é decisão da operação que movimenta, não da coluna: `CreateStockMovement` recusa com 409, e uma carga que escreva direto no cache não passa por ela."
          },
          "updatedAt": {
            "type": "string",
            "format": "date-time"
          }
        }
      },
      "PagedResultOfStockBalanceDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/StockBalanceDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "PermissionDto": {
        "description": "Proposto. Uma permissão do catálogo — uma AÇÃO, a unidade que vira uma caixa marcável.",
        "required": ["key", "label"],
        "type": "object",
        "properties": {
          "key": {
            "description": "A chave que o papel guarda, no formato `modulo:acao` — `produtos:editar`, `depositos:gerenciar`. É o valor que viaja em `RoleDetailDto.permissions`, e o front nunca o escreve à mão: quem enumera é este catálogo.",
            "type": "string"
          },
          "label": {
            "description": "Rótulo em PT-BR, o texto da caixa. Do servidor, porque a lista é dele.",
            "type": "string"
          },
          "description": {
            "description": "O que a permissão libera, em uma frase — a ajuda ao lado da caixa. `null` quando o rótulo já diz tudo; a tela não inventa texto no lugar.",
            "type": ["string", "null"]
          }
        }
      },
      "PermissionModuleDto": {
        "description": "Proposto. Um módulo do catálogo com as permissões dele — o agrupamento é do servidor porque é ele quem sabe a que módulo a permissão nova pertence.",
        "required": ["key", "label", "permissions"],
        "type": "object",
        "properties": {
          "key": {
            "description": "Identificador do módulo — o prefixo das chaves deste grupo (`produtos`, `estoque`).",
            "type": "string"
          },
          "label": {
            "description": "Rótulo em PT-BR, o título da seção de caixas.",
            "type": "string"
          },
          "permissions": {
            "description": "Nunca vazio: módulo sem permissão não é módulo, é ruído na tela.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PermissionDto"
            }
          }
        }
      },
      "PermissionCatalogDto": {
        "description": "Proposto. O catálogo inteiro. Não é `PagedResult`: não há página, não há total a paginar — o que vem é tudo que existe.",
        "required": ["version", "modules"],
        "type": "object",
        "properties": {
          "version": {
            "description": "Versão do catálogo, OPACA. Muda quando o servidor acrescenta ou aposenta permissão, e serve para o cliente cachear com validade longa e comparar por IGUALDADE. Não tem formato prometido e não se ordena — tratar como texto.",
            "type": "string"
          },
          "modules": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PermissionModuleDto"
            }
          }
        }
      },
      "RoleDto": {
        "description": "Proposto. A LINHA da listagem de papéis — o que a tabela desenha, sem o conjunto de permissões.",
        "required": ["id", "name", "system", "template", "active", "permissionCount"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "name": {
            "description": "Nome do papel, como o admin o batizou (\"Financeiro\", \"Vendedor externo\"). Único na organização.",
            "type": "string"
          },
          "description": {
            "description": "Para que serve, em uma frase. `null` é o normal — papel criado às pressas não ganha texto.",
            "type": ["string", "null"]
          },
          "system": {
            "description": "Papel de SISTEMA (`owner`, `admin`): existe em toda organização, não é editável nem desativável, e alterar é 409 `urn:cabinet:erro:papel-de-sistema`. A tela esconde `Alterar` na linha em vez de deixar o admin descobrir pelo erro.",
            "type": "boolean"
          },
          "template": {
            "description": "Papel de FÁBRICA, entregue junto (Vendedor, Visualizador, Estoquista) como ponto de partida. Marca de ORIGEM, não de proteção: template é da organização, o admin edita, desativa e clona à vontade — quem recusa edição é `system`. Usar direto, sem clonar, também vale.",
            "type": "boolean"
          },
          "active": {
            "description": "Desativação lógica: papel aposentado some da escolha do vínculo e continua legível em quem já o tem. É o que substitui o `DELETE` que este recurso não publica.",
            "type": "boolean"
          },
          "permissionCount": {
            "description": "Quantas permissões o papel concede — a coluna da listagem. O conjunto está em `GET /api/roles/{id}`.",
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "RoleDetailDto": {
        "description": "Proposto. O papel com o conjunto de permissões — o corpo que marca as caixas da tela e o que se lê para clonar um template.",
        "required": [
          "id",
          "name",
          "system",
          "template",
          "active",
          "permissionCount",
          "permissions"
        ],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "name": {
            "description": "Nome do papel, único na organização.",
            "type": "string"
          },
          "description": {
            "type": ["string", "null"]
          },
          "system": {
            "description": "Papel de sistema — alterar é 409 `urn:cabinet:erro:papel-de-sistema`.",
            "type": "boolean"
          },
          "template": {
            "description": "Papel de fábrica; marca de origem, editável como qualquer outro.",
            "type": "boolean"
          },
          "active": {
            "description": "Desativação lógica.",
            "type": "boolean"
          },
          "permissionCount": {
            "description": "O tamanho de `permissions`, repetido para a linha e o detalhe terem a mesma forma.",
            "type": "integer",
            "format": "int32"
          },
          "permissions": {
            "description": "As chaves de `PermissionDto.key` que este papel concede. Papel de sistema `admin` pode devolver o catálogo inteiro, e `[]` quer dizer papel que não concede nada — não é \"não sei\".",
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      },
      "RoleWriteRequest": {
        "description": "Proposto. Corpo de criação e de alteração do papel. `system` e `template` NÃO entram: são marcas do servidor, e papel que se declarasse de sistema pelo corpo burlaria a recusa de edição.",
        "required": ["name", "permissions", "active"],
        "type": "object",
        "properties": {
          "name": {
            "description": "Único na organização; repetido é 409.",
            "type": "string"
          },
          "description": {
            "type": ["string", "null"]
          },
          "permissions": {
            "description": "O conjunto FINAL de chaves do catálogo — substitui o que havia, não acrescenta. Chave fora de `GET /api/permissions` é 400 `urn:cabinet:erro:campos-invalidos`, com `fields[]` em `permissions`.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "active": {
            "description": "Desativação lógica; `false` tira o papel da escolha do vínculo.",
            "type": "boolean"
          }
        }
      },
      "PagedResultOfRoleDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RoleDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "PaymentTermInstallmentDto": {
        "required": ["number", "daysAfterIssue", "percent", "amountCents"],
        "type": "object",
        "description": "Proposto. Uma PARCELA da condição — `Forma_Pagamento_Parcela` do legado, PK `(Fpg_codigo, Fpp_parcela)`. Não tem id: a identidade dela é o par condição + `number`, e é por isso que ela viaja embutida (ver `ListPaymentTerms`).",
        "properties": {
          "number": {
            "type": "integer",
            "format": "int32",
            "description": "Ordinal da parcela dentro da condição, 1-based e único nela — `Fpp_parcela`. É a ordem de exibição e a metade legível da chave."
          },
          "daysAfterIssue": {
            "type": "integer",
            "format": "int32",
            "description": "Dias corridos entre a EMISSÃO do documento e o vencimento desta parcela — `Fpp_dias`. Absoluto, não incremental: `30/60/90` são três linhas com 30, 60 e 90.\n\n`Fpg_DiasPrimeiraParcela` e `Fpg_DiasEntreParcelas` do cabeçalho do legado ficaram de fora porque são o GERADOR destas linhas, não dado a mais — guardar os dois lados deixaria duas verdades sobre o mesmo vencimento, e a tela que edita uma parcela do meio quebraria a fórmula sem ninguém ver. Entrada à vista é uma parcela com `0`."
          },
          "percent": {
            "type": ["null", "integer"],
            "format": "int32",
            "description": "Fatia do total, int com 4 casas implícitas — `1000000` = 100%, `500000` = 50%. Mesma escala de `QuoteDetailDto.discountPercent`. `null` quando a parcela é de valor fixo."
          },
          "amountCents": {
            "type": ["null", "integer"],
            "format": "int64",
            "description": "Valor fixo da parcela, em centavos — a entrada de R$ 500 que não é fração do total. `null` quando a parcela é percentual."
          }
        }
      },
      "PaymentTermDto": {
        "required": ["id", "name", "active", "installmentCount", "installments"],
        "type": "object",
        "description": "Proposto. Uma CONDIÇÃO DE PAGAMENTO da empresa ativa — `Forma_Pagamento` do legado.\n\n**O ajuste por GRUPO DE PRODUTO entra aqui, em `groupAdjustments`** — `Forma_PagamentoGrupProd` do legado. Esta descrição declarava a dívida e o motivo de ela esperar (grupo de produto sem id, e casar por texto faria \"PENDENTES\" e \"Pendentes\" renderem descontos diferentes no mesmo documento); o id chegou com `QuoteItemDto.productGroupId`, e a dívida foi paga. Ver `PaymentTermGroupAdjustmentDto` para o que entrou e o que continua fora — as SEIS colunas de desconto/acréscimo do cabeçalho (`Fpg_desconto`, `Fpg_acrescimo` e os pares `_lu`/`_ma`/`_se`) não entram porque são o GERADOR destas linhas, medido no SQL do executável.\n\n**E há uma SEGUNDA verdade sobre condição de pagamento neste contrato, hoje:** `PartnerDto.paymentTerms` é `string` livre, sem descrição — a condição habitual do parceiro, digitada. Ela NÃO foi trocada por `paymentTermId` aqui de propósito: o campo é do VÍNCULO do parceiro com a empresa, converter texto em id é migração de dado (e escolha de para qual condição cada texto aponta), e fazer as duas coisas na mesma PR deixaria a segunda sem quem a revisasse. Enquanto isso não acontece, as duas convivem e o documento manda: quem grava orçamento escolhe `paymentTermId`, e o texto do parceiro é sugestão para quem digita.",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "name": {
            "type": "string",
            "description": "Como o operador a chama — `Fpg_descricao`. Único na empresa (409 no repetido)."
          },
          "active": {
            "type": "boolean",
            "description": "Desativação lógica — `Fpg_situacao` (A/I). Condição inativa sai do combo e continua legível nos documentos que a usaram."
          },
          "installmentCount": {
            "type": "integer",
            "format": "int32",
            "description": "Quantas parcelas a condição tem — `Fpg_quantidade`. DERIVADO de `installments`, calculado pelo servidor, e por isso não existe na escrita.\n\nEle é redundante de propósito: existe porque é COLUNA de grade e chave de `sortBy`, e ordenar por tamanho de array não é coisa que a fronteira de listagem saiba fazer. No legado os dois lados podiam divergir (a coluna era gravada à mão); aqui não podem, e é essa a diferença que justifica manter o campo em vez de copiar a coluna."
          },
          "installments": {
            "type": "array",
            "description": "As parcelas, em ordem de `number`.",
            "items": {
              "$ref": "#/components/schemas/PaymentTermInstallmentDto"
            }
          },
          "groupAdjustments": {
            "description": "Os ajustes por grupo de produto desta condição, em ordem de `productGroupName`. Lista **vazia** quando a condição não ajusta grupo nenhum — que é o caso comum, e não ausência de dado.\n\nViajam embutidos pela razão de `installments`: a linha não tem identidade fora da condição, e gravá-las uma a uma faria um `Gravar` virar N requisições sem transação entre elas.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PaymentTermGroupAdjustmentDto"
            }
          }
        }
      },
      "PaymentTermInstallmentWriteRequest": {
        "required": ["number", "daysAfterIssue"],
        "type": "object",
        "description": "Proposto. Uma parcela no corpo de escrita.\n\n**Mesma forma do DTO, e ainda assim schema próprio:** o dia em que a leitura ganhar campo derivado (como `installmentCount` já é na condição), reusar o DTO na escrita deixaria o cliente mandando o que o servidor calcula, e o Fastify o apagaria em silêncio.\n\n**Exatamente UM entre `percent` e `amountCents`.** Os dois preenchidos, ou nenhum, é **400** com `fields[]` apontando a linha — nunca \"vale o percentual e ignora o valor\", que é a aparadura silenciosa que faz o operador conferir a soma e não achar o erro.",
        "properties": {
          "number": {
            "type": "integer",
            "format": "int32"
          },
          "daysAfterIssue": {
            "type": "integer",
            "format": "int32"
          },
          "percent": {
            "type": ["null", "integer"],
            "format": "int32"
          },
          "amountCents": {
            "type": ["null", "integer"],
            "format": "int64"
          }
        }
      },
      "PaymentTermWriteRequest": {
        "required": ["name", "active", "installments"],
        "type": "object",
        "description": "Proposto. Corpo de criação e de alteração. **`PUT` substitui a condição INTEIRA**, parcelas junto. Sem `id` e sem `installmentCount` (o servidor deriva).\n\n**Três recusas, e nenhuma delas apara em silêncio** (todas 400, com `fields[]`):\n\n1. `installments` VAZIO. Condição sem parcela não diz quando se paga; \"à vista\" é uma parcela com `daysAfterIssue: 0` e `percent: 1000000`, que é dado, não ausência de dado.\n2. `number` repetido ou com buraco na sequência. A chave do legado é `(condição, número)`, e uma sequência `1,2,4` não tem leitura única na hora de imprimir.\n3. Parcelas todas percentuais cuja soma não fecha **exatamente** `1000000` (100%). Somar 99,99% e deixar o resto para o arredondamento do servidor põe centavo órfão num documento assinado.\n\n**Misturar percentual e valor fixo na mesma condição também é 400**, e essa é a única das quatro que é dívida e não regra: o legado permite (`Fpp_porc_valor` e `Fpp_PorcentagemDoTotal` convivem na linha), mas a ordem de aplicação — se o fixo sai do total antes de os percentuais incidirem, e o que acontece quando o fixo é maior que o total — **não foi decidida por ninguém**. Publicar uma regra de arredondamento aqui a fixaria no servidor para sempre. Recusar deixa a falta visível.",
        "properties": {
          "name": {
            "type": ["null", "string"]
          },
          "active": {
            "type": ["null", "boolean"]
          },
          "installments": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PaymentTermInstallmentWriteRequest"
            }
          },
          "groupAdjustments": {
            "description": "SUBSTITUI as linhas de ajuste por grupo, como `installments` substitui as parcelas — `PUT` troca a condição INTEIRA.\n\n**Omitir o campo é diferente de mandá-lo vazio, e essa é a única assimetria deste corpo:** ausente quer dizer \"não mexi nos ajustes\" e conserva os que estão gravados; `[]` quer dizer \"apague todos\". A distinção existe porque este campo nasce DEPOIS das telas que já gravam condição — sem ela, a primeira tela antiga a salvar um nome apagaria em silêncio os ajustes que alguém configurou na tela nova. `installments` não precisa disso: é obrigatório desde o primeiro dia.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PaymentTermGroupAdjustmentWriteRequest"
            }
          }
        }
      },
      "PagedResultOfPaymentTermDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PaymentTermDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "InstallmentPolicyDto": {
        "required": ["minTotalToInstallCents", "minInstallmentCents", "maxInstallments"],
        "type": "object",
        "description": "Proposto. Os três limites de parcelamento — e o mesmo schema serve para o CARIMBO no documento (`QuoteDetailDto.installmentPolicy`), porque é literalmente o valor que valia na gravação.\n\n**Nenhum campo é anulável, e é decisão.** \"Sem teto\" seria um segundo modo de ler o mesmo campo, e o legado não o tem: `Par_QuantMaxParcela` é sempre um número. Quem não quer teto grava um alto.\n\n---\n\n**DECISÃO PENDENTE — org × empresa, e ela está aqui em cima da mesa.**\n\nNo legado os dois níveis DISCORDAM, e não por descuido de leitura: `Forma_Pagamento` tem `Emp_codigo` (condição é por empresa), enquanto `Paramentros` — a tabela onde os três limites moram — tem **uma linha, sem chave primária e sem `Emp_codigo`**, ou seja, vale para a instalação inteira.\n\nPublicado aqui **por empresa ativa**, por dois motivos: o isolamento do Cabinet é por `tenant_id` e uma tabela sem ele seria a segunda global do repo (`catalog_lookups` é a primeira, e cada uma custa uma exceção à RLS); e um limite que governa uma tabela POR EMPRESA não se sustenta acima dela — a empresa que precisasse de 10× não teria como, sem mudar o limite das outras.\n\nSe o user decidir org, o que muda é onde a linha mora: a LEITURA continua sendo \"a política da empresa ativa\", então nenhum caminho deste contrato muda. E em qualquer das duas, o carimbo no documento é o que protege o histórico.",
        "properties": {
          "minTotalToInstallCents": {
            "type": "integer",
            "format": "int64",
            "description": "Total mínimo do documento para que ele possa ser parcelado — `par_ParcelarVlAcima` (R$ 100 na Vertz = `10000`). Abaixo dele, só condição de parcela única."
          },
          "minInstallmentCents": {
            "type": "integer",
            "format": "int64",
            "description": "Valor mínimo de CADA parcela — `Par_VlMinParcela` (R$ 50 = `5000`). É o limite que corta o plano na prática: um total de R$ 240 em 6× dá R$ 40 e não passa."
          },
          "maxInstallments": {
            "type": "integer",
            "format": "int32",
            "description": "Teto de parcelas — `Par_QuantMaxParcela` (6 na Vertz)."
          }
        }
      },
      "InstallmentPolicyWriteRequest": {
        "required": ["minTotalToInstallCents", "minInstallmentCents", "maxInstallments"],
        "type": "object",
        "description": "Proposto. Os três, todos obrigatórios — `PUT` de singleton substitui a política inteira, e política pela metade não tem leitura.",
        "properties": {
          "minTotalToInstallCents": {
            "type": "integer",
            "format": "int64"
          },
          "minInstallmentCents": {
            "type": "integer",
            "format": "int64"
          },
          "maxInstallments": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "DocumentInstallmentDto": {
        "required": ["number", "dueDate", "amountCents"],
        "type": "object",
        "description": "Proposto. Uma parcela DO DOCUMENTO — o plano já resolvido em datas e centavos, não mais a regra.\n\n**Um schema só para orçamento e pedido**, ao contrário de `QuoteEnvironmentDto`/`OrderEnvironmentDto`. Aqueles são duas coisas que já divergem; esta é a MESMA conta a partir da MESMA condição, e duas cópias idênticas seriam dois lugares para mudar quando a soma dos centavos mudar de regra.",
        "properties": {
          "number": {
            "type": "integer",
            "format": "int32",
            "description": "Ordinal, 1-based."
          },
          "dueDate": {
            "type": "string",
            "format": "date",
            "description": "Vencimento resolvido: `issuedAt` + `daysAfterIssue` da parcela da condição. Data, não instante — parcela vence no DIA."
          },
          "amountCents": {
            "type": "integer",
            "format": "int64",
            "description": "Valor da parcela em centavos, já resolvido. A soma das parcelas é `totalCents` EXATA: a sobra do arredondamento vai para a ÚLTIMA, que é onde o legado a põe e onde ela é conferível a olho contra o total impresso."
          }
        }
      },
      "QuoteGroupDiscountDto": {
        "description": "Proposto. Uma linha de desconto por grupo de produto — `VendaDesconto` do legado (300.337 linhas). Vive DENTRO do documento, como ambiente e item: não tem identidade fora dele, e sub-recurso por linha faria um `Gravar` virar N requisições sem transação entre elas.\n\n**Ficaram de fora, e a omissão é declarada:** `VenDesc_DescontoValor` (desconto digitado em dinheiro) seria uma segunda forma de dizer a mesma coisa que `discountPercent`, e conviver com as duas exige uma regra de desempate que a extração não fixou; `VenDesc_DescPorcUsuario` (o percentual que o usuário pediu, ao lado do que valeu) só faz sentido junto com a regra que os separa — o teto — e essa regra é do servidor. Entram quando houver decisão escrita, não antes.",
        "required": [
          "productGroupId",
          "productGroupName",
          "discountPercent",
          "subtotalCents",
          "discountCents",
          "totalCents"
        ],
        "type": "object",
        "properties": {
          "productGroupId": {
            "description": "Grupo de produto — `CatalogLookupDto.id`, kind `GRUPO_PRODUTO` (`GrupoProduto` no legado: 12 linhas, por empresa). É a CHAVE da linha: um grupo aparece uma vez só no documento, como no legado, cuja PK é `(Ven_CodigoPre, GrupoProduto_Codigo)`. Repetido é 400 apontando `groupDiscounts.<n>.productGroupId` — duas linhas do mesmo grupo não dizem qual desconto vale.",
            "type": "string",
            "format": "uuid"
          },
          "productGroupName": {
            "description": "Nome do grupo CONGELADO na emissão, pelo mesmo motivo de `QuoteEnvironmentDto.name`: renomear o grupo no catálogo não pode reescrever orçamento fechado.",
            "type": "string"
          },
          "discountPercent": {
            "description": "Desconto do grupo (`VenDesc_DescontoPorc`), em percentual com 4 casas decimais escaladas por 10.000: `10000` = 1%. Mesma unidade e mesmo teto do desconto do documento.",
            "type": "integer",
            "format": "int32"
          },
          "subtotalCents": {
            "description": "Soma das linhas do grupo ANTES do desconto (`VenDesc_ValorGrupo`), em centavos. Calculado pelo servidor.",
            "type": "integer",
            "format": "int64"
          },
          "discountCents": {
            "description": "Quanto o desconto tirou, em centavos. Calculado pelo servidor, e não refeito na tela: percentual de 4 casas sobre centavos arredonda, e a conta refeita no cliente erra o último centavo em parte dos casos — o total do documento deixaria de fechar com a soma dos grupos.",
            "type": "integer",
            "format": "int64"
          },
          "totalCents": {
            "description": "Total do grupo depois do desconto (`VenDesc_ValorGrupoDesc`), em centavos. Calculado pelo servidor.",
            "type": "integer",
            "format": "int64"
          },
          "quantity": {
            "description": "Quantidade somada das linhas do grupo (`VenDesc_Quantidade`), até 3 casas. Calculada pelo servidor.",
            "type": ["number", "null"],
            "format": "double"
          }
        }
      },
      "QuoteGroupDiscountWriteRequest": {
        "description": "Proposto. Uma linha de desconto por grupo na ESCRITA — só o que o operador decide: o grupo e o percentual. `subtotalCents`, `discountCents`, `totalCents` e `quantity` são do servidor, como `totalCents` do item: a tela que os mandasse estaria propondo o próprio total.",
        "required": ["productGroupId", "discountPercent"],
        "type": "object",
        "properties": {
          "productGroupId": {
            "description": "Grupo de produto — `CatalogLookupDto.id`, kind `GRUPO_PRODUTO` (`GrupoProduto` no legado: 12 linhas, por empresa). É a CHAVE da linha: um grupo aparece uma vez só no documento, como no legado, cuja PK é `(Ven_CodigoPre, GrupoProduto_Codigo)`. Repetido é 400 apontando `groupDiscounts.<n>.productGroupId` — duas linhas do mesmo grupo não dizem qual desconto vale.",
            "type": "string",
            "format": "uuid"
          },
          "discountPercent": {
            "description": "Desconto do grupo (`VenDesc_DescontoPorc`), em percentual com 4 casas decimais escaladas por 10.000: `10000` = 1%. Mesma unidade e mesmo teto do desconto do documento.",
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "OrderGroupDiscountDto": {
        "description": "Proposto. Uma linha de desconto por grupo de produto — `VendaDesconto` do legado (300.337 linhas). Vive DENTRO do documento, como ambiente e item: não tem identidade fora dele, e sub-recurso por linha faria um `Gravar` virar N requisições sem transação entre elas.\n\n**Ficaram de fora, e a omissão é declarada:** `VenDesc_DescontoValor` (desconto digitado em dinheiro) seria uma segunda forma de dizer a mesma coisa que `discountPercent`, e conviver com as duas exige uma regra de desempate que a extração não fixou; `VenDesc_DescPorcUsuario` (o percentual que o usuário pediu, ao lado do que valeu) só faz sentido junto com a regra que os separa — o teto — e essa regra é do servidor. Entram quando houver decisão escrita, não antes.",
        "required": [
          "productGroupId",
          "productGroupName",
          "discountPercent",
          "subtotalCents",
          "discountCents",
          "totalCents"
        ],
        "type": "object",
        "properties": {
          "productGroupId": {
            "description": "Grupo de produto — `CatalogLookupDto.id`, kind `GRUPO_PRODUTO` (`GrupoProduto` no legado: 12 linhas, por empresa). É a CHAVE da linha: um grupo aparece uma vez só no documento, como no legado, cuja PK é `(Ven_CodigoPre, GrupoProduto_Codigo)`. Repetido é 400 apontando `groupDiscounts.<n>.productGroupId` — duas linhas do mesmo grupo não dizem qual desconto vale.",
            "type": "string",
            "format": "uuid"
          },
          "productGroupName": {
            "description": "Nome do grupo CONGELADO na emissão, pelo mesmo motivo de `OrderEnvironmentDto.name`: renomear o grupo no catálogo não pode reescrever orçamento fechado.",
            "type": "string"
          },
          "discountPercent": {
            "description": "Desconto do grupo (`VenDesc_DescontoPorc`), em percentual com 4 casas decimais escaladas por 10.000: `10000` = 1%. Mesma unidade e mesmo teto do desconto do documento.",
            "type": "integer",
            "format": "int32"
          },
          "subtotalCents": {
            "description": "Soma das linhas do grupo ANTES do desconto (`VenDesc_ValorGrupo`), em centavos. Calculado pelo servidor.",
            "type": "integer",
            "format": "int64"
          },
          "discountCents": {
            "description": "Quanto o desconto tirou, em centavos. Calculado pelo servidor, e não refeito na tela: percentual de 4 casas sobre centavos arredonda, e a conta refeita no cliente erra o último centavo em parte dos casos — o total do documento deixaria de fechar com a soma dos grupos.",
            "type": "integer",
            "format": "int64"
          },
          "totalCents": {
            "description": "Total do grupo depois do desconto (`VenDesc_ValorGrupoDesc`), em centavos. Calculado pelo servidor.",
            "type": "integer",
            "format": "int64"
          },
          "quantity": {
            "description": "Quantidade somada das linhas do grupo (`VenDesc_Quantidade`), até 3 casas. Calculada pelo servidor.",
            "type": ["number", "null"],
            "format": "double"
          }
        }
      },
      "OrderGroupDiscountWriteRequest": {
        "description": "Proposto. Uma linha de desconto por grupo na ESCRITA — só o que o operador decide: o grupo e o percentual. `subtotalCents`, `discountCents`, `totalCents` e `quantity` são do servidor, como `totalCents` do item: a tela que os mandasse estaria propondo o próprio total.",
        "required": ["productGroupId", "discountPercent"],
        "type": "object",
        "properties": {
          "productGroupId": {
            "description": "Grupo de produto — `CatalogLookupDto.id`, kind `GRUPO_PRODUTO` (`GrupoProduto` no legado: 12 linhas, por empresa). É a CHAVE da linha: um grupo aparece uma vez só no documento, como no legado, cuja PK é `(Ven_CodigoPre, GrupoProduto_Codigo)`. Repetido é 400 apontando `groupDiscounts.<n>.productGroupId` — duas linhas do mesmo grupo não dizem qual desconto vale.",
            "type": "string",
            "format": "uuid"
          },
          "discountPercent": {
            "description": "Desconto do grupo (`VenDesc_DescontoPorc`), em percentual com 4 casas decimais escaladas por 10.000: `10000` = 1%. Mesma unidade e mesmo teto do desconto do documento.",
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "AbcCurveRowDto": {
        "description": "Uma faixa da curva. `cumulativePercent` é o acumulado ATÉ esta linha na ordem de faturamento decrescente — é ele, e não o `sharePercent` isolado, que responde \"quais produtos fazem 80% da receita\".",
        "required": [
          "description",
          "quantity",
          "revenueCents",
          "sharePercent",
          "cumulativePercent",
          "abcClass"
        ],
        "type": "object",
        "properties": {
          "variantId": {
            "type": ["string", "null"],
            "format": "uuid",
            "description": "Nulo quando a linha do documento perdeu a variante do catálogo (`ON DELETE SET NULL`): o produto sumiu do cadastro, a venda não."
          },
          "description": {
            "type": "string",
            "description": "Descrição CONGELADA na linha do documento, não a do cadastro de hoje."
          },
          "productGroup": {
            "type": ["string", "null"]
          },
          "supplierName": {
            "type": ["string", "null"]
          },
          "quantity": {
            "description": "Somada no período. Texto porque `numeric(18,3)` não cabe em double sem perder a terceira casa.",
            "type": "string"
          },
          "revenueCents": {
            "description": "Faturamento da linha no período, em CENTAVOS.",
            "type": "integer",
            "format": "int64"
          },
          "sharePercent": {
            "type": "string",
            "description": "Fatia deste item no faturamento do período, com 4 casas como todo percentual do contrato (`10000` seria 1% se fosse inteiro; aqui é texto decimal para o relatório poder ser lido sem conversão)."
          },
          "cumulativePercent": {
            "type": "string",
            "description": "Acumulado até esta linha, inclusive."
          },
          "abcClass": {
            "type": "string",
            "enum": ["A", "B", "C"],
            "description": "Corte clássico e DECLARADO aqui para o front não reimplementá-lo: `A` até 80% acumulados, `B` até 95%, `C` o resto. O item que CRUZA o corte pertence à classe que ele fecha — senão os 80% nunca seriam alcançados por ninguém."
          }
        }
      },
      "AbcCurveReportDto": {
        "description": "Envelope comum da família `/api/reports/*`. **`summary` é do PERÍODO INTEIRO e `rows` é só da PÁGINA** — relatório paginado que soma as linhas visíveis mente exatamente onde dói: quem abre a página 1 de 500 produtos concluiria que o faturamento da empresa é o dos 100 primeiros. `total` conta as linhas AGREGADAS, não os documentos.",
        "required": ["from", "to", "page", "pageSize", "total", "summary", "rows"],
        "type": "object",
        "properties": {
          "from": {
            "description": "O período apurado, ecoado. O cliente pediu, o servidor confirma o que usou.",
            "type": "string",
            "format": "date"
          },
          "to": {
            "type": "string",
            "format": "date"
          },
          "page": {
            "type": "integer",
            "format": "int32"
          },
          "pageSize": {
            "type": "integer",
            "format": "int32"
          },
          "total": {
            "description": "Linhas AGREGADAS no período — o denominador da paginação. Não é a contagem de documentos.",
            "type": "integer",
            "format": "int32"
          },
          "summary": {
            "$ref": "#/components/schemas/AbcCurveSummaryDto"
          },
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/AbcCurveRowDto"
            }
          }
        }
      },
      "AbcCurveSummaryDto": {
        "description": "Totais do PERÍODO, não da página.",
        "required": ["revenueCents", "classACount", "classBCount", "classCCount"],
        "type": "object",
        "properties": {
          "revenueCents": {
            "description": "Faturamento do período inteiro — o denominador de todo `sharePercent`.",
            "type": "integer",
            "format": "int64"
          },
          "classACount": {
            "description": "Itens na classe A.",
            "type": "integer",
            "format": "int32"
          },
          "classBCount": {
            "description": "Itens na classe B.",
            "type": "integer",
            "format": "int32"
          },
          "classCCount": {
            "description": "Itens na classe C.",
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "ProductSoldRowDto": {
        "description": "Um produto somado no período.",
        "required": ["description", "quantity", "revenueCents", "orderCount"],
        "type": "object",
        "properties": {
          "variantId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "description": {
            "type": "string"
          },
          "productGroup": {
            "type": ["string", "null"]
          },
          "pieceType": {
            "type": ["string", "null"]
          },
          "supplierName": {
            "type": ["string", "null"]
          },
          "quantity": {
            "description": "Quantidade vendida no período.",
            "type": "string"
          },
          "revenueCents": {
            "description": "Faturamento do item no período.",
            "type": "integer",
            "format": "int64"
          },
          "orderCount": {
            "description": "Em quantos PEDIDOS distintos ele apareceu — o item vendido 100 unidades num pedido só é outro negócio do vendido 1 em 100 pedidos.",
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "ProductsSoldReportDto": {
        "description": "Envelope comum da família `/api/reports/*`. **`summary` é do PERÍODO INTEIRO e `rows` é só da PÁGINA** — relatório paginado que soma as linhas visíveis mente exatamente onde dói: quem abre a página 1 de 500 produtos concluiria que o faturamento da empresa é o dos 100 primeiros. `total` conta as linhas AGREGADAS, não os documentos.",
        "required": ["from", "to", "page", "pageSize", "total", "summary", "rows"],
        "type": "object",
        "properties": {
          "from": {
            "description": "O período apurado, ecoado. O cliente pediu, o servidor confirma o que usou.",
            "type": "string",
            "format": "date"
          },
          "to": {
            "type": "string",
            "format": "date"
          },
          "page": {
            "type": "integer",
            "format": "int32"
          },
          "pageSize": {
            "type": "integer",
            "format": "int32"
          },
          "total": {
            "description": "Linhas AGREGADAS no período — o denominador da paginação. Não é a contagem de documentos.",
            "type": "integer",
            "format": "int32"
          },
          "summary": {
            "$ref": "#/components/schemas/ProductsSoldSummaryDto"
          },
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ProductSoldRowDto"
            }
          }
        }
      },
      "ProductsSoldSummaryDto": {
        "description": "Totais do PERÍODO.",
        "required": ["quantity", "revenueCents"],
        "type": "object",
        "properties": {
          "quantity": {
            "description": "Quantidade total vendida no período.",
            "type": "string"
          },
          "revenueCents": {
            "description": "Faturamento total do período.",
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "SalesComparisonRowDto": {
        "description": "Um período fechado e o mesmo número no período ANTERIOR ao lado. O comparativo é o produto: valor solto não diz se o mês foi bom.",
        "required": ["bucket", "label", "orderCount", "revenueCents"],
        "type": "object",
        "properties": {
          "bucket": {
            "type": "string",
            "description": "A chave ordenável do período: `2026-08` (mês), `2026-Q3` (trimestre), `2026-S2` (semestre), `2026` (ano)."
          },
          "label": {
            "type": "string",
            "description": "O mesmo período por extenso, em pt-BR, escolhido pelo SERVIDOR — senão cada tela inventa o seu e o CSV exportado sai diferente do impresso."
          },
          "orderCount": {
            "description": "Pedidos fechados no período.",
            "type": "integer",
            "format": "int32"
          },
          "revenueCents": {
            "description": "Faturamento do período.",
            "type": "integer",
            "format": "int64"
          },
          "previousRevenueCents": {
            "description": "O mesmo faturamento no período imediatamente anterior — nulo no primeiro período da série, que não tem anterior DENTRO do recorte pedido.",
            "type": ["integer", "null"],
            "format": "int64"
          },
          "deltaCents": {
            "description": "`revenueCents - previousRevenueCents`. Nulo quando não há anterior.",
            "type": ["integer", "null"],
            "format": "int64"
          },
          "deltaPercent": {
            "description": "Variação percentual, texto decimal. **Nulo quando o período anterior foi ZERO** — divisão por zero não é \"crescimento infinito\", é ausência de base de comparação, e mandar `0` faria a tela desenhar estabilidade onde houve estreia.",
            "type": ["string", "null"]
          }
        }
      },
      "SalesComparisonReportDto": {
        "description": "Envelope comum da família `/api/reports/*`. **`summary` é do PERÍODO INTEIRO e `rows` é só da PÁGINA** — relatório paginado que soma as linhas visíveis mente exatamente onde dói: quem abre a página 1 de 500 produtos concluiria que o faturamento da empresa é o dos 100 primeiros. `total` conta as linhas AGREGADAS, não os documentos.",
        "required": ["from", "to", "granularity", "page", "pageSize", "total", "summary", "rows"],
        "type": "object",
        "properties": {
          "from": {
            "description": "O período apurado, ecoado. O cliente pediu, o servidor confirma o que usou.",
            "type": "string",
            "format": "date"
          },
          "to": {
            "type": "string",
            "format": "date"
          },
          "page": {
            "type": "integer",
            "format": "int32"
          },
          "pageSize": {
            "type": "integer",
            "format": "int32"
          },
          "total": {
            "description": "Linhas AGREGADAS no período — o denominador da paginação. Não é a contagem de documentos.",
            "type": "integer",
            "format": "int32"
          },
          "granularity": {
            "type": "string",
            "enum": ["month", "quarter", "semester", "year"],
            "description": "Ecoada."
          },
          "summary": {
            "$ref": "#/components/schemas/SalesComparisonSummaryDto"
          },
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SalesComparisonRowDto"
            }
          }
        }
      },
      "SalesComparisonSummaryDto": {
        "description": "Totais do PERÍODO inteiro, somando todos os buckets.",
        "required": ["revenueCents", "orderCount"],
        "type": "object",
        "properties": {
          "revenueCents": {
            "description": "Faturamento do recorte inteiro.",
            "type": "integer",
            "format": "int64"
          },
          "orderCount": {
            "description": "Pedidos do recorte inteiro.",
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "SalespersonRowDto": {
        "description": "Um atendente num período.",
        "required": ["salespersonName", "bucket", "quoteCount", "orderCount", "revenueCents"],
        "type": "object",
        "properties": {
          "salespersonId": {
            "type": ["string", "null"],
            "format": "uuid",
            "description": "Nulo agrega os documentos SEM atendente, e essa linha aparece de propósito: omiti-la faria a soma das linhas não bater com o `summary`, e ninguém explica a diferença olhando a tela."
          },
          "salespersonName": {
            "type": "string",
            "description": "Referência VIVA ao cadastro, como no documento. Sem atendente, o servidor escolhe o rótulo — deixar nulo faria cada tela inventar o seu."
          },
          "bucket": {
            "type": "string",
            "description": "O período desta linha, na mesma chave do comparativo."
          },
          "quoteCount": {
            "description": "Orçamentos emitidos no período.",
            "type": "integer",
            "format": "int32"
          },
          "orderCount": {
            "description": "Pedidos no período.",
            "type": "integer",
            "format": "int32"
          },
          "revenueCents": {
            "description": "Faturamento dos pedidos.",
            "type": "integer",
            "format": "int64"
          },
          "conversionPercent": {
            "description": "`orderCount / quoteCount`, texto decimal. **Nulo quando não houve orçamento** — 0% diria \"não converteu nada\" a quem não tentou nada.",
            "type": ["string", "null"]
          },
          "averageTicketCents": {
            "description": "Faturamento ÷ pedidos. Nulo sem pedido.",
            "type": ["integer", "null"],
            "format": "int64"
          }
        }
      },
      "SalespersonReportDto": {
        "description": "Envelope comum da família `/api/reports/*`. **`summary` é do PERÍODO INTEIRO e `rows` é só da PÁGINA** — relatório paginado que soma as linhas visíveis mente exatamente onde dói: quem abre a página 1 de 500 produtos concluiria que o faturamento da empresa é o dos 100 primeiros. `total` conta as linhas AGREGADAS, não os documentos.",
        "required": ["from", "to", "granularity", "page", "pageSize", "total", "summary", "rows"],
        "type": "object",
        "properties": {
          "from": {
            "description": "O período apurado, ecoado. O cliente pediu, o servidor confirma o que usou.",
            "type": "string",
            "format": "date"
          },
          "to": {
            "type": "string",
            "format": "date"
          },
          "page": {
            "type": "integer",
            "format": "int32"
          },
          "pageSize": {
            "type": "integer",
            "format": "int32"
          },
          "total": {
            "description": "Linhas AGREGADAS no período — o denominador da paginação. Não é a contagem de documentos.",
            "type": "integer",
            "format": "int32"
          },
          "granularity": {
            "type": "string",
            "enum": ["month", "quarter", "semester"],
            "description": "Ecoada. O legado imprime este demonstrativo mensal, trimestral e semestral, e são esses três."
          },
          "summary": {
            "$ref": "#/components/schemas/SalespersonSummaryDto"
          },
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SalespersonRowDto"
            }
          }
        }
      },
      "SalespersonSummaryDto": {
        "description": "Totais do PERÍODO.",
        "required": ["revenueCents", "orderCount", "quoteCount"],
        "type": "object",
        "properties": {
          "revenueCents": {
            "description": "Faturamento de todos os atendentes no recorte.",
            "type": "integer",
            "format": "int64"
          },
          "orderCount": {
            "description": "Pedidos.",
            "type": "integer",
            "format": "int32"
          },
          "quoteCount": {
            "description": "Orçamentos.",
            "type": "integer",
            "format": "int32"
          },
          "conversionPercent": {
            "description": "Conversão do recorte inteiro. Nulo sem orçamento.",
            "type": ["string", "null"]
          }
        }
      },
      "ProfessionalRankingRowDto": {
        "description": "Um profissional (arquiteto, projetista, marceneiro indicador) no período.",
        "required": [
          "professionalId",
          "professionalName",
          "orderCount",
          "revenueCents",
          "averageTicketCents"
        ],
        "type": "object",
        "properties": {
          "professionalId": {
            "type": "string",
            "format": "uuid"
          },
          "professionalName": {
            "type": "string"
          },
          "orderCount": {
            "description": "Pedidos indicados por ele no período.",
            "type": "integer",
            "format": "int32"
          },
          "revenueCents": {
            "description": "Faturamento indicado.",
            "type": "integer",
            "format": "int64"
          },
          "averageTicketCents": {
            "description": "Faturamento ÷ pedidos.",
            "type": "integer",
            "format": "int64"
          },
          "lastSaleAt": {
            "description": "A última venda indicada por ele DENTRO do período. É o que separa quem indica sempre de quem indicou muito uma vez.",
            "type": ["string", "null"],
            "format": "date"
          }
        }
      },
      "ProfessionalRankingReportDto": {
        "description": "Envelope comum da família `/api/reports/*`. **`summary` é do PERÍODO INTEIRO e `rows` é só da PÁGINA** — relatório paginado que soma as linhas visíveis mente exatamente onde dói: quem abre a página 1 de 500 produtos concluiria que o faturamento da empresa é o dos 100 primeiros. `total` conta as linhas AGREGADAS, não os documentos.",
        "required": ["from", "to", "page", "pageSize", "total", "summary", "rows"],
        "type": "object",
        "properties": {
          "from": {
            "description": "O período apurado, ecoado. O cliente pediu, o servidor confirma o que usou.",
            "type": "string",
            "format": "date"
          },
          "to": {
            "type": "string",
            "format": "date"
          },
          "page": {
            "type": "integer",
            "format": "int32"
          },
          "pageSize": {
            "type": "integer",
            "format": "int32"
          },
          "total": {
            "description": "Linhas AGREGADAS no período — o denominador da paginação. Não é a contagem de documentos.",
            "type": "integer",
            "format": "int32"
          },
          "summary": {
            "$ref": "#/components/schemas/ProfessionalRankingSummaryDto"
          },
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ProfessionalRankingRowDto"
            }
          }
        }
      },
      "ProfessionalRankingSummaryDto": {
        "description": "Totais do PERÍODO.",
        "required": ["revenueCents", "orderCount"],
        "type": "object",
        "properties": {
          "revenueCents": {
            "description": "Faturamento com profissional indicado.",
            "type": "integer",
            "format": "int64"
          },
          "orderCount": {
            "description": "Pedidos com profissional indicado.",
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "SupplierMovementRowDto": {
        "description": "Um fornecedor no período.",
        "required": [
          "supplierName",
          "quantity",
          "revenueCents",
          "lineCount",
          "orderCount",
          "productCount"
        ],
        "type": "object",
        "properties": {
          "supplierId": {
            "type": ["string", "null"],
            "format": "uuid",
            "description": "Nulo agrega as linhas sem fornecedor informado — item de produção própria, ou lançamento antigo."
          },
          "supplierName": {
            "type": "string"
          },
          "quantity": {
            "description": "Quantidade movimentada.",
            "type": "string"
          },
          "revenueCents": {
            "description": "Valor vendido de produtos deste fornecedor.",
            "type": "integer",
            "format": "int64"
          },
          "lineCount": {
            "description": "Linhas de documento.",
            "type": "integer",
            "format": "int32"
          },
          "orderCount": {
            "description": "Pedidos distintos em que ele apareceu.",
            "type": "integer",
            "format": "int32"
          },
          "productCount": {
            "description": "Produtos distintos dele que saíram.",
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "SupplierMovementReportDto": {
        "description": "Envelope comum da família `/api/reports/*`. **`summary` é do PERÍODO INTEIRO e `rows` é só da PÁGINA** — relatório paginado que soma as linhas visíveis mente exatamente onde dói: quem abre a página 1 de 500 produtos concluiria que o faturamento da empresa é o dos 100 primeiros. `total` conta as linhas AGREGADAS, não os documentos.",
        "required": ["from", "to", "page", "pageSize", "total", "summary", "rows"],
        "type": "object",
        "properties": {
          "from": {
            "description": "O período apurado, ecoado. O cliente pediu, o servidor confirma o que usou.",
            "type": "string",
            "format": "date"
          },
          "to": {
            "type": "string",
            "format": "date"
          },
          "page": {
            "type": "integer",
            "format": "int32"
          },
          "pageSize": {
            "type": "integer",
            "format": "int32"
          },
          "total": {
            "description": "Linhas AGREGADAS no período — o denominador da paginação. Não é a contagem de documentos.",
            "type": "integer",
            "format": "int32"
          },
          "summary": {
            "$ref": "#/components/schemas/SupplierMovementSummaryDto"
          },
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SupplierMovementRowDto"
            }
          }
        }
      },
      "SupplierMovementSummaryDto": {
        "description": "Totais do PERÍODO.",
        "required": ["quantity", "revenueCents"],
        "type": "object",
        "properties": {
          "quantity": {
            "description": "Quantidade total movimentada.",
            "type": "string"
          },
          "revenueCents": {
            "description": "Valor total.",
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "StockValuationRowDto": {
        "description": "Um item do estoque, hoje.",
        "required": ["variantId", "description", "quantity", "minStock", "belowMinimum"],
        "type": "object",
        "properties": {
          "variantId": {
            "type": "string",
            "format": "uuid"
          },
          "description": {
            "type": "string"
          },
          "productGroup": {
            "type": ["string", "null"],
            "description": "O TIPO DE PRODUTO do cadastro, pelo nome. Nulo quando o produto não tem tipo — que é caso real e não ausência de dado: o cadastro mínimo do legado não exigia tipo."
          },
          "quantity": {
            "description": "Saldo atual.",
            "type": "string"
          },
          "unitPriceCents": {
            "description": "O preço usado na valoração. Nulo quando o item não tem preço na empresa — e aí `valueCents` também é nulo, em vez de zero: item sem preço não vale zero, vale desconhecido, e somar zero esconderia o buraco de cadastro que este relatório é bom para achar.",
            "type": ["integer", "null"],
            "format": "int64"
          },
          "valueCents": {
            "description": "`quantity × unitPriceCents`, arredondado ao centavo. Nulo sem preço.",
            "type": ["integer", "null"],
            "format": "int64"
          },
          "minStock": {
            "description": "Mínimo cadastrado na empresa.",
            "type": "string"
          },
          "belowMinimum": {
            "type": "boolean",
            "description": "`quantity < minStock`. Calculado pelo servidor porque a mesma comparação em duas telas vira duas respostas no dia em que uma esquecer o `<=`."
          }
        }
      },
      "StockValuationReportDto": {
        "description": "Envelope comum da família `/api/reports/*`. **`summary` é do PERÍODO INTEIRO e `rows` é só da PÁGINA** — relatório paginado que soma as linhas visíveis mente exatamente onde dói: quem abre a página 1 de 500 produtos concluiria que o faturamento da empresa é o dos 100 primeiros. `total` conta as linhas AGREGADAS, não os documentos.",
        "required": ["asOf", "valuationBasis", "page", "pageSize", "total", "summary", "rows"],
        "type": "object",
        "properties": {
          "page": {
            "type": "integer",
            "format": "int32"
          },
          "pageSize": {
            "type": "integer",
            "format": "int32"
          },
          "total": {
            "description": "Linhas AGREGADAS no período — o denominador da paginação. Não é a contagem de documentos.",
            "type": "integer",
            "format": "int32"
          },
          "asOf": {
            "type": "string",
            "format": "date-time",
            "description": "O INSTANTE da foto. Estoque não tem período — tem agora — e sem este carimbo o CSV exportado às 9h é indistinguível do das 18h."
          },
          "valuationBasis": {
            "type": "string",
            "enum": ["sale_price"],
            "description": "Sobre qual preço a valoração foi feita. Hoje só existe `sale_price`: **o custo ainda não é dado do sistema** (decisão D1, Custo+Índice, pendente). O campo nasce declarado para que, quando o custo entrar, a tela antiga não passe a mostrar outro número sem avisar — hoje ela pode dizer \"a preço de venda\" com base no que o servidor afirma, não no que ela supõe."
          },
          "summary": {
            "$ref": "#/components/schemas/StockValuationSummaryDto"
          },
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/StockValuationRowDto"
            }
          }
        }
      },
      "StockValuationSummaryDto": {
        "description": "Totais do ESTOQUE inteiro, não da página.",
        "required": ["valueCents", "itemCount", "belowMinimumCount", "withoutPriceCount"],
        "type": "object",
        "properties": {
          "valueCents": {
            "description": "Valor total do estoque considerando só os itens COM preço.",
            "type": "integer",
            "format": "int64"
          },
          "itemCount": {
            "description": "Itens no recorte.",
            "type": "integer",
            "format": "int32"
          },
          "belowMinimumCount": {
            "description": "Quantos estão abaixo do mínimo.",
            "type": "integer",
            "format": "int32"
          },
          "withoutPriceCount": {
            "description": "Quantos ficaram de fora do valor por não terem preço. **É a medida da confiança no total** — 3 de 4000 é ruído, 900 de 4000 quer dizer que o número lá em cima não significa nada.",
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "StockAgingRowDto": {
        "description": "Um item parado.",
        "required": ["variantId", "description", "quantity"],
        "type": "object",
        "properties": {
          "variantId": {
            "type": "string",
            "format": "uuid"
          },
          "description": {
            "type": "string"
          },
          "productGroup": {
            "description": "O TIPO DE PRODUTO do cadastro, pelo nome. Nulo quando o produto não tem tipo.",
            "type": ["string", "null"]
          },
          "quantity": {
            "description": "Saldo atual — quanto dinheiro está parado nele.",
            "type": "string"
          },
          "valueCents": {
            "description": "Saldo × preço. Nulo sem preço.",
            "type": ["integer", "null"],
            "format": "int64"
          },
          "lastSaleAt": {
            "description": "Data do último PEDIDO que levou este item. Nulo = nunca vendeu.",
            "type": ["string", "null"],
            "format": "date"
          },
          "daysWithoutSale": {
            "description": "Dias desde a última venda. **Nulo quando nunca vendeu** — e não um número gigante: \"nunca\" e \"há 4 anos\" são perguntas diferentes, e o item que nunca vendeu pode ter entrado ontem.",
            "type": ["integer", "null"],
            "format": "int32"
          },
          "daysInStock": {
            "description": "Dias desde o primeiro movimento de entrada — é ele que distingue o item que nunca vendeu porque chegou ontem do que nunca vendeu em três anos.",
            "type": ["integer", "null"],
            "format": "int32"
          }
        }
      },
      "StockAgingReportDto": {
        "description": "Envelope comum da família `/api/reports/*`. **`summary` é do PERÍODO INTEIRO e `rows` é só da PÁGINA** — relatório paginado que soma as linhas visíveis mente exatamente onde dói: quem abre a página 1 de 500 produtos concluiria que o faturamento da empresa é o dos 100 primeiros. `total` conta as linhas AGREGADAS, não os documentos.",
        "required": ["asOf", "page", "pageSize", "total", "summary", "rows"],
        "type": "object",
        "properties": {
          "page": {
            "type": "integer",
            "format": "int32"
          },
          "pageSize": {
            "type": "integer",
            "format": "int32"
          },
          "total": {
            "description": "Linhas AGREGADAS no período — o denominador da paginação. Não é a contagem de documentos.",
            "type": "integer",
            "format": "int32"
          },
          "asOf": {
            "type": "string",
            "format": "date-time",
            "description": "O instante da foto — `daysWithoutSale` é contado a partir dele."
          },
          "summary": {
            "$ref": "#/components/schemas/StockAgingSummaryDto"
          },
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/StockAgingRowDto"
            }
          }
        }
      },
      "StockAgingSummaryDto": {
        "description": "Totais do recorte.",
        "required": ["itemCount", "neverSoldCount", "valueCents"],
        "type": "object",
        "properties": {
          "itemCount": {
            "description": "Itens no recorte.",
            "type": "integer",
            "format": "int32"
          },
          "neverSoldCount": {
            "description": "Quantos nunca venderam.",
            "type": "integer",
            "format": "int32"
          },
          "valueCents": {
            "description": "Dinheiro parado nesses itens (só os com preço).",
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "QuoteVsStockRowDto": {
        "description": "Um item pedido em orçamento aberto, contra o que há em casa.",
        "required": [
          "variantId",
          "description",
          "quotedQuantity",
          "stockQuantity",
          "shortageQuantity",
          "quoteCount",
          "sufficient"
        ],
        "type": "object",
        "properties": {
          "variantId": {
            "type": "string",
            "format": "uuid"
          },
          "description": {
            "type": "string"
          },
          "quotedQuantity": {
            "description": "Somado dos orçamentos ATIVOS no período.",
            "type": "string"
          },
          "stockQuantity": {
            "description": "Saldo atual na empresa.",
            "type": "string"
          },
          "shortageQuantity": {
            "description": "`quotedQuantity - stockQuantity`, **zero quando sobra** — número negativo aqui viraria \"falta -5\" na tela e alguém compraria assim mesmo.",
            "type": "string"
          },
          "quoteCount": {
            "description": "Em quantos orçamentos ele aparece.",
            "type": "integer",
            "format": "int32"
          },
          "sufficient": {
            "type": "boolean",
            "description": "Tem em casa para atender tudo o que foi orçado."
          }
        }
      },
      "QuoteVsStockReportDto": {
        "description": "Envelope comum da família `/api/reports/*`. **`summary` é do PERÍODO INTEIRO e `rows` é só da PÁGINA** — relatório paginado que soma as linhas visíveis mente exatamente onde dói: quem abre a página 1 de 500 produtos concluiria que o faturamento da empresa é o dos 100 primeiros. `total` conta as linhas AGREGADAS, não os documentos.",
        "required": ["from", "to", "page", "pageSize", "total", "summary", "rows"],
        "type": "object",
        "properties": {
          "from": {
            "description": "O período apurado, ecoado. O cliente pediu, o servidor confirma o que usou.",
            "type": "string",
            "format": "date"
          },
          "to": {
            "type": "string",
            "format": "date"
          },
          "page": {
            "type": "integer",
            "format": "int32"
          },
          "pageSize": {
            "type": "integer",
            "format": "int32"
          },
          "total": {
            "description": "Linhas AGREGADAS no período — o denominador da paginação. Não é a contagem de documentos.",
            "type": "integer",
            "format": "int32"
          },
          "summary": {
            "$ref": "#/components/schemas/QuoteVsStockSummaryDto"
          },
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/QuoteVsStockRowDto"
            }
          }
        }
      },
      "QuoteVsStockSummaryDto": {
        "description": "Totais do recorte.",
        "required": ["variantCount", "shortageCount"],
        "type": "object",
        "properties": {
          "variantCount": {
            "description": "Produtos distintos orçados no período.",
            "type": "integer",
            "format": "int32"
          },
          "shortageCount": {
            "description": "Quantos deles não têm saldo suficiente.",
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "BirthdayRowDto": {
        "description": "Um aniversariante do mês.",
        "required": [
          "partnerId",
          "name",
          "birthDate",
          "day",
          "age",
          "isCustomer",
          "isProfessional"
        ],
        "type": "object",
        "properties": {
          "partnerId": {
            "type": "string",
            "format": "uuid"
          },
          "name": {
            "type": "string",
            "description": "Nome fantasia quando há, razão social senão — o mesmo critério de exibição das outras telas."
          },
          "birthDate": {
            "type": "string",
            "format": "date",
            "description": "A data completa. O ANO importa: é dele que sai a idade, e sem ele o cartão de 50 anos passa batido."
          },
          "day": {
            "description": "Dia do mês, repetido para a tela agrupar sem reparsear a data.",
            "type": "integer",
            "format": "int32"
          },
          "age": {
            "description": "Idade que a pessoa COMPLETA neste aniversário — não a de hoje. Quem faz 40 no dia 28 deve aparecer como 40 na lista do dia 1º, senão a mensagem sai com o número errado justamente na data redonda.",
            "type": "integer",
            "format": "int32"
          },
          "mobilePhone": {
            "type": ["string", "null"]
          },
          "email": {
            "type": ["string", "null"]
          },
          "isCustomer": {
            "type": "boolean"
          },
          "isProfessional": {
            "type": "boolean"
          }
        }
      },
      "BirthdaysReportDto": {
        "description": "Envelope comum da família `/api/reports/*`. **`summary` é do PERÍODO INTEIRO e `rows` é só da PÁGINA** — relatório paginado que soma as linhas visíveis mente exatamente onde dói: quem abre a página 1 de 500 produtos concluiria que o faturamento da empresa é o dos 100 primeiros. `total` conta as linhas AGREGADAS, não os documentos.",
        "required": ["month", "page", "pageSize", "total", "summary", "rows"],
        "type": "object",
        "properties": {
          "page": {
            "type": "integer",
            "format": "int32"
          },
          "pageSize": {
            "type": "integer",
            "format": "int32"
          },
          "total": {
            "description": "Linhas AGREGADAS no período — o denominador da paginação. Não é a contagem de documentos.",
            "type": "integer",
            "format": "int32"
          },
          "month": {
            "description": "O mês apurado, ecoado.",
            "type": "integer",
            "format": "int32"
          },
          "summary": {
            "$ref": "#/components/schemas/BirthdaysSummaryDto"
          },
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/BirthdayRowDto"
            }
          }
        }
      },
      "BirthdaysSummaryDto": {
        "description": "Totais do mês.",
        "required": ["partnerCount", "withoutContactCount"],
        "type": "object",
        "properties": {
          "partnerCount": {
            "description": "Aniversariantes no mês.",
            "type": "integer",
            "format": "int32"
          },
          "withoutContactCount": {
            "description": "Quantos não têm telefone nem e-mail — a lista serve para FALAR com eles, e o aniversariante inalcançável é a falha de cadastro que o relatório revela.",
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "SupplierBuyingCompanyDto": {
        "description": "Proposto. Uma vigência de EMPRESA COMPRADORA do fornecedor — a aba `Histórico Emp. Compradora` do cadastro de fornecedor no legado.\n\n**Por que é lista com vigência e não um campo.** No legado a Empresa Compradora aparece em fornecedor, produto e ordem de compra, e é ela que decide se a compra sai pela Vertz ou pela Via HF. O legado guarda o HISTÓRICO dela numa aba própria justamente porque a resposta muda no tempo: o fornecedor que era comprado pela Via em 2023 é comprado pela Vertz hoje, e a ordem emitida naquele ano tem de continuar dizendo a verdade sobre quem comprou. Um campo único reescreveria o passado a cada troca.\n\n**A ordem de compra NÃO lê esta lista para congelar a empresa** — ela guarda a sua própria `buyingTenantId`, copiada na emissão. Esta lista é o PADRÃO que a tela sugere, não a fonte do que já foi emitido.\n\nA empresa é um TENANT (`AuthTenantDto.tenantId`), não um cadastro à parte: multi-empresa neste produto é multi-tenant, e o legado confirma o modelo — 99,91% do volume dele está na empresa 1, e a multi-empresa que ele de fato opera é a de COMPRA.",
        "required": ["tenantId", "tenantName", "validFrom"],
        "type": "object",
        "properties": {
          "tenantId": {
            "description": "A empresa compradora — `AuthTenantDto.tenantId`.",
            "type": "string",
            "format": "uuid"
          },
          "tenantName": {
            "description": "Nome da empresa, ecoado para a tela não ter de resolver o id.",
            "type": "string"
          },
          "validFrom": {
            "description": "Início da vigência. Data, não instante: a troca de empresa compradora é decisão comercial com data, e hora aqui seria precisão inventada.",
            "type": "string",
            "format": "date"
          },
          "validTo": {
            "description": "Fim da vigência, ou `null` na linha VIGENTE. Só uma linha por fornecedor pode ter `validTo` nulo — duas empresas comprando ao mesmo tempo do mesmo fornecedor não é estado, é cadastro por terminar.",
            "type": ["string", "null"],
            "format": "date"
          }
        }
      },
      "SupplierBuyingCompanyWriteRequest": {
        "description": "Proposto. Uma linha de vigência de empresa compradora, na escrita do parceiro. `tenantName` não entra: ele é ecoado na leitura, e aceitá-lo aqui deixaria o cliente renomear a empresa de outra pessoa pelo cadastro do fornecedor.",
        "required": ["tenantId", "validFrom"],
        "type": "object",
        "properties": {
          "tenantId": {
            "type": "string",
            "format": "uuid"
          },
          "validFrom": {
            "type": "string",
            "format": "date"
          },
          "validTo": {
            "type": ["string", "null"],
            "format": "date"
          }
        }
      },
      "SupplierGroupMinimumDto": {
        "description": "Proposto. O FATURAMENTO MÍNIMO do fornecedor para um GRUPO DE PRODUTO — a exceção à regra geral de `minimumBillingCents`.\n\n**Por que por grupo, e por que só agora.** O fornecedor que exige R$ 3.000 por ordem costuma exigir outra coisa para a linha de perfil ou para a de LED, e o legado carrega isso: `Forma_PagamentoGrupProd` já pendura desconto e acréscimo por grupo de produto. O `PaymentTermDto` deste contrato declarou essa mesma dívida por escrito e disse que ela só entraria quando o grupo de produto tivesse id — \"casar por texto faria PENDENTES e Pendentes renderem descontos diferentes no mesmo documento\". **O id existe desde `QuoteItemDto.productGroupId`** (kind `GRUPO_PRODUTO` de `catalog-lookups`), e é nele que esta linha pendura. A dívida do `PaymentTermDto` continua aberta — ela é sobre desconto na CONDIÇÃO DE PAGAMENTO, não sobre mínimo do fornecedor —, mas o argumento que a bloqueava não vale mais.\n\n**Como as duas se combinam:** o mínimo por grupo SUBSTITUI o geral para os itens daquele grupo, não soma com ele. Uma ordem com itens de dois grupos com mínimo próprio tem de atender os dois; os itens de grupo sem linha aqui somam contra `minimumBillingCents`. Somar tudo num total só deixaria o fornecedor que exige R$ 3.000 de LED aceitar uma ordem de R$ 3.000 de perfil.",
        "required": ["productGroupId", "productGroupName", "minimumBillingCents"],
        "type": "object",
        "properties": {
          "productGroupId": {
            "description": "O grupo — `CatalogLookupDto.id`, kind `GRUPO_PRODUTO`. O mesmo id que a linha do documento carrega em `productGroupId`.",
            "type": "string",
            "format": "uuid"
          },
          "productGroupName": {
            "description": "Nome do grupo, ecoado. NÃO é chave: ver a descrição de `QuoteItemDto.productGroupId`.",
            "type": "string"
          },
          "minimumBillingCents": {
            "description": "O mínimo em CENTAVOS para este grupo. Inteiro, como todo dinheiro deste contrato.",
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "SupplierGroupMinimumWriteRequest": {
        "description": "Proposto. Uma linha de faturamento mínimo por grupo, na escrita do parceiro.",
        "required": ["productGroupId", "minimumBillingCents"],
        "type": "object",
        "properties": {
          "productGroupId": {
            "type": "string",
            "format": "uuid"
          },
          "minimumBillingCents": {
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "PurchaseRequestItemDto": {
        "description": "Proposto. Uma linha do PEDIDO DE COMPRA — o que se quer comprar, de quem, e para quê.\n\nA linha carrega o FORNECEDOR porque o pedido tem N deles: no legado a compra nasce da necessidade (o pedido de venda fechado, ou o saldo abaixo do mínimo), e a necessidade não chega separada por fornecedor. Quem separa é a ORDEM, que é por fornecedor — e é por isso que um pedido vira várias ordens, e uma ordem agrupa vários pedidos.",
        "required": [
          "lineNumber",
          "description",
          "quantity",
          "destination",
          "supplierId",
          "supplierName",
          "status"
        ],
        "type": "object",
        "properties": {
          "lineNumber": {
            "description": "A ordem da linha DENTRO do pedido, 1-based. É por ela que a ordem de compra rastreia a origem (`PurchaseOrderItemDto.sourceLineNumber`) — o id da linha não sai daqui, e o par (pedido, número) já a identifica.",
            "type": "integer",
            "format": "int32"
          },
          "variantId": {
            "description": "A variante do catálogo, quando a linha aponta para uma. `null` no item que o operador digitou à mão — o \"Pré Produto\" do legado, que existe justamente para comprar o que ainda não está cadastrado.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "description": {
            "description": "A descrição CONGELADA na emissão. Não é lida da variante em tempo de leitura, pelo mesmo motivo que a linha do orçamento congela a sua: renomear o produto não pode reescrever o que foi pedido.",
            "type": "string"
          },
          "finish": {
            "description": "Acabamento. Junto de `size`, é a variante em texto — o legado compra por acabamento e tamanho, e a tela de compras é a mesma do orçamento nesse ponto.",
            "type": ["string", "null"]
          },
          "size": {
            "description": "Tamanho.",
            "type": ["string", "null"]
          },
          "unit": {
            "description": "Unidade de COMPRA, que pode não ser a de venda: o legado tem unidade de entrada ≠ unidade de saída com fator de conversão, e este campo é a de entrada.",
            "type": ["string", "null"]
          },
          "quantity": {
            "description": "Quantidade pedida. Fracionária — metro de fita de LED é a unidade normal deste negócio.",
            "type": "number",
            "format": "double"
          },
          "destination": {
            "description": "O DESTINO da peça: `stock` repõe o galpão, `sale` é encomenda de um cliente. É o eixo que separa as duas metades do módulo de compras do legado — os botões \"Produtos Estoque\" e \"Produtos Pedidos\" da ordem de compra —, e por isso é campo da LINHA e não do documento: a mesma ordem mistura reposição e encomenda, e é exatamente essa mistura que faz o fornecedor atingir o faturamento mínimo.\n\n**`sale` sem `orderId` no pedido é recusado** (400): encomenda sem venda que a encomendou é reposição com outro nome, e é ela que faria a previsão de chegada mostrar cliente vazio.",
            "type": "string",
            "enum": ["stock", "sale"]
          },
          "supplierId": {
            "description": "De QUEM se quer comprar esta linha — `PartnerDto.id` com `isSupplier`.",
            "type": "string",
            "format": "uuid"
          },
          "supplierName": {
            "description": "Nome do fornecedor, ecoado.",
            "type": "string"
          },
          "sourceOrderItemLine": {
            "description": "A linha do PEDIDO DE VENDA que originou esta — `OrderItemDto.lineNumber` dentro do `orderId` do cabeçalho. `null` quando `destination` é `stock`, e obrigatório quando é `sale`: é este par que faz a previsão de chegada dizer para QUAL cliente a peça está vindo.",
            "type": ["integer", "null"],
            "format": "int32"
          },
          "purchaseOrderId": {
            "description": "A ORDEM que já levou esta linha, ou `null` enquanto ela está aberta. Derivado — quem escreve é a criação da ordem, nunca o PUT do pedido.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "purchaseOrderNumber": {
            "description": "Número da ordem que levou a linha, ecoado.",
            "type": ["string", "null"]
          },
          "status": {
            "description": "Estado da LINHA, que não é o do pedido: um pedido com metade das linhas em ordem fica `partially_ordered`, e é a linha que diz qual metade. `cancelled` é linha desistida sem cancelar o pedido inteiro.",
            "type": "string",
            "enum": ["open", "ordered", "cancelled"]
          },
          "notes": {
            "description": "Observação da linha — onde vai o recado ao comprador que não cabe em campo nenhum.",
            "type": ["string", "null"]
          }
        }
      },
      "PurchaseRequestDto": {
        "description": "Proposto. Um PEDIDO DE COMPRA — a NECESSIDADE de comprar, antes de ela virar compromisso com fornecedor. É o menu `Compras → Pedido` do legado, e o primeiro elo da cadeia real que o comparativo levantou: **Pedido de Venda → Pedido de Compra → Ordem de Compra por fornecedor → envio → recebimento**.\n\n**Por que pedido e ordem são DOIS documentos e não um com status.** Eles têm cardinalidade diferente em relação ao fornecedor: o pedido tem N (a necessidade vem misturada), a ordem tem 1 (o compromisso é com um fornecedor só). Um documento único obrigaria a escolher — ou quebrar a necessidade em N documentos na hora errada (antes de saber se dá para agrupar com a de outro pedido e atingir o faturamento mínimo), ou deixar a ordem com N fornecedores, que é o que o faturamento mínimo, a transportadora e a condição de pagamento não admitem.\n\n**A relação com a ordem é N↔N, rastreada POR ITEM.** Um pedido vira várias ordens (uma por fornecedor) e uma ordem agrupa vários pedidos (para atingir o mínimo). Guardar o elo no cabeçalho não representaria nem um caso nem o outro; ele mora em `PurchaseOrderItemDto.sourceRequestId` + `sourceLineNumber`.\n\n**`status` é DERIVADO das linhas** e não escrito à mão: `open` enquanto nenhuma linha entrou em ordem, `partially_ordered` com algumas, `ordered` com todas. Só `cancelled` é decisão de gente, e é por isso que ele tem operação própria (`POST /{id}/cancel`) em vez de campo no PUT — o mesmo desenho de `quotes` e `orders`.",
        "required": ["id", "number", "status", "issuedAt", "itemCount", "items"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "number": {
            "description": "O número do documento na empresa. Texto e não inteiro, pelo mesmo motivo de `OrderDto.number`: o legado traz numeração com prefixo e série.",
            "type": "string"
          },
          "status": {
            "description": "Derivado das linhas, exceto `cancelled`. Ver a descrição do schema.",
            "type": "string",
            "enum": ["open", "partially_ordered", "ordered", "cancelled"]
          },
          "issuedAt": {
            "description": "Data de emissão do pedido.",
            "type": "string",
            "format": "date"
          },
          "orderId": {
            "description": "O PEDIDO DE VENDA que originou este pedido de compra, quando ele nasceu de uma venda. `null` no pedido de REPOSIÇÃO, que nasce do saldo e não de cliente nenhum.\n\nO elo é do CABEÇALHO e não da linha porque o legado o descreve assim (\"Pedido de Compra: vinculado a Ped. Venda\"), e porque um pedido de compra que misturasse duas vendas não teria a quem mostrar a previsão de chegada. As LINHAS apontam para as linhas daquela venda, em `sourceOrderItemLine`.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "orderNumber": {
            "description": "Número do pedido de venda de origem, ecoado.",
            "type": ["string", "null"]
          },
          "customerId": {
            "description": "Cliente do pedido de venda de origem, ecoado — a previsão de chegada precisa dele e não vai buscar a venda inteira para ter um nome.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "customerName": {
            "description": "Nome do cliente, ecoado.",
            "type": ["string", "null"]
          },
          "itemCount": {
            "description": "Quantas linhas o pedido tem. Existe para a LISTAGEM: contar itens na tela exigiria trazer as linhas de todos os pedidos da página.",
            "type": "integer",
            "format": "int32"
          },
          "items": {
            "description": "As linhas. Vêm na listagem TAMBÉM, e é decisão: o pedido de compra é documento curto (o legado tem 34.863 linhas em `Pedido_compra_det` para o histórico inteiro), e a tela de compras precisa do fornecedor de cada linha para agrupar em ordem sem uma requisição por pedido.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PurchaseRequestItemDto"
            }
          },
          "notes": {
            "description": "Observação do documento.",
            "type": ["string", "null"]
          }
        }
      },
      "PurchaseRequestItemWriteRequest": {
        "description": "Proposto. Uma linha na escrita do pedido de compra. Os campos DERIVADOS não entram — `status`, `purchaseOrderId` e `purchaseOrderNumber` são escritos pela criação da ordem, e aceitá-los aqui deixaria o cliente declarar que a linha já foi comprada.",
        "required": ["lineNumber", "description", "quantity", "destination", "supplierId"],
        "type": "object",
        "properties": {
          "lineNumber": {
            "type": "integer",
            "format": "int32"
          },
          "variantId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "description": {
            "type": "string"
          },
          "finish": {
            "type": ["string", "null"]
          },
          "size": {
            "type": ["string", "null"]
          },
          "unit": {
            "type": ["string", "null"]
          },
          "quantity": {
            "type": "number",
            "format": "double"
          },
          "destination": {
            "type": "string",
            "enum": ["stock", "sale"]
          },
          "supplierId": {
            "type": "string",
            "format": "uuid"
          },
          "sourceOrderItemLine": {
            "type": ["integer", "null"],
            "format": "int32"
          },
          "notes": {
            "type": ["string", "null"]
          }
        }
      },
      "PurchaseRequestWriteRequest": {
        "description": "Proposto. Cria ou SUBSTITUI um pedido de compra inteiro — PUT troca o registro completo, `items` incluído.\n\n**O PUT recusa (409) o pedido que já tem linha em ordem.** Reescrever a linha que virou compromisso com fornecedor mudaria o que a ordem diz ter comprado, e a ordem já saiu. Nesse estado o caminho é cancelar a linha e abrir outra, que é o que deixa rastro.",
        "required": ["issuedAt", "items"],
        "type": "object",
        "properties": {
          "issuedAt": {
            "type": "string",
            "format": "date"
          },
          "orderId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PurchaseRequestItemWriteRequest"
            }
          },
          "notes": {
            "type": ["string", "null"]
          }
        }
      },
      "PagedResultOfPurchaseRequestDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PurchaseRequestDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "PurchaseOrderItemDto": {
        "description": "Proposto. Uma linha da ORDEM DE COMPRA, com a ORIGEM rastreada.\n\n**`sourceRequestId` + `sourceLineNumber` são o que faz o agrupamento reversível.** A ordem agrupa itens de N pedidos; sem o par, \"de qual pedido veio esta peça\" só se responderia casando descrição e quantidade — que é adivinhação assim que dois pedidos pedem a mesma peça do mesmo fornecedor, que é o caso NORMAL num negócio de reposição.",
        "required": [
          "lineNumber",
          "sourceRequestId",
          "sourceRequestNumber",
          "sourceLineNumber",
          "description",
          "quantity",
          "unitCostCents",
          "totalCents",
          "destination"
        ],
        "type": "object",
        "properties": {
          "lineNumber": {
            "description": "A ordem da linha DENTRO da ordem de compra, 1-based.",
            "type": "integer",
            "format": "int32"
          },
          "sourceRequestId": {
            "description": "O PEDIDO DE COMPRA de onde esta linha veio.",
            "type": "string",
            "format": "uuid"
          },
          "sourceRequestNumber": {
            "description": "Número do pedido de origem, ecoado — a tela mostra \"veio do pedido 1234\" sem resolver o id.",
            "type": "string"
          },
          "sourceLineNumber": {
            "description": "A linha DAQUELE pedido — `PurchaseRequestItemDto.lineNumber`.",
            "type": "integer",
            "format": "int32"
          },
          "variantId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "description": {
            "description": "Descrição congelada na emissão da ordem.",
            "type": "string"
          },
          "finish": {
            "type": ["string", "null"]
          },
          "size": {
            "type": ["string", "null"]
          },
          "unit": {
            "type": ["string", "null"]
          },
          "quantity": {
            "description": "Quantidade desta linha na ordem. **Pode ser menor que a da linha do pedido**: o fornecedor atende parcialmente, e o resto continua aberto no pedido para entrar noutra ordem.",
            "type": "number",
            "format": "double"
          },
          "unitCostCents": {
            "description": "Custo unitário NEGOCIADO, em centavos. É o preço de COMPRA — não tem relação com o de venda, que sai do índice por fornecedor.",
            "type": "integer",
            "format": "int64"
          },
          "totalCents": {
            "description": "Total da linha em centavos. Vem calculado do servidor: `quantity × unitCostCents` arredondado uma vez, e não N vezes na tela.",
            "type": "integer",
            "format": "int64"
          },
          "destination": {
            "description": "O DESTINO da peça: `stock` repõe o galpão, `sale` é encomenda de um cliente. É o eixo que separa as duas metades do módulo de compras do legado — os botões \"Produtos Estoque\" e \"Produtos Pedidos\" da ordem de compra —, e por isso é campo da LINHA e não do documento: a mesma ordem mistura reposição e encomenda, e é exatamente essa mistura que faz o fornecedor atingir o faturamento mínimo.\n\n**`sale` sem `orderId` no pedido é recusado** (400): encomenda sem venda que a encomendou é reposição com outro nome, e é ela que faria a previsão de chegada mostrar cliente vazio.",
            "type": "string",
            "enum": ["stock", "sale"]
          },
          "productGroupId": {
            "description": "Grupo de produto da linha — `CatalogLookupDto.id`, kind `GRUPO_PRODUTO`. É por ele que a linha entra na conta do faturamento mínimo POR GRUPO do fornecedor.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "productGroupName": {
            "description": "Nome do grupo, ecoado.",
            "type": ["string", "null"]
          }
        }
      },
      "PurchaseOrderDto": {
        "description": "Proposto. Uma ORDEM DE COMPRA — o compromisso com UM fornecedor. Menu `Compras → Ordem` do legado.\n\n**Um fornecedor, N pedidos.** É a inversão de cardinalidade que justifica o segundo documento: só com um fornecedor só fazem sentido o faturamento mínimo, a transportadora, a condição de pagamento e a data prometida. E é agrupando pedidos de dias diferentes que o comprador chega ao mínimo — o que o legado apoia com os botões \"Produtos Estoque\" e \"Produtos Pedidos\", que enchem a mesma ordem com reposição e encomenda.\n\n**As quatro datas são quatro fatos diferentes, e nenhuma deriva da outra:** `orderedAt` é quando a ordem foi montada, `sentAt` é quando ela foi de fato mandada ao fornecedor (e é o que a transição `send` grava), `expectedAt` é a data PROMETIDA e `rescheduledAt` é a data reprometida depois de o fornecedor furar a primeira. Guardar só a última destruiria a medição que o negócio mais quer — de quanto o fornecedor atrasa —, porque a promessa original teria sido sobrescrita pela desculpa.\n\n**Não há DELETE**, como em todo o contrato: ordem some é ordem que o fornecedor recebeu e o sistema esqueceu. Desistir é `POST /{id}/cancel`.",
        "required": [
          "id",
          "number",
          "status",
          "supplierId",
          "supplierName",
          "buyingTenantId",
          "buyingTenantName",
          "orderedAt",
          "subtotalCents",
          "totalCents",
          "items"
        ],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "number": {
            "description": "O número da ordem na empresa.",
            "type": "string"
          },
          "status": {
            "description": "`draft` enquanto a ordem está sendo montada, `sent` depois de `POST /{id}/send`, `cancelled` na desistência.\n\n**Reagendar NÃO é status.** `POST /{id}/reschedule` move `expectedAt` para `rescheduledAt` e a ordem continua `sent` — uma ordem reagendada é uma ordem enviada cuja data mudou, e um status próprio faria a listagem de \"enviadas\" perder exatamente as que mais interessam ao comprador.",
            "type": "string",
            "enum": ["draft", "sent", "cancelled"]
          },
          "supplierId": {
            "description": "O ÚNICO fornecedor da ordem.",
            "type": "string",
            "format": "uuid"
          },
          "supplierName": {
            "type": "string"
          },
          "buyingTenantId": {
            "description": "A EMPRESA COMPRADORA — qual empresa do grupo está comprando. Copiada na emissão a partir da vigência do fornecedor (`SupplierBuyingCompanyDto`), e congelada aqui: trocar a empresa compradora do cadastro amanhã não pode reescrever quem comprou ontem.",
            "type": "string",
            "format": "uuid"
          },
          "buyingTenantName": {
            "type": "string"
          },
          "orderedAt": {
            "description": "Data em que a ordem foi montada.",
            "type": "string",
            "format": "date"
          },
          "sentAt": {
            "description": "Data em que a ordem foi ENVIADA ao fornecedor. `null` enquanto `draft`.",
            "type": ["string", "null"],
            "format": "date"
          },
          "expectedAt": {
            "description": "Data PROMETIDA na emissão. Sugerida a partir de `PartnerDto.deliveryDays`, mas guardada como data acordada — ver a descrição daquele campo.",
            "type": ["string", "null"],
            "format": "date"
          },
          "rescheduledAt": {
            "description": "A data REPROMETIDA, quando houve reagendamento. `null` na ordem nunca reagendada. A previsão de chegada usa esta quando existe, e `expectedAt` quando não — mas as duas ficam, e é a diferença entre elas que mede o atraso do fornecedor.",
            "type": ["string", "null"],
            "format": "date"
          },
          "rescheduleReason": {
            "description": "O motivo do último reagendamento, como o comprador o registrou.",
            "type": ["string", "null"]
          },
          "minimumBillingCents": {
            "description": "O faturamento mínimo do fornecedor ECOADO na emissão — cópia de `PartnerDto.minimumBillingCents` no momento em que a ordem foi criada.\n\nEcoado e não lido em tempo de leitura porque a validação de ontem tem de continuar explicável: o fornecedor sobe o mínimo, e a ordem antiga passaria a parecer irregular sem que nada nela tivesse mudado.",
            "type": ["integer", "null"],
            "format": "int64"
          },
          "carrierId": {
            "description": "A TRANSPORTADORA — `PartnerDto.id`. Transportadora é parceiro como qualquer outro no cadastro do legado (item próprio do menu Cadastros), e não ganha entidade nova.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "carrierName": {
            "type": ["string", "null"]
          },
          "paymentTermId": {
            "description": "A CONDIÇÃO DE PAGAMENTO da ordem — `PaymentTermDto.id`, o cadastro que já existe. A aba Pagamento da ordem de compra REUSA o que a venda usa: parcelamento é parcelamento, e duplicar o cadastro faria a mesma condição existir com dois ids.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "paymentTermName": {
            "type": ["string", "null"]
          },
          "discountPercent": {
            "description": "Desconto GERAL da ordem, em percentual com **4 casas decimais**, transportado como inteiro escalado por 10.000: `10000` = 1%, `1000000` = 100%.\n\n**A escala é a da casa, e este campo nasceu fora dela.** `QuoteDetailDto`, `OrderDetailDto` e as quatro de linha e de grupo já dizem `10000` = 1%; só esta dizia \"pontos percentuais inteiros\". Duas escalas para o mesmo nome não dão erro, dão desconto errado: quem segue a convenção manda `50000` para 5% e leva recusa, e quem manda `5` recebe 5% aqui e 0,0005% em todo outro documento. O `cabinet-erp-api` já corrigiu o lado dele — a migração `0057` trocou o `CHECK` de `BETWEEN 0 AND 100` para `BETWEEN 0 AND 1000000` e converteu o que existisse.\n\nInteiro e não `number` pela razão de sempre: float em desconto empilha arredondamento em cima de dinheiro.",
            "type": "integer",
            "format": "int32"
          },
          "surchargeCents": {
            "description": "ACRÉSCIMO em centavos — frete cobrado à parte, embalagem, taxa. Valor e não percentual: o que o fornecedor acrescenta chega em reais.",
            "type": "integer",
            "format": "int64"
          },
          "subtotalCents": {
            "description": "Soma das linhas, antes de desconto e acréscimo.",
            "type": "integer",
            "format": "int64"
          },
          "totalCents": {
            "description": "O total da ordem: `subtotalCents` menos o desconto, mais o acréscimo.\n\n**Não é contra ele que o faturamento mínimo é conferido**, e a frase que dizia isso contradizia o `POST` desta mesma família. A conferência é contra a soma das LINHAS com o desconto geral aplicado e **sem** o acréscimo: frete e embalagem não são mercadoria, e deixá-los completar o mínimo faria o fornecedor recusar o faturamento que este contrato aprovou.",
            "type": "integer",
            "format": "int64"
          },
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PurchaseOrderItemDto"
            }
          },
          "notes": {
            "type": ["string", "null"]
          }
        }
      },
      "PurchaseOrderItemWriteRequest": {
        "description": "Proposto. Uma linha na escrita da ordem. `totalCents` não entra — quem multiplica é o servidor, e aceitar o total do cliente deixaria a soma da tela virar o valor gravado.",
        "required": [
          "lineNumber",
          "sourceRequestId",
          "sourceLineNumber",
          "quantity",
          "unitCostCents"
        ],
        "type": "object",
        "properties": {
          "lineNumber": {
            "type": "integer",
            "format": "int32"
          },
          "sourceRequestId": {
            "type": "string",
            "format": "uuid"
          },
          "sourceLineNumber": {
            "type": "integer",
            "format": "int32"
          },
          "quantity": {
            "type": "number",
            "format": "double"
          },
          "unitCostCents": {
            "type": "integer",
            "format": "int64"
          },
          "productGroupId": {
            "type": ["string", "null"],
            "format": "uuid"
          }
        }
      },
      "PurchaseOrderWriteRequest": {
        "description": "Proposto. Cria ou SUBSTITUI a ordem inteira.\n\n**A linha traz só o par de origem e o que foi negociado.** Descrição, acabamento, tamanho, unidade, variante e destino NÃO vêm do cliente: eles são COPIADOS da linha do pedido que `sourceRequestId` + `sourceLineNumber` apontam. Aceitá-los aqui deixaria a ordem dizer que comprou uma peça e o pedido dizer que pediu outra, com o par intacto entre as duas.\n\n**O PUT recusa (409) a ordem já enviada.** Depois do `send` o fornecedor tem a ordem na mão, e o que muda é data (por `reschedule`) ou nada.",
        "required": [
          "supplierId",
          "buyingTenantId",
          "orderedAt",
          "discountPercent",
          "surchargeCents",
          "items"
        ],
        "type": "object",
        "properties": {
          "supplierId": {
            "type": "string",
            "format": "uuid"
          },
          "buyingTenantId": {
            "type": "string",
            "format": "uuid"
          },
          "orderedAt": {
            "type": "string",
            "format": "date"
          },
          "expectedAt": {
            "type": ["string", "null"],
            "format": "date"
          },
          "carrierId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "paymentTermId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "discountPercent": {
            "description": "Desconto GERAL da ordem, na escala da casa: inteiro com 4 casas implícitas, `10000` = 1%. Ver `PurchaseOrderDto.discountPercent`.",
            "type": "integer",
            "format": "int32"
          },
          "surchargeCents": {
            "description": "ACRÉSCIMO em centavos — frete, embalagem, taxa. Não conta para o faturamento mínimo.",
            "type": "integer",
            "format": "int64"
          },
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PurchaseOrderItemWriteRequest"
            }
          },
          "notes": {
            "type": ["string", "null"]
          }
        }
      },
      "PurchaseOrderSendRequest": {
        "description": "Proposto. O corpo do envio. `sentAt` é opcional porque o caso normal é \"mandei agora\" — omitido, o servidor grava a data de hoje. Ele existe para o lançamento retroativo, que num ERP de operação real é rotina, não exceção.",
        "required": [],
        "type": "object",
        "properties": {
          "sentAt": {
            "type": ["string", "null"],
            "format": "date"
          }
        }
      },
      "PurchaseOrderRescheduleRequest": {
        "description": "Proposto. O corpo do reagendamento — a data nova e por quê.\n\n`reason` é obrigatório e é decisão: reagendamento sem motivo registrado transforma a medição de atraso do fornecedor numa lista de datas que mudaram sozinhas. O legado tem o campo, e a razão de ele existir é a conversa com o fornecedor no mês seguinte.",
        "required": ["expectedAt", "reason"],
        "type": "object",
        "properties": {
          "expectedAt": {
            "description": "A data REPROMETIDA. Vai para `rescheduledAt`; a promessa original fica onde está.",
            "type": "string",
            "format": "date"
          },
          "reason": {
            "description": "Por que a data mudou.",
            "type": "string"
          }
        }
      },
      "GoodsReceiptDto": {
        "description": "Proposto. Um RECEBIMENTO — a nota do fornecedor virando entrada no estoque. É a maior tela do legado (`Nota_entrada`) decomposta em fluxo.\n\n**O que entra aqui é a metade FÍSICA:** quem entregou, o que veio, o que veio a MENOS do que se pediu, e o efeito disso no estoque. A metade FISCAL — chave de 44 dígitos, XML da NF-e, manifestação do destinatário, ICMS e IPI linha a linha — fica de fora de propósito: ela é trilho próprio, e misturar as duas faria a entrada de mercadoria depender de a SEFAZ estar no ar.\n\n**Os três estados, e o do meio não é decoração.** `draft` é conferindo, e nada disso tocou o estoque; `checked` é a conferência fechada, com as divergências justificadas; `posted` é lançado, cada linha virou movimento de entrada. O estado do meio existe porque conferir e lançar são decisões de pessoas diferentes em horas diferentes — e é por isso que `POST /{id}/post` pede outra permissão que o resto da família.\n\n**Não guarda dinheiro, e isso é decisão.** Custo de compra, ICMS e IPI da linha ficam fora: quase tudo o que o legado guarda ali é FISCAL, e o que não é (custo de reposição) depende da regra de formação de custo, que ainda não está decidida. Coluna errada sai por migração de correção; coluna que falta entra por acréscimo.\n\n**Também não existe \"não gera estoque\".** O legado tem a flag (`Nen_NaoGeraEstoque`): nota que entra no sistema e não mexe no saldo. Aqui lançar é, por definição, mexer no saldo — um `posted` que não movesse estoque seria um estado que a palavra não descreve. Quando o caso real aparecer (remessa em consignação, retorno de conserto), ele entra como TIPO de recebimento, não como flag negativa no cabeçalho.\n\n**A porta de DESCARTAR o rascunho ficou de fora, e é dívida declarada.** A tabela reserva o privilégio para isso — rascunho se apaga: nota trocada, fornecedor errado, caminhão que era de outra empresa —, e o contrato não a abre porque não tem DELETE em lugar nenhum (§9 padrão 8, guardado por `src/data/contrato-bloco2.test.ts`): cadastro se desativa, documento se cancela com estado próprio. As duas saídas custam decisão que não cabe nesta publicação — abrir a exceção à regra, ou acrescentar `cancelled` ao vocabulário de estados, que é migração. Enquanto nenhuma delas acontece, o rascunho errado se corrige pelo PUT, que substitui o documento inteiro; o que não dá é fazê-lo sumir da fila.",
        "required": [
          "id",
          "status",
          "supplierId",
          "supplierName",
          "locationId",
          "locationName",
          "receivedAt",
          "items"
        ],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "status": {
            "description": "`draft` enquanto se confere, `checked` depois de `POST /{id}/check`, `posted` depois de `POST /{id}/post`.\n\nVocabulário FECHADO: o preço de abrir é uma migração de uma linha, e o de não fechar é a tela que filtra por estado devolvendo lista curta demais, meses depois, por causa de um valor digitado errado.",
            "type": "string",
            "enum": ["draft", "checked", "posted"]
          },
          "supplierId": {
            "description": "Quem entregou. É um `PartnerDto` no papel de fornecedor.",
            "type": "string",
            "format": "uuid"
          },
          "supplierName": {
            "type": "string"
          },
          "carrierId": {
            "description": "A TRANSPORTADORA, quando houve uma. Também é um `PartnerDto` — o papel é a coluna em que o id aparece, e não uma flag no cadastro: é o mesmo motivo pelo qual o mesmo CNPJ é cliente e fornecedor ao mesmo tempo.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "carrierName": {
            "type": ["string", "null"]
          },
          "locationId": {
            "description": "ONDE a mercadoria descarregou — o depósito que recebe as entradas do lançamento. Resolvido para o padrão da empresa quando a escrita o omite.",
            "type": "string",
            "format": "uuid"
          },
          "locationName": {
            "type": "string"
          },
          "invoiceNumber": {
            "description": "O número da nota como o operador o lê no papel. **Texto, e não inteiro:** número de nota tem série, tem zero à esquerda e tem letra em regime especial — o `int` do legado é a forma que já perdeu informação. Nulo é caso real: remessa que chega antes da nota.",
            "type": ["string", "null"]
          },
          "issuedAt": {
            "description": "Emissão da NOTA. Diferente de `receivedAt`, e a diferença entre as duas é o que a operação chama de trânsito.",
            "type": ["string", "null"],
            "format": "date-time"
          },
          "receivedAt": {
            "description": "Chegada da MERCADORIA.",
            "type": "string",
            "format": "date-time"
          },
          "postedAt": {
            "description": "Quando o recebimento foi lançado. `null` até lá, e nunca preenchido fora de `posted`: estado e carimbo andam juntos nos dois sentidos, e o par incoerente não é representável no banco.",
            "type": ["string", "null"],
            "format": "date-time"
          },
          "employeeId": {
            "description": "Quem respondeu pelo recebimento. Nulo na carga do legado.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "employeeName": {
            "type": ["string", "null"]
          },
          "notes": {
            "type": ["string", "null"]
          },
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/GoodsReceiptItemDto"
            }
          }
        }
      },
      "GoodsReceiptItemDto": {
        "description": "Proposto. Uma linha CONFERIDA: o que a ordem pedia, o que o caminhão trouxe, e a diferença entre os dois.\n\n**A conferência é o par `quantityOrdered` × `quantityReceived`, e é só isso.** No legado ela mora em `nota_entrada_det`, no par `Ned_quantidade_solicit` × `Ned_quantidade_recebida`. A divergência de VALOR é outra coisa, mora em outra tabela (`Nota_Entrada_Dif`, por nota, com trilha de desbloqueio) e é assunto do trilho financeiro — misturar as duas faria a conferência do galpão travar por diferença de preço, que quem descarrega o caminhão não tem como resolver.",
        "required": ["lineNumber", "variantId", "description", "quantityReceived"],
        "type": "object",
        "properties": {
          "lineNumber": {
            "description": "A ordem da linha DENTRO do recebimento, 1-based.",
            "type": "integer",
            "format": "int32"
          },
          "variantId": {
            "description": "A variante recebida. **Obrigatória, ao contrário da linha do orçamento:** recebimento move ESTOQUE, e estoque é de variante. Item digitado à mão — que o orçamento permite — não tem saldo a mexer, e uma linha dessas aqui seria uma entrada que o kardex não sabe onde lançar.",
            "type": "string",
            "format": "uuid"
          },
          "description": {
            "description": "Descrição congelada na gravação. Quem lê a conferência de ontem lê o que estava escrito ontem, não o cadastro de hoje.",
            "type": "string"
          },
          "quantityOrdered": {
            "description": "O que a ordem de compra pedia, em SNAPSHOT.\n\n**`null` é o recebimento AVULSO** — mercadoria que chegou sem ordem, que existe na operação. Nulo quer dizer \"não havia o que comparar\", e é diferente de zero, que diria \"pediram nada\".\n\nÉ por esta quantidade que a chegada baixa a reserva de CHEGADA FUTURA que `GetPurchaseArrivalForecast` e `GetPurchaseStockReplenishment` consomem — e não a reserva de VENDA (`PurchaseReplenishmentRowDto.qtyAllocated`), que entrada de compra nunca desfaz. Ver `POST /{id}/post`.",
            "type": ["number", "null"],
            "format": "double"
          },
          "quantityReceived": {
            "description": "O que de fato chegou. Nunca negativo: devolução ao fornecedor é documento próprio, e um recebimento com sinal trocado sairia como baixa de estoque rotulada de compra.",
            "type": "number",
            "format": "double"
          },
          "divergence": {
            "description": "Recebido menos pedido, calculado pelo servidor. Negativo quando veio a menos, positivo quando veio a mais — e a mais acontece, com o fornecedor que arredonda a caixa.\n\n**`null` no avulso, e isso não é o mesmo que zero.** Zero diz \"bateu certinho\"; nulo diz que não há com o que comparar. Devolver zero para o avulso seria afirmação sobre uma ordem que não existe. Vem calculado daqui pela mesma razão que os totais das outras famílias: a conta é uma só, e não uma por tela.",
            "type": ["number", "null"],
            "format": "double"
          },
          "divergenceReason": {
            "description": "Por que veio diferente, em texto livre. **Exigido por `POST /{id}/check` em toda linha que divergiu**, e ignorado nas que bateram.\n\n**É ADIÇÃO, não porte:** o legado registra que veio diferente e não registra por quê — quem precisava saber perguntava por telefone. O campo entra porque a informação se perde em horas, e é o único da família que não tem coluna correspondente no legado.\n\nTexto e não vocabulário fechado justamente porque não há enumerado a portar: fechar aqui seria inventá-lo. Ele vira lista quando a operação disser quais são os motivos de verdade.",
            "type": ["string", "null"]
          }
        }
      },
      "GoodsReceiptWriteRequest": {
        "description": "Proposto. Cria ou SUBSTITUI o recebimento inteiro.\n\n**A linha traz o que foi CONTADO, e nada além disso.** Descrição vem do cadastro da variante e `divergence` é calculada — aceitar qualquer uma das duas do cliente deixaria a grade dizer uma coisa e o catálogo (ou a subtração) outra.\n\n**O PUT recusa (409) fora do rascunho.** Depois de `check` a grade é uma decisão tomada; depois de `post` ela já virou saldo.",
        "required": ["supplierId", "items"],
        "type": "object",
        "properties": {
          "supplierId": {
            "type": "string",
            "format": "uuid"
          },
          "carrierId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "locationId": {
            "description": "Depósito que recebe. Omitido, o servidor resolve o padrão da empresa — ver `POST /api/goods-receipts`.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "invoiceNumber": {
            "type": ["string", "null"]
          },
          "issuedAt": {
            "type": ["string", "null"],
            "format": "date-time"
          },
          "receivedAt": {
            "description": "Chegada da mercadoria. Omitida, o servidor grava agora — o caso normal é \"chegou agora\", e o campo existe para o lançamento retroativo, que num ERP de operação real é rotina.",
            "type": ["string", "null"],
            "format": "date-time"
          },
          "employeeId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "notes": {
            "type": ["string", "null"]
          },
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/GoodsReceiptItemWriteRequest"
            }
          }
        }
      },
      "GoodsReceiptItemWriteRequest": {
        "description": "Proposto. Uma linha na escrita da conferência. `description` e `divergence` não entram: a primeira é copiada do cadastro da variante, a segunda é a subtração que o servidor faz uma vez.",
        "required": ["lineNumber", "variantId", "quantityReceived"],
        "type": "object",
        "properties": {
          "lineNumber": {
            "type": "integer",
            "format": "int32"
          },
          "variantId": {
            "type": "string",
            "format": "uuid"
          },
          "quantityOrdered": {
            "description": "O que a ordem pedia. `null` no recebimento avulso — ver `GoodsReceiptItemDto.quantityOrdered`. Quando presente, é maior que zero: zero diria \"a ordem pedia nada\", que não é linha de ordem nenhuma.",
            "type": ["number", "null"],
            "format": "double"
          },
          "quantityReceived": {
            "type": "number",
            "format": "double"
          },
          "divergenceReason": {
            "description": "Preenchível a qualquer momento do rascunho, e cobrado só na transição: durante a contagem, divergir sem motivo é o estado normal.",
            "type": ["string", "null"]
          }
        }
      },
      "PagedResultOfGoodsReceiptDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/GoodsReceiptDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "PagedResultOfPurchaseOrderDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PurchaseOrderDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "PurchaseArrivalRowDto": {
        "description": "Proposto. Uma linha da PREVISÃO DE CHEGADA — uma peça em ordem enviada, com a data em que ela é esperada e o cliente que a espera.\n\n**É linha de ITEM e não de ordem**, e é o que a consulta existe para responder: a pergunta do operador é \"quando chega a peça do cliente X\", não \"quando chega a ordem 4711\". Uma ordem agrupa reposição e encomenda de clientes diferentes; agregada por ordem, a consulta devolveria uma data e nenhum cliente.\n\n**`expectedAt` aqui é a data VÁLIDA** — `rescheduledAt` quando existe, `expectedAt` da ordem quando não. As duas cruas continuam disponíveis no `PurchaseOrderDto`; esta consulta é para a tela que pergunta \"quando\", e obrigá-la a escolher entre dois campos é o tipo de regra que se implementa diferente em duas telas.",
        "required": [
          "purchaseOrderId",
          "purchaseOrderNumber",
          "supplierId",
          "supplierName",
          "description",
          "quantity",
          "destination"
        ],
        "type": "object",
        "properties": {
          "purchaseOrderId": {
            "type": "string",
            "format": "uuid"
          },
          "purchaseOrderNumber": {
            "type": "string"
          },
          "supplierId": {
            "type": "string",
            "format": "uuid"
          },
          "supplierName": {
            "type": "string"
          },
          "sentAt": {
            "description": "Quando a ordem foi enviada.",
            "type": ["string", "null"],
            "format": "date"
          },
          "expectedAt": {
            "description": "A data válida — reagendada quando houve reagendamento. `null` na ordem enviada sem data prometida, e a tela mostra isso em vez de inventar uma.",
            "type": ["string", "null"],
            "format": "date"
          },
          "originalExpectedAt": {
            "description": "A promessa ORIGINAL, quando houve reagendamento. `null` quando `expectedAt` já é a original — a tela só destaca a linha reagendada.",
            "type": ["string", "null"],
            "format": "date"
          },
          "daysLate": {
            "description": "Dias de atraso contra a data válida, quando ela já passou. `null` na linha que ainda não venceu. Calculado no servidor porque \"hoje\" na tela é o relógio do navegador, e num ERP multiusuário isso já bastou para duas pessoas verem atrasos diferentes.",
            "type": ["integer", "null"],
            "format": "int32"
          },
          "variantId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "description": {
            "type": "string"
          },
          "finish": {
            "type": ["string", "null"]
          },
          "size": {
            "type": ["string", "null"]
          },
          "quantity": {
            "type": "number",
            "format": "double"
          },
          "destination": {
            "description": "Para que a peça vem. `sale` traz cliente; `stock` não tem cliente e é isso que a tela mostra.",
            "type": "string",
            "enum": ["stock", "sale"]
          },
          "orderId": {
            "description": "O pedido de VENDA que espera a peça, quando `destination` é `sale`.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "orderNumber": {
            "type": ["string", "null"]
          },
          "customerId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "customerName": {
            "description": "Quem está esperando. `null` na linha de reposição, e a tela mostra \"estoque\" — não vazio.",
            "type": ["string", "null"]
          }
        }
      },
      "PagedResultOfPurchaseArrivalRowDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PurchaseArrivalRowDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "PurchaseReplenishmentRowDto": {
        "description": "Proposto. Uma linha da consulta COMPRAS PARA ESTOQUE / RESERVA — quanto de cada variante há, quanto está PROMETIDO a alguém, e quanto já vem a caminho.\n\nÉ a `CompraEstoque` do legado, cuja definição o levantamento decifrou como **disponibilidade futura = ordem aberta − reserva − entrada por nota**. Aqui ela aparece aberta em parcelas, e não como um número só: um saldo futuro negativo não diz se falta comprar ou se sobra reserva, e são decisões opostas.\n\n**`qtyAllocated` é a reserva de verdade** — `stock_balances.quantity_allocated`, a coluna que a migração `0035` criou e que até aqui não saía por API nenhuma. Sem ela, \"disponível\" era o saldo bruto, e o comprador repunha peça que já estava prometida a um cliente.",
        "required": [
          "variantId",
          "description",
          "qtyOnHand",
          "qtyAllocated",
          "qtyAvailable",
          "qtyOnOrder",
          "qtySuggested"
        ],
        "type": "object",
        "properties": {
          "variantId": {
            "type": "string",
            "format": "uuid"
          },
          "description": {
            "type": "string"
          },
          "finish": {
            "type": ["string", "null"]
          },
          "size": {
            "type": ["string", "null"]
          },
          "qtyOnHand": {
            "description": "Saldo FÍSICO somado nos depósitos da empresa ativa. Pode ser negativo, e o contrato não o esconde: a venda sai antes de a nota entrar, e esse é o estado normal desta operação por alguns dias.",
            "type": "number",
            "format": "double"
          },
          "qtyAllocated": {
            "description": "Quanto do saldo já está RESERVADO para venda — `stock_balances.quantity_allocated`. Nunca negativo.",
            "type": "number",
            "format": "double"
          },
          "qtyAvailable": {
            "description": "`qtyOnHand − qtyAllocated`. É o que dá para prometer a um cliente novo hoje, e pode ser negativo quando se prometeu mais do que há.",
            "type": "number",
            "format": "double"
          },
          "qtyOnOrder": {
            "description": "Quanto já vem em ORDEM ENVIADA e ainda não recebida. Ordem `draft` não entra: intenção do comprador não é peça a caminho, e contá-la faria a sugestão zerar por causa de uma ordem que ninguém mandou.",
            "type": "number",
            "format": "double"
          },
          "qtySuggested": {
            "description": "A sugestão de compra: quanto falta para `qtyAvailable + qtyOnOrder` alcançar o mínimo de estoque da variante. `0` quando não falta nada.\n\nÉ SUGESTÃO e o nome diz isso — quem decide comprar é o comprador. O legado tem `EstoqueMinimo` com trigger, e a alteração da quantidade sugerida é permissão especial no RBAC dele (uma das 287 opções): a sugestão nunca foi tratada como ordem.",
            "type": "number",
            "format": "double"
          },
          "minimumQty": {
            "description": "O estoque mínimo cadastrado da variante, quando existe. `null` na variante sem mínimo — e então `qtySuggested` é `0`, porque não há alvo contra o qual faltar.",
            "type": ["number", "null"],
            "format": "double"
          },
          "supplierId": {
            "description": "O fornecedor PADRÃO da variante, para a tela já saber a quem pedir. `null` na variante sem fornecedor padrão.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "supplierName": {
            "type": ["string", "null"]
          }
        }
      },
      "PagedResultOfPurchaseReplenishmentRowDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PurchaseReplenishmentRowDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "PaymentTermGroupAdjustmentDto": {
        "description": "Proposto. O AJUSTE que esta condição de pagamento dá a um GRUPO DE PRODUTO — `Forma_PagamentoGrupProd` do legado, PK `(Fpg_codigo, GrupoProduto_Codigo)`, **169 linhas** com `Emp_codigo` na coluna. É a dívida que `PaymentTermDto` declarou por escrito e que `SupplierGroupMinimumDto` registrou como ainda aberta: *\"ela é sobre desconto na CONDIÇÃO DE PAGAMENTO, não sobre mínimo do fornecedor\"*.\n\n**O argumento que a adiava está vencido, e é por isso que ela entra agora.** Ele era que grupo de produto não tinha id — *\"casar por texto faria PENDENTES e Pendentes renderem descontos diferentes no mesmo documento\"*. Há id desde `QuoteItemDto.productGroupId` (kind `GRUPO_PRODUTO` de `catalog-lookups`), e é nele que esta linha pendura, como já penduram `QuoteGroupDiscountDto` e `SupplierGroupMinimumDto`.\n\n**Não tem id próprio**, pela razão de `PaymentTermInstallmentDto`: a identidade é o par (condição, grupo), e é assim no legado. Por isso ela viaja EMBUTIDA na condição, e o `PUT` substitui a coleção inteira.\n\n---\n\n**O QUE ESTA LINHA É, e o que ela NÃO é.** Ela é o PADRÃO que a condição sugere — não o que ficou gravado no documento. Quem grava é `QuoteDetailDto.groupDiscounts` (`VendaDesconto` do legado), e as duas coisas convivem no legado exatamente assim: `frmPreview_orcamento` abre as DUAS queries lado a lado, `QryFormaPagGrupProd` sobre esta tabela e `QryVendaGrupo` sobre `VendaDesconto`.\n\n**Trocar o ajuste NÃO reescreve documento gravado** — mesma garantia de `UpdatePaymentTerm` e do carimbo em `installmentPolicy`. O legado não tem essa garantia: ele lê esta tabela na hora de IMPRIMIR, então o orçamento impresso hoje sai diferente do impresso ontem sem ninguém ter tocado no documento. Aqui o que vale no documento é o que `groupDiscounts` carimbou.\n\n**Os seis campos de desconto/acréscimo do CABEÇALHO da condição continuam de fora, e agora com motivo medido:** `Fpg_desconto_lu`/`_ma`/`_se` e os pares de acréscimo são o **GERADOR** destas linhas, não dado a mais. `docs/legado/exe/sql-do-codigo.sql:379-381` são três `UPDATE ... FROM` em massa que copiam `_lu` para o grupo 1, `_ma` para os grupos 2..999 e `_se` para o grupo 1000. Mesmo argumento que manteve `Fpg_DiasPrimeiraParcela`/`Fpg_DiasEntreParcelas` fora: guardar os dois lados deixaria duas verdades sobre o mesmo número, e a tela que editasse uma linha do meio quebraria a fórmula sem ninguém ver.",
        "required": ["productGroupId", "productGroupName", "discountPercent", "surchargePercent"],
        "type": "object",
        "properties": {
          "productGroupId": {
            "description": "O grupo — `CatalogLookupDto.id`, kind `GRUPO_PRODUTO`. O mesmo id que a linha do documento carrega em `productGroupId`. É a CHAVE da linha: um grupo aparece uma vez só na condição, como no legado, cuja PK é `(Fpg_codigo, GrupoProduto_Codigo)`.",
            "type": "string",
            "format": "uuid"
          },
          "productGroupName": {
            "description": "Nome do grupo, ecoado pelo servidor. NÃO é chave — ver `QuoteItemDto.productGroupId`.\n\n**E aqui ele NÃO é congelado**, ao contrário de `QuoteGroupDiscountDto.productGroupName`: isto é CADASTRO, não documento emitido. Renomear o grupo no catálogo tem de mudar o que esta tela mostra, senão a condição fica exibindo um nome que não existe mais no combo ao lado.",
            "type": "string"
          },
          "discountPercent": {
            "description": "Desconto que a condição dá a este grupo — `Fpgprod_desconto`. Percentual com 4 casas decimais escaladas por 10.000: `10000` = 1%, `1000000` = 100%. Mesma unidade de `QuoteGroupDiscountDto.discountPercent` e de `QuoteDetailDto.discountPercent` — os três se encontram no mesmo rodapé, e uma segunda escala ali seria dois jeitos de calcular no mesmo documento.\n\n`0` quer dizer \"sem desconto\", e é valor legítimo — não ausência de dado.",
            "type": "integer",
            "format": "int32"
          },
          "surchargePercent": {
            "description": "Acréscimo que a condição cobra neste grupo — `Fpgprod_acrescimo`. Percentual com 4 casas decimais escaladas por 10.000: `10000` = 1%, `1000000` = 100%. Mesma unidade de `QuoteGroupDiscountDto.discountPercent` e de `QuoteDetailDto.discountPercent` — os três se encontram no mesmo rodapé, e uma segunda escala ali seria dois jeitos de calcular no mesmo documento.\n\n**É o par do desconto, e é por isso que ele entra junto:** condição à vista desconta, condição em 6× acresce — o juro embutido, por grupo. Publicar só metade das 169 linhas perderia o lado que dá sentido à tabela e faria a próxima PR reabrir o mesmo schema.\n\n**DÍVIDA DECLARADA: o acréscimo ainda não tem onde pousar no DOCUMENTO.** `QuoteDetailDto.groupDiscounts` carimba desconto e mais nada — e não é omissão deste contrato: `VendaDesconto` do legado também só tem `VenDesc_DescontoPorc`/`_DescontoValor`, sem acréscimo. Ou seja, no legado o acréscimo por grupo nunca foi carimbado em documento nenhum. Onde ele entra na conta do documento é decisão que ninguém tomou; inventar um `surchargePercent` em `QuoteGroupDiscountDto` sem fonte a fixaria no servidor para sempre. Enquanto isso ele é dado do CADASTRO, e a tela o lê ao montar o plano.\n\n`0` quer dizer \"sem acréscimo\".",
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "PaymentTermGroupAdjustmentWriteRequest": {
        "description": "Proposto. Uma linha de ajuste por grupo no corpo de escrita da condição.\n\n**Sem `productGroupName`** — ele é ecoado, e a tela que o mandasse estaria propondo o nome do catálogo. Mesmo motivo de `QuoteGroupDiscountWriteRequest` não mandar `subtotalCents`.\n\n**Os dois percentuais são obrigatórios**, e nenhum é anulável — mesma decisão de `InstallmentPolicyWriteRequest`: \"sem ajuste\" é `0`, que é dado. Um `null` seria um segundo modo de ler o mesmo campo, e o legado não o tem — os três `UPDATE` em massa gravam `0` explicitamente (`case when ... > 0 then ... else 0 end`).",
        "required": ["productGroupId", "discountPercent", "surchargePercent"],
        "type": "object",
        "properties": {
          "productGroupId": {
            "type": "string",
            "format": "uuid"
          },
          "discountPercent": {
            "type": "integer",
            "format": "int32"
          },
          "surchargePercent": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "CostProfileDto": {
        "type": "object",
        "description": "Proposto. O PERFIL DE CUSTO de um fornecedor — `Custo` do legado (40 colunas, 385 linhas na operação real), a metade de COMPRA da formação de preço.\n\n**É por FORNECEDOR e por EMPRESA.** Por fornecedor porque é a negociação com ele que define a cascata de descontos; por empresa porque a mesma rede negocia diferente em cada loja. O mesmo fornecedor pode ter mais de um perfil (`name`) — o legado tem \"ILUMINAR\", \"ILUMINAR ESPECIAL\" e \"ILUMINAR PREÇO 2\" no mesmo cadastro, e são condições comerciais distintas, não duplicatas.\n\n**O que este schema NÃO publica, e não é esquecimento:** `Cus_TributacaoICMS` e os seis campos que dependem dele (`Cus_Icms`, `Cus_IcmsDestino`, `Cus_MargValAgreg`, `Cus_ICMSDifAlqIPI`, `Cus_STEmbalagem`, `Cus_ICMSValorCompra`). Eles ramificam o custo em **sete caminhos** — Substituição Tributária domina 317 dos 385 perfis reais —, e qual deles reproduzir é decisão de CONTADOR, ainda pendente. Publicá-los agora congelaria no contrato uma conta que ninguém decidiu; a simulação deste trilho devolve o custo **sem imposto de ICMS** e diz isso no campo.\n\nTambém fora: `Cus_Importacao`/`Cus_Cambio` (mudam a base da VENDA, e venda é o outro trilho) e os nove campos de valor na nota do fornecedor (`Cus_TpVlNFo` + os oito `Cus_NFo*`), que dependem do ICMS para significar alguma coisa.",
        "required": [
          "id",
          "supplierId",
          "supplierName",
          "name",
          "active",
          "discount1Percent",
          "discount2Percent",
          "discount3Percent",
          "discount4Percent",
          "ipiPercent",
          "packagingPercent",
          "financialPercent",
          "freightPercent",
          "otherPercent",
          "simplesPercent",
          "cardPercent",
          "fixedCostPercent",
          "costDiscountPercent",
          "icmsCreditPercent",
          "pisCreditPercent",
          "cofinsCreditPercent",
          "freightInPurchase",
          "createdAt",
          "updatedAt"
        ],
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "supplierId": {
            "type": "string",
            "format": "uuid",
            "description": "O fornecedor (`partners` com `isSupplier`), dono da negociação."
          },
          "supplierName": {
            "type": "string",
            "description": "Nome do fornecedor, para a grade não precisar de uma segunda consulta."
          },
          "name": {
            "type": "string",
            "maxLength": 70,
            "description": "O nome da condição comercial (`Cus_Nome`). Único dentro do fornecedor na empresa — nome repetido é 409 `codigo-ja-cadastrado`."
          },
          "active": {
            "type": "boolean",
            "description": "`Cus_situacao` do legado (`char(1)`, `A` nos 385 perfis). Os 385 estão ativos.\n\n**Perfil não se apaga — este contrato não tem `DELETE` em lugar nenhum** (§9 padrão 8), e aqui a razão é forte: enquanto o perfil existir, dá para reler COMO o preço de um item foi formado. Apagado, a decomposição de todo orçamento que ele precificou vira número sem procedência."
          },
          "discount1Percent": {
            "type": "integer",
            "format": "int32",
            "description": "Primeiro desconto da cascata do fornecedor (`Cus_desconto1`), aplicado sobre o preço de TABELA.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo."
          },
          "discount2Percent": {
            "type": "integer",
            "format": "int32",
            "description": "Segundo desconto, aplicado sobre o que **sobrou** do primeiro — a cascata é sequencial, nunca somada. 20% + 10% em cascata é 28%, não 30%.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo."
          },
          "discount3Percent": {
            "type": "integer",
            "format": "int32",
            "description": "Terceiro desconto da cascata, sobre o resto dos dois anteriores.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo."
          },
          "discount4Percent": {
            "type": "integer",
            "format": "int32",
            "description": "Quarto desconto da cascata, sobre o resto dos três anteriores.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo."
          },
          "ipiPercent": {
            "type": "integer",
            "format": "int32",
            "description": "IPI do fornecedor (`Cus_IPI`), sobre o líquido da cascata.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo."
          },
          "packagingPercent": {
            "type": "integer",
            "format": "int32",
            "description": "Embalagem (`Cus_Embalagens`), sobre o líquido da cascata.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo."
          },
          "financialPercent": {
            "type": "integer",
            "format": "int32",
            "description": "Encargo financeiro (`Cus_Financeira`) — incide sobre **líquido + embalagem + IPI**, não sobre o líquido puro.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo."
          },
          "freightPercent": {
            "type": "integer",
            "format": "int32",
            "description": "Frete (`Cus_Frete`). A BASE depende de `freightInPurchase`: dentro da compra incide sobre o líquido; fora, sobre líquido + embalagem + IPI + financeiro.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo."
          },
          "otherPercent": {
            "type": "integer",
            "format": "int32",
            "description": "Outras despesas (`Cus_outros`), sobre líquido + embalagem + IPI + financeiro.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo."
          },
          "simplesPercent": {
            "type": "integer",
            "format": "int32",
            "description": "Simples Nacional (`Cus_Simples`). Incide sobre a **venda líquida**, não sobre a compra — é tributo de saída entrando no custo para apurar lucro.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo."
          },
          "cardPercent": {
            "type": "integer",
            "format": "int32",
            "description": "Taxa de cartão (`Cus_PorcCartao`), sobre a venda líquida — mesma base do Simples.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo."
          },
          "fixedCostPercent": {
            "type": "integer",
            "format": "int32",
            "description": "Custo fixo rateado (`Cus_CustoFixo`), sobre a venda líquida. É o overhead da casa cobrado por venda, não despesa da compra.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo."
          },
          "costDiscountPercent": {
            "type": "integer",
            "format": "int32",
            "description": "Desconto embutido no custo (`Cus_Desconto`), sobre a venda líquida — a provisão de desconto que o vendedor pode dar sem furar a margem.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo."
          },
          "icmsCreditPercent": {
            "type": "integer",
            "format": "int32",
            "description": "Crédito de ICMS da entrada (`Cus_CreditoICMS`), abatido do líquido da cascata.\n\n**Crédito é DADO, e a subtração é única.** A `CalcularProduto` do legado tem dois blocos que subtraem os três créditos, e eles são os ramos alternativos de `Cus_FreteemCompra` — não execução em sequência. A suspeita de subtração dupla, registrada por onze dias, está refutada contra a fonte.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo."
          },
          "pisCreditPercent": {
            "type": "integer",
            "format": "int32",
            "description": "Crédito de PIS da entrada (`Cus_CreditoPIS`), abatido do líquido.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo."
          },
          "cofinsCreditPercent": {
            "type": "integer",
            "format": "int32",
            "description": "Crédito de COFINS da entrada (`Cus_CreditoCOFINS`), abatido do líquido.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo."
          },
          "freightInPurchase": {
            "type": "boolean",
            "description": "Se o frete entra no VALOR DE COMPRA (`Cus_FreteemCompra`).\n\nMuda duas coisas de uma vez, e é por isso que é flag e não convenção: quando ligado, o frete incide sobre o líquido puro e soma no valor de compra; quando desligado, incide sobre líquido + embalagem + IPI + financeiro e fica fora da compra — entrando só no custo. O valor final do frete difere nos dois modos."
          },
          "createdAt": {
            "type": "string",
            "format": "date-time"
          },
          "updatedAt": {
            "type": "string",
            "format": "date-time"
          }
        }
      },
      "CostProfileWriteRequest": {
        "type": "object",
        "description": "Proposto. Cria ou substitui um perfil de custo. `PUT` substitui o registro INTEIRO, como toda escrita deste contrato — campo omitido volta a zero, não conserva o valor anterior.",
        "required": ["supplierId", "name"],
        "properties": {
          "supplierId": {
            "type": "string",
            "format": "uuid"
          },
          "name": {
            "type": "string",
            "maxLength": 70
          },
          "active": {
            "type": "boolean",
            "description": "Ausente = `true`. Perfil nasce valendo; desativar é gesto explícito."
          },
          "discount1Percent": {
            "type": "integer",
            "format": "int32",
            "description": "Primeiro desconto da cascata do fornecedor (`Cus_desconto1`), aplicado sobre o preço de TABELA.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo.\n\nAusente = zero."
          },
          "discount2Percent": {
            "type": "integer",
            "format": "int32",
            "description": "Segundo desconto, aplicado sobre o que **sobrou** do primeiro — a cascata é sequencial, nunca somada. 20% + 10% em cascata é 28%, não 30%.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo.\n\nAusente = zero."
          },
          "discount3Percent": {
            "type": "integer",
            "format": "int32",
            "description": "Terceiro desconto da cascata, sobre o resto dos dois anteriores.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo.\n\nAusente = zero."
          },
          "discount4Percent": {
            "type": "integer",
            "format": "int32",
            "description": "Quarto desconto da cascata, sobre o resto dos três anteriores.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo.\n\nAusente = zero."
          },
          "ipiPercent": {
            "type": "integer",
            "format": "int32",
            "description": "IPI do fornecedor (`Cus_IPI`), sobre o líquido da cascata.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo.\n\nAusente = zero."
          },
          "packagingPercent": {
            "type": "integer",
            "format": "int32",
            "description": "Embalagem (`Cus_Embalagens`), sobre o líquido da cascata.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo.\n\nAusente = zero."
          },
          "financialPercent": {
            "type": "integer",
            "format": "int32",
            "description": "Encargo financeiro (`Cus_Financeira`) — incide sobre **líquido + embalagem + IPI**, não sobre o líquido puro.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo.\n\nAusente = zero."
          },
          "freightPercent": {
            "type": "integer",
            "format": "int32",
            "description": "Frete (`Cus_Frete`). A BASE depende de `freightInPurchase`: dentro da compra incide sobre o líquido; fora, sobre líquido + embalagem + IPI + financeiro.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo.\n\nAusente = zero."
          },
          "otherPercent": {
            "type": "integer",
            "format": "int32",
            "description": "Outras despesas (`Cus_outros`), sobre líquido + embalagem + IPI + financeiro.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo.\n\nAusente = zero."
          },
          "simplesPercent": {
            "type": "integer",
            "format": "int32",
            "description": "Simples Nacional (`Cus_Simples`). Incide sobre a **venda líquida**, não sobre a compra — é tributo de saída entrando no custo para apurar lucro.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo.\n\nAusente = zero."
          },
          "cardPercent": {
            "type": "integer",
            "format": "int32",
            "description": "Taxa de cartão (`Cus_PorcCartao`), sobre a venda líquida — mesma base do Simples.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo.\n\nAusente = zero."
          },
          "fixedCostPercent": {
            "type": "integer",
            "format": "int32",
            "description": "Custo fixo rateado (`Cus_CustoFixo`), sobre a venda líquida. É o overhead da casa cobrado por venda, não despesa da compra.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo.\n\nAusente = zero."
          },
          "costDiscountPercent": {
            "type": "integer",
            "format": "int32",
            "description": "Desconto embutido no custo (`Cus_Desconto`), sobre a venda líquida — a provisão de desconto que o vendedor pode dar sem furar a margem.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo.\n\nAusente = zero."
          },
          "icmsCreditPercent": {
            "type": "integer",
            "format": "int32",
            "description": "Crédito de ICMS da entrada (`Cus_CreditoICMS`), abatido do líquido da cascata.\n\n**Crédito é DADO, e a subtração é única.** A `CalcularProduto` do legado tem dois blocos que subtraem os três créditos, e eles são os ramos alternativos de `Cus_FreteemCompra` — não execução em sequência. A suspeita de subtração dupla, registrada por onze dias, está refutada contra a fonte.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo.\n\nAusente = zero."
          },
          "pisCreditPercent": {
            "type": "integer",
            "format": "int32",
            "description": "Crédito de PIS da entrada (`Cus_CreditoPIS`), abatido do líquido.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo.\n\nAusente = zero."
          },
          "cofinsCreditPercent": {
            "type": "integer",
            "format": "int32",
            "description": "Crédito de COFINS da entrada (`Cus_CreditoCOFINS`), abatido do líquido.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo.\n\nAusente = zero."
          },
          "freightInPurchase": {
            "type": "boolean",
            "description": "Se o frete entra no VALOR DE COMPRA (`Cus_FreteemCompra`).\n\nMuda duas coisas de uma vez, e é por isso que é flag e não convenção: quando ligado, o frete incide sobre o líquido puro e soma no valor de compra; quando desligado, incide sobre líquido + embalagem + IPI + financeiro e fica fora da compra — entrando só no custo. O valor final do frete difere nos dois modos.\n\nAusente = `false`."
          }
        }
      },
      "PagedResultOfCostProfileDto": {
        "type": "object",
        "required": ["rows", "total"],
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CostProfileDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "CostSimulationRequest": {
        "type": "object",
        "description": "Proposto. O que a simulação precisa saber sobre **um item**: quanto ele custa na tabela do fornecedor e, se já houver preço de venda, por quanto ele sai.",
        "required": ["tablePriceCents"],
        "properties": {
          "tablePriceCents": {
            "type": "integer",
            "format": "int64",
            "description": "Preço de TABELA do item no fornecedor (`Preco_Produto.Pre_Tabela`), em centavos. É a entrada da cascata.\n\nViaja no corpo e não sai de um cadastro porque **ainda não existe onde guardá-lo**: `product_suppliers` publica o código e a descrição da peça no fornecedor, e o `divergido:` daquela migração declara, por escrito, que preço de compra ali não tem quem o escreva. Enquanto não tiver, quem simula informa.",
            "minimum": 1
          },
          "netSaleCents": {
            "type": ["integer", "null"],
            "format": "int64",
            "description": "A venda LÍQUIDA do item (já descontada), em centavos — de onde sai o lucro.\n\n**É entrada, não resultado, e a separação é deliberada.** O preço de venda nasce do índice do fornecedor (`Ipr_Indice`), que é o outro trilho do módulo Preço; recalculá-lo aqui daria duas autoridades sobre o mesmo número. Ausente ou `null`: a resposta traz a decomposição da COMPRA e deixa `profitCents` nulo, porque quatro parcelas do custo (Simples, cartão, custo fixo e desconto de custo) incidem sobre a venda e sem ela valem zero — o custo sairia menor do que é."
          },
          "internalCommissionPercent": {
            "type": "integer",
            "format": "int32",
            "description": "Comissão INTERNA sobre a venda líquida (`Ipr_vl_com_inter`). Ausente = zero.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo."
          },
          "externalCommissionPercent": {
            "type": "integer",
            "format": "int32",
            "description": "Comissão EXTERNA sobre a venda líquida (`Ipr_vl_com_exter`). Ausente = zero.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo."
          }
        }
      },
      "CostSimulationDto": {
        "type": "object",
        "description": "Proposto. A decomposição do custo de um item sob um perfil, e o lucro quando a venda foi informada.\n\n**Toda parcela vem em centavos, arredondada a duas casas — e a ordem do arredondamento é dado do legado, não escolha estética.** Medido contra os 376 índices reais: o lucro gravado bate em 357 deles quando o custo é arredondado ANTES da subtração, e em 179 quando não é. A conta é em centavos porque o legado a fazia em centavos.\n\n**`costCents` NÃO inclui ICMS.** Os sete caminhos de `Cus_TributacaoICMS` estão fora deste trilho (ver `CostProfileDto`), e o campo `icmsCents` não existe de propósito: publicá-lo zerado faria a tela mostrar \"ICMS: R$ 0,00\" para 317 perfis em que ele é o maior componente do custo.",
        "required": [
          "tablePriceCents",
          "discount1Cents",
          "discount2Cents",
          "discount3Cents",
          "discount4Cents",
          "grossNetCents",
          "icmsCreditCents",
          "pisCreditCents",
          "cofinsCreditCents",
          "netPurchaseCents",
          "ipiCents",
          "packagingCents",
          "financialCents",
          "freightCents",
          "otherCents",
          "simplesCents",
          "cardCents",
          "fixedCostCents",
          "costDiscountCents",
          "purchaseCents",
          "costCents",
          "internalCommissionCents",
          "externalCommissionCents",
          "excludesIcms"
        ],
        "properties": {
          "tablePriceCents": {
            "type": "integer",
            "format": "int64",
            "description": "O que entrou: preço de tabela do item."
          },
          "discount1Cents": {
            "type": "integer",
            "format": "int64",
            "description": "Valor do 1º desconto, sobre a tabela."
          },
          "discount2Cents": {
            "type": "integer",
            "format": "int64",
            "description": "Valor do 2º desconto, sobre a tabela já sem o 1º."
          },
          "discount3Cents": {
            "type": "integer",
            "format": "int64",
            "description": "Valor do 3º desconto, sobre o resto dos dois anteriores."
          },
          "discount4Cents": {
            "type": "integer",
            "format": "int64",
            "description": "Valor do 4º desconto, sobre o resto dos três anteriores."
          },
          "grossNetCents": {
            "type": "integer",
            "format": "int64",
            "description": "Líquido da cascata, **antes** de abater os créditos. É a base do IPI, da embalagem e dos créditos."
          },
          "icmsCreditCents": {
            "type": "integer",
            "format": "int64",
            "description": "Crédito de ICMS abatido."
          },
          "pisCreditCents": {
            "type": "integer",
            "format": "int64",
            "description": "Crédito de PIS abatido."
          },
          "cofinsCreditCents": {
            "type": "integer",
            "format": "int64",
            "description": "Crédito de COFINS abatido."
          },
          "netPurchaseCents": {
            "type": "integer",
            "format": "int64",
            "description": "Líquido depois dos créditos — `grossNetCents` menos os três. É a primeira parcela do custo."
          },
          "ipiCents": {
            "type": "integer",
            "format": "int64",
            "description": "IPI, sobre `grossNetCents`."
          },
          "packagingCents": {
            "type": "integer",
            "format": "int64",
            "description": "Embalagem, sobre `grossNetCents`."
          },
          "financialCents": {
            "type": "integer",
            "format": "int64",
            "description": "Encargo financeiro, sobre líquido + embalagem + IPI."
          },
          "freightCents": {
            "type": "integer",
            "format": "int64",
            "description": "Frete, na base que `freightInPurchase` decide."
          },
          "otherCents": {
            "type": "integer",
            "format": "int64",
            "description": "Outras despesas."
          },
          "simplesCents": {
            "type": "integer",
            "format": "int64",
            "description": "Simples, sobre a venda líquida. Zero quando a venda não foi informada."
          },
          "cardCents": {
            "type": "integer",
            "format": "int64",
            "description": "Taxa de cartão, sobre a venda líquida. Zero sem venda."
          },
          "fixedCostCents": {
            "type": "integer",
            "format": "int64",
            "description": "Custo fixo rateado, sobre a venda líquida. Zero sem venda."
          },
          "costDiscountCents": {
            "type": "integer",
            "format": "int64",
            "description": "Desconto de custo, sobre a venda líquida. Zero sem venda."
          },
          "purchaseCents": {
            "type": "integer",
            "format": "int64",
            "description": "VALOR DE COMPRA (`Pre_compra`) — o que se paga ao fornecedor: líquido + embalagem + IPI + financeiro, mais o frete quando `freightInPurchase`. Não é o custo: não carrega o que incide sobre a venda."
          },
          "costCents": {
            "type": "integer",
            "format": "int64",
            "description": "CUSTO do item (`Pre_Custo`), sem ICMS: líquido + embalagem + IPI + financeiro + frete + outras + Simples + cartão + custo fixo + desconto de custo."
          },
          "internalCommissionCents": {
            "type": "integer",
            "format": "int64",
            "description": "Comissão interna sobre a venda líquida."
          },
          "externalCommissionCents": {
            "type": "integer",
            "format": "int64",
            "description": "Comissão externa sobre a venda líquida."
          },
          "profitCents": {
            "type": ["integer", "null"],
            "format": "int64",
            "description": "LUCRO (`Pre_Lucro`) = venda líquida − custo − comissão interna − comissão externa. `null` quando `netSaleCents` não foi informado."
          },
          "profitPercent": {
            "type": ["integer", "null"],
            "format": "int32",
            "description": "MARGEM (`Pre_PorLucro`): lucro sobre a venda líquida. `null` quando não há venda informada.\n\nPercentual com **4 casas decimais**, inteiro escalado por 10.000: `10000` = 1%, `1` = 0,0001%, `1000000` = 100%. Mesma escala de `QuoteDetailDto.discountPercent` — inteiro e não `number` porque percentual aqui multiplica dinheiro, e float empilha arredondamento em cima de centavo."
          },
          "excludesIcms": {
            "type": "boolean",
            "description": "Sempre `true` neste trilho, e o campo existe para a tela poder DIZER isso ao operador em vez de deixá-lo somar um custo incompleto sem saber. Vira `false` quando a decisão do contador entrar e os sete caminhos de ICMS forem implementados."
          }
        }
      },
      "CommissionTierOperator": {
        "description": "O OPERADOR da faixa — `ComPreCond_SimbDesc char(2)` do legado, com o vocabulário fechado.\n\nEle compara a faixa contra o **desconto do DOCUMENTO** (`discountPercent` do cabeçalho), não contra o da linha: é o que `sql-por-tela.sql:169` do legado mede, onde a consulta que casa `VendaIndicacaoGrupProd` com a venda puxa `Venda.Ven_DescontoPorc` na mesma instrução. Usar o desconto da linha faria a mesma venda pagar percentuais diferentes por item por dois motivos ao mesmo tempo — grupo e desconto —, e nenhuma apuração conseguiria explicar o número.\n\nLeitura da regra: **quanto mais desconto o vendedor dá, menor a participação**. É por isso que `VendaIndicacaoGrupProd` tem 232.415 linhas para 34.136 vendas — 6,7 por (documento × pessoa). Não são \"12 grupos\": são VÁRIAS FAIXAS por grupo, uma por condição de desconto.\n\n**Confiança MÉDIA nesta leitura, e está dito de propósito:** ela vem da FORMA — a PK das três tabelas do legado inclui o próprio percentual, o join puxa o desconto da venda, e a coluna irmã se chama \"símbolo\". Não vem do dado. O operador é DADO e não regra em código exatamente por isso: se o caso real conferido disser outra coisa, muda linha de tabela, não muda contrato.\n\nTexto livre aqui produziria `=>` num cadastro e faixa que nunca casa em produção, sem erro nenhum na hora de gravar.",
        "type": "string",
        "enum": [">=", "<=", ">", "<", "="]
      },
      "OrderParticipantTierDto": {
        "description": "Proposto. Uma FAIXA de participação CONGELADA no documento — `VendaIndicacaoGrupProd` do legado (232.415 linhas, a tabela mais usada do Financeiro de lá).\n\n**Congelada, e é a razão de ela existir separada do cadastro.** O perfil diz quanto uma pessoa COSTUMA ganhar; esta linha diz quanto ela ganhou NESTE documento. O legado faz o mesmo, e a distância entre os dois números é medida: `IndicacaoGrupProd` (o perfil) tem 7.569 linhas contra as 232.415 daqui — a cópia por documento é 30× o cadastro. Quem guardasse só o cadastro teria uma tabela pequena e um relatório que muda de valor sozinho quando alguém edita o perfil.\n\n**Não se edita.** Ela nasce na cópia do perfil, quando a pessoa entra no documento, e daí em diante é passado — ver `OrderParticipantWriteRequest.id`.",
        "required": [
          "productGroupId",
          "productGroupName",
          "operator",
          "discountPercent",
          "percent"
        ],
        "type": "object",
        "properties": {
          "productGroupId": {
            "description": "O grupo desta faixa — `CatalogLookupDto.id`, kind `GRUPO_PRODUTO`; o mesmo id que a linha do documento carrega em `QuoteItemDto.productGroupId`.\n\n**É o id e não o nome, pelo motivo já escrito em `QuoteItemDto`:** nome nunca foi chave, e dois grupos homônimos (ou um renomeado depois da emissão) mandariam a participação para o grupo errado sem ninguém acusar. No legado a chave é `GrupoProduto_Codigo`, código — não a descrição.",
            "type": "string",
            "format": "uuid"
          },
          "productGroupName": {
            "description": "Nome do grupo CONGELADO na emissão, pelo mesmo motivo de `QuoteGroupDiscountDto.productGroupName`: renomear o grupo no catálogo não pode reescrever apuração fechada. É o que se lê; o que se casa é o id.",
            "type": "string"
          },
          "operator": {
            "$ref": "#/components/schemas/CommissionTierOperator"
          },
          "discountPercent": {
            "description": "O desconto CONTRA o qual o operador compara — a metade que dá sentido ao `percent`. Percentual com 4 casas decimais escaladas por 10.000: `10000` = 1%, `1000000` = 100%. Mesma unidade e mesma escala de `QuoteDetailDto.discountPercent`, e é de propósito: os dois lados da comparação precisam ser inteiros da MESMA escala, ou a comparação passa a depender de conversão.",
            "type": "integer",
            "format": "int32"
          },
          "percent": {
            "description": "O percentual que esta faixa paga quando ela é a que casa. Mesma escala de `discountPercent` — `10000` = 1%.",
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "OrderParticipantDto": {
        "description": "Proposto. Uma pessoa que PARTICIPA do documento, com o percentual e as faixas congelados — `VendaAtendente` e `VendaIndicacao` do legado, que têm o MESMO formato: `(TpDoc, NDocPre, <pessoa>, Emp_Codigo)` + `Porcentagem` + `Principal` + `DtVigencia`.\n\n**N por documento, e isso é o caso COMUM e não a exceção:** `VendaAtendente` tem 37.707 linhas para 34.136 vendas. O `salespersonId` singular do documento continua existindo e passa a ser uma LEITURA — o atendente `isPrincipal` desta lista —, não um segundo lugar onde se grava.\n\n**Os dois papéis são simétricos.** A auditoria de 24/08 leu que \"atendente principal é N e profissional é 1\", e a fonte não diz isso: `bdprincipal-rotinas.sql:1088` e `:1116` são o mesmo cursor com o mesmo corpo, `Principal = 1` aparece duas vezes e as duas dentro de cursores, e `sql-por-tela.sql:3670`/`:3673` são `SELECT top 1 *` para os DOIS papéis — `TOP 1` é o que se escreve quando a consulta pode voltar mais de uma linha.",
        "required": ["id", "role", "personName", "percent", "isPrincipal", "tiers"],
        "type": "object",
        "properties": {
          "id": {
            "description": "Identidade da PARTICIPAÇÃO, não da pessoa. É ela que o `PUT` ecoa para dizer \"esta linha é a mesma\" e preservar as faixas congeladas.",
            "type": "string",
            "format": "uuid"
          },
          "role": {
            "description": "O papel na venda. Vocabulário FECHADO de dois valores, e fechado de propósito: o legado tem duas tabelas (`VendaAtendente`, `VendaIndicacao`) e não um catálogo de papéis, e cada uma paga por um caminho diferente — comissão de colaborador de um lado, Reserva Técnica do outro.\n\n- `attendant` — colaborador (`EmployeeDto.id`), o consultor que atende.\n- `professional` — profissional externo (`PartnerDto.id` com papel `professional`), o arquiteto/especificador que indicou.",
            "type": "string",
            "enum": ["attendant", "professional"]
          },
          "employeeId": {
            "description": "O colaborador, quando `role` é `attendant`. `null` no outro papel.",
            "type": ["null", "string"],
            "format": "uuid"
          },
          "employeeName": {
            "type": ["null", "string"]
          },
          "partnerId": {
            "description": "O profissional externo, quando `role` é `professional`. `null` no outro papel.",
            "type": ["null", "string"],
            "format": "uuid"
          },
          "partnerName": {
            "type": ["null", "string"]
          },
          "personName": {
            "description": "O nome que a grade mostra — colaborador ou parceiro, o que houver. Existe para a tela não ter de escolher entre dois campos a cada linha; o par id+nome específico continua publicado ao lado, e é ele que se casa.",
            "type": "string"
          },
          "percent": {
            "description": "O percentual GERAL desta participação, CONGELADO — `Porcentagem` do legado. Escala de 4 casas: `10000` = 1%.\n\nÉ o que vale para item cujo grupo não tem faixa, e para SERVIÇO, que não tem grupo de produto nenhum: as faixas do legado vivem em tabelas `…GrupProd`, que só o produto alcança. Não é omissão — é onde o percentual geral mora.",
            "type": "integer",
            "format": "int32"
          },
          "isPrincipal": {
            "description": "O `Principal` do legado. **Um por papel, no máximo** — dois principais não significam nada, e o documento com dois é 400 apontando as linhas.\n\nÉ o `isPrincipal` do papel `attendant` que responde por `salespersonId`, e o do `professional` que responde por `professionalId`.",
            "type": "boolean"
          },
          "validFrom": {
            "description": "Início da vigência desta participação — `DtVigencia` do legado. `null` na linha que nasceu com o documento.\n\nExiste porque a participação MUDA depois do fato: o legado transfere venda de um profissional para outro (`TransfIndicacao`/`TransfIndicacaoDet`, a opção \"Transferência de Venda entre Profissionais\" do menu), e a comissão pergunta a partir de quando. Quem faz essa troca aqui é `POST /api/orders/{id}/professional`, que tem data e justificativa — ver a recusa em `PUT /api/orders/{id}/participants`.",
            "type": ["null", "string"],
            "format": "date"
          },
          "tiers": {
            "description": "As faixas por grupo de produto CONGELADAS nesta participação, em ordem de `productGroupName`. Lista **vazia** quando a pessoa não tem faixa por grupo — que é situação legítima e comum, e significa \"vale o `percent` geral em tudo\", não ausência de dado.\n\nVêm embutidas e não em sub-recurso pelo argumento de `QuoteGroupDiscountDto`: a faixa não tem identidade fora da participação, e ler linha a linha faria a aba Participação virar N requisições.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/OrderParticipantTierDto"
            }
          }
        }
      },
      "PagedResultOfOrderParticipantDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/OrderParticipantDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "OrderParticipantWriteRequest": {
        "description": "Proposto. Uma linha da grade de participação, na escrita.\n\n**O que este corpo NÃO traz, e a ausência é a regra:** as FAIXAS. Elas saem do perfil da pessoa (`GET /api/partners/{id}/commission-tiers`), que é cadastro da empresa, e são copiadas pelo servidor na hora em que a linha nasce. Faixa vinda do corpo seria o cliente escolhendo quanto ganha por grupo — a mesma família do percentual vindo do cliente, que este contrato já recusa em toda parte.",
        "required": ["role", "isPrincipal"],
        "type": "object",
        "properties": {
          "id": {
            "description": "**O ECO que preserva o congelamento.** Linha que volta com o `id` que veio da leitura é a MESMA linha: o servidor mantém as faixas já congeladas nela e mexe só no que este corpo traz. Linha SEM `id` é nova, e é nela que o servidor copia o perfil de hoje.\n\nSem esse eco, o `PUT` integral (que apaga e regrava) recopiaria o perfil a cada gravação — e um documento de março passaria a pagar pelo perfil de agosto na primeira vez que alguém corrigisse um nome na tela. É o defeito que o congelamento existe para impedir, entrando pela porta da escrita.\n\n`id` que não é participação DESTE documento é 400 apontando o campo.",
            "type": ["null", "string"],
            "format": "uuid"
          },
          "role": {
            "description": "`attendant` ou `professional` — ver `OrderParticipantDto.role`.",
            "type": "string",
            "enum": ["attendant", "professional"]
          },
          "employeeId": {
            "description": "Obrigatório quando `role` é `attendant`, e proibido no outro papel — 400 apontando o campo nos dois casos. Papel sem pessoa, ou com as duas, é linha que a apuração não sabe pagar.",
            "type": ["null", "string"],
            "format": "uuid"
          },
          "partnerId": {
            "description": "Obrigatório quando `role` é `professional`, e proibido no outro papel. Parceiro sem o papel `professional` é 400 apontando o campo — mesma recusa de `TransferProfessionalRequest.professionalId`.",
            "type": ["null", "string"],
            "format": "uuid"
          },
          "percent": {
            "description": "O percentual geral DESTA venda, quando o operador quer dizer \"aqui é 3%\". Escala de 4 casas: `10000` = 1%.\n\n**`null` não é zero: é \"use o perfil\".** Ausente ou nulo, o servidor lê a faixa GERAL do cadastro da pessoa (a de `productGroupId` nulo) e congela o valor dela na linha. Zero explícito é participação sem comissão — o atendente que responde pela venda e não ganha por ela —, e é caso real: o legado tem 1.212 lançamentos de Reserva Técnica para 11.103 pedidos justamente porque nem toda participação vira pagamento.\n\nFora de `0`–`1000000` é 400.",
            "type": ["integer", "null"],
            "format": "int32"
          },
          "isPrincipal": {
            "description": "Um por papel, no máximo. Duas linhas do mesmo papel com `true` é 400 apontando as duas — e a recusa é mais restrita que o legado nos DOIS papéis, de propósito: lá o `Principal` não tem unicidade e o risco é de ETL, com documento antigo trazendo dois.",
            "type": "boolean"
          },
          "validFrom": {
            "description": "Início da vigência. `null` = vale desde a emissão do documento.",
            "type": ["null", "string"],
            "format": "date"
          }
        }
      },
      "OrderParticipantsWriteRequest": {
        "description": "Proposto. A grade de participação INTEIRA — `PUT` substitui o conjunto, como todo `PUT` deste contrato. Linha que não vier no corpo é linha REMOVIDA.\n\n**É uma requisição só, e não N, porque as linhas se conferem entre si:** \"um principal por papel\" e a soma dos percentuais são regras do CONJUNTO, e conjunto que chega em pedaços tem um instante em que ele é inválido. Mesmo argumento de `installments` na condição de pagamento e de `environments` no documento.",
        "required": ["participants"],
        "type": "object",
        "properties": {
          "participants": {
            "description": "As linhas, em qualquer ordem. Lista **vazia** apaga a participação do documento — e é gesto legítimo: venda sem indicação existe.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/OrderParticipantWriteRequest"
            }
          }
        }
      },
      "CommissionTierDto": {
        "description": "Proposto. Uma faixa do PERFIL de participação de uma pessoa — o cadastro, não o documento. `IndicacaoGrupProd` (perfil do profissional, 7.569 linhas) e `ComissaoPremiacaoGrup` (perfil do colaborador, 16 linhas) do legado, que carregam o MESMO trio percentual · desconto · operador.\n\n**Um schema só para os dois papéis, e a economia não é de digitação:** separar duplicaria o motor de cálculo, que é a única parte onde errar custa dinheiro de verdade. O que muda entre colaborador e profissional é a porta (`/api/employees/{id}/…` × `/api/partners/{id}/…`) e o que o ganho vira depois — comissão de um lado, Reserva Técnica do outro.\n\n**O cadastro NÃO tem vigência**, e o legado também não a tem em `IndicacaoGrupProd`. Quem protege a apuração de ontem de um perfil editado hoje é o CONGELAMENTO no documento (`OrderParticipantTierDto`), não uma data aqui. Sem esse congelamento, a ausência de vigência seria defeito; com ele, é simplificação correta — e fica escrito para quem for tentado a acrescentá-la.",
        "required": ["id", "operator", "discountPercent", "percent", "active"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "productGroupId": {
            "description": "O grupo — `CatalogLookupDto.id`, kind `GRUPO_PRODUTO`.\n\n**`null` é a faixa GERAL**, a que vale para grupo sem linha própria, e é ela que vira o `percent` do participante quando a pessoa entra num documento. Não é ausência de dado: é a linha que responde por todo o resto, e por isso `null` aqui é significado e não buraco.",
            "type": ["null", "string"],
            "format": "uuid"
          },
          "productGroupName": {
            "description": "Nome do grupo, ecoado pelo servidor; `null` na faixa geral. **Aqui ele NÃO é congelado**, ao contrário do homônimo em `OrderParticipantTierDto`: isto é cadastro, e cadastro mostra o nome de hoje.",
            "type": ["null", "string"]
          },
          "operator": {
            "$ref": "#/components/schemas/CommissionTierOperator"
          },
          "discountPercent": {
            "description": "O desconto contra o qual o operador compara. Escala de 4 casas: `10000` = 1%. Fora de `0`–`1000000` é 400.",
            "type": "integer",
            "format": "int32"
          },
          "percent": {
            "description": "O percentual que esta faixa paga. Mesma escala — `10000` = 1%.",
            "type": "integer",
            "format": "int32"
          },
          "active": {
            "description": "Desativação lógica (padrão 8). Faixa inativa não entra no cálculo e continua legível nos documentos que a congelaram — que é o ponto: apagar a faixa não pode mudar o que já foi apurado, e nem conseguiria, porque o documento tem a cópia dela.",
            "type": "boolean"
          }
        }
      },
      "PagedResultOfCommissionTierDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CommissionTierDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "CommissionTierWriteRequest": {
        "description": "Proposto. Uma faixa do perfil, na escrita.",
        "required": ["operator", "discountPercent", "percent", "active"],
        "type": "object",
        "properties": {
          "id": {
            "description": "Ecoa a linha existente. Ausente = linha nova. `id` que não é faixa DESTA pessoa é 400 apontando o campo.",
            "type": ["null", "string"],
            "format": "uuid"
          },
          "productGroupId": {
            "description": "O grupo, ou `null` para a faixa GERAL. Id que não é um `catalog-lookups` **ativo** de kind `GRUPO_PRODUTO` é 400 e não 404: o que está errado é o campo do corpo, não o recurso pedido.",
            "type": ["null", "string"],
            "format": "uuid"
          },
          "operator": {
            "$ref": "#/components/schemas/CommissionTierOperator"
          },
          "discountPercent": {
            "type": "integer",
            "format": "int32"
          },
          "percent": {
            "type": "integer",
            "format": "int32"
          },
          "active": {
            "type": "boolean"
          }
        }
      },
      "CommissionTiersWriteRequest": {
        "description": "Proposto. O perfil INTEIRO de uma pessoa — `PUT` substitui o conjunto. Faixa que não vier é faixa removida.\n\nConjunto e não linha a linha porque a validação é do conjunto: a condição de uma faixa (grupo + operador + desconto) tem de ser única, e duas linhas com a MESMA condição e percentuais diferentes fazem o cálculo depender da ordem de leitura — o defeito que ninguém reproduz.",
        "required": ["tiers"],
        "type": "object",
        "properties": {
          "tiers": {
            "description": "As faixas, em qualquer ordem. Lista **vazia** apaga o perfil — a pessoa passa a participar sem percentual próprio, e é o estado de quem ainda não foi configurado.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CommissionTierWriteRequest"
            }
          }
        }
      },
      "TechnicalReserveDto": {
        "description": "Proposto. Um LANÇAMENTO de Reserva Técnica — `Reserva_tecnica` do legado (1.212 linhas para 11.103 pedidos: nem toda participação vira pagamento).\n\n**RT é o que o profissional externo recebe pela indicação.** O documento guarda a participação; este lançamento é o momento em que ela vira valor apurado, com situação e tipo próprios — e é a partir dele que o fechamento gera título no contas a pagar.\n\n**O valor vem quebrado por NATUREZA e não num total só**, porque é assim que o legado apura (`Ret_tec_luminaria` / `_materiais` / `_servico` / `_total`): produto e serviço pagam por regras diferentes — as faixas por grupo só alcançam produto, e serviço cai no percentual geral. Um total único apagaria a distinção que decide o número.",
        "required": [
          "id",
          "orderId",
          "orderNumber",
          "partnerId",
          "partnerName",
          "kind",
          "status",
          "productCents",
          "serviceCents",
          "totalCents",
          "issuedAt"
        ],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "orderId": {
            "description": "O pedido de venda que originou a reserva.",
            "type": "string",
            "format": "uuid"
          },
          "orderNumber": {
            "description": "Número do pedido, resolvido pelo servidor — a tela mostra a origem sem uma segunda requisição.",
            "type": "string"
          },
          "partnerId": {
            "description": "O profissional externo — `PartnerDto.id` com papel `professional`.",
            "type": "string",
            "format": "uuid"
          },
          "partnerName": {
            "type": "string"
          },
          "kind": {
            "description": "`Ret_tipo` do legado, e o vocabulário sai de uma medição e não de um palpite: o legado faz `update … ParSV_serie='1' where Ret_tipo='PROJETO'` e `'2' where <> 'PROJETO'`, então são dois valores e a série do documento depende de qual.\n\n- `project` — a reserva de um projeto (`PROJETO`).\n- `standalone` — a avulsa, o resto.",
            "type": "string",
            "enum": ["project", "standalone"]
          },
          "status": {
            "description": "`Ret_situacao` do legado. Lançamento CANCELA, não desativa — `active` de cadastro não serve aqui, pela mesma razão de `QuoteDetailDto.status`.",
            "type": "string",
            "enum": ["active", "cancelled"]
          },
          "productCents": {
            "description": "A parte da reserva que veio de PRODUTO, em centavos. **Calculada pelo servidor** — ver `TechnicalReserveWriteRequest`.",
            "type": "integer",
            "format": "int64"
          },
          "serviceCents": {
            "description": "A parte que veio de SERVIÇO, em centavos. Calculada pelo servidor.",
            "type": "integer",
            "format": "int64"
          },
          "totalCents": {
            "description": "A soma das duas, em centavos. Vem calculada e NÃO é refeita na tela: percentual de 4 casas sobre centavos arredonda, e a conta refeita no cliente erra o último centavo em parte dos casos — mesmo argumento de `QuoteGroupDiscountDto.discountCents`.",
            "type": "integer",
            "format": "int64"
          },
          "issuedAt": {
            "description": "Data do lançamento.",
            "type": "string",
            "format": "date"
          },
          "note": {
            "type": ["null", "string"]
          },
          "cancelledAt": {
            "description": "Quando foi cancelado. `null` enquanto não for.",
            "type": ["null", "string"],
            "format": "date-time"
          }
        }
      },
      "PagedResultOfTechnicalReserveDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/TechnicalReserveDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "TechnicalReserveWriteRequest": {
        "description": "Proposto. Lança a Reserva Técnica de um profissional sobre um pedido.\n\n**O corpo NÃO tem valor, e a ausência é a decisão central deste trilho.** Quem calcula `productCents` e `serviceCents` é o SERVIDOR, sobre a participação congelada no documento, as linhas dele e a devolução já abatida. Valor vindo do corpo seria o cliente afirmando quanto o profissional ganha — a mesma família do percentual, e a mesma recusa.\n\n**É aqui que mora o \"automático\" do legado.** `Par_RTautomatico` liga o CÁLCULO da participação (o rótulo do checkbox é `Calcular Automaticamente a Part.`), não um carimbo no documento: `Ven_RtAutomatico` e `Ven_RtCalcular` são colunas órfãs, vazias em 34.136 linhas, sem editor em tela nenhuma e escritas por ninguém no corpus inteiro de SQL do executável. Não existe flag de \"RT automática\" para portar — o automático é propriedade do cálculo, e o cálculo é do servidor.",
        "required": ["orderId", "partnerId", "kind"],
        "type": "object",
        "properties": {
          "orderId": {
            "description": "O pedido. Documento cancelado é 409 — venda desfeita não paga indicação. Pedido sem participação deste profissional é 409: não há o que reservar.",
            "type": "string",
            "format": "uuid"
          },
          "partnerId": {
            "description": "O profissional externo. Parceiro sem o papel `professional` é 400 apontando o campo.",
            "type": "string",
            "format": "uuid"
          },
          "kind": {
            "type": "string",
            "enum": ["project", "standalone"]
          },
          "issuedAt": {
            "description": "Data do lançamento. Ausente = hoje.",
            "type": ["null", "string"],
            "format": "date"
          },
          "note": {
            "type": ["null", "string"]
          }
        }
      },
      "CommissionEarningRowDto": {
        "description": "Proposto. O que UMA participação rendeu no período — a linha da apuração.\n\n**Os três números vêm juntos de propósito: base, percentual e valor.** Guardar os três é o que permite conferir a conta um ano depois sem reprocessar nada, e é exatamente o que o relatório do legado não tinha — lá se via o valor, e para saber de onde ele saiu era preciso refazer a consulta com os cadastros de hoje.",
        "required": [
          "orderId",
          "orderNumber",
          "participantId",
          "role",
          "personName",
          "orderDiscountPercent",
          "baseCents",
          "percent",
          "amountCents"
        ],
        "type": "object",
        "properties": {
          "orderId": {
            "type": "string",
            "format": "uuid"
          },
          "orderNumber": {
            "type": "string"
          },
          "participantId": {
            "description": "A participação que rendeu — `OrderParticipantDto.id`.",
            "type": "string",
            "format": "uuid"
          },
          "role": {
            "type": "string",
            "enum": ["attendant", "professional"]
          },
          "employeeId": {
            "type": ["null", "string"],
            "format": "uuid"
          },
          "partnerId": {
            "type": ["null", "string"],
            "format": "uuid"
          },
          "personName": {
            "description": "Colaborador ou parceiro, o que houver.",
            "type": "string"
          },
          "orderDiscountPercent": {
            "description": "O desconto do DOCUMENTO que decidiu qual faixa casou. Vem na linha porque sem ele a apuração é um número sem explicação: é este valor que o `operator` da faixa comparou.",
            "type": "integer",
            "format": "int32"
          },
          "baseCents": {
            "description": "A base sobre a qual o percentual incidiu, em centavos — produto e serviço do MESMO documento, com devolução já abatida. **Devolução reduz a base**: o legado desconta `DevolucaoProduto` por grupo antes de apurar (`FrmRT.Qrydevolucao`), e fechamento que a ignorasse pagaria a mais.",
            "type": "integer",
            "format": "int64"
          },
          "percent": {
            "description": "O percentual aplicado, escala de 4 casas. É o da FAIXA que casou, quando casou alguma; o geral da participação, quando não.",
            "type": "integer",
            "format": "int32"
          },
          "amountCents": {
            "description": "O ganho, em centavos.",
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "CommissionEarningsSummaryDto": {
        "description": "Os totais do período inteiro — não da página. Somar as linhas da página daria o total de 20 registros com cara de total do mês.",
        "required": ["amountCents", "participationCount", "orderCount"],
        "type": "object",
        "properties": {
          "amountCents": {
            "type": "integer",
            "format": "int64"
          },
          "participationCount": {
            "type": "integer",
            "format": "int32"
          },
          "orderCount": {
            "description": "Quantos DOCUMENTOS distintos entraram na conta — não é a soma das participações, porque um documento paga várias pessoas.",
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "CommissionEarningsReportDto": {
        "description": "Proposto. A apuração de um período: o que cada pessoa ganhou, calculado NO SERVIDOR sobre documento fechado.\n\n**É consulta e não fechamento: nada aqui grava.** Ver a apuração antes de fechar é o gesto normal, e ele não pode ter efeito — no legado, quem só queria conferir acabava criando lançamento.\n\n**Tudo o que entra na conta sai de linha gravada** — base, percentual, faixa e desconto. Quem chama escolhe o PERÍODO, e só.",
        "required": ["from", "to", "page", "pageSize", "total", "summary", "rows"],
        "type": "object",
        "properties": {
          "from": {
            "type": "string",
            "format": "date"
          },
          "to": {
            "type": "string",
            "format": "date"
          },
          "page": {
            "type": "integer",
            "format": "int32"
          },
          "pageSize": {
            "type": "integer",
            "format": "int32"
          },
          "total": {
            "type": "integer",
            "format": "int32"
          },
          "summary": {
            "$ref": "#/components/schemas/CommissionEarningsSummaryDto"
          },
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CommissionEarningRowDto"
            }
          }
        }
      },
      "CommissionClosingDto": {
        "description": "Proposto. O FECHAMENTO de um período — `FechamentoComissao(MesAno, dataInicial, dataFinal, Situacao, ContaUnica)` do legado.\n\nFechar é o passo em que participação apurada vira dinheiro: cada linha do fechamento é um título a pagar. Por isso o período fechado é PASSADO — não se reabre, não se reescreve, e correção se faz com fechamento novo.",
        "required": ["id", "periodStart", "periodEnd", "status", "entryCount", "totalCents"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "periodStart": {
            "description": "Primeiro dia, inclusive.",
            "type": "string",
            "format": "date"
          },
          "periodEnd": {
            "description": "Último dia, inclusive.",
            "type": "string",
            "format": "date"
          },
          "status": {
            "description": "`Situacao` do legado.\n\n- `open` — criado e ainda em conferência.\n- `closed` — fechado; `closedAt` preenchido. **Fechado sem data de fechamento é estado que não existe no mundo**, e o servidor não o produz.",
            "type": "string",
            "enum": ["open", "closed"]
          },
          "closedAt": {
            "type": ["null", "string"],
            "format": "date-time"
          },
          "closedByEmployeeId": {
            "type": ["null", "string"],
            "format": "uuid"
          },
          "closedByEmployeeName": {
            "type": ["null", "string"]
          },
          "entryCount": {
            "description": "Quantas linhas o fechamento gerou. Zero não acontece — período sem ganho não vira fechamento (ver `CommissionClosingResultDto`).",
            "type": "integer",
            "format": "int32"
          },
          "totalCents": {
            "description": "A soma das linhas, em centavos.",
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "PagedResultOfCommissionClosingDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CommissionClosingDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "CommissionClosingWriteRequest": {
        "description": "Proposto. Fecha um período.\n\n**Não há corpo além das datas — nem lista de quem pagar, nem valores.** O que se paga é o que a apuração devolve, e a apuração sai de linha gravada. Um corpo com pessoas e valores seria o cliente escolhendo quem recebe quanto.",
        "required": ["periodStart", "periodEnd"],
        "type": "object",
        "properties": {
          "periodStart": {
            "description": "Primeiro dia, inclusive. Recorte por DIA e não por instante: quem pede agosto quer o dia 31 inteiro.",
            "type": "string",
            "format": "date"
          },
          "periodEnd": {
            "description": "Último dia, inclusive. Anterior a `periodStart` é 400 apontando o campo.",
            "type": "string",
            "format": "date"
          }
        }
      },
      "CommissionClosingResultDto": {
        "description": "Proposto. O que o fechamento fez — e ele tem DOIS desfechos bem-sucedidos, não um.\n\n**`nothing-to-close` não é erro, e essa é a decisão que o desenho carrega.** Período sem ganho é o mês parado, não pedido inválido: responder 4xx faria a tela mostrar erro para uma operação que funcionou. E criar um fechamento VAZIO seria pior — travaria o período para quando o lançamento atrasado entrasse, e o lançamento atrasado é o caso comum de quem fecha comissão.\n\nO desfecho viaja em `outcome`, campo próprio, porque aqui não é erro: o discriminador em `type` é a regra do `ProblemDetails`, e resposta de sucesso não tem `ProblemDetails`.",
        "required": ["outcome"],
        "type": "object",
        "properties": {
          "outcome": {
            "description": "- `closed` — o período fechou; `closing` traz o fechamento gravado.\n- `nothing-to-close` — não havia ganho no período. Nada foi gravado e `closing` é `null`.",
            "type": "string",
            "enum": ["closed", "nothing-to-close"]
          },
          "closing": {
            "description": "O fechamento gravado, ou `null` em `nothing-to-close`.",
            "oneOf": [
              {
                "$ref": "#/components/schemas/CommissionClosingDto"
              },
              {
                "type": "null"
              }
            ]
          }
        }
      },
      "CommissionClosingEntryDto": {
        "description": "Proposto. Uma LINHA do fechamento — o que uma pessoa recebeu por um fato.\n\n**Append-only.** Linha de fechamento é o registro de que alguém foi pago; ela não se edita nem se apaga, e correção se faz com fechamento novo. Mesmo regime do `audit_log`.\n\n**Ganho zero não vira linha** — o participante sem comissão (o atendente que responde pela venda e não ganha por ela) apareceria como título de R$ 0,00 no contas a pagar.",
        "required": [
          "id",
          "personName",
          "sourceType",
          "sourceId",
          "baseCents",
          "percent",
          "amountCents"
        ],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "employeeId": {
            "type": ["null", "string"],
            "format": "uuid"
          },
          "partnerId": {
            "type": ["null", "string"],
            "format": "uuid"
          },
          "personName": {
            "type": "string"
          },
          "sourceType": {
            "description": "QUAL fato gerou este pagamento. Com `sourceId`, forma a chave que o refechamento reconhece — a chave natural do legado transportada: lá o par era (tipo de documento RESERVADO do sistema, id do lançamento), o título de RT sendo `contas_apagar` com `Tpd_codigo = 1004` e `Ctp_cod_documento = Reserva_tecnica.Ret_codigo`. Os tipos 1001–1007 são reservados e a lista escolhível pelo usuário os exclui.\n\n- `order` — a participação num pedido de venda.\n- `technical-reserve` — um lançamento de Reserva Técnica.",
            "type": "string",
            "enum": ["order", "technical-reserve"]
          },
          "sourceId": {
            "type": "string",
            "format": "uuid"
          },
          "sourceNumber": {
            "description": "O número do documento de origem, resolvido pelo servidor — o que a tela mostra.",
            "type": ["null", "string"]
          },
          "baseCents": {
            "type": "integer",
            "format": "int64"
          },
          "percent": {
            "type": "integer",
            "format": "int32"
          },
          "amountCents": {
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "PagedResultOfCommissionClosingEntryDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CommissionClosingEntryDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "OrderItemFulfillmentDto": {
        "description": "Proposto. O progresso físico de UMA linha do pedido. As três quantidades convivem — a linha de 10 peças com 10 liberadas, 6 separadas e 2 entregues é o caso normal de uma cozinha que sai em três viagens —, e por isso o estado é derivação e não coluna. O legado guarda os mesmos parciais em `controle_entrega_prod` (`cep_quantidade_separada`, `cep_quantidade_entregue`).",
        "required": [
          "lineNumber",
          "description",
          "quantity",
          "quantityReleased",
          "quantityPicked",
          "quantityDelivered",
          "physicalState",
          "partial",
          "pendingRelease",
          "pendingPick",
          "pendingDelivery",
          "percentDelivered"
        ],
        "type": "object",
        "properties": {
          "lineNumber": {
            "description": "Coluna `Item` do pedido.",
            "type": "integer",
            "format": "int32"
          },
          "description": {
            "description": "Descrição congelada na emissão.",
            "type": "string"
          },
          "environmentCode": {
            "description": "Ambiente da linha (`CatalogLookupDto.id`, kind `AMBIENTE`).",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "environmentName": {
            "description": "Nome congelado do ambiente.",
            "type": ["string", "null"]
          },
          "quantity": {
            "description": "Quantidade vendida. Até 3 casas.",
            "type": "number",
            "format": "double"
          },
          "quantityReleased": {
            "description": "Quanto já foi LIBERADO para separação — e, com isso, reservado no depósito.",
            "type": "number",
            "format": "double"
          },
          "quantityPicked": {
            "description": "Quanto já saiu da prateleira. É o ato que BAIXA estoque.",
            "type": "number",
            "format": "double"
          },
          "quantityDelivered": {
            "description": "Quanto já saiu com romaneio. NÃO baixa estoque de novo: a peça já saiu do galpão na separação, e baixar aqui é o defeito clássico da expedição — o saldo cai duas vezes e a conferência do mês não fecha.",
            "type": "number",
            "format": "double"
          },
          "physicalState": {
            "description": "O degrau da escada física, DERIVADO das três quantidades e nunca guardado. É o mais avançado que cobre a linha INTEIRA: `delivered` só quando entregue == quantidade, `picked` só quando separado == quantidade, e assim por diante. A alternativa — o degrau mais avançado com QUALQUER progresso — marcaria como entregue a linha de 10 com 1 entregue, e é assim que peça fica no galpão com o pedido dizendo que saiu. Errar para baixo mostra item a mais na fila de separação; errar para cima faz o cliente ligar. O `partial` ao lado carrega o resto da verdade.",
            "type": "string",
            "enum": ["pending", "released", "picked", "delivered"]
          },
          "partial": {
            "description": "Há progresso ACIMA do degrau exibido em `physicalState`. É o que permite a tela pintar \"6 de 10\" sem mentir sobre o degrau.",
            "type": "boolean"
          },
          "pendingRelease": {
            "description": "Quanto ainda falta liberar.",
            "type": "number",
            "format": "double"
          },
          "pendingPick": {
            "description": "Quanto está liberado e ainda não foi separado.",
            "type": "number",
            "format": "double"
          },
          "pendingDelivery": {
            "description": "Quanto está separado e ainda não foi entregue.",
            "type": "number",
            "format": "double"
          },
          "percentDelivered": {
            "description": "Percentual ENTREGUE, não separado. Quem pergunta \"quanto falta\" quer saber o que chegou na casa dele; o separado é informação do galpão. Inteiro de 0 a 100.",
            "type": "integer",
            "format": "int32"
          },
          "scheduledDeliveryAt": {
            "description": "Data prometida do item. HERDA a do ambiente quando o item não tem uma própria — e a herança é de LEITURA, não coluna: gravá-la nos dois lugares faria mudar a data do ambiente deixar os itens antigos apontando para o dia velho. `scheduledDateInherited` diz qual dos dois respondeu.",
            "type": ["string", "null"],
            "format": "date"
          },
          "scheduledDateInherited": {
            "description": "A data acima veio do AMBIENTE, não do item.",
            "type": "boolean"
          }
        }
      },
      "OrderFulfillmentDto": {
        "description": "Proposto. A \"Situação do Pedido de Venda\" — a consulta nº 17 do volume 02 do legado (`FrmConsultaSeparacaoPedidoVenda`). É a tela que a loja abre quando o cliente liga perguntando onde está a cozinha: item a item, quanto foi liberado, separado e entregue, com a data prometida e o percentual.",
        "required": [
          "orderId",
          "orderNumber",
          "status",
          "physicalState",
          "percentDelivered",
          "items"
        ],
        "type": "object",
        "properties": {
          "orderId": {
            "type": "string",
            "format": "uuid"
          },
          "orderNumber": {
            "description": "Número do pedido (`OrderDto.number`).",
            "type": "string"
          },
          "status": {
            "description": "Situação do DOCUMENTO (`OrderDto.status`) — outra coisa que o degrau físico.",
            "type": "string"
          },
          "physicalState": {
            "description": "O degrau da escada física, DERIVADO das três quantidades e nunca guardado. É o mais avançado que cobre a linha INTEIRA: `delivered` só quando entregue == quantidade, `picked` só quando separado == quantidade, e assim por diante. A alternativa — o degrau mais avançado com QUALQUER progresso — marcaria como entregue a linha de 10 com 1 entregue, e é assim que peça fica no galpão com o pedido dizendo que saiu. Errar para baixo mostra item a mais na fila de separação; errar para cima faz o cliente ligar. O `partial` ao lado carrega o resto da verdade.",
            "type": "string",
            "enum": ["pending", "released", "picked", "delivered"]
          },
          "percentDelivered": {
            "description": "Percentual entregue do pedido inteiro, ponderado pela QUANTIDADE de cada linha: a linha de 100 peças e a de 1 não pesam igual no que o cliente ainda espera.",
            "type": "integer",
            "format": "int32"
          },
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/OrderItemFulfillmentDto"
            }
          }
        }
      },
      "FulfillmentFactDto": {
        "description": "Proposto. O ato gravado — uma linha do log append-only de `order_fulfillments` — junto do progresso RESULTANTE da linha. Devolver os dois numa resposta só evita a segunda consulta que toda tela faria em seguida, e garante que o número exibido é o que ficou gravado, não o que o cliente calculou.",
        "required": ["id", "kind", "quantity", "occurredAt", "item"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "kind": {
            "description": "Qual ato aconteceu.",
            "type": "string",
            "enum": ["release", "pick", "deliver"]
          },
          "quantity": {
            "description": "Quanto foi lançado NESTE ato.",
            "type": "number",
            "format": "double"
          },
          "locationId": {
            "description": "Depósito envolvido. Sai da LIBERAÇÃO e a separação o herda: reservar num galpão e baixar de outro deixaria a reserva presa no primeiro para sempre. `null` na linha de serviço, que não tem peça.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "deliveryId": {
            "description": "Romaneio do ato de entrega. Sempre presente em `kind: deliver`.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "stockMovementId": {
            "description": "Movimento do kardex que a separação causou (`StockMovementDto.id`). `null` fora de `kind: pick` e na linha de serviço.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "occurredAt": {
            "description": "Quando o ato foi lançado.",
            "type": "string",
            "format": "date-time"
          },
          "item": {
            "$ref": "#/components/schemas/OrderItemFulfillmentDto"
          }
        }
      },
      "ReleaseOrderItemRequest": {
        "description": "Proposto. Liberar é o gesto que autoriza a peça a sair — e o único da escada que exige a permissão `venda:liberar-entrega`. No legado é a flag de cabeçalho `Ven_LiberaSeparacao` do `FrmLiberarProdutos`; aqui é quantidade por linha, porque a liberação parcial existe na operação e a flag não a representa.",
        "required": ["quantity"],
        "type": "object",
        "properties": {
          "quantity": {
            "description": "Quanto liberar NESTE ato, somando ao já liberado. Acima do vendido é 409 `urn:cabinet:erro:liberacao-acima-do-vendido`.",
            "type": "number",
            "format": "double"
          },
          "locationId": {
            "description": "Depósito de onde a peça vai sair. Omitido, o servidor resolve o padrão da empresa. É AQUI que ele se decide, e não na separação.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "notes": {
            "description": "Observação do ato, guardada no log.",
            "type": ["string", "null"]
          }
        }
      },
      "PickOrderItemRequest": {
        "description": "Proposto. Separar tira a peça da prateleira e BAIXA o estoque, na mesma transação do fato. A baixa não recusa saldo negativo, ao contrário do lançamento de tela: peça vendida sai antes de a nota de entrada do fornecedor ser lançada, e recusar aqui pararia a expedição pela papelada de terceiro.",
        "required": ["quantity"],
        "type": "object",
        "properties": {
          "quantity": {
            "description": "Quanto separar NESTE ato. Acima do LIBERADO é 409 `urn:cabinet:erro:separacao-sem-liberacao` — quem pula a liberação não separa, e o gate é CHECK no banco, não regra de handler.",
            "type": "number",
            "format": "double"
          },
          "locationId": {
            "description": "Exceção declarada: o depósito da liberação é o que vale, e este campo só deve ser usado pelo operador que SABE que a peça mudou de galpão — é ele quem paga o acerto da reserva.",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "notes": {
            "type": ["string", "null"]
          }
        }
      },
      "AddDeliveryItemRequest": {
        "description": "Proposto. Lança um item entregue DENTRO de um romaneio aberto. A entrega sempre tem romaneio — não existe entregar avulso, porque seis meses depois, quando o cliente diz que não recebeu, é o romaneio que responde.",
        "required": ["lineNumber", "quantity"],
        "type": "object",
        "properties": {
          "lineNumber": {
            "description": "A linha do pedido a que o romaneio pertence.",
            "type": "integer",
            "format": "int32"
          },
          "quantity": {
            "description": "Acima do SEPARADO é 409 `urn:cabinet:erro:entrega-sem-separacao`. Não se entrega o que ninguém tirou da prateleira.",
            "type": "number",
            "format": "double"
          },
          "notes": {
            "type": ["string", "null"]
          }
        }
      },
      "PickingQueueItemDto": {
        "description": "Proposto. Uma linha da FILA DE SEPARAÇÃO: o que já pode sair da prateleira e ainda não saiu. É a \"Situação do Pedido\" virada do avesso, e o recorte (`liberado > separado`, pedido ativo) mora no servidor — a fila de uma loja com dois anos de pedidos é uma varredura de tudo que já se vendeu se a conta subir para a tela.",
        "required": [
          "orderId",
          "orderNumber",
          "customerName",
          "lineNumber",
          "description",
          "pendingPick"
        ],
        "type": "object",
        "properties": {
          "orderId": {
            "type": "string",
            "format": "uuid"
          },
          "orderNumber": {
            "type": "string"
          },
          "customerName": {
            "type": "string"
          },
          "lineNumber": {
            "type": "integer",
            "format": "int32"
          },
          "description": {
            "type": "string"
          },
          "environmentName": {
            "type": ["string", "null"]
          },
          "pendingPick": {
            "description": "Quanto está liberado e esperando separação.",
            "type": "number",
            "format": "double"
          },
          "scheduledDeliveryAt": {
            "description": "Data prometida, já com a herança do ambiente aplicada. É a primeira chave da ordenação, `NULLS LAST`: item sem data combinada não fura a fila de quem tem uma.",
            "type": ["string", "null"],
            "format": "date"
          }
        }
      },
      "DeliveryDto": {
        "description": "Proposto. O ROMANEIO — o `Controle_entrega` do legado. Cabeçalho do que saiu numa viagem, com transportadora, data prometida e, depois de fechado, quando saiu e quem recebeu.",
        "required": ["id", "number", "orderId", "status"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "number": {
            "description": "Número do romaneio, sequência global do grupo atribuída pelo servidor. String pelo mesmo motivo de `OrderDto.number`: número de documento se lê e se digita, não se soma.",
            "type": "string"
          },
          "orderId": {
            "description": "Pedido de venda a que o romaneio pertence.",
            "type": "string",
            "format": "uuid"
          },
          "orderNumber": {
            "type": ["string", "null"]
          },
          "customerName": {
            "type": ["string", "null"]
          },
          "status": {
            "description": "`open` recebe itens; `closed` e `cancelled` são terminais. **Cancelar NÃO desfaz os fatos já lançados** — o log é append-only por GRANT, e corrigir é estornar, não editar o passado.",
            "type": "string",
            "enum": ["open", "closed", "cancelled"]
          },
          "scheduledFor": {
            "description": "Data prometida da viagem.",
            "type": ["string", "null"],
            "format": "date"
          },
          "deliveredAt": {
            "description": "Quando a entrega aconteceu. Obrigatória para fechar.",
            "type": ["string", "null"],
            "format": "date-time"
          },
          "carrierId": {
            "description": "Transportadora (`PartnerDto.id`).",
            "type": ["string", "null"],
            "format": "uuid"
          },
          "carrierName": {
            "description": "Nome da transportadora CONGELADO ao lado do id, como o do cliente no pedido: renomear a empresa de frete não pode reescrever romaneio assinado.",
            "type": ["string", "null"]
          },
          "receivedBy": {
            "description": "Quem recebeu. Obrigatório para fechar.",
            "type": ["string", "null"]
          },
          "receivedDocument": {
            "description": "Documento de quem recebeu.",
            "type": ["string", "null"]
          },
          "closedAt": {
            "type": ["string", "null"],
            "format": "date-time"
          },
          "notes": {
            "type": ["string", "null"]
          }
        }
      },
      "DeliveryItemDto": {
        "description": "Proposto. Uma linha do romaneio. **Não existe tabela própria para ela** (diverge do `ShipmentItem` do OFBiz): o conteúdo do romaneio SÃO os fatos `deliver` que apontam para ele, e uma segunda lista teria de ser mantida igual à primeira.",
        "required": ["lineNumber", "description", "quantity", "occurredAt"],
        "type": "object",
        "properties": {
          "lineNumber": {
            "type": "integer",
            "format": "int32"
          },
          "description": {
            "type": "string"
          },
          "environmentName": {
            "type": ["string", "null"]
          },
          "quantity": {
            "description": "Quanto saiu NESTE romaneio.",
            "type": "number",
            "format": "double"
          },
          "occurredAt": {
            "type": "string",
            "format": "date-time"
          },
          "notes": {
            "type": ["string", "null"]
          }
        }
      },
      "DeliveryDetailDto": {
        "description": "Proposto. O romaneio com o que foi lançado nele.",
        "required": ["id", "number", "orderId", "status", "items"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "number": {
            "type": "string"
          },
          "orderId": {
            "type": "string",
            "format": "uuid"
          },
          "orderNumber": {
            "type": ["string", "null"]
          },
          "customerName": {
            "type": ["string", "null"]
          },
          "status": {
            "type": "string",
            "enum": ["open", "closed", "cancelled"]
          },
          "scheduledFor": {
            "type": ["string", "null"],
            "format": "date"
          },
          "deliveredAt": {
            "type": ["string", "null"],
            "format": "date-time"
          },
          "carrierId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "carrierName": {
            "type": ["string", "null"]
          },
          "receivedBy": {
            "type": ["string", "null"]
          },
          "receivedDocument": {
            "type": ["string", "null"]
          },
          "signatureRef": {
            "description": "Referência da assinatura do recebedor — o `Controle_entrega_Assinaturas` do legado. **Texto, e o upload está ADIADO:** binário numa tabela sob RLS mistura documento e arquivo e faz o `pg_dump` crescer sem teto. A coluna existe para que o dia do upload seja migração de zero linhas.",
            "type": ["string", "null"]
          },
          "closedAt": {
            "type": ["string", "null"],
            "format": "date-time"
          },
          "notes": {
            "type": ["string", "null"]
          },
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DeliveryItemDto"
            }
          }
        }
      },
      "CreateDeliveryRequest": {
        "description": "Proposto. Abre o romaneio. Nasce `open` e VAZIO — os itens entram um a um.",
        "required": ["orderId"],
        "type": "object",
        "properties": {
          "orderId": {
            "description": "Pedido de venda. Pedido cancelado não recebe entrega (409).",
            "type": "string",
            "format": "uuid"
          },
          "scheduledFor": {
            "type": ["string", "null"],
            "format": "date"
          },
          "carrierId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "carrierName": {
            "description": "Congelado. Omitido, o servidor copia o nome atual do parceiro.",
            "type": ["string", "null"]
          },
          "notes": {
            "type": ["string", "null"]
          }
        }
      },
      "CloseDeliveryRequest": {
        "description": "Proposto. Fechar é o que faz o romaneio valer alguma coisa, e por isso exige QUANDO e para QUEM — a exigência é CHECK no banco, não regra de handler. **Romaneio vazio não fecha** (409 `urn:cabinet:erro:entrega-vazia`): fechar entrega sem item é assinar papel em branco, some da fila de abertos e ninguém percebe até alguém procurar a peça. Quem não saiu se CANCELA.",
        "required": ["deliveredAt", "receivedBy"],
        "type": "object",
        "properties": {
          "deliveredAt": {
            "description": "Quando a entrega aconteceu.",
            "type": "string",
            "format": "date-time"
          },
          "receivedBy": {
            "description": "Quem recebeu, por extenso.",
            "type": "string"
          },
          "receivedDocument": {
            "description": "Documento de quem recebeu.",
            "type": ["string", "null"]
          },
          "signatureRef": {
            "description": "Referência da assinatura, quando houver.",
            "type": ["string", "null"]
          }
        }
      },
      "PagedResultOfPickingQueueItemDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PickingQueueItemDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "PagedResultOfDeliveryDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DeliveryDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "PriceIndexDto": {
        "required": ["id", "supplierId", "indexValue", "active"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "supplierId": {
            "type": "string",
            "format": "uuid",
            "description": "O fornecedor a que o índice pertence — `PartnerDto.id` com o papel `supplier`. **Um índice por fornecedor por empresa:** o segundo cadastro para o mesmo fornecedor é 409, porque dois índices ativos deixariam a precificação depender de qual linha a consulta encontrasse primeiro, e o sintoma seria preço mudando sozinho entre dois orçamentos."
          },
          "supplierName": {
            "type": ["string", "null"],
            "description": "Nome do fornecedor, para exibição. Não é escrita — o `PUT` ignora o que vier aqui, como todo campo de junção deste contrato."
          },
          "costProfileId": {
            "type": ["string", "null"],
            "format": "uuid",
            "description": "O perfil de custo de onde saem as deduções da compra — `CostProfileDto.id`. **`null` é caso real e não pendência:** fornecedor sem cascata e sem crédito existe às dezenas no legado, e ali o líquido É o preço de tabela. Um perfil vazio obrigatório seria linha de zeros que alguém teria de criar para cada fornecedor simples.\n\nO perfil tem de ser do MESMO fornecedor: apontar para o de outro faria a peça ser precificada com o desconto que ninguém negociou para ela. É 400."
          },
          "costProfileName": {
            "type": ["string", "null"],
            "description": "Nome do perfil, para exibição. Não é escrita."
          },
          "indexValue": {
            "type": "integer",
            "format": "int32",
            "description": "O multiplicador. **Int com 4 casas implícitas sobre 1: `25600` = 2,5600.** É `Ipr_Indice`. Tem de ser `> 0`; `10000` (1,0000) é válido e quer dizer vender pelo líquido de compra."
          },
          "active": {
            "type": "boolean",
            "description": "Desativação lógica. Índice inativo **não precifica** — a variante daquele fornecedor volta a devolver `calculatedUnitPriceCents` nulo, em vez de sair com índice velho."
          }
        },
        "description": "Proposto. O **ÍNDICE DE VALOR DE VENDA** por fornecedor — `Indice_preco` do legado (376 linhas, mediana 2,5600). É a METADE VENDA da formação de preço; `CostProfileDto` é a de compra e custo.\n\n**A fórmula que esta linha fecha:**\n\n```\nliquido = tablePriceCents − discount1..4 em cascata − créditos ICMS/PIS/COFINS   <- do PERFIL DE CUSTO\nVENDA   = round(liquido × indexValue)                                            <- daqui\n```\n\nO preço de venda é o líquido de COMPRA multiplicado pelo índice, **aplicado ANTES de qualquer imposto de saída**. Imposto e taxa entram só na apuração de CUSTO e LUCRO — eles não empurram preço. É a mesma decomposição que `CostSimulationDto` publica: o que este índice multiplica é exatamente o `netPurchaseCents` de lá.\n\n**As deduções NÃO se repetem aqui, e é o ponto do desenho.** Os quatro descontos em cascata e os três créditos moram em `CostProfileDto`, e esta linha aponta para ele por `costProfileId` — como no legado, onde `Indice_preco` é \"ligado a um `Custo` e a um fornecedor\". Copiar os sete campos para cá daria duas autoridades sobre os mesmos números, e elas divergiriam na primeira edição: o custo apurado por um caminho e o preço pelo outro, com o operador sem saber qual dos dois acreditar.\n\n**O índice é do FORNECEDOR, não do produto nem da categoria.** Quem o pendurar no produto reproduz outra coisa: no legado a mesma peça comprada de dois fornecedores rende dois preços, porque cada fornecedor tem seu índice e sua tabela. É o desenho que dá sentido ao multi-fornecedor de `ProductDto.suppliers` — sem ele, o segundo fornecedor é rótulo.\n\n**Escala: inteiro com 4 casas implícitas sobre 1** — `25600` = 2,5600. Repare que ela NÃO é a de `discountPercent` nem a dos campos de `CostProfileDto`, que são 4 casas sobre 100%: o índice é MULTIPLICADOR, e escrever 2,56 como 256% seria a mesma informação com um fator 100 de armadilha entre as duas leituras. Ponto flutuante é vetado para dinheiro, e `numeric` chegaria como string ao cliente, que a converteria para multiplicar — voltando ao float na única conta que importa.\n\n**`indexValue` = `10000` (1,0000) é VÁLIDO e não é erro de cadastro.** São 16 assim no legado, 11 de fornecedores reais, e pela fórmula significam vender pelo líquido de compra. Parte é legítima — há fornecedor cuja tabela já sai com a margem embutida. Recusar 1,0000 faria o servidor rejeitar o cadastro real desses 11; \"corrigir\" o índice na migração inventaria margem que ninguém decidiu. O limite é `> 0`."
      },
      "PriceIndexWriteRequest": {
        "required": ["supplierId", "indexValue"],
        "type": "object",
        "properties": {
          "supplierId": {
            "type": "string",
            "format": "uuid"
          },
          "indexValue": {
            "type": "integer",
            "format": "int32",
            "description": "4 casas implícitas, `> 0`."
          },
          "costProfileId": {
            "type": ["string", "null"],
            "format": "uuid",
            "description": "Ausente ou nulo = sem deduções, e o líquido é o preço de tabela."
          },
          "active": {
            "type": ["boolean", "null"],
            "description": "Ausente = `true`."
          }
        },
        "description": "Proposto. Cria ou substitui o índice de um fornecedor. **`PUT` substitui a linha INTEIRA** — campo ausente NÃO preserva o valor anterior, como todo `PUT` deste contrato: omitir `costProfileId` DESLIGA o perfil, não o mantém."
      },
      "PagedResultOfPriceIndexDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PriceIndexDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "VariantTablePriceDto": {
        "required": ["supplierId", "tablePriceCents"],
        "type": "object",
        "properties": {
          "supplierId": {
            "type": "string",
            "format": "uuid",
            "description": "O fornecedor de quem é esta tabela — `PartnerDto.id`."
          },
          "supplierName": {
            "type": ["string", "null"],
            "description": "Para exibição. Não é escrita."
          },
          "supplierCode": {
            "type": ["string", "null"],
            "description": "Código da peça NO fornecedor, vindo de `ProductSupplierDto`. É a língua em que o comprador confere a tabela recebida — o código do Cabinet não aparece na lista de preços que o fornecedor manda."
          },
          "tablePriceCents": {
            "type": "integer",
            "format": "int64",
            "description": "O **preço de tabela** do fornecedor para esta variante, em centavos — `Preco_Produto.Pre_Tabela`. É o preço de LISTA da compra, antes dos descontos em cascata: não é o que se paga nem o que se vende. É exatamente o `tablePriceCents` que `CostSimulationRequest` recebe."
          }
        },
        "description": "Proposto. O preço de tabela de UM fornecedor para UMA variante.\n\n**Por que a chave é (variante × fornecedor), e não só a variante.** A tabela é do fornecedor, e o índice também. A mesma peça comprada de dois fornecedores tem duas tabelas e dois índices, e rende dois preços de venda diferentes — é o caso que `ProductDto.suppliers` publica e que `isDefault` desempata. Guardar um preço de tabela por variante forçaria escolher um fornecedor no cadastro e jogar a segunda tabela fora, que é justamente a informação de que o comprador precisa para decidir de quem comprar.\n\n**Variante e não produto** porque acabamento e tamanho mudam o preço de lista: é o que `Preco_Produto` faz no legado."
      },
      "VariantTablePricesWriteRequest": {
        "required": ["prices"],
        "type": "object",
        "properties": {
          "prices": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/VariantTablePriceDto"
            },
            "description": "A lista INTEIRA. Fornecedor que não vier é apagado."
          }
        },
        "description": "Proposto. **`PUT` substitui a lista inteira**, como o `PUT` de parcelas da condição de pagamento: a lista que vier no corpo passa a ser a lista, e o fornecedor que não veio deixa de ter tabela para esta variante. Corpo parcial não preserva linha.\n\nÉ uma requisição só, e não N escritas linha a linha, pela mesma razão do `QuoteDetailDto`: a tabela chega do fornecedor como uma lista e é conferida como uma lista. Gravar linha a linha faria um `Gravar` virar N requisições sem transação entre elas, deixando meia tabela no banco quando a terceira falhasse — e meia tabela de preço precifica errado em silêncio."
      },
      "FinancialTitleDto": {
        "description": "Proposto. Um TÍTULO financeiro — a conta a pagar ou a receber. Menus `Financeiro → Contas a Pagar → Lançamento` e `Financeiro → Contas a Receber → Lançamentos` do legado (`contas_apagar`, 30.043 linhas; `contas_receber`, 9.076).\n\n**Um recurso para as duas telas, discriminado por `direction`** — e não `/api/payables` + `/api/receivables`. O precedente é `/api/partners` com filtro `role`: no legado as duas tabelas são a MESMA tabela com o prefixo trocado (`Ctp_`/`Ctr_`), coluna a coluna, e é assim que elas envelheceram — a correção feita num lado só chegava ao outro por cópia manual. Dois caminhos aqui duplicariam parcela, baixa, quitação em lote e conciliação, que não têm uma diferença sequer entre pagar e receber. O que MUDA é o sinal do dinheiro e a parte (fornecedor de um lado, cliente do outro), e as duas coisas cabem numa coluna.\n\n**O título não guarda valor pago.** `totalCents` é o que foi lançado; o que sobrou sai da soma das parcelas (`openCents`), porque quem quita é a PARCELA. Materializar o pago no cabeçalho criaria um segundo lugar onde a mesma verdade mora, e o legado paga essa conta: `Ctp_valor_total_original` diverge da soma de `contas_apagar_det` em base de produção.\n\n**Não há DELETE.** Título que some é dinheiro que o sistema esqueceu de dever. Desistir é `POST /{id}/cancel`, e ele recusa (409) o título que já tem baixa.\n\n**Não há `TipoContaFinanceira` aqui, e a ausência é DECISÃO PENDENTE, não esquecimento.** O legado classifica o título em seis categorias — OFICIAL, SIMULAÇÃO, APROVISIONAMENTO, CONTA CLONE, HISTÓRICO, AGRUPADA — com FK declarada, e guardar ensaio na mesma tabela do que vale obriga TODO relatório a filtrar. `docs/harvest/financeiro.md` §11.3 põe as três saídas na mesa (replicar · virar rascunho de status · não migrar as não-oficiais) e nenhuma foi escolhida. O campo entra quando a escolha existir; inventá-lo agora fixaria a mais cara das três.",
        "required": [
          "id",
          "direction",
          "number",
          "partnerId",
          "partnerName",
          "issuedAt",
          "totalCents",
          "settledCents",
          "openCents",
          "status",
          "sourceType",
          "installments"
        ],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "description": "A CONTRAPARTE — fornecedor quando `payable`, cliente quando `receivable`.\n\n**Dívida herdada do legado, e ela é real:** lá o contraparte é polimórfico por TEXTO (`Ctp_vinculo`, sem FK nem CHECK) e assume oito valores — `CLIENTE`, `FORNECEDOR`, `INDICAÇÃO`, `PROFISSIONAL EXTERNO`, `PESSOAL`, `TRANSPORTADORA`, `MATRIZ/FILIAL`, `OUTROS` (`docs/harvest/financeiro.md` §3.3). Os quatro primeiros são UMA tabela aqui (`partners`, com os três papéis), e a tradução melhora o modelo. Os outros quatro NÃO cabem: título a pagar para um colaborador (comissão) ou para outra empresa do grupo não tem parceiro para apontar.\n\nFica assim porque é o que o banco decidiu — `financial_titles.partner_id` nasceu `NOT NULL` referenciando `partners` —, e alargar isso é migração, não campo de contrato. Enquanto durar, a comissão do profissional passa por parceiro (ele é `is_professional`) e a do colaborador não tem como virar título."
          },
          "direction": {
            "description": "`payable` é conta a PAGAR (a parte é o fornecedor), `receivable` é conta a RECEBER (a parte é o cliente). É o único eixo que separa as duas telas.",
            "type": "string",
            "enum": ["payable", "receivable"]
          },
          "number": {
            "description": "O número do título, atribuído pelo servidor e sequencial POR DIREÇÃO — pagar e receber contam separado, como no legado.\n\n**Texto e não inteiro, pelo mesmo motivo de `OrderDto.number`:** o legado carrega SÉRIE junto do número (`ParSV_serie`, três caracteres, presente nas duas tabelas de título), e número com série não é um inteiro. Publicar `int64` aqui obrigaria a inventar um segundo campo no dia em que a série viesse junto — e a tela já teria formatado o primeiro como número.",
            "type": "string"
          },
          "documentNumber": {
            "type": ["string", "null"],
            "description": "O número do DOCUMENTO que originou o título — nota fiscal, duplicata, recibo. É o que o financeiro procura quando o fornecedor liga, e por isso entra na busca (`q`)."
          },
          "partnerId": {
            "type": "string",
            "format": "uuid",
            "description": "A CONTRAPARTE — fornecedor quando `payable`, cliente quando `receivable`.\n\n**Dívida herdada do legado, e ela é real:** lá o contraparte é polimórfico por TEXTO (`Ctp_vinculo`, sem FK nem CHECK) e assume oito valores — `CLIENTE`, `FORNECEDOR`, `INDICAÇÃO`, `PROFISSIONAL EXTERNO`, `PESSOAL`, `TRANSPORTADORA`, `MATRIZ/FILIAL`, `OUTROS` (`docs/harvest/financeiro.md` §3.3). Os quatro primeiros são UMA tabela aqui (`partners`, com os três papéis), e a tradução melhora o modelo. Os outros quatro NÃO cabem: título a pagar para um colaborador (comissão) ou para outra empresa do grupo não tem parceiro para apontar.\n\nFica assim porque é o que o banco decidiu — `financial_titles.partner_id` nasceu `NOT NULL` referenciando `partners` —, e alargar isso é migração, não campo de contrato. Enquanto durar, a comissão do profissional passa por parceiro (ele é `is_professional`) e a do colaborador não tem como virar título."
          },
          "partnerName": {
            "type": "string",
            "description": "Nome da parte CONGELADO na emissão. Renomear o cadastro amanhã não reescreve quem devia ontem — o mesmo motivo de `QuoteDto`."
          },
          "documentTypeId": {
            "type": ["string", "null"],
            "format": "uuid",
            "description": "A ESPÉCIE do documento (`Tipo_documento`, 33 linhas no legado). Cadastro por empresa, sem caminho no contrato ainda — ver a dívida declarada em `FinancialTitleWriteRequest`."
          },
          "chartAccountId": {
            "type": ["string", "null"],
            "format": "uuid",
            "description": "A conta do PLANO DE CONTAS. Hoje vem de `GET /api/catalog-lookups?kind=PLANO_CONTA`."
          },
          "costCenterId": {
            "type": ["string", "null"],
            "format": "uuid",
            "description": "O CENTRO DE CUSTO. Cadastro por empresa, sem caminho no contrato ainda."
          },
          "paymentTermId": {
            "type": ["string", "null"],
            "format": "uuid",
            "description": "A condição de pagamento que gerou as parcelas (`GET /api/payment-terms`). Referência histórica: as parcelas já estão gravadas e não se recalculam se a condição mudar."
          },
          "sourceType": {
            "description": "De ONDE o título nasceu. `manual` é digitado na tela; `sale_order` é o título que o PEDIDO DE VENDA gerou; `goods_receipt` é o que a entrada de nota gerou.\n\n**É o campo que sustenta o DoD do trilho** — \"título nascido de um pedido é quitado pela tela\" só é verificável se a tela souber dizer de onde a linha veio. Sem ele, o título automático e o digitado ficam indistinguíveis, e o operador reemite à mão o que o pedido já emitiu.",
            "type": "string",
            "enum": ["manual", "sale_order", "goods_receipt"]
          },
          "sourceId": {
            "type": ["string", "null"],
            "format": "uuid",
            "description": "O documento de origem. `null` quando `sourceType` é `manual`, e preenchido nos outros dois — tipo e id andam juntos."
          },
          "issuedAt": {
            "type": "string",
            "format": "date",
            "description": "Data de EMISSÃO do título."
          },
          "competenceMonth": {
            "type": ["string", "null"],
            "format": "date",
            "description": "Mês de COMPETÊNCIA, sempre no dia 1. Separado da emissão porque a despesa de dezembro paga em janeiro pertence a dezembro no resultado, e é essa a pergunta que o DRE faz."
          },
          "totalCents": {
            "type": "integer",
            "format": "int64",
            "description": "Total LANÇADO do título, em centavos. É a soma das parcelas na emissão."
          },
          "settledCents": {
            "type": "integer",
            "format": "int64",
            "description": "Quanto já foi QUITADO, somando as baixas das parcelas. Derivado — o servidor calcula, não guarda."
          },
          "openCents": {
            "type": "integer",
            "format": "int64",
            "description": "Quanto FALTA: `totalCents - settledCents`. Derivado. Vem pronto porque é a coluna que a tela ordena e soma no rodapé, e calculá-la no cliente sobre uma página daria total de página, não de título."
          },
          "status": {
            "description": "`open` enquanto sobra saldo, `settled` quando a última parcela fecha, `cancelled` na desistência. **Não é campo de escrita**: quem o move é a baixa e o `cancel`.",
            "type": "string",
            "enum": ["open", "settled", "cancelled"]
          },
          "notes": {
            "type": ["string", "null"]
          },
          "installments": {
            "description": "As PARCELAS do título, em ordem de `sequence`. Sempre pelo menos uma — título à vista é título de uma parcela, e não um título sem parcela: sem essa regra, quitar à vista precisaria de um caminho próprio.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FinancialInstallmentDto"
            }
          }
        }
      },
      "FinancialInstallmentDto": {
        "description": "Proposto. Uma PARCELA do título — e a unidade de quitação. `contas_apagar_det` (42.161 linhas) e `contas_Receber_det` (18.555) do legado.\n\n**A parcela existe para ser quitada em PARTES.** `settledCents` menor que `amountCents` com a parcela ainda `open` é o pagamento parcial, que o legado faz por permissão especial (`PERMITIR QUITAÇÃO COM VALOR A MENOS QUE O VALOR DO VENCIMENTO`) e que aqui é a mesma coisa: ver `SettlementWriteRequest`.",
        "required": [
          "id",
          "titleId",
          "direction",
          "sequence",
          "dueDate",
          "amountCents",
          "settledCents",
          "openCents",
          "status"
        ],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "description": "A CONTRAPARTE — fornecedor quando `payable`, cliente quando `receivable`.\n\n**Dívida herdada do legado, e ela é real:** lá o contraparte é polimórfico por TEXTO (`Ctp_vinculo`, sem FK nem CHECK) e assume oito valores — `CLIENTE`, `FORNECEDOR`, `INDICAÇÃO`, `PROFISSIONAL EXTERNO`, `PESSOAL`, `TRANSPORTADORA`, `MATRIZ/FILIAL`, `OUTROS` (`docs/harvest/financeiro.md` §3.3). Os quatro primeiros são UMA tabela aqui (`partners`, com os três papéis), e a tradução melhora o modelo. Os outros quatro NÃO cabem: título a pagar para um colaborador (comissão) ou para outra empresa do grupo não tem parceiro para apontar.\n\nFica assim porque é o que o banco decidiu — `financial_titles.partner_id` nasceu `NOT NULL` referenciando `partners` —, e alargar isso é migração, não campo de contrato. Enquanto durar, a comissão do profissional passa por parceiro (ele é `is_professional`) e a do colaborador não tem como virar título."
          },
          "titleId": {
            "type": "string",
            "format": "uuid",
            "description": "A CONTRAPARTE — fornecedor quando `payable`, cliente quando `receivable`.\n\n**Dívida herdada do legado, e ela é real:** lá o contraparte é polimórfico por TEXTO (`Ctp_vinculo`, sem FK nem CHECK) e assume oito valores — `CLIENTE`, `FORNECEDOR`, `INDICAÇÃO`, `PROFISSIONAL EXTERNO`, `PESSOAL`, `TRANSPORTADORA`, `MATRIZ/FILIAL`, `OUTROS` (`docs/harvest/financeiro.md` §3.3). Os quatro primeiros são UMA tabela aqui (`partners`, com os três papéis), e a tradução melhora o modelo. Os outros quatro NÃO cabem: título a pagar para um colaborador (comissão) ou para outra empresa do grupo não tem parceiro para apontar.\n\nFica assim porque é o que o banco decidiu — `financial_titles.partner_id` nasceu `NOT NULL` referenciando `partners` —, e alargar isso é migração, não campo de contrato. Enquanto durar, a comissão do profissional passa por parceiro (ele é `is_professional`) e a do colaborador não tem como virar título."
          },
          "direction": {
            "type": "string",
            "description": "Ecoado do título, porque a listagem de parcelas é o que a tela de quitação em lote consome — e ali a linha precisa dizer sozinha se é dinheiro que entra ou que sai.",
            "enum": ["payable", "receivable"]
          },
          "titleNumber": {
            "description": "Número do título, ecoado na listagem de parcelas para a tela não resolver o id. Texto, como em `FinancialTitleDto.number`.",
            "type": ["string", "null"]
          },
          "partnerId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "partnerName": {
            "type": ["string", "null"],
            "description": "Nome da parte, ecoado — presente na listagem de parcelas, dispensável dentro do título, que já o traz no cabeçalho."
          },
          "sequence": {
            "type": "integer",
            "format": "int32",
            "description": "A ordem da parcela DENTRO do título, 1-based."
          },
          "dueDate": {
            "type": "string",
            "format": "date",
            "description": "Data de VENCIMENTO. É por ela que a tela de contas a pagar abre o dia — e o `sortBy` padrão da listagem de parcelas."
          },
          "amountCents": {
            "type": "integer",
            "format": "int64",
            "description": "Valor da parcela no vencimento, em centavos."
          },
          "settledCents": {
            "type": "integer",
            "format": "int64",
            "description": "Quanto já foi baixado nesta parcela, somando as baixas."
          },
          "openCents": {
            "type": "integer",
            "format": "int64",
            "description": "Saldo em aberto: `amountCents - settledCents`. É contra ele que a baixa é conferida."
          },
          "status": {
            "description": "`open` enquanto há saldo, `settled` quando `settledCents` alcança `amountCents`. **Derivado no banco**, não escrito: parcela cujo estado é coluna solta fica `open` com saldo zero na primeira baixa que esquecer de atualizá-lo.",
            "type": "string",
            "enum": ["open", "settled"]
          },
          "overdue": {
            "type": "boolean",
            "description": "`true` quando a parcela está `open` e `dueDate` já passou. Vem do servidor porque \"hoje\" é o dia do SERVIDOR: calculado no cliente, o relógio errado da estação marca vencido o que não está — e é essa a coluna que o operador usa para decidir o que pagar."
          },
          "paymentModeId": {
            "type": ["string", "null"],
            "format": "uuid",
            "description": "O MODO previsto para esta parcela (dinheiro, transferência, cartão). Previsão, não fato: o modo do pagamento efetivo é o da baixa."
          },
          "documentNumber": {
            "type": ["string", "null"],
            "description": "Número do documento DESTA parcela — a duplicata 3/5 tem número próprio, diferente do da nota."
          },
          "settlements": {
            "description": "As BAIXAS desta parcela, em ordem de data. Presente no detalhe do título; ausente na listagem de parcelas, que é grade e não extrato.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FinancialSettlementDto"
            }
          }
        }
      },
      "FinancialSettlementDto": {
        "description": "Proposto. Uma BAIXA — o fato de que dinheiro andou contra uma parcela. `Contas_apagar_pag` (41.981 linhas) e `Contas_receber_pag` (17.885) do legado.\n\n**A baixa aponta a CONTA, não só o modo** — e é aqui que este contrato fecha um elo que estava aberto por escrito (issue api#112). O módulo financeiro já grava o modo (`paymentModeId`: dinheiro, PIX, cheque), e saber que foi PIX não diz em qual conta bancária o dinheiro entrou. É a conta que o extrato lê, que a conciliação casa e que o saldo soma. Por isso o destino é obrigatório na escrita, e a baixa produz o movimento de caixa correspondente na MESMA transação — `CashMovementDto.sourceType = 'settlement'`.\n\n**Não há estorno neste contrato, e a ausência é declarada.** A baixa errada se desfaz por decisão de quem pode desfazê-la, e essa decisão não foi tomada; sem ela, um caminho de estorno seria eu escolhendo quem apaga dinheiro lançado. Fica como dívida nomeada, não como esquecimento.",
        "required": [
          "id",
          "installmentId",
          "settledOn",
          "amountCents",
          "interestCents",
          "fineCents",
          "discountCents",
          "paidCents",
          "paymentModeId"
        ],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "description": "A CONTRAPARTE — fornecedor quando `payable`, cliente quando `receivable`.\n\n**Dívida herdada do legado, e ela é real:** lá o contraparte é polimórfico por TEXTO (`Ctp_vinculo`, sem FK nem CHECK) e assume oito valores — `CLIENTE`, `FORNECEDOR`, `INDICAÇÃO`, `PROFISSIONAL EXTERNO`, `PESSOAL`, `TRANSPORTADORA`, `MATRIZ/FILIAL`, `OUTROS` (`docs/harvest/financeiro.md` §3.3). Os quatro primeiros são UMA tabela aqui (`partners`, com os três papéis), e a tradução melhora o modelo. Os outros quatro NÃO cabem: título a pagar para um colaborador (comissão) ou para outra empresa do grupo não tem parceiro para apontar.\n\nFica assim porque é o que o banco decidiu — `financial_titles.partner_id` nasceu `NOT NULL` referenciando `partners` —, e alargar isso é migração, não campo de contrato. Enquanto durar, a comissão do profissional passa por parceiro (ele é `is_professional`) e a do colaborador não tem como virar título."
          },
          "installmentId": {
            "type": "string",
            "format": "uuid",
            "description": "A CONTRAPARTE — fornecedor quando `payable`, cliente quando `receivable`.\n\n**Dívida herdada do legado, e ela é real:** lá o contraparte é polimórfico por TEXTO (`Ctp_vinculo`, sem FK nem CHECK) e assume oito valores — `CLIENTE`, `FORNECEDOR`, `INDICAÇÃO`, `PROFISSIONAL EXTERNO`, `PESSOAL`, `TRANSPORTADORA`, `MATRIZ/FILIAL`, `OUTROS` (`docs/harvest/financeiro.md` §3.3). Os quatro primeiros são UMA tabela aqui (`partners`, com os três papéis), e a tradução melhora o modelo. Os outros quatro NÃO cabem: título a pagar para um colaborador (comissão) ou para outra empresa do grupo não tem parceiro para apontar.\n\nFica assim porque é o que o banco decidiu — `financial_titles.partner_id` nasceu `NOT NULL` referenciando `partners` —, e alargar isso é migração, não campo de contrato. Enquanto durar, a comissão do profissional passa por parceiro (ele é `is_professional`) e a do colaborador não tem como virar título."
          },
          "settledOn": {
            "type": "string",
            "format": "date",
            "description": "Data em que o dinheiro andou. É ela que o FECHAMENTO de período compara — baixa com data dentro de período fechado é 409."
          },
          "amountCents": {
            "type": "integer",
            "format": "int64",
            "description": "Valor ABATIDO do saldo da parcela, em centavos. É este que reduz `openCents`, e é ele que a quitação a menor deixa abaixo do saldo."
          },
          "interestCents": {
            "type": "integer",
            "format": "int64",
            "description": "JUROS cobrados no atraso. Somam ao que saiu do caixa e NÃO abatem a parcela — a dívida de R$ 100 paga com R$ 10 de juros quita 100 e tira 110 da conta."
          },
          "fineCents": {
            "type": "integer",
            "format": "int64",
            "description": "MULTA por atraso. Mesma mecânica dos juros."
          },
          "discountCents": {
            "type": "integer",
            "format": "int64",
            "description": "DESCONTO concedido na quitação. Subtrai do que saiu do caixa e não muda o abatimento: parcela de R$ 100 com R$ 5 de desconto continua quitada em 100 e tira 95 da conta."
          },
          "paidCents": {
            "type": "integer",
            "format": "int64",
            "description": "O que de fato ANDOU na conta: `amountCents + interestCents + fineCents - discountCents`. Derivado no banco, e vem pronto porque é ele — e não `amountCents` — que tem de bater com o extrato bancário."
          },
          "paymentModeId": {
            "type": "string",
            "format": "uuid",
            "description": "O MEIO pelo qual o dinheiro andou (`GET /api/payment-modes`). Obrigatório: baixa sem meio não se concilia."
          },
          "bankAccountId": {
            "type": ["string", "null"],
            "format": "uuid",
            "description": "A CONTA BANCÁRIA que recebeu ou pagou. Exclusiva com `cashRegisterId` — exatamente um dos dois vem preenchido."
          },
          "cashRegisterId": {
            "type": ["string", "null"],
            "format": "uuid",
            "description": "O CAIXA que recebeu ou pagou. Exclusivo com `bankAccountId`."
          },
          "batchId": {
            "type": ["string", "null"],
            "format": "uuid",
            "description": "O LOTE de quitação, quando a baixa veio de `POST /api/financial-settlements/batch`. `null` na baixa avulsa. É o que permite listar \"o que foi pago naquele lote\" — o `LotePagCtRec` do legado."
          },
          "notes": {
            "type": ["string", "null"]
          },
          "createdAt": {
            "type": ["string", "null"],
            "format": "date-time"
          }
        }
      },
      "FinancialTitleWriteRequest": {
        "description": "Proposto. Cria ou SUBSTITUI o título inteiro, parcelas incluídas (PUT substitui o registro inteiro, como todo PUT deste contrato).\n\n**As parcelas vêm do cliente, e não de um `paymentTermId` que o servidor expandiria.** A condição de pagamento sugere o plano na TELA; o que se grava é o que o operador aceitou, porque no financeiro a parcela negociada quase nunca é a calculada — o fornecedor concede uma data, o cliente pede outra. Expandir aqui faria a condição mandar mais que o acordo, e reescreveria o plano de todo título ao mudar a condição.\n\n**O PUT recusa (409) o título que já tem baixa.** Reescrever um título quitado moveria dinheiro já conciliado; o que se corrige depois da primeira baixa é a próxima baixa, não o passado.\n\n**Dívida declarada:** `documentTypeId` e `costCenterId` são ids de cadastros por empresa (`document_types`, `cost_centers`) que **ainda não têm caminho neste contrato** — o menu `Tabelas → Financeiro` do legado é trilho próprio. Os dois são opcionais aqui de propósito: publicar o campo antes do combo deixa a tela em branco, que é honesto; deixá-lo de fora obrigaria a migrar o título depois.",
        "required": ["direction", "partnerId", "issuedAt", "installments"],
        "type": "object",
        "properties": {
          "direction": {
            "type": "string",
            "enum": ["payable", "receivable"]
          },
          "partnerId": {
            "type": "string",
            "format": "uuid",
            "description": "Fornecedor quando `payable`, cliente quando `receivable`. O servidor confere o PAPEL da parte — parceiro sem o papel da direção é 400."
          },
          "documentNumber": {
            "type": ["string", "null"]
          },
          "documentTypeId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "chartAccountId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "costCenterId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "paymentTermId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "issuedAt": {
            "type": "string",
            "format": "date"
          },
          "competenceMonth": {
            "type": ["string", "null"],
            "format": "date",
            "description": "Mês de competência. Se vier com dia diferente de 1, é 400 — o mês é o dado, e aceitar o dia faria duas linhas do mesmo mês competirem."
          },
          "notes": {
            "type": ["string", "null"]
          },
          "installments": {
            "description": "As parcelas, ao menos uma. `sequence` tem de ser 1..N sem buraco, e a soma dos valores tem de bater com o total pretendido do título — o servidor não arredonda diferença.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FinancialInstallmentWriteRequest"
            }
          }
        }
      },
      "FinancialInstallmentWriteRequest": {
        "description": "Proposto. Uma parcela na escrita do título. Valor em centavos e sempre maior que zero: parcela de zero é linha que a grade mostra e o caixa nunca vê.",
        "required": ["sequence", "dueDate", "amountCents"],
        "type": "object",
        "properties": {
          "sequence": {
            "type": "integer",
            "format": "int32"
          },
          "dueDate": {
            "type": "string",
            "format": "date"
          },
          "amountCents": {
            "type": "integer",
            "format": "int64"
          },
          "paymentModeId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "documentNumber": {
            "type": ["string", "null"]
          }
        }
      },
      "SettlementWriteRequest": {
        "description": "Proposto. A BAIXA de uma parcela.\n\n**O destino é obrigatório e exclusivo:** exatamente um entre `bankAccountId` e `cashRegisterId`. Os dois juntos, ou nenhum, é 400. É o que faz a baixa virar linha de extrato — sem conta, o dinheiro é quitado no sistema e invisível no caixa.\n\n**Quitação A MENOS é permissão, não erro.** `amountCents` abaixo de `openCents` da parcela deixa saldo e é recusado com **403** para quem não tem a ação fina — o legado a tem como permissão especial nº 45 (`PERMITIR QUITAÇÃO COM VALOR A MENOS QUE O VALOR DO VENCIMENTO`), e é o mesmo desenho de `venda:desconto-acima-do-teto`: a recusa depende de QUEM pede, não do valor, então é 403 e não 400.\n\n`amountCents` ACIMA do saldo é 409 e não tem permissão que libere: pagar mais do que se deve não é decisão de alçada, é engano de digitação — e o troco não tem onde ser lançado.",
        "required": ["settledOn", "amountCents", "paymentModeId"],
        "type": "object",
        "properties": {
          "settledOn": {
            "type": "string",
            "format": "date"
          },
          "amountCents": {
            "type": "integer",
            "format": "int64"
          },
          "interestCents": {
            "type": "integer",
            "format": "int64",
            "description": "Juros. Ausente = 0."
          },
          "fineCents": {
            "type": "integer",
            "format": "int64",
            "description": "Multa. Ausente = 0."
          },
          "discountCents": {
            "type": "integer",
            "format": "int64",
            "description": "Desconto concedido. Ausente = 0."
          },
          "paymentModeId": {
            "type": "string",
            "format": "uuid",
            "description": "A CONTRAPARTE — fornecedor quando `payable`, cliente quando `receivable`.\n\n**Dívida herdada do legado, e ela é real:** lá o contraparte é polimórfico por TEXTO (`Ctp_vinculo`, sem FK nem CHECK) e assume oito valores — `CLIENTE`, `FORNECEDOR`, `INDICAÇÃO`, `PROFISSIONAL EXTERNO`, `PESSOAL`, `TRANSPORTADORA`, `MATRIZ/FILIAL`, `OUTROS` (`docs/harvest/financeiro.md` §3.3). Os quatro primeiros são UMA tabela aqui (`partners`, com os três papéis), e a tradução melhora o modelo. Os outros quatro NÃO cabem: título a pagar para um colaborador (comissão) ou para outra empresa do grupo não tem parceiro para apontar.\n\nFica assim porque é o que o banco decidiu — `financial_titles.partner_id` nasceu `NOT NULL` referenciando `partners` —, e alargar isso é migração, não campo de contrato. Enquanto durar, a comissão do profissional passa por parceiro (ele é `is_professional`) e a do colaborador não tem como virar título."
          },
          "bankAccountId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "cashRegisterId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "notes": {
            "type": ["string", "null"]
          }
        }
      },
      "SettlementBatchRequest": {
        "description": "Proposto. QUITAÇÃO EM LOTE — N parcelas pagas de uma vez, na mesma data, pelo mesmo meio e contra a mesma conta. É a tela que o financeiro usa todo dia: seleciona os vencimentos do dia e paga o bloco.\n\n**O lote é TUDO OU NADA.** Uma parcela recusada derruba a requisição inteira, e nenhuma baixa fica gravada. O contrário — gravar as que passam e listar as que falharam — parece gentil e é a forma de perder dinheiro: o operador vê o erro, corrige, reenvia o lote inteiro, e as que já tinham passado são pagas duas vezes.\n\n**A resposta traz `batchId`**, e é ele que amarra as baixas como um ato só — o `LotePagCtRec` do legado. Sem o agrupador, desfazer ou conferir um pagamento em bloco exigiria casar data, valor e meio, que é adivinhação assim que dois lotes saem no mesmo dia.\n\n**Cabeçalho decide meio, data e conta; a linha decide valor, juros, multa e desconto.** É a divisão que o caso real pede: paga-se o bloco pelo mesmo banco no mesmo dia, e cada título tem o seu acréscimo.",
        "required": ["settledOn", "paymentModeId", "items"],
        "type": "object",
        "properties": {
          "settledOn": {
            "type": "string",
            "format": "date"
          },
          "paymentModeId": {
            "type": "string",
            "format": "uuid",
            "description": "A CONTRAPARTE — fornecedor quando `payable`, cliente quando `receivable`.\n\n**Dívida herdada do legado, e ela é real:** lá o contraparte é polimórfico por TEXTO (`Ctp_vinculo`, sem FK nem CHECK) e assume oito valores — `CLIENTE`, `FORNECEDOR`, `INDICAÇÃO`, `PROFISSIONAL EXTERNO`, `PESSOAL`, `TRANSPORTADORA`, `MATRIZ/FILIAL`, `OUTROS` (`docs/harvest/financeiro.md` §3.3). Os quatro primeiros são UMA tabela aqui (`partners`, com os três papéis), e a tradução melhora o modelo. Os outros quatro NÃO cabem: título a pagar para um colaborador (comissão) ou para outra empresa do grupo não tem parceiro para apontar.\n\nFica assim porque é o que o banco decidiu — `financial_titles.partner_id` nasceu `NOT NULL` referenciando `partners` —, e alargar isso é migração, não campo de contrato. Enquanto durar, a comissão do profissional passa por parceiro (ele é `is_professional`) e a do colaborador não tem como virar título."
          },
          "bankAccountId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "cashRegisterId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "notes": {
            "type": ["string", "null"]
          },
          "items": {
            "description": "As parcelas do lote, ao menos uma. Parcela repetida na mesma requisição é 400 — duas linhas para a mesma parcela é o mesmo pagamento contado duas vezes, e o cliente consegue ver isso sozinho.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SettlementBatchItem"
            }
          }
        }
      },
      "SettlementBatchItem": {
        "description": "Proposto. Uma parcela dentro do lote. `amountCents` ausente quita o SALDO INTEIRO da parcela — é o caso normal do lote, e obrigar o cliente a repetir um número que o servidor já sabe faria a tela pagar a mais quando o saldo mudasse entre a leitura e o envio.",
        "required": ["installmentId"],
        "type": "object",
        "properties": {
          "installmentId": {
            "type": "string",
            "format": "uuid",
            "description": "A CONTRAPARTE — fornecedor quando `payable`, cliente quando `receivable`.\n\n**Dívida herdada do legado, e ela é real:** lá o contraparte é polimórfico por TEXTO (`Ctp_vinculo`, sem FK nem CHECK) e assume oito valores — `CLIENTE`, `FORNECEDOR`, `INDICAÇÃO`, `PROFISSIONAL EXTERNO`, `PESSOAL`, `TRANSPORTADORA`, `MATRIZ/FILIAL`, `OUTROS` (`docs/harvest/financeiro.md` §3.3). Os quatro primeiros são UMA tabela aqui (`partners`, com os três papéis), e a tradução melhora o modelo. Os outros quatro NÃO cabem: título a pagar para um colaborador (comissão) ou para outra empresa do grupo não tem parceiro para apontar.\n\nFica assim porque é o que o banco decidiu — `financial_titles.partner_id` nasceu `NOT NULL` referenciando `partners` —, e alargar isso é migração, não campo de contrato. Enquanto durar, a comissão do profissional passa por parceiro (ele é `is_professional`) e a do colaborador não tem como virar título."
          },
          "amountCents": {
            "type": ["integer", "null"],
            "format": "int64"
          },
          "interestCents": {
            "type": "integer",
            "format": "int64"
          },
          "fineCents": {
            "type": "integer",
            "format": "int64"
          },
          "discountCents": {
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "SettlementBatchResultDto": {
        "description": "Proposto. O resultado do lote: o agrupador e as baixas que ele gravou.",
        "required": ["batchId", "totalPaidCents", "settlements"],
        "type": "object",
        "properties": {
          "batchId": {
            "type": "string",
            "format": "uuid",
            "description": "A CONTRAPARTE — fornecedor quando `payable`, cliente quando `receivable`.\n\n**Dívida herdada do legado, e ela é real:** lá o contraparte é polimórfico por TEXTO (`Ctp_vinculo`, sem FK nem CHECK) e assume oito valores — `CLIENTE`, `FORNECEDOR`, `INDICAÇÃO`, `PROFISSIONAL EXTERNO`, `PESSOAL`, `TRANSPORTADORA`, `MATRIZ/FILIAL`, `OUTROS` (`docs/harvest/financeiro.md` §3.3). Os quatro primeiros são UMA tabela aqui (`partners`, com os três papéis), e a tradução melhora o modelo. Os outros quatro NÃO cabem: título a pagar para um colaborador (comissão) ou para outra empresa do grupo não tem parceiro para apontar.\n\nFica assim porque é o que o banco decidiu — `financial_titles.partner_id` nasceu `NOT NULL` referenciando `partners` —, e alargar isso é migração, não campo de contrato. Enquanto durar, a comissão do profissional passa por parceiro (ele é `is_professional`) e a do colaborador não tem como virar título."
          },
          "totalPaidCents": {
            "type": "integer",
            "format": "int64",
            "description": "A soma de `paidCents` das baixas — o que de fato saiu (ou entrou) na conta. É o número que o operador confere contra o comprovante do banco."
          },
          "settlements": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FinancialSettlementDto"
            }
          }
        }
      },
      "CashMovementDto": {
        "description": "Proposto. Um MOVIMENTO de caixa ou de conta bancária — a linha do extrato. Menus `Financeiro → Caixa → Lançamento` e `Financeiro → Movimentos Bancários → Lançamentos` do legado.\n\n**Uma tabela para as duas telas, e a conta é banco OU caixa** — exatamente um dos dois ids vem preenchido. Caixa e conta bancária diferem no cadastro e em nada no movimento: mesmo sentido, mesmo valor, mesma classificação, mesma transferência entre eles. Dois recursos exigiriam duas conciliações e dois extratos.\n\n**Sentido e valor são campos separados, e `amountCents` é sempre positivo.** Guardar `-1500` para a saída encurtaria o saldo (`SUM`) e estragaria todo o resto: \"quanto entrou no mês\" viraria um filtro por sinal, e o dia em que um estorno gravasse entrada negativa ele contaria como saída.",
        "required": [
          "id",
          "direction",
          "amountCents",
          "occurredOn",
          "description",
          "sourceType",
          "accountKind",
          "accountName"
        ],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "description": "A CONTRAPARTE — fornecedor quando `payable`, cliente quando `receivable`.\n\n**Dívida herdada do legado, e ela é real:** lá o contraparte é polimórfico por TEXTO (`Ctp_vinculo`, sem FK nem CHECK) e assume oito valores — `CLIENTE`, `FORNECEDOR`, `INDICAÇÃO`, `PROFISSIONAL EXTERNO`, `PESSOAL`, `TRANSPORTADORA`, `MATRIZ/FILIAL`, `OUTROS` (`docs/harvest/financeiro.md` §3.3). Os quatro primeiros são UMA tabela aqui (`partners`, com os três papéis), e a tradução melhora o modelo. Os outros quatro NÃO cabem: título a pagar para um colaborador (comissão) ou para outra empresa do grupo não tem parceiro para apontar.\n\nFica assim porque é o que o banco decidiu — `financial_titles.partner_id` nasceu `NOT NULL` referenciando `partners` —, e alargar isso é migração, não campo de contrato. Enquanto durar, a comissão do profissional passa por parceiro (ele é `is_professional`) e a do colaborador não tem como virar título."
          },
          "accountKind": {
            "description": "Se a linha é de conta bancária ou de caixa. Redundante com o par de ids e presente de propósito: é o que a grade agrupa, e derivá-lo de \"qual id não é nulo\" espalha a regra por toda tela que mostrar a lista.",
            "type": "string",
            "enum": ["bank", "cash"]
          },
          "accountName": {
            "type": "string",
            "description": "Nome da conta ou do caixa, ecoado."
          },
          "bankAccountId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "cashRegisterId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "direction": {
            "description": "`in` é dinheiro que ENTRA, `out` é dinheiro que SAI.",
            "type": "string",
            "enum": ["in", "out"]
          },
          "amountCents": {
            "type": "integer",
            "format": "int64",
            "description": "Valor do movimento, sempre positivo. Quem dá o sinal é `direction`."
          },
          "occurredOn": {
            "type": "string",
            "format": "date",
            "description": "O DIA DE CAIXA do movimento. É com ele que o fechamento de período compara — e por isso é data, não instante: o extrato fecha por dia, não por hora."
          },
          "chartAccountId": {
            "type": ["string", "null"],
            "format": "uuid",
            "description": "Classificação no PLANO DE CONTAS (`GET /api/catalog-lookups?kind=PLANO_CONTA`)."
          },
          "costCenterId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "description": {
            "type": "string",
            "description": "O HISTÓRICO do lançamento. Obrigatório: linha de extrato sem histórico é o que torna a conciliação impossível três meses depois."
          },
          "sourceType": {
            "description": "De onde o movimento veio. `manual` é digitado; `transfer` é a perna de uma transferência; `settlement` é o movimento que uma BAIXA gerou — o elo que liga contas a pagar/receber ao caixa. Os demais são as origens que o servidor já reserva (`sale`, `purchase`, `installment`, `commission`).\n\n**A tela não lança `transfer` nem `settlement` direto**: eles nascem da operação que os causou, e aceitar um deles em `POST /api/cash-movements` deixaria existir transferência de uma perna só.",
            "type": "string",
            "enum": [
              "manual",
              "transfer",
              "settlement",
              "sale",
              "purchase",
              "installment",
              "commission"
            ]
          },
          "sourceId": {
            "type": ["string", "null"],
            "format": "uuid",
            "description": "O documento de origem. Nulo exatamente quando `sourceType` é `manual` ou `transfer` — nos dois casos não há outro documento, e na transferência quem amarra é `transferId`."
          },
          "sourceNumber": {
            "type": ["integer", "null"],
            "format": "int32",
            "description": "A PARCELA de origem, quando `sourceType` é `installment`."
          },
          "transferId": {
            "type": ["string", "null"],
            "format": "uuid",
            "description": "A transferência que gerou este movimento. As duas pernas do par carregam o mesmo id — é assim que \"estas duas linhas são uma transferência\" fica verificável em vez de convencionado."
          },
          "reconciledAt": {
            "type": ["string", "null"],
            "format": "date-time",
            "description": "Quando a linha foi CONCILIADA contra o extrato do banco. Nulo = ainda não conferida, e é esse o filtro que a tela de conciliação abre."
          },
          "createdAt": {
            "type": ["string", "null"],
            "format": "date-time"
          }
        }
      },
      "CashMovementWriteRequest": {
        "description": "Proposto. Um lançamento MANUAL de caixa ou banco.\n\n**Exatamente um destino:** `bankAccountId` ou `cashRegisterId`. Os dois, ou nenhum, é 400.\n\n**Não há PUT nem DELETE de movimento**, e a ausência é a regra do módulo: extrato que se reescreve não concilia com o do banco. O erro se corrige com o lançamento contrário, que é o que o contador chama de estorno e o que deixa os dois fatos visíveis.\n\n**Lançar com `occurredOn` dentro de período já fechado é 409** (`urn:cabinet:erro:periodo-fechado`): o fechamento existe para que o saldo conferido de ontem não mude hoje.",
        "required": ["direction", "amountCents", "occurredOn", "description"],
        "type": "object",
        "properties": {
          "bankAccountId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "cashRegisterId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "direction": {
            "type": "string",
            "enum": ["in", "out"]
          },
          "amountCents": {
            "type": "integer",
            "format": "int64",
            "description": "Sempre positivo, em centavos. Zero ou negativo é 400."
          },
          "occurredOn": {
            "type": "string",
            "format": "date"
          },
          "chartAccountId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "costCenterId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "description": {
            "type": "string"
          }
        }
      },
      "CashTransferRequest": {
        "description": "Proposto. TRANSFERÊNCIA entre contas — as quatro direções que o legado tem como quatro itens de menu (caixa→conta, caixa→caixa, conta→caixa, conta→conta) e que aqui são um caminho só: o que muda entre elas é qual par de ids vem preenchido.\n\n**Dois movimentos ou nenhum.** A transferência grava a saída na origem e a entrada no destino na MESMA transação, as duas apontando o mesmo `transferId`. Meia transferência gravada é dinheiro que sumiu do extrato de uma conta sem aparecer na outra — e é o defeito que só é descoberto no fechamento do mês.\n\n**Origem e destino não podem ser a mesma conta** (400): o par teria as duas pernas na mesma linha do extrato e somaria zero, escondendo um erro de digitação atrás de um saldo correto.",
        "required": ["occurredOn", "amountCents", "description"],
        "type": "object",
        "properties": {
          "occurredOn": {
            "type": "string",
            "format": "date"
          },
          "amountCents": {
            "type": "integer",
            "format": "int64",
            "description": "Valor transferido, positivo, em centavos."
          },
          "description": {
            "type": "string"
          },
          "fromBankAccountId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "fromCashRegisterId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "toBankAccountId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "toCashRegisterId": {
            "type": ["string", "null"],
            "format": "uuid"
          }
        }
      },
      "CashTransferDto": {
        "description": "Proposto. A transferência gravada, com as DUAS pernas. A resposta traz os dois movimentos porque é o que prova o par — e é o que a tela precisa para atualizar os dois extratos sem reconsultar.",
        "required": ["id", "occurredOn", "amountCents", "description", "movements"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "description": "A CONTRAPARTE — fornecedor quando `payable`, cliente quando `receivable`.\n\n**Dívida herdada do legado, e ela é real:** lá o contraparte é polimórfico por TEXTO (`Ctp_vinculo`, sem FK nem CHECK) e assume oito valores — `CLIENTE`, `FORNECEDOR`, `INDICAÇÃO`, `PROFISSIONAL EXTERNO`, `PESSOAL`, `TRANSPORTADORA`, `MATRIZ/FILIAL`, `OUTROS` (`docs/harvest/financeiro.md` §3.3). Os quatro primeiros são UMA tabela aqui (`partners`, com os três papéis), e a tradução melhora o modelo. Os outros quatro NÃO cabem: título a pagar para um colaborador (comissão) ou para outra empresa do grupo não tem parceiro para apontar.\n\nFica assim porque é o que o banco decidiu — `financial_titles.partner_id` nasceu `NOT NULL` referenciando `partners` —, e alargar isso é migração, não campo de contrato. Enquanto durar, a comissão do profissional passa por parceiro (ele é `is_professional`) e a do colaborador não tem como virar título."
          },
          "occurredOn": {
            "type": "string",
            "format": "date"
          },
          "amountCents": {
            "type": "integer",
            "format": "int64"
          },
          "description": {
            "type": "string"
          },
          "movements": {
            "description": "As duas pernas: a `out` da origem e a `in` do destino, nesta ordem.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CashMovementDto"
            }
          }
        }
      },
      "BankAccountDto": {
        "description": "Proposto. Uma CONTA BANCÁRIA da empresa — o destino dos movimentos e das baixas. `Contas_Bancarias` (19 linhas) do legado.\n\n**Só LEITURA neste contrato.** O cadastro (banco, agência, conta, caixa) é o menu `Tabelas → Financeiro`, trilho próprio; publicar a escrita aqui traria junto banco e agência, que são cadastro nacional. O que a FASE do lançamento precisa é escolher a conta, e escolher é ler.",
        "required": ["id", "name", "number", "kind", "active", "openingBalanceCents"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "description": "A CONTRAPARTE — fornecedor quando `payable`, cliente quando `receivable`.\n\n**Dívida herdada do legado, e ela é real:** lá o contraparte é polimórfico por TEXTO (`Ctp_vinculo`, sem FK nem CHECK) e assume oito valores — `CLIENTE`, `FORNECEDOR`, `INDICAÇÃO`, `PROFISSIONAL EXTERNO`, `PESSOAL`, `TRANSPORTADORA`, `MATRIZ/FILIAL`, `OUTROS` (`docs/harvest/financeiro.md` §3.3). Os quatro primeiros são UMA tabela aqui (`partners`, com os três papéis), e a tradução melhora o modelo. Os outros quatro NÃO cabem: título a pagar para um colaborador (comissão) ou para outra empresa do grupo não tem parceiro para apontar.\n\nFica assim porque é o que o banco decidiu — `financial_titles.partner_id` nasceu `NOT NULL` referenciando `partners` —, e alargar isso é migração, não campo de contrato. Enquanto durar, a comissão do profissional passa por parceiro (ele é `is_professional`) e a do colaborador não tem como virar título."
          },
          "name": {
            "type": "string"
          },
          "bankId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "bankCode": {
            "type": ["string", "null"],
            "description": "O código do banco na FEBRABAN (`001`, `341`), ecoado — é como o operador reconhece a conta na lista."
          },
          "bankName": {
            "type": ["string", "null"]
          },
          "branchId": {
            "type": ["string", "null"],
            "format": "uuid"
          },
          "branchNumber": {
            "type": ["string", "null"]
          },
          "number": {
            "type": "string",
            "description": "Número da conta, sem o dígito."
          },
          "digit": {
            "type": ["string", "null"]
          },
          "kind": {
            "type": "string",
            "enum": ["checking", "savings", "investment", "payment"]
          },
          "openingBalanceCents": {
            "type": "integer",
            "format": "int64",
            "description": "SALDO DE ABERTURA da conta, em centavos. O saldo de hoje é ele mais a soma dos movimentos — não há coluna de saldo atual, porque saldo materializado é cache que precisa de quem o reconcilie."
          },
          "active": {
            "type": "boolean"
          }
        }
      },
      "CashRegisterDto": {
        "description": "Proposto. Um CAIXA da empresa. `Bancos_Caixas` do legado, do lado `tipo = caixa`. Só leitura, pelo mesmo motivo de `BankAccountDto`.",
        "required": ["id", "code", "name", "active", "openingBalanceCents"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "description": "A CONTRAPARTE — fornecedor quando `payable`, cliente quando `receivable`.\n\n**Dívida herdada do legado, e ela é real:** lá o contraparte é polimórfico por TEXTO (`Ctp_vinculo`, sem FK nem CHECK) e assume oito valores — `CLIENTE`, `FORNECEDOR`, `INDICAÇÃO`, `PROFISSIONAL EXTERNO`, `PESSOAL`, `TRANSPORTADORA`, `MATRIZ/FILIAL`, `OUTROS` (`docs/harvest/financeiro.md` §3.3). Os quatro primeiros são UMA tabela aqui (`partners`, com os três papéis), e a tradução melhora o modelo. Os outros quatro NÃO cabem: título a pagar para um colaborador (comissão) ou para outra empresa do grupo não tem parceiro para apontar.\n\nFica assim porque é o que o banco decidiu — `financial_titles.partner_id` nasceu `NOT NULL` referenciando `partners` —, e alargar isso é migração, não campo de contrato. Enquanto durar, a comissão do profissional passa por parceiro (ele é `is_professional`) e a do colaborador não tem como virar título."
          },
          "code": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "openingBalanceCents": {
            "type": "integer",
            "format": "int64",
            "description": "Saldo de abertura do caixa, em centavos."
          },
          "active": {
            "type": "boolean"
          }
        }
      },
      "PaymentModeDto": {
        "description": "Proposto. Um MODO DE PAGAMENTO — dinheiro, transferência, cartão, boleto. `Modo` (26 linhas) do legado.\n\nPublicado porque a BAIXA não existe sem ele: `paymentModeId` é obrigatório em toda quitação, e sem a lista a tela de contas a pagar não tem o que oferecer. Só leitura; o cadastro é do trilho de tabelas.",
        "required": ["id", "code", "name", "usableInSettlement", "active"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid",
            "description": "A CONTRAPARTE — fornecedor quando `payable`, cliente quando `receivable`.\n\n**Dívida herdada do legado, e ela é real:** lá o contraparte é polimórfico por TEXTO (`Ctp_vinculo`, sem FK nem CHECK) e assume oito valores — `CLIENTE`, `FORNECEDOR`, `INDICAÇÃO`, `PROFISSIONAL EXTERNO`, `PESSOAL`, `TRANSPORTADORA`, `MATRIZ/FILIAL`, `OUTROS` (`docs/harvest/financeiro.md` §3.3). Os quatro primeiros são UMA tabela aqui (`partners`, com os três papéis), e a tradução melhora o modelo. Os outros quatro NÃO cabem: título a pagar para um colaborador (comissão) ou para outra empresa do grupo não tem parceiro para apontar.\n\nFica assim porque é o que o banco decidiu — `financial_titles.partner_id` nasceu `NOT NULL` referenciando `partners` —, e alargar isso é migração, não campo de contrato. Enquanto durar, a comissão do profissional passa por parceiro (ele é `is_professional`) e a do colaborador não tem como virar título."
          },
          "code": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "adminFeePercent": {
            "type": "integer",
            "format": "int32",
            "description": "Taxa de administração, na escala da casa: inteiro com 4 casas implícitas, `10000` = 1%. É o custo do cartão, e a tela o mostra ao escolher o modo."
          },
          "termDays": {
            "type": "integer",
            "format": "int32",
            "description": "Prazo em dias até o dinheiro cair — 30 no cartão, 0 no dinheiro."
          },
          "fixedDay": {
            "type": ["integer", "null"],
            "format": "int32",
            "description": "Dia fixo do mês em que o modo liquida, quando houver."
          },
          "usableInSettlement": {
            "type": "boolean",
            "description": "Se este modo pode ser escolhido numa BAIXA.\n\n**Não é campo inventado para o caso geral — é o que o legado faz com uma cláusula solta.** A quitação em lote de lá exclui os modos 1000 e 1001 por número, escrito na consulta (`mdo_codigo not in (1000, 1001)`): são pseudo-modos que classificam venda e não quitam nada, o mesmo padrão do `GrupoProduto` 1000=SERVIÇOS / 1001=FRETE (`docs/harvest/financeiro.md` §3.6). Número mágico na consulta é a regra que some no dia em que alguém cadastrar o terceiro; em coluna, ela é cadastro."
          },
          "active": {
            "type": "boolean"
          }
        }
      },
      "PagedResultOfFinancialTitleDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FinancialTitleDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "PagedResultOfFinancialInstallmentDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FinancialInstallmentDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "PagedResultOfCashMovementDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CashMovementDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "PagedResultOfBankAccountDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/BankAccountDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "PagedResultOfCashRegisterDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CashRegisterDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "PagedResultOfPaymentModeDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PaymentModeDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "TemporaryPasswordDto": {
        "description": "Proposto. A senha provisória existe AQUI e em nenhum outro lugar — não há operação que a devolva de novo. Quem chama mostra uma vez e descarta.",
        "required": ["temporaryPassword"],
        "type": "object",
        "properties": {
          "temporaryPassword": {
            "type": "string"
          }
        }
      },
      "PrintClauseDto": {
        "required": ["order", "text"],
        "type": "object",
        "description": "Proposto. Uma cláusula das condições comerciais da última página do orçamento. O legado imprime 12.",
        "properties": {
          "order": {
            "type": "integer",
            "format": "int32",
            "description": "Posição na página final. É o que numera; a ordem do array não conta."
          },
          "text": {
            "type": "string",
            "description": "O texto como sai no papel. Empresa que ainda não configurou lê um marcador de pendência, e não uma cláusula plausível."
          }
        }
      },
      "PrintSettingsDto": {
        "required": ["quoteTitle", "clauses"],
        "type": "object",
        "description": "Proposto. Os textos configuráveis do impresso, por empresa.",
        "properties": {
          "quoteTitle": {
            "type": "string",
            "description": "`Par_TituloImpOrcamento` do legado — a linha de título do orçamento impresso."
          },
          "clauses": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PrintClauseDto"
            }
          }
        }
      },
      "PrintSettingsWriteRequest": {
        "required": ["quoteTitle", "clauses"],
        "type": "object",
        "description": "Proposto. Corpo de `UpdatePrintSettings`. Substitui os dois campos.",
        "properties": {
          "quoteTitle": {
            "type": "string"
          },
          "clauses": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PrintClauseDto"
            }
          }
        }
      },
      "LabelLayoutDto": {
        "required": [
          "id",
          "name",
          "pageWidthMm",
          "pageHeightMm",
          "marginTopMm",
          "marginBottomMm",
          "marginLeftMm",
          "marginRightMm",
          "columns",
          "columnGapMm",
          "labelHeightMm",
          "barcodeField",
          "isDefault",
          "active"
        ],
        "type": "object",
        "description": "Proposto. Um layout de etiqueta da empresa ativa — as MEDIDAS da folha e da etiqueta.\n\n**O que o layout NÃO guarda é o desenho.** O legado tem as duas coisas: `EtiquetaPronta` (medidas) e o trio `Etiquetas` / `Etiquetas_Campos` / `Etiquetas_Textos` (posicionamento livre de cada campo, com x, y, fonte e cor). Medido no dump do legado: `EtiquetaPronta` tem **21 linhas**, e as outras três têm **zero** — o editor de posicionamento existe há seis anos e nunca gravou nada. O que muda na prática é o rolo de papel, não onde o código fica dentro da etiqueta.",
        "properties": {
          "id": {
            "type": "string",
            "format": "uuid"
          },
          "name": {
            "type": "string",
            "description": "`etq_NomeModelo` do legado. Único na empresa."
          },
          "pageWidthMm": {
            "type": "number",
            "format": "double",
            "description": "A folha. Default do legado: 210."
          },
          "pageHeightMm": {
            "type": "number",
            "format": "double",
            "description": "Default do legado: 297."
          },
          "marginTopMm": {
            "type": "number",
            "format": "double"
          },
          "marginBottomMm": {
            "type": "number",
            "format": "double"
          },
          "marginLeftMm": {
            "type": "number",
            "format": "double"
          },
          "marginRightMm": {
            "type": "number",
            "format": "double"
          },
          "columns": {
            "type": "integer",
            "format": "int32",
            "description": "`Page.Columns` do QuickReport — 4 na etiqueta de produto da Vertz. A LARGURA de cada etiqueta é DERIVADA, não guardada: `(pageWidthMm − marginLeftMm − marginRightMm − (columns − 1) × columnGapMm) / columns`. É como o legado faz, e guardar as duas coisas deixaria a folha inconsistente com a etiqueta no dia em que uma mudasse sem a outra."
          },
          "columnGapMm": {
            "type": "number",
            "format": "double",
            "description": "`etq_EspacoColunas`."
          },
          "labelHeightMm": {
            "type": "number",
            "format": "double",
            "description": "`etq_AlturaColunas` — a altura de UMA etiqueta. Quantas cabem na folha é divisão, não configuração."
          },
          "barcodeField": {
            "type": ["null", "string"],
            "enum": [null, "code", "barcode"],
            "description": "`EtqPront_codigobarra`: qual campo do produto vira código de barras, ou `null` para etiqueta sem código. Vocabulário FECHADO de dois — a coluna do legado é `varchar(50)` livre, e campo livre aqui seria nome de coluna escolhido pelo cliente."
          },
          "isDefault": {
            "type": "boolean",
            "description": "Um por empresa. Marcar outro desmarca o anterior — é troca, não acúmulo."
          },
          "active": {
            "type": "boolean"
          }
        }
      },
      "LabelLayoutWriteRequest": {
        "required": [
          "name",
          "pageWidthMm",
          "pageHeightMm",
          "marginTopMm",
          "marginBottomMm",
          "marginLeftMm",
          "marginRightMm",
          "columns",
          "columnGapMm",
          "labelHeightMm",
          "barcodeField",
          "isDefault",
          "active"
        ],
        "type": "object",
        "description": "Proposto. Corpo de `CreateLabelLayout` e `UpdateLabelLayout`. Substitui o layout inteiro.",
        "properties": {
          "name": {
            "type": "string",
            "description": "`etq_NomeModelo` do legado. Único na empresa."
          },
          "pageWidthMm": {
            "type": "number",
            "format": "double",
            "description": "A folha. Default do legado: 210."
          },
          "pageHeightMm": {
            "type": "number",
            "format": "double",
            "description": "Default do legado: 297."
          },
          "marginTopMm": {
            "type": "number",
            "format": "double"
          },
          "marginBottomMm": {
            "type": "number",
            "format": "double"
          },
          "marginLeftMm": {
            "type": "number",
            "format": "double"
          },
          "marginRightMm": {
            "type": "number",
            "format": "double"
          },
          "columns": {
            "type": "integer",
            "format": "int32",
            "description": "`Page.Columns` do QuickReport — 4 na etiqueta de produto da Vertz. A LARGURA de cada etiqueta é DERIVADA, não guardada: `(pageWidthMm − marginLeftMm − marginRightMm − (columns − 1) × columnGapMm) / columns`. É como o legado faz, e guardar as duas coisas deixaria a folha inconsistente com a etiqueta no dia em que uma mudasse sem a outra."
          },
          "columnGapMm": {
            "type": "number",
            "format": "double",
            "description": "`etq_EspacoColunas`."
          },
          "labelHeightMm": {
            "type": "number",
            "format": "double",
            "description": "`etq_AlturaColunas` — a altura de UMA etiqueta. Quantas cabem na folha é divisão, não configuração."
          },
          "barcodeField": {
            "type": ["null", "string"],
            "enum": [null, "code", "barcode"],
            "description": "`EtqPront_codigobarra`: qual campo do produto vira código de barras, ou `null` para etiqueta sem código. Vocabulário FECHADO de dois — a coluna do legado é `varchar(50)` livre, e campo livre aqui seria nome de coluna escolhido pelo cliente."
          },
          "isDefault": {
            "type": "boolean",
            "description": "Um por empresa. Marcar outro desmarca o anterior — é troca, não acúmulo."
          },
          "active": {
            "type": "boolean"
          }
        }
      },
      "PagedResultOfLabelLayoutDto": {
        "required": ["rows", "total"],
        "type": "object",
        "properties": {
          "rows": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/LabelLayoutDto"
            }
          },
          "total": {
            "type": "integer",
            "format": "int32"
          }
        }
      }
    }
  },
  "tags": [
    {
      "name": "health"
    },
    {
      "name": "catalogo"
    },
    {
      "name": "parceiros"
    },
    {
      "name": "estoque"
    },
    {
      "name": "auth"
    },
    {
      "name": "dashboard"
    },
    {
      "name": "planner"
    },
    {
      "name": "vendas"
    },
    {
      "name": "cadastros"
    },
    {
      "name": "crm"
    },
    {
      "name": "tarefas"
    },
    {
      "name": "acesso"
    },
    {
      "name": "relatorios"
    },
    {
      "name": "compras"
    },
    {
      "name": "precos"
    },
    {
      "name": "comissoes"
    },
    {
      "name": "impressao"
    },
    {
      "name": "financeiro"
    }
  ]
}
